---
phase: 01-ffi-contract-design
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - docs/ffi-contract.md
  - Makefile
  - tests/abi/check_header.py
  - tests/abi/handle_layout.c
  - tests/abi/README.md
autonomous: true
requirements:
  - FFI-05
  - FFI-06
  - DOC-02
user_setup: []
must_haves:
  truths:
    - "A downstream implementer can read one normative contract and understand the exact lifetime, ownership, error, and diagnostics rules."
    - "Static verification fails if the header drifts or if unsafe ABI shapes reappear."
    - "The panic and exception policy is documented precisely without claiming release-mode recoverability."
  artifacts:
    - path: docs/ffi-contract.md
      provides: "Normative FFI contract document"
      contains: "ffi_fn!"
    - path: Makefile
      provides: "Repeatable contract verification commands"
      contains: "verify-contract"
    - path: tests/abi/check_header.py
      provides: "Header linter for signature-shape rules"
      contains: "int32-outparams"
    - path: tests/abi/handle_layout.c
      provides: "Compile-time layout assertions"
      contains: "static_assert"
    - path: tests/abi/README.md
      provides: "Verification rule mapping to Phase 1 requirements"
      contains: "FFI-01"
  key_links:
    - from: docs/ffi-contract.md
      to: include/pure_simdjson.h
      via: "exact symbol names, numeric codes, and lifecycle rules"
      pattern: "pure_simdjson_"
    - from: Makefile
      to: tests/abi/check_header.py and tests/abi/handle_layout.c
      via: "`verify-contract` target"
      pattern: "verify-contract"
---

<objective>
Write the normative FFI contract and the static checks that keep the contract, header, and documented safety rules aligned.

Purpose: Complete `DOC-02` and lock the panic/exception, ownership, and lifecycle policies so later phases implement a fixed contract instead of reinterpret it.
Output: `docs/ffi-contract.md` plus `Makefile` and `tests/abi/*` verification scaffolding that diff-check and lint the contract.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ffi-contract-design/01-CONTEXT.md
@.planning/phases/01-ffi-contract-design/01-RESEARCH.md
@.planning/phases/01-ffi-contract-design/01-VALIDATION.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `docs/ffi-contract.md` as the normative ABI contract</name>
  <files>docs/ffi-contract.md</files>
  <read_first>src/lib.rs, include/pure_simdjson.h, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-ffi-contract-design/01-CONTEXT.md, .planning/phases/01-ffi-contract-design/01-RESEARCH.md, .planning/phases/01-ffi-contract-design/01-VALIDATION.md, .planning/research/PITFALLS.md, docs/ffi-contract.md</read_first>
  <action>Write `docs/ffi-contract.md` as the single normative contract document. Implement the locked decisions per D-01, D-05, D-07, D-08, D-10, and D-11, while restating the already-encoded ABI decisions from Plan 02 where they matter for downstream implementers. Use exact top-level sections: `# Scope`, `# ABI invariants`, `# Error code space`, `# Handle format`, `# Value and iterator model`, `# Parser lifecycle`, `# Ownership and padding`, `# Strings and diagnostics`, `# ABI version handshake`, `# Panic and exception policy`, `# Worked call sequences`, and `# Deferred items`. In `# Scope`, state that `v0.1` is DOM-only and On-Demand stays out of scope per D-01. In `# Parser lifecycle`, state that `pure_simdjson_parser_parse` returns `PURE_SIMDJSON_ERR_PARSER_BUSY` while a live `Doc` exists and that only `pure_simdjson_doc_free` clears the busy state per D-07 and D-08. In `# Ownership and padding`, state that every parse copies the input into Rust-owned padded storage with `SIMDJSON_PADDING` semantics per D-02. In `# Strings and diagnostics`, document `pure_simdjson_element_get_string` + `pure_simdjson_bytes_free`, plus the bounded diagnostics helpers from D-10 and the advisory-only rule from D-11. In `# Panic and exception policy`, require `ffi_fn!` and `catch_unwind` for every export in unwind-enabled builds, require non-throwing simdjson `.get(err)` usage at the Rust/C++ seam, and explicitly say that release `panic = "abort"` is a policy choice and not a promise that panics become recoverable error codes. Include a numeric error-code table with the exact values from Plan 02 and a worked sequence showing string copy/free and parser-busy failure.</action>
  <verify>
    <automated>test -f docs/ffi-contract.md && rg '^# Scope|^# ABI invariants|^# Error code space|^# Parser lifecycle|ffi_fn!|catch_unwind|panic = \"abort\"|\\.get\\(err\\)|PURE_SIMDJSON_ERR_PARSER_BUSY|SIMDJSON_PADDING|\\^0\\.1\\.x' docs/ffi-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/ffi-contract.md` succeeds.
    - `rg '^# Scope|^# ABI invariants|^# Error code space|^# Handle format|^# Parser lifecycle|^# Panic and exception policy' docs/ffi-contract.md` succeeds.
    - `rg 'ffi_fn!|catch_unwind|panic = \"abort\"|\\.get\\(err\\)' docs/ffi-contract.md` succeeds.
    - `rg 'PURE_SIMDJSON_ERR_PARSER_BUSY|Rust-owned|SIMDJSON_PADDING|pure_simdjson_bytes_free|\\^0\\.1\\.x' docs/ffi-contract.md` succeeds.
    - `! rg 'recoverable in release|implicit invalidation|zero-copy string views in v0\\.1' docs/ffi-contract.md` succeeds.
  </acceptance_criteria>
  <done>`docs/ffi-contract.md` fully specifies the ABI, lifecycle, ownership, diagnostics, and boundary-policy rules that later phases must implement verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Add static verification scaffolding for header drift, signature rules, and layout invariants</name>
  <files>Makefile, tests/abi/check_header.py, tests/abi/handle_layout.c, tests/abi/README.md</files>
  <read_first>docs/ffi-contract.md, include/pure_simdjson.h, .planning/phases/01-ffi-contract-design/01-VALIDATION.md, Makefile, tests/abi/check_header.py, tests/abi/handle_layout.c, tests/abi/README.md</read_first>
  <action>Create `Makefile` targets `generate-header`, `verify-contract`, and `verify-docs`. `generate-header` must run `cbindgen --config cbindgen.toml --crate pure_simdjson --output include/pure_simdjson.h`. `verify-contract` must run `cargo check`, regenerate the header to a temp file and diff it against `include/pure_simdjson.h`, run `python3 tests/abi/check_header.py --rule int32-outparams --rule no-mixed-float-int --rule required-symbols --rule string-copy-ownership --rule diag-surface include/pure_simdjson.h`, and compile `tests/abi/handle_layout.c` with `cc -Iinclude`. `verify-docs` must grep `docs/ffi-contract.md` for `ffi_fn!`, `catch_unwind`, `panic = "abort"`, `.get(err)`, `PURE_SIMDJSON_ERR_PARSER_BUSY`, `SIMDJSON_PADDING`, and `^0.1.x`. In `tests/abi/check_header.py`, implement rule handlers with those exact rule names; reject any prototype that does not return `int32_t`, any struct-by-value return, any scalar `float`/`double` parameter, any missing required symbol from Plan 02, any string getter that is not `uint8_t **out_ptr` + `size_t *out_len`, and any missing `pure_simdjson_bytes_free` / diagnostics helpers. In `tests/abi/handle_layout.c`, use `static_assert` to enforce `sizeof(pure_simdjson_handle_t) == 8`, `sizeof(pure_simdjson_handle_parts_t) == 8`, `offsetof(pure_simdjson_handle_parts_t, generation) == 4`, `sizeof(pure_simdjson_value_view_t) == 32`, `sizeof(pure_simdjson_array_iter_t) == 32`, and `sizeof(pure_simdjson_object_iter_t) == 32`. In `tests/abi/README.md`, map each verification rule to `FFI-01` through `FFI-08` and `DOC-02` so the checks remain auditable.</action>
  <verify>
    <automated>make verify-contract && make verify-docs</automated>
  </verify>
  <acceptance_criteria>
    - `rg '^generate-header:|^verify-contract:|^verify-docs:' Makefile` succeeds.
    - `python3 tests/abi/check_header.py --rule int32-outparams --rule no-mixed-float-int --rule required-symbols --rule string-copy-ownership --rule diag-surface include/pure_simdjson.h` exits 0.
    - `cc -Iinclude tests/abi/handle_layout.c -c -o /tmp/pure_simdjson_handle_layout.o` exits 0.
    - `rg 'FFI-01|FFI-08|DOC-02' tests/abi/README.md` succeeds.
    - `make verify-contract && make verify-docs` exits 0.
  </acceptance_criteria>
  <done>The repository can mechanically detect contract drift, unsafe prototype changes, and layout regressions with the exact static commands described in the phase validation strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract doc -> downstream implementers | Overstated or ambiguous policy language would create unsafe assumptions in later phases |
| verification scripts -> public ABI artifacts | If the static checks miss drift, the committed header and docs can silently diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | R | panic/exception policy docs | mitigate | Explicitly state `ffi_fn!` / `catch_unwind` policy and separately state that release `panic = "abort"` remains fatal, preventing misleading recoverability claims |
| T-03-02 | T | header/doc drift | mitigate | `verify-contract` diffs a regenerated header, lints prototype rules, and compiles layout assertions before the plan can finish |
| T-03-03 | D | missing verification coverage | mitigate | Map `FFI-01..08` and `DOC-02` into `tests/abi/README.md` and make `make verify-contract && make verify-docs` the required automated gate |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `make verify-contract`
- [ ] `make verify-docs`
- [ ] `rg 'ffi_fn!|catch_unwind|panic = \"abort\"|\\.get\\(err\\)' docs/ffi-contract.md`
</verification>

<success_criteria>

- `docs/ffi-contract.md` is sufficient for later phases to implement the contract without reinterpretation
- The static suite catches header drift, signature regressions, and layout changes
- Panic and exception policy language is precise and non-misleading
- Phase 1 ends with the ABI source, generated header, normative doc, and verification scaffolding all committed together
</success_criteria>

<output>
After completion, create `.planning/phases/01-ffi-contract-design/01-03-SUMMARY.md`
</output>
