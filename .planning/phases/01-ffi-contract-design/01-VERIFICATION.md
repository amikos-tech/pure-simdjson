---
phase: 01-ffi-contract-design
verified: 2026-04-14T12:29:16Z
status: passed
score: 9/9 must-haves verified
---

# Phase 1: FFI Contract Design Verification Report

**Phase Goal:** A committed, reviewable C ABI contract that gates every implementation phase. Addresses 7 of 12 P0 pitfalls at the contract level so they cannot be re-introduced later.
**Verified:** 2026-04-14T12:29:16Z
**Status:** passed

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The repository contains a compilable Rust ABI-source crate that reproducibly drives header generation. | ✓ VERIFIED | [Cargo.toml](Cargo.toml:1) defines crate `pure_simdjson` with `cdylib` + `staticlib`; [Makefile](Makefile:3) regenerates the header from that crate; fresh `make verify-contract` ran `cargo check` successfully. |
| 2 | The committed `include/pure_simdjson.h` can be regenerated byte-for-byte from repo state without hand edits. | ✓ VERIFIED | Fresh `make verify-contract` re-ran `cbindgen --config cbindgen.toml --crate pure_simdjson` and `diff -u include/pure_simdjson.h "$tmp"` with exit code 0; the generation config is committed in [cbindgen.toml](cbindgen.toml:1). |
| 3 | The bootstrap ABI version export is present in both the ABI source and the generated header, giving later plans a reproducible contract baseline. | ✓ VERIFIED | [src/lib.rs](src/lib.rs:6) defines `PURE_SIMDJSON_ABI_VERSION`, and [src/lib.rs](src/lib.rs:134) exports `pure_simdjson_get_abi_version`; the generated declaration appears in [include/pure_simdjson.h](include/pure_simdjson.h:113). |
| 4 | Every exported ABI prototype returns `int32_t` and uses out-params instead of struct-by-value returns. | ✓ VERIFIED | [include/pure_simdjson.h](include/pure_simdjson.h:113) through [include/pure_simdjson.h](include/pure_simdjson.h:268) declares only `int32_t`-returning exports; [tests/abi/check_header.py](tests/abi/check_header.py:84) enforces that rule and passed in the fresh verification run. |
| 5 | Parser and Doc are represented as generation-stamped `u64` handles, while element and iterator results are lightweight doc-tied views. | ✓ VERIFIED | [src/lib.rs](src/lib.rs:42) through [src/lib.rs](src/lib.rs:82) define the packed handle split plus doc-tied view/iterator structs; matching generated layouts appear in [include/pure_simdjson.h](include/pure_simdjson.h:65) through [include/pure_simdjson.h](include/pure_simdjson.h:104), and [tests/abi/handle_layout.c](tests/abi/handle_layout.c:9) locks the byte sizes. |
| 6 | The generated header encodes parser-busy, split-number, string-copy, diagnostics, and ABI-version contract shapes without needing implementation code. | ✓ VERIFIED | The generated header includes `PURE_SIMDJSON_ERR_PARSER_BUSY`, `PURE_SIMDJSON_ERR_NUMBER_OUT_OF_RANGE`, and `PURE_SIMDJSON_ERR_PRECISION_LOSS` in [include/pure_simdjson.h](include/pure_simdjson.h:23); ABI version in [include/pure_simdjson.h](include/pure_simdjson.h:113); parser diagnostics in [include/pure_simdjson.h](include/pure_simdjson.h:154); string copy/free in [include/pure_simdjson.h](include/pure_simdjson.h:217); and iterator/object access in [include/pure_simdjson.h](include/pure_simdjson.h:247). |
| 7 | A downstream implementer can read one normative contract and understand the exact lifetime, ownership, error, diagnostics, and split-number accessor rules. | ✓ VERIFIED | [docs/ffi-contract.md](docs/ffi-contract.md:1) contains the required normative sections, with split numeric rules at [docs/ffi-contract.md](docs/ffi-contract.md:79), parser lifecycle at [docs/ffi-contract.md](docs/ffi-contract.md:90), ownership/padding at [docs/ffi-contract.md](docs/ffi-contract.md:110), and string ownership at [docs/ffi-contract.md](docs/ffi-contract.md:123). |
| 8 | Static verification fails if the header drifts or if unsafe ABI shapes reappear. | ✓ VERIFIED | [Makefile](Makefile:6) defines `verify-contract` and [Makefile](Makefile:15) defines `verify-docs`; [tests/abi/check_header.py](tests/abi/check_header.py:101) enforces required symbols and ABI shape; [tests/abi/handle_layout.c](tests/abi/handle_layout.c:9) enforces layout invariants; fresh `make verify-contract && make verify-docs` exited 0. |
| 9 | The panic and exception policy is documented precisely without claiming release-mode recoverability. | ✓ VERIFIED | [docs/ffi-contract.md](docs/ffi-contract.md:188) requires `ffi_wrap`, `catch_unwind`, and `.get(err)`, while [docs/ffi-contract.md](docs/ffi-contract.md:194) explicitly says `panic = "abort"` remains fatal in release builds. |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Cargo.toml` | ABI-source crate metadata with release panic policy | ✓ EXISTS + SUBSTANTIVE | [Cargo.toml](Cargo.toml:1) declares crate `pure_simdjson`, `crate-type = ["cdylib", "staticlib"]`, and `panic = "abort"`. |
| `src/lib.rs` | Contract-only ABI source with exported symbols and contract comments | ✓ EXISTS + SUBSTANTIVE | [src/lib.rs](src/lib.rs:1) through [src/lib.rs](src/lib.rs:376) define the ABI version, error space, handle/view layouts, and full stub export surface. |
| `cbindgen.toml` | Stable header-generation config | ✓ EXISTS + SUBSTANTIVE | [cbindgen.toml](cbindgen.toml:1) defines C output, include guard, pragma once, and explicit exported structs/enums. |
| `include/pure_simdjson.h` | Generated public ABI header | ✓ EXISTS + SUBSTANTIVE | [include/pure_simdjson.h](include/pure_simdjson.h:1) through [include/pure_simdjson.h](include/pure_simdjson.h:271) contain the committed generated surface. |
| `docs/ffi-contract.md` | Normative contract document | ✓ EXISTS + SUBSTANTIVE | [docs/ffi-contract.md](docs/ffi-contract.md:1) through [docs/ffi-contract.md](docs/ffi-contract.md:215) define lifecycle, ownership, diagnostics, ABI compatibility, and deferred items. |
| `Makefile` | Repeatable contract verification commands | ✓ EXISTS + SUBSTANTIVE | [Makefile](Makefile:1) through [Makefile](Makefile:16) implement `generate-header`, `verify-contract`, and `verify-docs`. |
| `tests/abi/check_header.py` | Header linter for ABI-shape rules | ✓ EXISTS + SUBSTANTIVE | [tests/abi/check_header.py](tests/abi/check_header.py:12) parses prototypes and [tests/abi/check_header.py](tests/abi/check_header.py:162) registers the rule set used by `verify-contract`. |
| `tests/abi/handle_layout.c` | Compile-time ABI layout assertions | ✓ EXISTS + SUBSTANTIVE | [tests/abi/handle_layout.c](tests/abi/handle_layout.c:9) through [tests/abi/handle_layout.c](tests/abi/handle_layout.c:14) lock the expected struct sizes and offsets. |
| `tests/abi/README.md` | Requirement-to-check traceability | ✓ EXISTS + SUBSTANTIVE | [tests/abi/README.md](tests/abi/README.md:5) maps each verification rule back to `FFI-01` through `FFI-08` and `DOC-02`. |
| `01-01-SUMMARY.md` | Bootstrap execution record | ✓ EXISTS + SUBSTANTIVE | Summary file exists at [.planning/phases/01-ffi-contract-design/01-01-SUMMARY.md](.planning/phases/01-ffi-contract-design/01-01-SUMMARY.md:1). |
| `01-02-SUMMARY.md` | Contract-surface execution record | ✓ EXISTS + SUBSTANTIVE | Summary file exists at [.planning/phases/01-ffi-contract-design/01-02-SUMMARY.md](.planning/phases/01-ffi-contract-design/01-02-SUMMARY.md:1). |
| `01-03-SUMMARY.md` | Contract-doc and verification execution record | ✓ EXISTS + SUBSTANTIVE | Summary file exists at [.planning/phases/01-ffi-contract-design/01-03-SUMMARY.md](.planning/phases/01-ffi-contract-design/01-03-SUMMARY.md:1). |

**Artifacts:** 12/12 verified

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `Cargo.toml` | `src/lib.rs` | crate name `pure_simdjson` used by `cargo check` and `cbindgen --crate pure_simdjson` | ✓ WIRED | [Cargo.toml](Cargo.toml:2) declares `name = "pure_simdjson"`, and [Makefile](Makefile:4) / [Makefile](Makefile:9) regenerate the header from that crate name. |
| `cbindgen.toml` | `include/pure_simdjson.h` | header generation command | ✓ WIRED | [Makefile](Makefile:3) through [Makefile](Makefile:10) wire `cbindgen.toml` into header generation and diff verification; fresh `make verify-contract` proved the header round-trip. |
| `src/lib.rs` | `include/pure_simdjson.h` | cbindgen regeneration | ✓ WIRED | Fresh `make verify-contract` regenerated the header from [src/lib.rs](src/lib.rs:134) through [src/lib.rs](src/lib.rs:376) with no diff against [include/pure_simdjson.h](include/pure_simdjson.h:113). |
| `pure_simdjson_value_view_t` | `pure_simdjson_array_iter_t` / `pure_simdjson_object_iter_t` | shared `doc` handle and doc-tied iterator state | ✓ WIRED | [src/lib.rs](src/lib.rs:56) defines `pure_simdjson_value_view_t.doc`, while [src/lib.rs](src/lib.rs:67) and [src/lib.rs](src/lib.rs:77) define iterators with the same leading `doc` handle; the generated C layouts match in [include/pure_simdjson.h](include/pure_simdjson.h:70), [include/pure_simdjson.h](include/pure_simdjson.h:81), and [include/pure_simdjson.h](include/pure_simdjson.h:91). |
| `docs/ffi-contract.md` | `include/pure_simdjson.h` | exact symbol names, numeric codes, and lifecycle rules | ✓ WIRED | The contract explicitly references the generated header as authoritative at [docs/ffi-contract.md](docs/ffi-contract.md:7) and documents symbols/prototypes that appear in [include/pure_simdjson.h](include/pure_simdjson.h:113), [include/pure_simdjson.h](include/pure_simdjson.h:146), and [include/pure_simdjson.h](include/pure_simdjson.h:217). |
| `Makefile` | `tests/abi/check_header.py` and `tests/abi/handle_layout.c` | `verify-contract` target | ✓ WIRED | [Makefile](Makefile:11) invokes `tests/abi/check_header.py`, and [Makefile](Makefile:12) compiles `tests/abi/handle_layout.c`. |

**Wiring:** 6/6 connections verified

## Requirements Coverage

| Requirement | Status | Blocking Issue |
|-------------|--------|----------------|
| `FFI-01`: C ABI surface defined in a single committed generated header; diff-checked | ✓ SATISFIED | - |
| `FFI-02`: Every export returns `int32`; out-params only | ✓ SATISFIED | - |
| `FFI-03`: No mixed float/int scalar arguments | ✓ SATISFIED | - |
| `FFI-04`: Generation-stamped packed handles and stable view/iterator layouts | ✓ SATISFIED | - |
| `FFI-05`: `ffi_wrap` + `catch_unwind` contract plus `panic = "abort"` policy | ✓ SATISFIED | Phase 1 ships the `ffi_wrap` helper and pins `panic = "abort"`; Phase 2 applies it to every new export. |
| `FFI-06`: `.get(err)` non-throwing seam requirement documented | ✓ SATISFIED | Phase 1 verifies the rule as normative contract and static gate, not full shim implementation. |
| `FFI-07`: ABI version export and `^0.1.x` compatibility rule | ✓ SATISFIED | - |
| `FFI-08`: Input copied into Rust-owned padded storage on every parse | ✓ SATISFIED | Verified as contract-level invariant in source/header/docs for this phase; implementation lands later. |
| `DOC-02`: `docs/ffi-contract.md` exists and tracks the ABI | ✓ SATISFIED | - |

**Coverage:** 9/9 requirements satisfied

## Anti-Patterns Found

None. A targeted scan across the phase artifacts found no `TODO`, `FIXME`, `XXX`, `HACK`, placeholder text, or analogous stub markers in the committed contract surface.

## Human Verification Required

None — this phase is a contract-definition and static-verification phase, and its must-haves are fully verifiable from the committed artifacts plus the fresh `make verify-contract && make verify-docs` run.

## Gaps Summary

**No gaps found.** Phase goal achieved. Ready to proceed.

## Verification Metadata

**Verification approach:** Goal-backward against Phase 1 must-haves from plan frontmatter and roadmap success criteria.
**Must-haves source:** `.planning/phases/01-ffi-contract-design/*-PLAN.md`
**Automated checks:** `make verify-contract` and `make verify-docs` passed fresh; `gsd-tools verify artifacts` passed for all three plans.
**Human checks required:** 0
**Total verification time:** ~15 minutes

---
*Verified: 2026-04-14T12:29:16Z*
*Verifier: Codex (direct phase verification after stalled verifier agent shutdown)*
