---
phase: 01-ffi-contract-design
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - cbindgen.toml
  - src/lib.rs
  - include/pure_simdjson.h
autonomous: true
requirements:
  - FFI-01
user_setup: []
must_haves:
  truths:
    - "`cbindgen` is installed locally before any contract-generation work proceeds."
    - "The repository contains a compilable Rust ABI-source crate that can drive header generation."
    - "`include/pure_simdjson.h` can be regenerated from committed repo state without hand edits."
  artifacts:
    - path: Cargo.toml
      provides: "ABI-source Rust crate metadata with `cdylib` and `staticlib` outputs"
      contains: "crate-type = [\"cdylib\", \"staticlib\"]"
    - path: src/lib.rs
      provides: "Bootstrap public C ABI items for header generation"
      contains: "pure_simdjson_get_abi_version"
    - path: cbindgen.toml
      provides: "Stable header-generation config"
      contains: "include_guard = \"PURE_SIMDJSON_H\""
    - path: include/pure_simdjson.h
      provides: "Committed generated header baseline"
      contains: "pure_simdjson_get_abi_version"
  key_links:
    - from: Cargo.toml
      to: src/lib.rs
      via: "crate name `pure_simdjson` used by `cargo check` and `cbindgen --crate pure_simdjson`"
      pattern: "name = \"pure_simdjson\""
    - from: cbindgen.toml
      to: include/pure_simdjson.h
      via: "header generation command"
      pattern: "cbindgen --config cbindgen.toml --crate pure_simdjson --output include/pure_simdjson.h"
---

<objective>
Bootstrap the ABI-source crate and generator path that make the FFI contract machine-generated instead of prose-only.

Purpose: Satisfy `FFI-01` early and unblock every later contract task with a working Wave 0 header pipeline.
Output: `Cargo.toml`, `src/lib.rs`, `cbindgen.toml`, and a committed generated `include/pure_simdjson.h`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ffi-contract-design/01-CONTEXT.md
@.planning/phases/01-ffi-contract-design/01-RESEARCH.md
@.planning/phases/01-ffi-contract-design/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install `cbindgen` and scaffold the ABI-source crate</name>
  <files>Cargo.toml, src/lib.rs</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-ffi-contract-design/01-CONTEXT.md, .planning/phases/01-ffi-contract-design/01-RESEARCH.md, Cargo.toml, src/lib.rs</read_first>
  <action>Run `cargo install --force cbindgen` first because the phase-specific Wave 0 constraint blocks contract work until the generator exists. Then create `Cargo.toml` for crate `pure_simdjson` with `edition = "2021"`, `[lib] crate-type = ["cdylib", "staticlib"]`, dependency `libc = "0.2"`, and `[profile.release] panic = "abort"` per `FFI-05` policy groundwork. Create `src/lib.rs` as a contract-only scaffold with `#![allow(non_camel_case_types)]`, public aliases `pure_simdjson_handle_t = u64` and `pure_simdjson_err_t = i32`, `pub const PURE_SIMDJSON_ABI_VERSION: u32 = 0x0001_0000`, and a minimal exported `pure_simdjson_get_abi_version(out_version: *mut u32) -> i32` stub that writes `0x0001_0000` and returns `0`. Do not add simdjson logic, raw pointer handles, or any parsing behavior in this plan.</action>
  <verify>
    <automated>command -v cbindgen >/dev/null && cargo check</automated>
  </verify>
  <acceptance_criteria>
    - `command -v cbindgen >/dev/null` succeeds from the repo root.
    - `rg 'crate-type *= *\\[\"cdylib\", \"staticlib\"\\]' Cargo.toml` succeeds.
    - `rg '^\\[profile\\.release\\]' Cargo.toml && rg 'panic *= *\"abort\"' Cargo.toml` succeeds.
    - `rg 'pub const PURE_SIMDJSON_ABI_VERSION: u32 = 0x0001_0000;' src/lib.rs` succeeds.
    - `rg 'pub unsafe extern \"C\" fn pure_simdjson_get_abi_version' src/lib.rs` succeeds.
  </acceptance_criteria>
  <done>`cbindgen` is installed and the repository contains a compilable Rust crate with a bootstrap ABI export that can drive header generation.</done>
</task>

<task type="auto">
  <name>Task 2: Add stable `cbindgen` config and commit the generated header baseline</name>
  <files>cbindgen.toml, include/pure_simdjson.h</files>
  <read_first>Cargo.toml, src/lib.rs, .planning/REQUIREMENTS.md, .planning/phases/01-ffi-contract-design/01-RESEARCH.md, .planning/phases/01-ffi-contract-design/01-VALIDATION.md, cbindgen.toml, include/pure_simdjson.h</read_first>
  <action>Create `cbindgen.toml` with `language = "C"`, `include_guard = "PURE_SIMDJSON_H"`, `pragma_once = true`, `cpp_compat = true`, `usize_is_size_t = true`, `documentation = true`, `style = "tag"`, and `sys_includes = ["stddef.h", "stdint.h", "stdbool.h"]`. Generate `include/pure_simdjson.h` with `cbindgen --config cbindgen.toml --crate pure_simdjson --output include/pure_simdjson.h` and commit the result as generated output, not handwritten source. Keep the header minimal in this bootstrap plan; the full ABI surface lands in Plan 02.</action>
  <verify>
    <automated>tmp="$(mktemp)" && cbindgen --config cbindgen.toml --crate pure_simdjson --output "$tmp" && diff -u include/pure_simdjson.h "$tmp" && rm -f "$tmp"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f cbindgen.toml` succeeds.
    - `rg 'include_guard = \"PURE_SIMDJSON_H\"' cbindgen.toml` succeeds.
    - `test -f include/pure_simdjson.h` succeeds.
    - `rg 'pure_simdjson_get_abi_version' include/pure_simdjson.h` succeeds.
    - `rg '^#ifndef PURE_SIMDJSON_H|^#pragma once' include/pure_simdjson.h` succeeds.
  </acceptance_criteria>
  <done>The repo contains a committed, reproducible generated header baseline produced from the bootstrap Rust ABI crate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local toolchain -> generated ABI artifacts | `cbindgen` output becomes the committed public C contract |
| Rust ABI source -> future Go bindings | Any drift between the crate and header would propagate ABI breakage downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `cbindgen` pipeline | mitigate | Install `cbindgen` in Task 1 and require regeneration parity in Task 2 so a hand-edited header cannot survive |
| T-01-02 | D | Wave 0 bootstrap | mitigate | Make `command -v cbindgen` and `cargo check` hard acceptance gates before any contract work proceeds |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `command -v cbindgen >/dev/null`
- [ ] `cargo check`
- [ ] `tmp="$(mktemp)" && cbindgen --config cbindgen.toml --crate pure_simdjson --output "$tmp" && diff -u include/pure_simdjson.h "$tmp" && rm -f "$tmp"`
</verification>

<success_criteria>

- Both Wave 0 tasks complete without introducing non-generated header edits
- The Rust crate compiles with `cargo check`
- The committed header round-trips exactly through `cbindgen`
- Plan 02 can extend the ABI from actual Rust/C contract files instead of starting from prose
</success_criteria>

<output>
After completion, create `.planning/phases/01-ffi-contract-design/01-01-SUMMARY.md`
</output>
