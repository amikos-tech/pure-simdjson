---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - src/lib.rs
  - src/runtime/mod.rs
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - tests/abi/check_header.py
  - tests/abi/test_check_header.py
  - tests/rust_shim_minify.rs
  - docs/ffi-contract.md
  - .github/workflows/phase2-rust-shim-smoke.yml
autonomous: true
requirements: [UTIL-01, UTIL-02]

must_haves:
  truths:
    - "psimdjson_minify pre-checks dst_cap >= src_len BEFORE calling simdjson::minify, because upstream's minify(buf,len,dst,dst_len) has no dst_cap parameter and cannot self-defend (D-09, RESEARCH.md Pitfall 5)"
    - "The MinifyInto dst==src aliasing contract implemented here matches the spike-004 ASan/UBSan-validated design exactly (arm64/fallback kernels); D-08/D-09 required no behavioral amendment based on that spike evidence (D-13)"
    - "psimdjson_minify and psimdjson_validate_utf8 both open with the same implementation_selection_state() lock-and-check block parser_new_configured_with_selection_lock uses, rejecting fallback with PURE_SIMDJSON_ERR_CPU_UNSUPPORTED and locking selection unconditionally (D-15)"
    - "Minify success does not imply the input was valid JSON: minify's only detectable error is UNCLOSED_STRING (RESEARCH.md Pitfall 6), documented in the Rust/C++ layer comments and in docs/ffi-contract.md"
    - "The spike-004 dst==src aliasing verification runs on x86-64 CI, folded into the existing linux-smoke job rather than a new job or a ported second implementation of probe.cpp (D-14)"
    - "make verify-contract and make verify-docs both pass with pure_simdjson_minify and pure_simdjson_validate_utf8 registered as required symbols, and ABI 1.3's complete mandatory-symbol sentence written in docs/ffi-contract.md"
  artifacts:
    - path: "src/native/simdjson_bridge.cpp"
      provides: "psimdjson_minify (dst_cap pre-check + CPU gate), psimdjson_validate_utf8 (CPU gate)"
    - path: "src/lib.rs"
      provides: "pure_simdjson_minify, pure_simdjson_validate_utf8 extern exports, both calling reject_fallback_implementation() before the native call"
    - path: "tests/rust_shim_minify.rs"
      provides: "overlap (dst==src), empty, malformed (UNCLOSED_STRING), and undersized-dst_cap Rust integration tests"
    - path: ".github/workflows/phase2-rust-shim-smoke.yml"
      provides: "new linux-smoke step invoking .planning/spikes/004-minify-buffer-safety/verify.sh"
    - path: "docs/ffi-contract.md"
      provides: "ABI 1.3 mandatory-symbol sentence; Wildcard match array copy and free worked call sequence"
  key_links:
    - from: "src/native/simdjson_bridge.cpp psimdjson_minify"
      to: "src/native/simdjson_bridge.cpp copy_bytes"
      via: "dst_cap >= src_len pre-check pattern reused verbatim before calling simdjson::minify"
      pattern: "PURE_SIMDJSON_ERR_BUFFER_TOO_SMALL"
    - from: "src/native/simdjson_bridge.cpp psimdjson_minify / psimdjson_validate_utf8"
      to: "src/native/simdjson_bridge.cpp implementation_selection_state()"
      via: "the same lock_guard + selection.locked=true + fallback-name check parser_new_configured_with_selection_lock uses"
      pattern: "implementation_selection_state"
    - from: ".github/workflows/phase2-rust-shim-smoke.yml linux-smoke"
      to: ".planning/spikes/004-minify-buffer-safety/verify.sh"
      via: "new run step invoking the spike script unmodified; the script stays in .planning/spikes/ per spike CONVENTIONS.md's promotion rule"
      pattern: "verify.sh"
---

<objective>
Complete the Phase 12 native surface with the two standalone SIMD utility entry points —
`Minify`/`MinifyInto` (UTIL-01) and `ValidateUTF8` (UTIL-02) — including the mandatory
`dst_cap` pre-check upstream `simdjson::minify` cannot perform itself, and the CPU-unsupported
gate D-15 requires all three standalone entry points to enforce. Close out the native-layer
contract work: fold the D-14 x86-64 aliasing verification into CI, and finalize
`docs/ffi-contract.md`'s ABI 1.3 section now that every new symbol exists.

Purpose: `Minify`/`ValidateUTF8` are the first public entry points with no `Doc`/`Parser` handle at
all — they call `activeLibrary()`-equivalent native code directly. Unlike parser construction, they
never triggered the CPU-unsupported check before this plan, silently running the slow `fallback`
kernel on unsupported CPUs (RESEARCH.md Pitfall 7). This plan closes that gap.
Output: `pure_simdjson_minify` and `pure_simdjson_validate_utf8` wired end-to-end with dst_cap
enforcement and CPU-unsupported rejection; D-14 CI wiring; docs/ffi-contract.md finalized for
ABI 1.3; `make verify-contract` and `make verify-docs` both green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-RESEARCH.md
@.planning/spikes/004-minify-buffer-safety/README.md
@.planning/spikes/CONVENTIONS.md

<interfaces>
<!-- Existing patterns this plan clones. -->

From src/native/simdjson_bridge.cpp (copy_bytes, ~line 125-150 — the EXACT dst_cap pre-check shape
psimdjson_minify must copy):
```cpp
pure_simdjson_error_code_t copy_bytes(
    std::string_view src, uint8_t *dst, size_t dst_cap, size_t *out_written
) noexcept {
  if (out_written == nullptr) return invalid_argument();
  *out_written = src.size();
  if (src.size() > dst_cap) return PURE_SIMDJSON_ERR_BUFFER_TOO_SMALL;
  if (!src.empty() && dst == nullptr) return invalid_argument();
  if (!src.empty()) std::memcpy(dst, src.data(), src.size());
  return PURE_SIMDJSON_OK;
}
```

From src/native/simdjson_bridge.cpp (implementation_selection_state and
parser_new_configured_with_selection_lock, ~line 110-119, 732-740 — the EXACT CPU-gate block
psimdjson_minify and psimdjson_validate_utf8 must open with):
```cpp
struct ImplementationSelectionState {
  std::mutex mutex{};
  bool locked{false};
  bool explicit_selection{false};
};
ImplementationSelectionState &implementation_selection_state() {
  static ImplementationSelectionState state;
  return state;
}
// inside parser_new_configured_with_selection_lock:
auto &selection = implementation_selection_state();
const std::lock_guard<std::mutex> lock(selection.mutex);
selection.locked = true;
const simdjson::implementation *implementation = simdjson::get_active_implementation();
if (!selection.explicit_selection && implementation->name() == "fallback") {
  return PURE_SIMDJSON_ERR_CPU_UNSUPPORTED;
}
```
Copy this block verbatim into both new functions (as its own scoped block, not a shared helper —
do not refactor parser_new_configured_with_selection_lock itself, to avoid regressing its existing
Phase 11 test coverage).

From src/lib.rs (reject_fallback_implementation, ~line 255-265, and its use inside
pure_simdjson_parser_new, ~line 448-465 — the Rust-side TEST-FORCING layer, separate from and
additional to the C++ check above; it lets `cargo test`/`go test` simulate "fallback selected" via
`pure_simdjson_test_set_forced_implementation_for_tests`/`PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION`
without needing real unsupported hardware):
```rust
fn reject_fallback_implementation() -> Result<(), pure_simdjson_error_code_t> {
    let forced_implementation = runtime::forced_implementation_name_for_parser_new();
    if matches!(forced_implementation.as_deref(), Some(b"fallback"))
        && !runtime::fallback_allowed_for_tests()
    { return Err(err_cpu_unsupported()); }
    Ok(())
}
// usage inside pure_simdjson_parser_new:
if let Err(rc) = reject_fallback_implementation() { return rc; }
```
`pure_simdjson_minify`/`pure_simdjson_validate_utf8` call this SAME function before invoking the
native bridge, giving them the same test-forcing mechanism `tests/rust_shim_fallback_gate.rs` and
Go's `kernel_test.go` (`t.Setenv("PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION", "fallback")`) already
use for parser construction.

Upstream signatures (verified against third_party/simdjson/include/simdjson/minify.h,
implementation.h): `simdjson::minify(const char *buf, size_t len, char *dst, size_t &dst_len)
noexcept -> error_code` — NO dst_cap parameter, cannot self-defend against an undersized dst
(third_party/simdjson/src/generic/stage1/json_minifier.h confirms zero bounds checking).
`simdjson::validate_utf8(const char *buf, size_t len) noexcept -> bool` — pure bool free function,
essentially cannot fail on its own; the "genuinely fail on first call" in D-11 is entirely about
activeLibrary() bootstrap (Go layer, plan 12-07), not this call itself.
`json_scanner.h` line 111 confirms UNCLOSED_STRING is the ONLY error minify's scanner can produce.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minify/ValidateUTF8 C++ bridge and Rust wiring</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, src/lib.rs</files>
  <read_first>
    src/native/simdjson_bridge.cpp (copy_bytes ~line 125-150; implementation_selection_state ~line
    110-119; parser_new_configured_with_selection_lock ~line 716-758; map_error ~line 152-199, which
    already maps every non-SUCCESS minify/validate_utf8-relevant code)
    src/lib.rs (reject_fallback_implementation ~line 255-265; pure_simdjson_parser_new ~line
    448-465 for the usage pattern; pure_simdjson_element_get_bool ~line 842-852 for a simple
    single-scalar-out-param export shape)
    src/runtime/mod.rs (native_object_get_field_index ~line 666-689 for the Result-returning thin
    wrapper shape; note minify/validate_utf8 need NO registry.rs changes — they are stateless, no
    Doc/handle involved)
  </read_first>
  <action>
    In src/native/simdjson_bridge.h: declare `psimdjson_minify(const uint8_t *src_ptr, size_t
    src_len, uint8_t *dst_ptr, size_t dst_cap, size_t *out_written)` and
    `psimdjson_validate_utf8(const uint8_t *data_ptr, size_t data_len, uint8_t *out_valid)`, both
    returning `pure_simdjson_error_code_t`.
    In src/native/simdjson_bridge.cpp: implement `psimdjson_minify`: null-check `out_written`;
    `*out_written = src_len;` (conservative upper bound, minified length is never greater than
    input length); `if (dst_cap < src_len) return PURE_SIMDJSON_ERR_BUFFER_TOO_SMALL;` BEFORE
    touching the CPU gate or calling into simdjson (bounds-first, matching copy_bytes' philosophy);
    `if (src_len != 0 && (src_ptr == nullptr || dst_ptr == nullptr)) return invalid_argument();`;
    then open the implementation_selection_state() lock-and-check block from this plan's
    &lt;interfaces&gt; verbatim, returning `PURE_SIMDJSON_ERR_CPU_UNSUPPORTED` on unsupported
    fallback; `if (src_len == 0) { *out_written = 0; return PURE_SIMDJSON_OK; }`; otherwise call
    `simdjson::minify(reinterpret_cast&lt;const char *&gt;(src_ptr), src_len,
    reinterpret_cast&lt;char *&gt;(dst_ptr), written)`, map any non-SUCCESS error through the
    existing `map_error()` (UNCLOSED_STRING -> PURE_SIMDJSON_ERR_INVALID_JSON, already mapped), else
    `*out_written = written; return PURE_SIMDJSON_OK;`. Wrap the whole function body in `try { ... }
    PSIMDJSON_CATCH_CPP_EXCEPTIONS(__func__)`.
    Implement `psimdjson_validate_utf8`: null-check `out_valid`; `if (data_len != 0 && data_ptr ==
    nullptr) return invalid_argument();`; open the SAME implementation_selection_state() block;
    then `*out_valid = simdjson::validate_utf8(reinterpret_cast&lt;const char
    *&gt;(data_ptr), data_len) ? 1 : 0; return PURE_SIMDJSON_OK;`. Same try/catch wrapper.
    In src/runtime/mod.rs: add both extern declarations. Add `native_minify(src_ptr: *const u8,
    src_len: usize, dst_ptr: *mut u8, dst_cap: usize) -> Result&lt;usize,
    pure_simdjson_error_code_t&gt;` (calls the extern fn, `Err(rc)` on non-OK, else
    `Ok(written)`) and `native_validate_utf8(data_ptr: *const u8, data_len: usize) ->
    Result&lt;bool, pure_simdjson_error_code_t&gt;` (same shape, `Ok(valid != 0)`). Both are `unsafe
    fn` (raw-pointer args, no registry lookup).
    In src/lib.rs: add `pure_simdjson_minify(src_ptr: *const u8, src_len: usize, dst_ptr: *mut u8,
    dst_cap: usize, out_written: *mut usize) -> pure_simdjson_error_code_t`: `ffi_wrap`, null-check
    `out_written`, call `reject_fallback_implementation()` first and return its `Err` immediately if
    any, then `runtime::native_minify(...)` and `write_out(out_written, written)`. Add
    `pure_simdjson_validate_utf8(data_ptr: *const u8, data_len: usize, out_valid: *mut u8) ->
    pure_simdjson_error_code_t` with the same `reject_fallback_implementation()`-first shape, then
    `write_out(out_valid, if valid { 1u8 } else { 0u8 })`.
  </action>
  <acceptance_criteria>
    - src/native/simdjson_bridge.cpp's psimdjson_minify checks `dst_cap < src_len` and returns `PURE_SIMDJSON_ERR_BUFFER_TOO_SMALL` before any call to `simdjson::minify`
    - src/native/simdjson_bridge.cpp's psimdjson_minify and psimdjson_validate_utf8 both reference `implementation_selection_state()`
    - src/lib.rs's pure_simdjson_minify and pure_simdjson_validate_utf8 both call `reject_fallback_implementation()` before the native call
    - `cargo build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && cargo build 2>&1 | tail -30</automated>
  </verify>
  <done>Minify and ValidateUTF8 are wired end-to-end with mandatory dst_cap bounds enforcement and the same CPU-unsupported gate parser construction already enforces.</done>
</task>

<task type="auto">
  <name>Task 2: Rust integration tests, REQUIRED_SYMBOLS, and verify-contract</name>
  <files>tests/rust_shim_minify.rs, tests/abi/check_header.py, tests/abi/test_check_header.py</files>
  <read_first>
    tests/rust_shim_accessors.rs (full file — harness shape to follow)
    tests/rust_shim_fallback_gate.rs (full file — the EnvGuard/TEST_OVERRIDE_LOCK pattern for
    testing the CPU-unsupported gate via `pure_simdjson_test_set_forced_implementation_for_tests`)
    tests/abi/check_header.py (REQUIRED_SYMBOLS)
    tests/abi/test_check_header.py (SURFACE_SIGNATURES)
  </read_first>
  <action>
    Create tests/rust_shim_minify.rs (new file) with its own local parser_new/parser_parse_literal/
    cleanup-style helpers where needed (minify/validate_utf8 tests mostly do not need a parser at
    all — they call `pure_simdjson_minify`/`pure_simdjson_validate_utf8` directly on raw byte
    slices). Add: `minify_in_place_dst_equals_src_matches_non_aliased_output` (minify `{"a":
    1,  "b":   2}` into a fresh dst buffer, then minify the SAME bytes again with dst pointing at
    the SAME buffer as src (dst==src), assert both produce byte-identical output and
    PURE_SIMDJSON_OK — this is the D-09/D-13 required "overlap" proof); `minify_empty_input_returns_zero_written`
    (src_len 0, assert PURE_SIMDJSON_OK and written 0); `minify_unclosed_string_returns_invalid_json`
    (src `"unterminated`, assert `PURE_SIMDJSON_ERR_INVALID_JSON`); `minify_undersized_dst_returns_buffer_too_small_before_writing`
    (src `{"a": 1}`, dst_cap 1 byte smaller than src_len, assert `PURE_SIMDJSON_ERR_BUFFER_TOO_SMALL`
    and that the dst buffer bytes are unchanged/unwritten -- proves the pre-check runs before any
    write); `validate_utf8_accepts_valid_and_rejects_invalid` (valid UTF-8 bytes -> out_valid 1;
    invalid byte sequence e.g. a lone continuation byte 0x80 -> out_valid 0; both PURE_SIMDJSON_OK).
    Following tests/rust_shim_fallback_gate.rs's exact EnvGuard/TEST_OVERRIDE_LOCK pattern, add
    `minify_rejects_fallback_without_bypass` and `validate_utf8_rejects_fallback_without_bypass`
    (force `pure_simdjson_test_set_forced_implementation_for_tests(Some(b"fallback"))`, assert
    `PURE_SIMDJSON_ERR_CPU_UNSUPPORTED` from `pure_simdjson_minify`/`pure_simdjson_validate_utf8`
    respectively).
    In tests/abi/check_header.py: append `"pure_simdjson_minify"` and
    `"pure_simdjson_validate_utf8"` to REQUIRED_SYMBOLS (this is the final append for Phase 12 — the
    tuple now contains all 9 new symbols across plans 12-01 through 12-04).
    In tests/abi/test_check_header.py: add matching SURFACE_SIGNATURES entries: `minify` ->
    `["const uint8_t *src_ptr", "size_t src_len", "uint8_t *dst_ptr", "size_t dst_cap", "size_t
    *out_written"]`; `validate_utf8` -> `["const uint8_t *data_ptr", "size_t data_len", "uint8_t
    *out_valid"]`.
    Run `make generate-header` then `make verify-contract`.
  </action>
  <acceptance_criteria>
    - `cargo test --test rust_shim_minify` exits 0 with all 7 tests passing
    - The undersized-dst test proves the pre-check happens before any write (buffer bytes unchanged)
    - tests/abi/check_header.py's REQUIRED_SYMBOLS contains all 9 Phase 12 symbol names (2 from 12-01, 3 from 12-02, 2 from 12-03, 2 from this plan)
    - `make verify-contract` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && cargo test --test rust_shim_minify && make verify-contract</automated>
  </verify>
  <done>Minify/MinifyInto/ValidateUTF8 are covered by passing Rust integration tests including the D-09 overlap proof, undersized-dst rejection, and the D-15 CPU-unsupported gate.</done>
</task>

<task type="auto">
  <name>Task 3: D-14 x86-64 CI wiring and docs/ffi-contract.md ABI 1.3 closeout</name>
  <files>.github/workflows/phase2-rust-shim-smoke.yml, docs/ffi-contract.md</files>
  <read_first>
    .github/workflows/phase2-rust-shim-smoke.yml (linux-smoke job, full job — note it currently does
    NOT run `cargo test`; it runs `make verify-contract` then builds release then
    `make phase2-smoke-linux`)
    .planning/spikes/004-minify-buffer-safety/verify.sh (full script — already builds+runs the ASan/
    UBSan probe against the untouched vendored singleheader; loops over every kernel
    `get_available_implementations()` reports as supported, so running unmodified on ubuntu-latest
    naturally exercises haswell/westmere/fallback)
    .planning/spikes/CONVENTIONS.md (promotion rule: promote the finding/invocation into CI; the
    spike script itself STAYS in .planning/spikes/, do not port probe.cpp into a second maintained
    implementation)
    docs/ffi-contract.md (Error code space table, already has rows 11/12 from plan 12-01; ABI
    version handshake section ~line 269-279, currently says "ABI 1.2 makes ... mandatory"; Worked
    call sequences section ~line 296-331, "String copy and free" example to model a new "Wildcard
    match array copy and free" subsection on)
  </read_first>
  <action>
    In .github/workflows/phase2-rust-shim-smoke.yml's `linux-smoke` job: add a new step after
    "Verify contract" and before "Build release library" (or after the smoke steps — placement
    within linux-smoke is flexible, but it must be a step in THIS existing job, not a new job) named
    "Verify minify dst==src aliasing (D-14, x86-64)" running `bash
    .planning/spikes/004-minify-buffer-safety/verify.sh`. Do not modify verify.sh or probe.cpp — the
    spike stays exactly as committed; only its invocation is promoted into CI.
    In docs/ffi-contract.md's ABI version handshake section: change "ABI 1.2 is the strict minimum
    for the Phase 11 wrapper..." to "ABI 1.3 is the strict minimum for the Phase 12 wrapper. ABI 1.2
    is rejected with an ABI-version mismatch before any ABI 1.3-only lookup." and change "ABI 1.2
    makes pure_simdjson_set_implementation, ..., and pure_simdjson_element_get_bigint mandatory." to
    add the 9 new Phase 12 symbols to that mandatory list: pure_simdjson_element_at_pointer,
    pure_simdjson_element_at_path, pure_simdjson_element_at_path_wildcard,
    pure_simdjson_value_views_free, pure_simdjson_array_at, pure_simdjson_array_len,
    pure_simdjson_object_size, pure_simdjson_minify, pure_simdjson_validate_utf8.
    Add a new "Wildcard match array copy and free" subsection under "Worked call sequences",
    modeled on the existing "String copy and free" C-code block: parse -> get root ->
    `pure_simdjson_element_at_path_wildcard(&root, path_ptr, path_len, &views_ptr, &views_len)` ->
    caller copies `views_ptr[0..views_len]` -> `pure_simdjson_value_views_free(views_ptr,
    views_len)` -> `pure_simdjson_doc_free(doc)` -> `pure_simdjson_parser_free(parser)`.
    Also add one sentence near the Minify description (create a short "Minify and UTF-8 validation"
    subsection near "Copied scalars and diagnostics" or "Process-global implementation selection")
    stating: minify does not parse or validate JSON (it detects only unterminated strings via
    UNCLOSED_STRING); MinifyInto's dst may alias src for in-place minification; an undersized dst
    returns PURE_SIMDJSON_ERR_BUFFER_TOO_SMALL before any write; both pure_simdjson_minify and
    pure_simdjson_validate_utf8 apply the same CPU-unsupported rejection and process-global
    selection-locking as parser construction.
  </action>
  <acceptance_criteria>
    - .github/workflows/phase2-rust-shim-smoke.yml's linux-smoke job contains a step running `.planning/spikes/004-minify-buffer-safety/verify.sh`
    - docs/ffi-contract.md contains the string "ABI 1.3 is the strict minimum"
    - docs/ffi-contract.md contains all 9 new Phase 12 symbol names in the ABI mandatory-surface sentence
    - docs/ffi-contract.md contains a "Wildcard match array copy and free" worked call sequence
    - `make verify-docs` exits 0
    - `make verify-contract` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && make verify-docs && make verify-contract && grep -c "ABI 1.3 is the strict minimum" docs/ffi-contract.md</automated>
  </verify>
  <done>The D-14 x86-64 aliasing verification runs as a step in the existing linux-smoke CI job; docs/ffi-contract.md fully documents ABI 1.3's complete mandatory-symbol surface and the new Minify/wildcard-free worked sequences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go caller -> Rust FFI (no Doc/Parser handle) | Untrusted, arbitrarily-sized byte buffers cross into native code with a caller-controlled destination capacity |
| Rust/C++ -> vendored simdjson | Malformed (not necessarily invalid-JSON) input crosses into a non-validating minifier scanner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-BOUNDS | Tampering / Denial of Service | psimdjson_minify undersized dst (upstream simdjson::minify has no self-defense, confirmed by direct source read of json_minifier.h) | mitigate | Mandatory dst_cap >= src_len pre-check in the C++ bridge, checked BEFORE any call into simdjson::minify or any write, mirroring copy_bytes verbatim (D-09) |
| T-12-ALIAS | Tampering | MinifyInto dst == src in-place aliasing | mitigate | Spike-004 ASan/UBSan-validated on arm64/fallback (D-13); this plan folds the equivalent x86-64 verification into linux-smoke CI (D-14) so all 3 x86-64 kernel families (haswell/westmere/fallback) are exercised on every push touching this surface |
| T-12-CPU | Spoofing (silent kernel downgrade) | Minify/ValidateUTF8 bypassing the CPU-unsupported gate that already protects parser construction | mitigate | Both new entry points open with the identical implementation_selection_state() lock-and-check block, and the Rust-side reject_fallback_implementation() test-forcing layer, matching parser_new_configured_with_selection_lock exactly (D-15) |
| T-12-10 | Repudiation (false validity signal) | Callers assuming Minify success implies valid JSON | accept | Documented explicitly in docs/ffi-contract.md and Go doc comments (plan 12-07): minify detects only UNCLOSED_STRING, nothing else — this is upstream's documented, unavoidable behavior, not a defect this project can close without reimplementing a validator |
| T-12-11 | Denial of Service | Malformed UTF-8 byte sequences passed to validate_utf8 | accept | simdjson::validate_utf8 is a pure, non-throwing SIMD scan with no unbounded work relative to input length; no new amplification vector |
</threat_model>

<verification>
- `cargo build`, `cargo test --test rust_shim_minify` pass
- `make verify-contract` and `make verify-docs` pass
- `.github/workflows/phase2-rust-shim-smoke.yml`'s linux-smoke job includes the D-14 verify.sh step
</verification>

<success_criteria>
Minify/MinifyInto and ValidateUTF8 are wired end-to-end with mandatory buffer-bounds enforcement and
CPU-unsupported rejection; the D-14 x86-64 aliasing gap is closed in CI; docs/ffi-contract.md fully
documents ABI 1.3. All 9 Phase 12 native symbols exist, are required, and are covered by Rust
integration tests.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-04-SUMMARY.md` when done
</output>
