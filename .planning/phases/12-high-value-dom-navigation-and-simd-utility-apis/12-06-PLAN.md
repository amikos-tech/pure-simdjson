---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 06
type: execute
wave: 6
depends_on: ["12-05"]
files_modified:
  - element.go
  - errors.go
  - element_pointer_test.go
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03]

must_haves:
  truths:
    - "Element.AtPointer implements RFC 6901 JSON Pointer via upstream element::at_pointer, with typed missing/invalid-path/out-of-bounds errors (DOM-01, D-01, D-03)"
    - "Element.AtPath exposes simdjson's documented dot/index path subset and its Go doc comment honestly documents the leading '.'/'[' requirement (Pitfall 2) and the bracket-key quote-non-awareness asymmetry (Pitfall 1) rather than papering over it (DOM-02, D-01)"
    - "Element.AtPathAll returns a non-nil, zero-length []Element (never an error) for a legitimately empty wildcard match, and does not claim full RFC 9535 JSONPath support in its doc comment (DOM-03, D-02)"
    - "ErrInvalidPath and ErrIndexOutOfRange are new typed sentinels distinguishable via errors.Is, and ErrElementNotFound/ErrWrongType are reused (not duplicated) for missing-field/wrong-type traversal failures (D-01, D-03)"
  artifacts:
    - path: "errors.go"
      provides: "ErrInvalidPath, ErrIndexOutOfRange sentinels; sentinelForStatus cases for ffi.ErrInvalidPath/ffi.ErrIndexOutOfRange"
    - path: "element.go"
      provides: "Element.AtPointer, Element.AtPath, Element.AtPathAll methods"
    - path: "element_pointer_test.go"
      provides: "Table-driven tests covering DOM-01/02/03 taxonomy and RESEARCH.md Pitfalls 1-3"
  key_links:
    - from: "element.go Element.AtPointer"
      to: "internal/ffi/bindings.go Bindings.ElementAtPointer"
      via: "doc.parser.library.bindings.ElementAtPointer(&e.view, pointer)"
      pattern: "bindings.ElementAtPointer"
    - from: "errors.go sentinelForStatus"
      to: "internal/ffi/types.go ffi.ErrInvalidPath / ffi.ErrIndexOutOfRange"
      via: "new switch cases returning ErrInvalidPath / ErrIndexOutOfRange"
      pattern: "case ffi.ErrInvalidPath"
---

<objective>
Expose RFC 6901 JSON Pointer navigation (`Element.AtPointer`, DOM-01), simdjson's documented
dot/index path subset (`Element.AtPath`, DOM-02), and ordered wildcard path selection
(`Element.AtPathAll`, DOM-03) as public `purejson` methods, plus the two new typed error sentinels
D-01 requires.

Purpose: this is the primary consumer-facing navigation surface of Phase 12. Every method is a thin
wrapper delegating grammar/traversal entirely to upstream simdjson (per the phase's "thin wrapper"
framing) — Go's job is only to marshal, type the result, and document upstream's real (sometimes
surprising) behavior honestly.
Output: `AtPointer`/`AtPath`/`AtPathAll` on `Element`, two new error sentinels, and a passing table-
driven test suite covering the documented pitfalls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-RESEARCH.md

<interfaces>
<!-- Existing Go-layer patterns this plan clones. -->

From element.go (Object.GetField, ~line 369-392 — the exact usableDoc-guard + bindings-call +
wrapStatus shape AtPointer/AtPath/AtPathAll copy):
```go
func (o Object) GetField(key string) (Element, error) {
	doc, err := o.element.usableDoc()
	if err != nil { return Element{}, err }
	if ffi.ValueKind(o.element.view.KindHint) != ffi.ValueKindObject { return Element{}, ErrWrongType }
	view, rc := doc.parser.library.bindings.ObjectGetField(&o.element.view, key)
	runtime.KeepAlive(doc)
	if err := normalizeIteratorError(doc, rc); err != nil { return Element{}, err }
	return Element{doc: doc, view: view}, nil
}
```
AtPointer/AtPath use plain `wrapStatus(rc)`, not `normalizeIteratorError` (that helper exists
specifically for iterator-lease semantics, which navigation calls do not have) — and they perform NO
kind pre-check before the FFI call (upstream `at_pointer`/`at_path` dispatch on kind internally and
work on scalars when the pointer/path is empty).

From element.go (usableDoc, ~line 69-80 — reused unmodified by every new method):
```go
func (e Element) usableDoc() (*Doc, error) {
	if e.doc == nil { return nil, ErrInvalidHandle }
	if e.doc.parser == nil || e.doc.parser.library == nil || e.doc.parser.library.bindings == nil {
		return nil, ErrInvalidHandle
	}
	if e.doc.isClosed() { return nil, ErrClosed }
	return e.doc, nil
}
```

Bindings methods available from plan 12-05:
`ElementAtPointer(view *ffi.ValueView, pointer string) (ffi.ValueView, int32)`,
`ElementAtPath(view *ffi.ValueView, path string) (ffi.ValueView, int32)`,
`ElementAtPathWildcard(view *ffi.ValueView, path string) ([]ffi.ValueView, int32)` (already returns a
non-nil empty slice on a zero-length native match, and already frees the native array internally).

From errors.go (sentinelForStatus, ~line 222-259 — extend this switch, do not invent a parallel
error-mapping path):
```go
func sentinelForStatus(code int32) error {
	switch ffi.ErrorCode(code) {
	case ffi.ErrKernelLocked: return ErrKernelLocked
	// ADD: case ffi.ErrInvalidPath: return ErrInvalidPath
	// ADD: case ffi.ErrIndexOutOfRange: return ErrIndexOutOfRange
	...
	}
}
```

Documented upstream pitfalls this plan's doc comments MUST state honestly (from RESEARCH.md, backed
by direct reading of third_party/simdjson/include/simdjson/jsonpathutil.h and element-inl.h — do not
soften or omit these):
- Pitfall 1: AtPath's bracket-key form is NOT quote-aware. `AtPath(".obj['foo']")` looks up the
  LITERAL 11-character key `'foo'` (including both quote characters), not `foo`. Unquoted bracket
  form `AtPath(".obj[foo]")` correctly looks up key `foo`. This is an upstream asymmetry (the
  wildcard code path DOES strip quotes; the plain at_path path does not) — document it as fact, not
  as a bug this project can silently "fix".
- Pitfall 2: AtPath requires a leading `.` or `[` (after an optional leading `$`). `AtPath("name")` is
  ALWAYS invalid (ErrInvalidPath); must be `AtPath(".name")` or `AtPath("$.name")`.
  `AtPath("")` is also invalid.
  `AtPath("$")` / `AtPath(".")` behavior is upstream's own edge-case handling — document per the test
  output.
- Pitfall 3: a trailing separator navigates into a literal empty-string key on the CHILD, not a
  no-op. `AtPointer("/a/")` on `{"a": 1}` returns ErrWrongType (1 is not an object to look up "" in);
  on `{"a": {"": "x"}}` it returns the string "x".
- D-02: AtPathAll's zero-match result is `([]Element{}, nil)`, never an error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: errors.go — ErrInvalidPath and ErrIndexOutOfRange sentinels</name>
  <files>errors.go</files>
  <read_first>errors.go (full file — sentinel var block ~line 11-54, sentinelForStatus switch ~line 222-259)</read_first>
  <action>
    Add two new sentinels to the `var (...)` block, placed after `ErrKernelLocked`: `ErrInvalidPath =
    errors.New("invalid path")` with a doc comment "ErrInvalidPath reports malformed RFC 6901 JSON
    Pointer or simdjson dot/index path syntax." and `ErrIndexOutOfRange = errors.New("index out of
    range")` with a doc comment "ErrIndexOutOfRange reports a syntactically valid array index or
    path segment that exceeds array bounds.". Add two new cases to `sentinelForStatus`'s switch,
    placed near the other `ffi.Err*` cases: `case ffi.ErrInvalidPath: return ErrInvalidPath` and
    `case ffi.ErrIndexOutOfRange: return ErrIndexOutOfRange`. Do not add a third or fourth sentinel
    (per D-01) — missing-field and wrong-type traversal failures continue to reuse the existing
    `ErrElementNotFound`/`ErrWrongType`.
  </action>
  <acceptance_criteria>
    - errors.go contains `ErrInvalidPath = errors.New(` and `ErrIndexOutOfRange = errors.New(`
    - errors.go's sentinelForStatus contains `case ffi.ErrInvalidPath:` and `case ffi.ErrIndexOutOfRange:`
    - `go build .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go build . && go vet .</automated>
  </verify>
  <done>Two new typed error sentinels exist and are wired to their native status codes via sentinelForStatus, distinguishable via errors.Is per D-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: element.go — AtPointer, AtPath, AtPathAll</name>
  <files>element.go</files>
  <behavior>
    - AtPointer(".../valid pointer") on a matching document resolves to the correct Element (type and scalar value match)
    - AtPointer with a pointer missing the leading "/" returns an error satisfying errors.Is(err, ErrInvalidPath)
    - AtPointer resolving to an out-of-range array index returns errors.Is(err, ErrIndexOutOfRange)
    - AtPointer resolving to a missing object key returns errors.Is(err, ErrElementNotFound)
    - AtPath(".a.b") resolves the same as AtPointer("/a/b") on the same document
    - AtPath("name") (no leading separator) returns errors.Is(err, ErrInvalidPath)
    - AtPathAll on a wildcard with zero matches returns a non-nil, len==0 []Element and a nil error
    - AtPathAll on a wildcard with N matches returns N Elements in document order
  </behavior>
  <read_first>
    element.go (full file — Object.GetField ~line 369-392 is the exact shape to clone per this
    plan's &lt;interfaces&gt; block; usableDoc ~line 69-80)
    internal/ffi/bindings.go (ElementAtPointer, ElementAtPath, ElementAtPathWildcard signatures from
    plan 12-05)
  </read_first>
  <action>
    Add `func (e Element) AtPointer(pointer string) (Element, error)`: `usableDoc()` guard, call
    `doc.parser.library.bindings.ElementAtPointer(&e.view, pointer)`, `runtime.KeepAlive(doc)`,
    `wrapStatus(rc)`, return `Element{doc: doc, view: view}, nil` on success. Doc comment states RFC
    6901 conformance and cites the exact error taxonomy (ErrInvalidPath for malformed syntax,
    ErrElementNotFound for missing keys/segments, ErrIndexOutOfRange for out-of-range array indices,
    ErrWrongType for traversal type mismatches) plus Pitfall 3's trailing-separator behavior
    verbatim from this plan's &lt;interfaces&gt; block.
    Add `func (e Element) AtPath(path string) (Element, error)` with the identical shape calling
    `bindings.ElementAtPath`. Doc comment states the exact leading-`.`/`[`/`$` requirement (Pitfall
    2, with a worked example) AND the bracket-key quote-non-awareness asymmetry (Pitfall 1, with the
    `.obj['foo']` example) verbatim from this plan's &lt;interfaces&gt; block — this is upstream's
    documented, non-obvious behavior and must not be softened or omitted.
    Add `func (e Element) AtPathAll(path string) ([]Element, error)`: `usableDoc()` guard, call
    `doc.parser.library.bindings.ElementAtPathWildcard(&e.view, path)`, `runtime.KeepAlive(doc)`,
    `wrapStatus(rc)` on error, then `out := make([]Element, len(views)); for i, v := range views {
    out[i] = Element{doc: doc, view: v} }; return out, nil`. Doc comment states wildcard syntax
    support (`*`/`[*]`), the D-02 empty-match-is-not-an-error guarantee, and explicitly disclaims
    full RFC 9535 JSONPath support.
  </action>
  <acceptance_criteria>
    - element.go exports AtPointer, AtPath, AtPathAll methods on Element with the exact signatures listed in this task's action
    - AtPath's doc comment contains the literal substring "['foo']" or an equivalent bracket-quote example demonstrating Pitfall 1
    - AtPathAll's doc comment contains "RFC 9535" (explicitly disclaiming full support)
    - `go build .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go build . && go vet .</automated>
  </verify>
  <done>Element.AtPointer, AtPath, and AtPathAll are implemented and documented, honestly reflecting upstream's exact (including surprising) grammar and behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Go table tests for navigation taxonomy and pitfalls</name>
  <files>element_pointer_test.go</files>
  <read_first>
    element_scalar_test.go (mustParseDoc helper ~line 17-38, TestElementTypeClassification ~line
    40-65 for the table-driven `testCases := []struct{...}{}` + `t.Run(tc.name, ...)` shape)
    errors.go (ErrInvalidPath, ErrIndexOutOfRange, ErrElementNotFound, ErrWrongType — the sentinels
    under test)
  </read_first>
  <action>
    Create element_pointer_test.go (new file) using the `mustParseDoc(t, json)` helper already
    defined in element_scalar_test.go. Add `TestElement_AtPointer` with table cases: valid nested
    pointer resolves correctly (`{"a":{"b":42}}`, pointer `/a/b`, want int64 42); missing leading `/`
    returns ErrInvalidPath; missing object key returns ErrElementNotFound; out-of-range array index
    (`[1,2,3]`, pointer `/5`) returns ErrIndexOutOfRange; empty pointer `""` returns the root element
    itself; trailing-separator case from Pitfall 3 (`{"a":{"":"x"}}`, pointer `/a/`, want string
    "x") and its ErrWrongType counterpart (`{"a":1}`, pointer `/a/`).
    Add `TestElement_AtPath` with table cases: dot-notation resolves the same value as the AtPointer
    equivalent; bare field name (no leading separator) returns ErrInvalidPath; empty path returns
    ErrInvalidPath; `$.`-prefixed path resolves the same as its bare `.`-prefixed equivalent; the
    Pitfall-1 bracket-quote case (`{"obj":{"'foo'":1,"foo":2}}`, path `.obj['foo']`, want the value
    under literal key `'foo'` which is 1, NOT the value under `foo`) proving the documented
    non-quote-aware behavior is real, not just documented.
    Add `TestElement_AtPathAll` with table cases: `{"items":[{"id":1},{"id":2},{"id":3}]}` with path
    `.items[*].id` returns 3 elements with int64 values 1,2,3 in order; `{"items":[]}` with the same
    path returns a non-nil, zero-length slice and nil error (assert `result != nil &&
    len(result)==0`, not just `len(result)==0`); malformed wildcard syntax returns ErrInvalidPath.
    Use `errors.Is(err, ExpectedSentinel)` for every error assertion, matching this codebase's
    existing test convention.
  </action>
  <acceptance_criteria>
    - `go test -run 'TestElement_AtPointer|TestElement_AtPath|TestElement_AtPathAll' ./...` exits 0
    - TestElement_AtPathAll's empty-match case asserts the result is non-nil (not just zero-length)
    - TestElement_AtPath's bracket-quote case asserts the value under the literal quoted key, proving Pitfall 1 is real behavior, not just a doc claim
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go test -run 'TestElement_AtPointer|TestElement_AtPath|TestElement_AtPathAll' ./... -v</automated>
  </verify>
  <done>Navigation taxonomy (D-01/D-02/D-03) and the documented upstream pitfalls (bracket-quote non-awareness, leading-separator requirement, trailing-separator semantics) are proven by passing Go tests, not just asserted in doc comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go public API -> untrusted caller input | Pointer/path strings supplied by the calling application (which may itself be handling untrusted data) cross into the native layer via these methods |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Tampering | Malformed or adversarial pointer/path strings passed to AtPointer/AtPath/AtPathAll | mitigate | Delegated entirely to the native layer's typed error handling (plan 12-01/12-03), which returns ErrInvalidPath/ErrElementNotFound/ErrWrongType rather than panicking; Go adds no additional parsing that could diverge from upstream's grammar |
| T-12-15 | Information Disclosure | A caller mistaking AtPath's bracket-quote non-awareness for a bug and building an access-control assumption on it | accept | Documented explicitly and tested (Task 3) rather than silently patched; this is upstream's own documented asymmetry, not a project-introduced vulnerability, and papering over it would create a false sense of RFC-9535-like safety this project explicitly does not claim |
</threat_model>

<verification>
- `go build .`, `go vet .` pass
- `go test -run 'TestElement_AtPointer|TestElement_AtPath|TestElement_AtPathAll' ./...` passes
</verification>

<success_criteria>
Element.AtPointer, AtPath, and AtPathAll are implemented, honestly documented (including upstream's
non-obvious grammar quirks), and proven correct by table-driven tests covering the full D-01/D-02/D-03
error taxonomy.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-06-SUMMARY.md` when done
</output>
