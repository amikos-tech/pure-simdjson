---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 10
type: execute
wave: 6
depends_on: ["12-04"]
files_modified:
  - tests/native/minify_buffer_safety_probe.cpp
  - scripts/ci/verify_minify_buffer_safety.sh
  - .github/workflows/phase2-rust-shim-smoke.yml
  - tests/smoke/ffi_export_surface.c
  - .github/workflows/phase3-go-wrapper-smoke.yml
  - scripts/release/test_release_workflow_contracts.py
  - docs/ffi-contract.md
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03, DOM-04, UTIL-01, UTIL-02]

must_haves:
  truths:
    - "The D-14 sanitizer probe lives outside .planning, derives its expected total from supported kernels, and requires fallback plus at least one x86 kernel on Linux x86-64"
    - "phase2-rust-shim-smoke.yml runs the durable D-14 gate when either its script or tests/native/** changes"
    - "tests/smoke/ffi_export_surface.c resolves, invokes, and validates all nine ABI 1.3 exports with their exact C signatures"
    - "The existing five-platform Go wrapper workflow runs automatically on pushes to the Phase 12 branch, not only on the old Phase 3 branch or main"
    - "Every current loader-contract statement names ABI 1.3 as the minimum and treats ABI 1.2 only as historical/rejected context"
    - "Focused workflow-contract tests pin the two push triggers, the nine native smoke calls, and the absence of production .planning/spikes dependencies"
  artifacts:
    - path: "scripts/ci/verify_minify_buffer_safety.sh"
      provides: "Durable dynamic three-run ASan/UBSan D-14 verifier"
    - path: "tests/smoke/ffi_export_surface.c"
      provides: "Runtime ABI 1.3 export-resolution and invocation smoke for all nine new functions"
    - path: ".github/workflows/phase3-go-wrapper-smoke.yml"
      provides: "Automatic five-platform Go race workflow for the Phase 12 branch"
    - path: "scripts/release/test_release_workflow_contracts.py"
      provides: "Static contracts for workflow triggers, durable D-14 paths, and expanded smoke coverage"
    - path: "docs/ffi-contract.md"
      provides: "Complete ABI 1.3 mandatory surface, ownership sequence, and utility safety contract"
  key_links:
    - from: ".github/workflows/phase2-rust-shim-smoke.yml"
      to: "scripts/ci/verify_minify_buffer_safety.sh"
      via: "linux-smoke run step plus push paths for the script and tests/native/**"
      pattern: "scripts/ci/verify_minify_buffer_safety\\.sh"
    - from: ".github/workflows/release.yml"
      to: "tests/smoke/ffi_export_surface.c"
      via: "existing scripts/release/run_native_smoke.sh path on every release target"
      pattern: "run_native_smoke\\.sh"
    - from: ".github/workflows/phase3-go-wrapper-smoke.yml"
      to: "gsd/phase-12-high-value-dom-navigation-and-simd-utility-apis"
      via: "on.push.branches entry"
      pattern: "phase-12-high-value-dom-navigation"
---

<objective>
Close the Phase 12 cross-platform and normative ABI contract after all nine native exports exist:
durable x86 minify proof, full native smoke invocation, automatic five-platform Go coverage, and
truthful ABI 1.3 documentation.

Purpose: separating CI/smoke/docs closeout from plan 12-04 keeps native utility implementation below
the file-surface warning and gives cross-platform verification one coherent owner.
Output: durable D-14 probe/script, two correctly-triggered workflows, expanded C smoke, workflow
contract tests, and completed ABI 1.3 documentation.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-RESEARCH.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-04-SUMMARY.md
@.planning/spikes/004-minify-buffer-safety/README.md
@.planning/spikes/CONVENTIONS.md

<interfaces>
The nine public signatures are the generated declarations produced by plans 12-01 through 12-04:
element_at_pointer, element_at_path, element_at_path_wildcard, value_views_free, array_at,
array_len, object_size, minify, and validate_utf8. Read include/pure_simdjson.h after
`make generate-header` and copy those declarations exactly into the smoke typedefs.

tests/smoke/ffi_export_surface.c already uses one closed-world structure for every public export:
enum index -> EXPORT_NAMES -> exact function-pointer typedef -> export_table field -> RESOLVE ->
mark_called -> require_called. Every new function must participate in every stage and be invoked
with a semantic assertion; name-only resolution is insufficient.

.github/workflows/phase3-go-wrapper-smoke.yml already contains the five required jobs:
linux-amd64, linux-arm64, darwin-amd64, darwin-arm64, and windows-amd64 Go race tests. Preserve
those jobs and add the current Phase 12 branch to its push trigger.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote D-14 into a durable, path-triggered native CI gate</name>
  <files>tests/native/minify_buffer_safety_probe.cpp, scripts/ci/verify_minify_buffer_safety.sh, .github/workflows/phase2-rust-shim-smoke.yml, scripts/release/test_release_workflow_contracts.py</files>
  <read_first>
    .planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md (D-13/D-14)
    .planning/spikes/004-minify-buffer-safety/probe.cpp (the 12 validated per-kernel cases)
    .planning/spikes/004-minify-buffer-safety/verify.sh (sanitizer build/run shape and the
    host-specific total=24 assertion that must not be copied)
    .github/workflows/phase2-rust-shim-smoke.yml (linux-smoke and on.push.paths)
    scripts/release/test_release_workflow_contracts.py (existing unittest style)
  </read_first>
  <action>
    Create tests/native/minify_buffer_safety_probe.cpp by adapting the validated Spike 004 probe.
    Preserve exactly its 12 exact-alias/disjoint/boundary checks per supported simdjson
    implementation; the bridge partial-overlap contract remains in plan 12-04's Rust tests. Emit a
    stable machine-readable summary with the ordered supported-kernel names, cases per kernel,
    actual total, failures, and sanitizer-clean completion. Comments must describe the durable
    behavioral contract and must not depend on a `.planning` path.
    Create scripts/ci/verify_minify_buffer_safety.sh with `set -euo pipefail`. Compile the durable
    source with ASan+UBSan against the vendored simdjson source, run it three times, fail on any
    probe/sanitizer error, and require identical kernel sets/totals across runs. Derive expected
    total as `12 * supported_kernel_count`; never freeze `total=24`. On Linux x86_64/amd64 require
    `fallback` plus at least one of `haswell`/`westmere`; on other hosts validate the dynamic total
    without pretending x86 support exists.
    In phase2-rust-shim-smoke.yml add the durable script step to linux-smoke and add BOTH
    `scripts/ci/verify_minify_buffer_safety.sh` and `tests/native/**` to `on.push.paths`, in addition
    to the existing source/smoke paths. No production workflow may reference `.planning/spikes`.
    Extend scripts/release/test_release_workflow_contracts.py with a focused test that pins the two
    new push paths, the linux-smoke invocation, the absence of `.planning/spikes`, the dynamic
    supported-kernel accounting, and the no-`total=24` rule.
  </action>
  <acceptance_criteria>
    - Durable probe and verifier exist outside .planning and run three deterministic sanitizer passes
    - Expected total is computed as 12 times discovered supported kernels
    - Linux x86-64 requires fallback plus haswell or westmere
    - phase2 workflow push paths include the exact script and tests/native/**
    - Workflow contract test rejects any planning-local invocation or hard-coded total=24
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && bash -n scripts/ci/verify_minify_buffer_safety.sh && python3 scripts/release/test_release_workflow_contracts.py ReleaseWorkflowContractTests.test_phase12_native_smoke_gate_tracks_durable_minify_probe</automated>
  </verify>
  <done>D-14 is a durable, dynamically-counted x86 CI gate whose own source changes trigger the workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the ABI smoke, current-branch five-platform gate, and ABI 1.3 docs</name>
  <files>tests/smoke/ffi_export_surface.c, .github/workflows/phase3-go-wrapper-smoke.yml, scripts/release/test_release_workflow_contracts.py, docs/ffi-contract.md</files>
  <read_first>
    include/pure_simdjson.h (exact final ABI 1.3 declarations after plans 12-01..12-04)
    tests/smoke/ffi_export_surface.c (export enum/name/typedef/table/RESOLVE/call-coverage pipeline,
    main sample document, and cleanup path)
    scripts/release/run_native_smoke.sh (existing five-target compile/run ownership)
    .github/workflows/phase3-go-wrapper-smoke.yml (push branches and all five jobs)
    docs/ffi-contract.md (all `ABI 1.2`/`0x00010002` occurrences, version handshake, worked call
    sequences, and process-global selection text)
  </read_first>
  <action>
    Extend tests/smoke/ffi_export_surface.c's closed-world export enum, name table, exact
    function-pointer typedefs, export_table fields, RESOLVE calls, mark_called coverage, and cleanup
    for all nine ABI 1.3 exports. Exercise them semantically in the existing process: AtPointer
    resolves `/int` to 42; AtPath resolves `.obj.x` to 7; AtPathWildcard resolves `.arr[*]` in order
    and its allocated array is freed exactly once with value_views_free; ArrayAt selects index 1;
    ArrayLen reports 2; ObjectSize reports the sample root's direct field count; Minify turns a
    whitespace-bearing stack buffer into the expected compact bytes and length; ValidateUTF8
    reports valid bytes as 1 and a lone continuation byte as 0. Use the exact generated C
    signatures, initialize all outputs, and free any allocated wildcard array in cleanup if a later
    assertion fails. `require_called` must fail if any new export is resolved but not invoked.
    In phase3-go-wrapper-smoke.yml retain the existing Phase 3 and main branches and add the exact
    current branch `gsd/phase-12-high-value-dom-navigation-and-simd-utility-apis` under
    `on.push.branches`; retain all five Go race jobs unchanged.
    Extend scripts/release/test_release_workflow_contracts.py with constants/tests that require the
    Phase 12 branch, the exact five job IDs/runners, and for each of the nine exports its name,
    typedef/table resolution, `mark_called`, and semantic invocation in ffi_export_surface.c.
    In docs/ffi-contract.md make ABI 1.3/0x00010003 the current normative scope and strict loader
    minimum. State explicitly that ABI 1.2 is rejected before ABI-1.3-only lookup, while retaining
    ABI 1.2 descriptions only when labeled as historical Phase 11 additions. Update every
    current-tense compatibility/build-policy statement, require the complete earlier surface plus
    all nine new symbols, and say later additive ABI 1.x values are accepted only after complete
    binding. Add the wildcard array copy/free worked sequence and the exact Minify/ValidateUTF8
    alias, capacity, non-validating, CPU-rejection, and selection-lock contract from D-08..D-15.
  </action>
  <acceptance_criteria>
    - ffi_export_surface.c contains exact typedefs, resolution, calls, assertions, and cleanup for all nine exports
    - Runtime call coverage cannot pass when any ABI 1.3 export is uninvoked
    - phase3-go-wrapper-smoke.yml automatically runs on the exact Phase 12 branch and retains five platform jobs
    - docs current loader/build contract says ABI 1.3; ABI 1.2 appears only in clearly historical/rejected statements
    - docs list all nine symbols, wildcard ownership, exact-alias/disjoint-only minify safety, and CPU/lock semantics
    - Focused workflow/smoke contract tests, C compilation, and docs verification pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && cc -std=c11 -Wall -Wextra -Iinclude -c tests/smoke/ffi_export_surface.c -o target/phase12-ffi-export-surface.o && python3 scripts/release/test_release_workflow_contracts.py && make verify-docs</automated>
  </verify>
  <done>The native smoke invokes the full ABI 1.3 surface, Phase 12 pushes run the five-platform Go race gate, and the normative contract consistently describes current ABI 1.3 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staged shared library -> external C smoke | Every claimed public symbol is dynamically resolved and invoked through its exact C signature |
| Repository paths -> GitHub Actions triggers | A security/safety gate is ineffective if edits to its script or probe do not schedule the workflow |
| Vendored SIMD implementation -> sanitizer probe | Kernel-specific in-place writes are checked under ASan/UBSan before the cross-platform claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-ALIAS | Tampering | Kernel-specific dst==src minification | mitigate | Three-run durable ASan/UBSan probe with dynamic per-kernel totals and explicit Linux x86 kernel requirements (D-13/D-14) |
| T-12-TRIGGER | Repudiation / Availability | D-14 script or probe changes bypassing CI | mitigate | Add exact script and tests/native/** push paths and pin them with workflow-contract tests |
| T-12-ABI-SMOKE | Tampering | ABI 1.3 artifact resolves symbols with wrong signatures or dead/unusable implementations | mitigate | Exact C typedefs plus semantic invocation and require_called coverage for every new export on the existing native smoke path |
| T-12-SC | Tampering | Workflow/action supply chain | accept | No new action, package, or external dependency; existing pinned workflow actions and vendored source remain unchanged |
</threat_model>

<verification>
- Task checks stay focused: shell syntax/static workflow contracts, C compile, and docs lint
- Wave 6 integration gate runs `cargo build --release && go test ./... -race` after plans 12-05
  and 12-10 complete
- The automatic Phase 12 branch run of phase3-go-wrapper-smoke must finish with all five jobs green
- Linux phase2-rust-shim-smoke must run the durable D-14 step successfully
</verification>

<success_criteria>
The D-14 gate is durable and self-triggering, every ABI 1.3 export is called by the existing
cross-platform native smoke harness, all five Go wrapper jobs run automatically on the Phase 12
branch, and current normative documentation consistently requires ABI 1.3.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-10-SUMMARY.md` when done
</output>
