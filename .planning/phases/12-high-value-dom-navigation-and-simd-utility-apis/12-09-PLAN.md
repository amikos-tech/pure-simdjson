---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - cbindgen.toml
  - include/pure_simdjson.h
  - tests/abi/handle_layout.c
  - tests/abi/check_header.py
  - tests/abi/test_check_header.py
  - docs/ffi-contract.md
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03, DOM-04, UTIL-01, UTIL-02]

must_haves:
  truths:
    - "The packed native/header ABI version is 0x00010003 before any Phase 12 export is implemented"
    - "PURE_SIMDJSON_ERR_INVALID_PATH=11 and PURE_SIMDJSON_ERR_INDEX_OUT_OF_RANGE=12 are additive, append-only, and distinguishable per D-01"
    - "Rust numeric tests, the generated-header macro, the C layout assertions, and Python contract fixtures agree exactly on ABI 1.3 and status values 11/12"
    - "The normative error-code table describes the two new statuses without weakening or renumbering any existing ABI value"
  artifacts:
    - path: "src/lib.rs"
      provides: "PURE_SIMDJSON_ABI_VERSION 0x0001_0003 plus additive status variants 11 and 12"
    - path: "include/pure_simdjson.h"
      provides: "Generated ABI 1.3 macro and new public error-code declarations"
    - path: "tests/abi/handle_layout.c"
      provides: "C compile-time pins for ABI 1.3 and status values 11/12"
    - path: "tests/abi/check_header.py"
      provides: "ABI 1.3 generated-header rule"
    - path: "docs/ffi-contract.md"
      provides: "ABI 1.3 scope/current-version statement and error-code rows 11/12"
  key_links:
    - from: "src/lib.rs PURE_SIMDJSON_ABI_VERSION"
      to: "cbindgen.toml after_includes"
      via: "both sources explicitly pin 0x00010003 because cbindgen does not derive the macro from the Rust const"
      pattern: "0x00010003|0x0001_0003"
    - from: "cbindgen.toml"
      to: "include/pure_simdjson.h"
      via: "make generate-header"
      pattern: "PURE_SIMDJSON_ABI_VERSION 0x00010003"
---

<objective>
Establish the small, independently-verifiable Phase 12 ABI foundation before navigation or utility
implementation begins: ABI 0x00010003 and the two locked navigation statuses.

Purpose: splitting numeric/header contract work out of plan 12-01 keeps the navigation executor
below the file-surface warning while preserving contract-first ordering.
Output: synchronized Rust, cbindgen, generated C header, C static assertions, Python fixtures, and
normative error-code documentation for ABI 1.3.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-RESEARCH.md

<interfaces>
From src/lib.rs:
`pub const PURE_SIMDJSON_ABI_VERSION: u32 = 0x0001_0002;` is the authoritative
native value before this plan. `pure_simdjson_get_abi_version` returns that constant.

From cbindgen.toml:
`after_includes` contains a hand-written `#define PURE_SIMDJSON_ABI_VERSION ...`; it is not
derived from the Rust constant and must change in the same task.

From tests/abi/check_header.py:
`ABI_VERSION_DEFINE_RE` and the diagnostic-surface failure message independently pin the generated
header macro. tests/abi/test_check_header.py carries a separate `ABI_VERSION_DEFINE` fixture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the Rust/generated-C ABI 1.3 numeric foundation</name>
  <files>src/lib.rs, cbindgen.toml, include/pure_simdjson.h, tests/abi/handle_layout.c</files>
  <read_first>
    .planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md (D-01 and the
    additive-ABI boundary are locked)
    .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md (ABI 1.2
    append-only precedent and Phase 16 release boundary)
    src/lib.rs (ABI constant, error enum, abi_version_getter_returns_the_pinned_constant, and
    public_enum_values_are_append_only)
    cbindgen.toml (after_includes ABI macro)
    tests/abi/handle_layout.c (ABI and public enum static assertions)
  </read_first>
  <action>
    In src/lib.rs change `PURE_SIMDJSON_ABI_VERSION` from `0x0001_0002` to `0x0001_0003`. Add
    `PURE_SIMDJSON_ERR_INVALID_PATH = 11` and
    `PURE_SIMDJSON_ERR_INDEX_OUT_OF_RANGE = 12` immediately after
    `PURE_SIMDJSON_ERR_KERNEL_LOCKED = 10`, per D-01 and the research-selected reserved-band
    placement. Update `abi_version_getter_returns_the_pinned_constant` to assert 0x0001_0003 for
    both the constant and getter result, and extend `public_enum_values_are_append_only` with exact
    numeric pins for 11 and 12. Do not renumber or remove any existing value.
    In cbindgen.toml change the hand-written ABI macro to `0x00010003`, then run
    `make generate-header` so include/pure_simdjson.h is regenerated from source rather than
    hand-edited. In tests/abi/handle_layout.c update the ABI static assertion to `0x00010003u` and
    add adjacent static assertions for both new status values.
  </action>
  <acceptance_criteria>
    - src/lib.rs contains ABI 0x0001_0003 and status values 11/12
    - Rust tests pin the getter, constant, and append-only numeric values
    - cbindgen.toml and include/pure_simdjson.h contain ABI 0x00010003
    - tests/abi/handle_layout.c statically asserts ABI 1.3 and both new statuses
    - No existing ABI enum value or layout changes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && cargo test --lib abi_version_getter_returns_the_pinned_constant && cargo test --lib public_enum_values_are_append_only && make generate-header && cc -Iinclude tests/abi/handle_layout.c -c -o target/phase12-handle-layout.o</automated>
  </verify>
  <done>The authoritative Rust value, generated header macro, Rust numeric tests, and C static assertions agree on ABI 1.3 plus additive status values 11/12.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize Python contract fixtures and normative status documentation</name>
  <files>tests/abi/check_header.py, tests/abi/test_check_header.py, docs/ffi-contract.md</files>
  <read_first>
    tests/abi/check_header.py (ABI_VERSION_DEFINE_RE and rule_diag_surface ABI message)
    tests/abi/test_check_header.py (ABI_VERSION_DEFINE fixture)
    docs/ffi-contract.md (scope line, Error code space table, and current packed-version line)
  </read_first>
  <action>
    Update tests/abi/check_header.py's ABI regex and diagnostic message to require 0x00010003.
    Update tests/abi/test_check_header.py's separate ABI_VERSION_DEFINE fixture to the same value.
    In docs/ffi-contract.md update the scope and current packed-version statements to ABI 1.3 /
    0x00010003. Insert error-table rows 11 and 12 after KERNEL_LOCKED and before the existing
    status-32 band: INVALID_PATH means malformed RFC 6901 or simdjson dot/index syntax;
    INDEX_OUT_OF_RANGE means a syntactically valid array index/path segment exceeds bounds.
    Leave clearly historical ABI 1.2 descriptions intact. Plan 12-10 owns the complete
    current-loader mandatory-symbol statement once every Phase 12 export exists.
  </action>
  <acceptance_criteria>
    - Both Python contract sources require `#define PURE_SIMDJSON_ABI_VERSION 0x00010003`
    - docs/ffi-contract.md identifies the current packed ABI as 0x00010003
    - docs/ffi-contract.md contains exact rows for INVALID_PATH=11 and INDEX_OUT_OF_RANGE=12
    - Historical ABI 1.2 notes remain clearly historical
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && python3 tests/abi/test_check_header.py && python3 tests/abi/check_header.py --rule diag-surface include/pure_simdjson.h && make verify-docs</automated>
  </verify>
  <done>Python header-contract fixtures and the normative status/version text are synchronized with the ABI 1.3 numeric foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated Rust ABI -> C consumers | Numeric version/status values cross language and build-system boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-ABI-NUM | Tampering / Availability | Divergent Rust, cbindgen, C, Python, or docs ABI pins | mitigate | Pin the same values in independent behavior/static/contract checks and regenerate the header; Wave 1 runs the complete contract suite |
| T-12-ABI-WAVE | Repudiation | ABI 1.3 numeric claim exists before the complete symbol surface | mitigate | All Phase 12 wave commits remain branch-local and unpublished; plan 12-10 closes the complete smoke/docs contract, and project policy uses a squash merge |
| T-12-SC | Tampering | Rust/C++ toolchain supply chain | accept | No package installation or dependency change; this plan only updates repository-owned contract sources |
</threat_model>

<verification>
- Task checks remain focused and below the task-feedback warning threshold
- Wave 1 gate: `make verify-contract`
</verification>

<success_criteria>
ABI 1.3 and the two D-01 statuses are independently pinned across Rust, generated C, C compilation,
Python contract fixtures, and normative documentation without changing any pre-existing ABI value.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-09-SUMMARY.md` when done
</output>
