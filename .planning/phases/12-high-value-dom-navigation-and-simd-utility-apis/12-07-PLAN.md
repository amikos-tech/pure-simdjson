---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 07
type: execute
wave: 6
depends_on: ["12-05"]
files_modified:
  - minify.go
  - utf8.go
  - minify_test.go
  - utf8_test.go
autonomous: true
requirements: [UTIL-01, UTIL-02]

must_haves:
  truths:
    - "Minify(data) is the simple allocating convenience; MinifyInto(dst, src) is the explicit buffer-reuse variant where dst may alias src for in-place minification (D-08)"
    - "MinifyInto is exercised by an overlap (dst==src) test as a first-class supported case, and an undersized dst returns an error rather than silently truncating (D-09)"
    - "Minify/MinifyInto are not gated by any Phase 14 zero-copy benchmark rule -- they operate on plain caller-owned byte slices with no Doc/Parser lifetime coupling (D-10)"
    - "ValidateUTF8 returns (bool, error), not a bare bool and not a Type()/TypeErr()-style dual method; its only realistic failure mode is first-call library resolution (D-11, D-12)"
    - "Minify, MinifyInto, and ValidateUTF8 all return ErrCPUUnsupported on an unsupported CPU and lock kernel selection on first success, exactly like NewParser; this behavior change is stated explicitly in all three doc comments (D-15)"
  artifacts:
    - path: "minify.go"
      provides: "Minify(data []byte) ([]byte, error); MinifyInto(dst, src []byte) (int, error)"
    - path: "utf8.go"
      provides: "ValidateUTF8(data []byte) (bool, error)"
    - path: "minify_test.go"
      provides: "Overlap, empty-input, malformed-input, undersized-dst, and CPU-gate tests"
    - path: "utf8_test.go"
      provides: "Valid/invalid/empty UTF-8 tests and a CPU-gate test"
  key_links:
    - from: "minify.go MinifyInto"
      to: "kernel.go kernelMu / kernelSelectionLocked"
      via: "the identical acquire-kernelMu / activeLibrary() / call / set-locked-on-success shape newParserWithConfig uses"
      pattern: "kernelSelectionLocked = true"
    - from: "utf8.go ValidateUTF8"
      to: "library_loading.go activeLibrary()"
      via: "the first standalone (no prior NewParser) caller of activeLibrary() alongside minify.go"
      pattern: "activeLibrary()"
---

<objective>
Expose the two standalone SIMD utility entry points as public `purejson` functions: `Minify`/
`MinifyInto` (UTIL-01, D-08/D-09/D-10) and `ValidateUTF8` (UTIL-02, D-11/D-12), both enforcing the
D-15 CPU-unsupported gate and kernel-selection lock the native layer (plan 12-04) already provides.

Purpose: these are the first public `purejson` functions with no `Doc`/`Element`/`Parser` receiver —
new top-level files following `kernel.go`'s established `activeLibrary()`-first style, not attached
to element.go. Grouping them in one plan is deliberate: both are small, standalone, and structurally
near-identical (same CPU-gate/kernel-lock shape), and they touch entirely disjoint files from plan
12-06's navigation work, letting the two plans run in the same wave.
Output: `Minify`, `MinifyInto`, `ValidateUTF8` public functions with honest doc comments and a test
suite covering overlap-aliasing, buffer sizing, malformed input, and the CPU-unsupported gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-RESEARCH.md

<interfaces>
<!-- Existing Go-layer patterns this plan clones. -->

From parser.go (newParserWithConfig, ~line 40-62 — the EXACT acquire-kernelMu/activeLibrary/call/
lock-on-success shape Minify/MinifyInto/ValidateUTF8 must copy):
```go
func newParserWithConfig(config parserConfig) (*Parser, error) {
	kernelMu.Lock()
	defer kernelMu.Unlock()
	library, err := activeLibrary()
	if err != nil { return nil, err }
	handle, rc := library.bindings.ParserNewConfigured(config.maxCapacity, config.maxDepth)
	if err := wrapStatus(rc); err != nil { return nil, err }
	kernelSelectionLocked = true
	...
}
```
The CPU-unsupported check happens NATIVELY inside the bindings call (plan 12-04's
implementation_selection_state() check in C++, plus the Rust reject_fallback_implementation() test
hook) — `wrapStatus(rc)` already turns `PURE_SIMDJSON_ERR_CPU_UNSUPPORTED` into `ErrCPUUnsupported`
via the existing `sentinelForStatus` case (errors.go, unchanged by this plan). No new Go-side CPU
check is needed — just this exact two-step shape: acquire kernelMu, call activeLibrary(), call the
bindings method, and on success set kernelSelectionLocked = true.

From kernel.go (kernelMu, kernelSelectionLocked package vars, ~line 10-13 — reused unmodified,
already exported at package scope):
```go
var (
	kernelMu              sync.Mutex
	kernelSelectionLocked bool
)
```

From library_loading.go (activeLibrary, ~line 57-68 — the resolution path Minify/MinifyInto/
ValidateUTF8 become the FIRST-EVER non-NewParser callers of):
```go
func activeLibrary() (*loadedLibrary, error) {
	return activeLibraryWithOps(libraryLoadOps{ ... })
}
```

Bindings methods available from plan 12-05: `Minify(dst, src []byte) (int, int32)` and
`ValidateUTF8(data []byte) (bool, int32)`.

From kernel_test.go (runKernelScenario, ~line 347-361, and TestKernelAutomaticFallbackRejected,
~line 124-137 — the subprocess-isolated test pattern for CPU-gate tests, since kernel selection is
process-global and can only be tested once per process):
```go
func TestKernelAutomaticFallbackRejected(t *testing.T) {
	runKernelScenario(t, "automatic-fallback", func(t *testing.T) {
		t.Setenv("PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION", "fallback")
		parser, err := NewParser()
		if parser != nil { _ = parser.Close(); t.Fatal("NewParser() returned a parser for automatic fallback") }
		if !errors.Is(err, ErrCPUUnsupported) { t.Fatalf("NewParser() error = %v, want ErrCPUUnsupported", err) }
	})
}
```
Minify/ValidateUTF8's CPU-gate tests must use this SAME `runKernelScenario` + `t.Setenv` pattern,
calling `Minify`/`ValidateUTF8` in place of `NewParser`.

Existing errors.go default-case comment that becomes stale once MinifyInto ships (~line 254-257):
```go
default:
    // ErrInvalidArg and ErrBufferTooSmall indicate a bug in the Go wrapper,
    // not user error; they intentionally map to ErrInternal.
    return ErrInternal
```
Do not change this mapping (no new sentinel per D-01's scope, which is limited to the two path/index
sentinels) — but MinifyInto's own doc comment must state plainly that an undersized dst surfaces as
an error unwrapping to ErrInternal via this existing status mapping, since that mapping is no longer
purely "a bug in the Go wrapper" once a real external caller can trigger it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: minify.go and utf8.go — public standalone entry points</name>
  <files>minify.go, utf8.go</files>
  <behavior>
    - Minify(data) returns a minified copy without mutating data
    - MinifyInto(dst, src) with dst == src (aliased, in-place) succeeds and produces the same output as the non-aliased path
    - MinifyInto with len(dst) < len(src) returns a non-nil error
    - MinifyInto on empty src returns (0, nil)
    - ValidateUTF8 on valid UTF-8 returns (true, nil); on invalid UTF-8 returns (false, nil), not an error
    - After a successful Minify/MinifyInto/ValidateUTF8 call, SetKernel returns ErrKernelLocked
  </behavior>
  <read_first>
    kernel.go (full file — SetKernel/lockKernelSelection, the activeLibrary()-first pattern)
    parser.go (newParserWithConfig, ~line 40-62 — copy this exact shape per this plan's
    &lt;interfaces&gt; block)
    internal/ffi/bindings.go (Minify, ValidateUTF8 methods from plan 12-05)
    errors.go (the default-case comment on ErrBufferTooSmall/ErrInvalidArg, ~line 254-257 — read
    before writing MinifyInto's doc comment about undersized-dst behavior)
  </read_first>
  <action>
    Create minify.go (new file, package purejson) with `func Minify(data []byte) ([]byte, error)`:
    allocate `dst := make([]byte, len(data))`, call `MinifyInto(dst, data)`, on error return `nil,
    err`, else return `dst[:n], nil`. Doc comment: SIMD-accelerated, allocating convenience; does not
    validate JSON (only unterminated strings are detected — a nil error does not mean data was valid
    JSON); states the CPU-unsupported rejection and kernel-selection-locking behavior verbatim (see
    below).
    Add `func MinifyInto(dst, src []byte) (int, error)`: `kernelMu.Lock(); defer kernelMu.Unlock()`,
    `library, err := activeLibrary()`, return early on error, `written, rc :=
    library.bindings.Minify(dst, src)`, `if err := wrapStatus(rc); err != nil { return 0, err }`,
    `kernelSelectionLocked = true`, `return written, nil`. Doc comment: dst may alias src (dst == src,
    or any overlap) for in-place minification, byte-identical to the non-aliased path (cite D-13/D-14
    spike evidence); dst must be at least len(src) bytes, a smaller dst returns an error (unwrapping
    to ErrInternal via the existing buffer-too-small status mapping) instead of silently truncating or
    writing out of bounds; states the SAME CPU-unsupported/kernel-lock behavior as Minify.
    The CPU-unsupported/kernel-lock doc sentence (verbatim, adapted per function, in BOTH Minify and
    MinifyInto): "Minify/MinifyInto trigger the same CPU-unsupported rejection NewParser uses: on an
    unsupported CPU they return ErrCPUUnsupported instead of silently running the slow fallback
    kernel. They lock kernel selection on their first successful call, exactly like NewParser, so
    SetKernel returns ErrKernelLocked afterward."
    Create utf8.go (new file, package purejson) with `func ValidateUTF8(data []byte) (bool, error)`:
    identical kernelMu/activeLibrary/call/lock-on-success shape, calling
    `library.bindings.ValidateUTF8(data)`. Doc comment: reports whether data is valid UTF-8 using
    simdjson's SIMD validator; returns a non-nil error ONLY when the native library cannot be
    resolved (first-call bootstrap/ABI failure) — invalid UTF-8 input itself is reported as (false,
    nil), never an error; states the identical CPU-unsupported/kernel-lock sentence as Minify.
  </action>
  <acceptance_criteria>
    - minify.go exports `Minify(data []byte) ([]byte, error)` and `MinifyInto(dst, src []byte) (int, error)`
    - utf8.go exports `ValidateUTF8(data []byte) (bool, error)`
    - MinifyInto's doc comment states dst may alias src
    - All three functions' doc comments contain the literal substring "ErrKernelLocked"
    - `go build .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go build . && go vet . && grep -l "ErrKernelLocked" minify.go utf8.go</automated>
  </verify>
  <done>Minify, MinifyInto, and ValidateUTF8 are implemented with honest doc comments covering aliasing, buffer sizing, validation scope, and the D-15 CPU-gate/kernel-lock behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: minify_test.go and utf8_test.go</name>
  <files>minify_test.go, utf8_test.go</files>
  <read_first>
    kernel_test.go (runKernelScenario ~line 347-361; TestKernelAutomaticFallbackRejected ~line
    124-137; TestKernelExplicitFallback ~line 102-122 for the SetKernel-after-lock assertion shape)
    element_scalar_test.go (table-test shape for the non-subprocess tests)
  </read_first>
  <action>
    Create minify_test.go (new file). Add `TestMinify` (table cases: `{"a":  1,   "b": 2}` minifies
    to `{"a":1,"b":2}`; empty input minifies to empty output; input with insignificant whitespace
    only around structural tokens). Add `TestMinifyInto_Overlap` (build a buffer containing the
    unminified JSON bytes, call `MinifyInto(buf, buf)` with dst and src as the SAME slice, assert the
    result equals `Minify` on a fresh unminified copy of the same input — this is the required
    "overlap" proof for D-09/success criterion 4, do not skip it as "already proven at the Rust
    layer"). Add `TestMinifyInto_UndersizedDst` (dst one byte shorter than len(src), assert a non-nil
    error). Add `TestMinifyInto_Empty` (src length 0, assert `(0, nil)`). Add
    `TestMinify_UnclosedString` (malformed input with an unterminated string, assert a non-nil error
    — note per Pitfall 6 this does NOT mean Minify validates JSON generally, only this one specific
    case is detectable; do not assert an error on other malformed-but-string-terminated inputs like
    `{"a":tru}`).
    Add `TestMinifyAutomaticFallbackRejected` following kernel_test.go's exact `runKernelScenario` +
    `t.Setenv("PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION", "fallback")` pattern, calling `Minify([]byte("{}"))`
    in place of `NewParser()`, asserting `errors.Is(err, ErrCPUUnsupported)`. Add
    `TestMinifyLocksKernelSelection` in its own `runKernelScenario` subprocess: call `Minify([]byte("{}"))`
    successfully, then assert `SetKernel("")` returns an error satisfying `errors.Is(err, ErrKernelLocked)`.
    Create utf8_test.go (new file). Add `TestValidateUTF8` (table cases: valid ASCII/multi-byte UTF-8
    -> `(true, nil)`; a lone continuation byte e.g. `[]byte{0x80}` -> `(false, nil)`; empty input ->
    `(true, nil)` per simdjson's own definition of empty-is-valid). Add
    `TestValidateUTF8AutomaticFallbackRejected` and `TestValidateUTF8LocksKernelSelection` mirroring
    the two Minify subprocess tests above, substituting `ValidateUTF8([]byte("ok"))`.
  </action>
  <acceptance_criteria>
    - `go test -run 'TestMinify|TestValidateUTF8' ./... -v` exits 0
    - TestMinifyInto_Overlap asserts dst==src output equals the non-aliased Minify output byte-for-byte
    - TestMinifyAutomaticFallbackRejected and TestValidateUTF8AutomaticFallbackRejected both assert errors.Is(err, ErrCPUUnsupported)
    - TestMinifyLocksKernelSelection and TestValidateUTF8LocksKernelSelection both assert errors.Is(err, ErrKernelLocked) from a subsequent SetKernel call
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go test -run 'TestMinify|TestValidateUTF8' ./... -v</automated>
  </verify>
  <done>Minify/MinifyInto/ValidateUTF8 are proven correct by passing tests covering the D-09 overlap case, buffer sizing, malformed-input scope, and the D-15 CPU-gate/kernel-lock contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go public API -> untrusted caller byte buffers | Minify/MinifyInto/ValidateUTF8 accept arbitrary caller-controlled byte slices with no Doc/Parser lifetime coupling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Tampering | Undersized dst passed to MinifyInto from Go | mitigate | Forwarded to the native dst_cap pre-check (plan 12-04), which rejects before any write; Go performs no separate redundant length check, keeping one authoritative bounds-check location per RESEARCH.md's Don't-Hand-Roll guidance |
| T-12-17 | Denial of Service / silent kernel downgrade | Minify/ValidateUTF8 called on an unsupported CPU without ever calling NewParser first | mitigate | Both functions route through the same kernelMu/activeLibrary()/native-CPU-gate path NewParser uses (plan 12-04's implementation_selection_state() check), returning ErrCPUUnsupported rather than silently running the slow fallback kernel (D-15) |
| T-12-18 | Repudiation | Caller treating a nil Minify error as proof of valid JSON | accept | Documented explicitly in Minify's doc comment and proven by TestMinify_UnclosedString's narrow scope; this is upstream's own minifier design (a non-validating scanner), not a project defect |
</threat_model>

<verification>
- `go build .`, `go vet .` pass
- `go test -run 'TestMinify|TestValidateUTF8' ./...` passes
</verification>

<success_criteria>
Minify, MinifyInto, and ValidateUTF8 are public, honestly documented, and proven correct including
the D-09 overlap-aliasing requirement and the D-15 CPU-unsupported/kernel-lock contract.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-07-SUMMARY.md` when done
</output>
