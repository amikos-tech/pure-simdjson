---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 05
type: execute
wave: 6
depends_on: ["12-04"]
files_modified:
  - internal/ffi/types.go
  - internal/ffi/types_test.go
  - internal/bootstrap/version.go
  - internal/bootstrap/abi_assertion.go
  - docs/bootstrap.md
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03, DOM-04, UTIL-01, UTIL-02]

must_haves:
  truths:
    - "Go ABI constants and numeric tests pin ABI 0x00010003 and error codes 11/12 in lockstep with native source (D-01)"
    - "Phase 12 uses an honest unreleased `bootstrap.Version = 0.2.0-dev` source pin for ABI 1.3; it neither downloads a published ABI 1.2 artifact nor publishes/tags the final 0.2.0 release reserved for Phase 16"
    - "docs/bootstrap.md identifies the current source tree as unpublished 0.2.0-dev/ABI 1.3, keeps v0.1.7/ABI 1.2 only as published historical context, and requires a fresh local-library override for source-tree runtime tests"
  artifacts:
    - path: "internal/ffi/types.go"
      provides: "ErrInvalidPath=11, ErrIndexOutOfRange=12 ErrorCode consts; ABIVersion=0x00010003"
    - path: "internal/ffi/types_test.go"
      provides: "Go ABI 1.3 and error-code numeric contract tests"
    - path: "internal/bootstrap/version.go"
      provides: "Unreleased 0.2.0-dev source pin for the ABI 1.3 tree"
    - path: "internal/bootstrap/abi_assertion.go"
      provides: "Compile-time 0.2.0-dev -> ABI 1.3 equality canary"
    - path: "docs/bootstrap.md"
      provides: "Current 0.2.0-dev/ABI 1.3 source-bootstrap instructions and explicit local-library override"
  key_links:
    - from: "internal/bootstrap/abi_assertion.go"
      to: "internal/ffi/types.go"
      via: "bidirectional compile-time equality assertions between the 0.2.0-dev source pin and ffi.ABIVersion"
      pattern: "ffi\\.ABIVersion"
    - from: "docs/bootstrap.md current source-state section"
      to: "internal/bootstrap/version.go"
      via: "documents the exact 0.2.0-dev identity and why published ABI 1.2 artifacts cannot satisfy the ABI 1.3 wrapper"
      pattern: "0\\.2\\.0-dev"
---

<objective>
Establish the Go-side ABI 1.3 identity and the honest unpublished `0.2.0-dev` bootstrap source state
before any Go binding or loader integration consumes that identity.

Purpose: keep the ABI/bootstrap policy and documentation boundary coherent and executable in five
files, then hand the stable contract to dependency-linked plan 12-11.
Output: Go ABI/status constants and tests, the bootstrap version/canary, and current bootstrap
documentation; no binding, loader, release-checker, or public `purejson` API changes yet.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-PATTERNS.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-09-SUMMARY.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-04-SUMMARY.md
@docs/releases.md

<interfaces>
From internal/ffi/types.go: `ABIVersion` is the Go loader's required packed ABI and the `ErrorCode`
block is additive. Plan 12-09 has already established native ABI 0x00010003 plus status values
11/12; this plan makes the Go numeric mirror exact.

From internal/bootstrap/abi_assertion.go: two compile-time array-length assertions enforce equality
between the bootstrap-version ABI canary and `ffi.ABIVersion`. Update the version-named canary and
both directions together so an identity mismatch remains a compile error.

From docs/releases.md: publication remains `main -> strict readiness -> annotated tag -> CI`; this
plan performs no publication step. Phase 16 retains final 0.2.0 publication and post-publish proof.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Go ABI 1.3 and document the unreleased 0.2.0-dev bootstrap state</name>
  <files>internal/ffi/types.go, internal/ffi/types_test.go, internal/bootstrap/version.go, internal/bootstrap/abi_assertion.go, docs/bootstrap.md</files>
  <read_first>
    .planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md (D-01)
    .planning/ROADMAP.md (Phase 16 owns final v0.2 release publication)
    .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md (published 0.1.7/ABI 1.2 compatibility baseline)
    internal/ffi/types.go (full file — small)
    internal/ffi/types_test.go (TestABINumericContract)
    internal/bootstrap/version.go (current published 0.1.7 pin and comment)
    internal/bootstrap/abi_assertion.go (compile-time 0.1.7 -> ABI 1.2 equality canary)
    docs/bootstrap.md ("ABI 1.2 Source State", local development override, and historical v0.1.7
    instructions; distinguish published history from current source behavior)
  </read_first>
  <action>
    Add `ErrInvalidPath ErrorCode = 11` and `ErrIndexOutOfRange ErrorCode = 12` to the `ErrorCode`
    const block, immediately after `ErrKernelLocked ErrorCode = 10`. Change `ABIVersion uint32 =
    0x00010002` to `0x00010003`. Do not touch `ValueView`, `InternalFrame`, `ArrayIter`,
    `ObjectIter`, or `NativeAllocStats` — none of the new Phase 12 exports change any struct layout.
    Update TestABINumericContract to assert ABI 0x00010003 and add exact numeric rows for
    ErrInvalidPath=11 and ErrIndexOutOfRange=12.
    As planner discretion, set `internal/bootstrap.Version` to `"0.2.0-dev"` and revise its comment
    to say this is the unreleased source-tree pin for ABI 1.3. This avoids asking the bootstrap
    loader for published v0.1.7 (ABI 1.2) while Phase 12 source requires ABI 1.3. Do not create a tag,
    upload an artifact, set Version to final `"0.2.0"`, or move Phase 16's release work here.
    Rename the bootstrap canary constant to
    `abiVersionForBootstrapVersion_0_2_0_dev`, set it to `0x00010003`, update the comment to the
    0.2.0-dev source state, and retain both compile-time array equality assertions against
    `ffi.ABIVersion`.
    Update docs/bootstrap.md's current-state section so it no longer directs the Phase 12 source
    tree to v0.1.7/ABI 1.2 or describes that already-published compatibility release as unproven.
    State that v0.1.7/ABI 1.2 is a published historical compatibility artifact, while the checked
    out source now identifies as unpublished `0.2.0-dev` and requires ABI 1.3. Explain that no
    0.2.0-dev checksum/artifact exists, so default bootstrap cannot satisfy this source state:
    repository developers must run `cargo build --release` and set
    `PURE_SIMDJSON_LIB_PATH` to the platform's freshly built target/release library (the root
    TestMain performs this override automatically when the artifact exists). Retain Phase 16 as the
    owner of final 0.2.0 publication and fresh-machine default-bootstrap proof. Keep v0.1.7 URLs and
    cosign examples only where they are explicitly labeled historical/published, never as the
    current wrapper's compatible default.
  </action>
  <acceptance_criteria>
    - internal/ffi/types.go contains `ErrInvalidPath ErrorCode = 11` and `ErrIndexOutOfRange ErrorCode = 12`
    - internal/ffi/types.go contains `ABIVersion             uint32 = 0x00010003`
    - TestABINumericContract pins ABI 1.3 and both new error-code numbers
    - internal/bootstrap/version.go is exactly `Version = "0.2.0-dev"` and abi_assertion.go pins that source state to ABI 1.3
    - docs/bootstrap.md states current `0.2.0-dev` + ABI `0x00010003`, requires `PURE_SIMDJSON_LIB_PATH` to a freshly built library for source-tree runtime tests, and labels v0.1.7/ABI 1.2 as historical rather than current-compatible
    - No release tag, publication command, or final `Version = "0.2.0"` is introduced
    - focused Go numeric/bootstrap tests and `make verify-docs` exit 0 without running the dependency-linked binding/loader regressions assigned to plan 12-11
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go test ./internal/ffi -run '^TestABINumericContract$' -count=1 && go test ./internal/bootstrap && make verify-docs && grep -n "ErrInvalidPath\\|ErrIndexOutOfRange\\|ABIVersion" internal/ffi/types.go && grep -n "0.2.0-dev\\|0x00010003" internal/bootstrap/version.go internal/bootstrap/abi_assertion.go docs/bootstrap.md</automated>
  </verify>
  <done>Go numeric pins, the unreleased bootstrap source canary, and current-state bootstrap documentation agree on ABI 1.3 without prematurely publishing or tagging v0.2.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go ABI identity -> bootstrap source state | The Go loader's required ABI must agree with the source version and compile-time bootstrap canary before later binding/loader integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-ABI-GO | Tampering / Availability | Divergent Go ABI/status constants and bootstrap canary | mitigate | Pin ABI 1.3 plus statuses 11/12 in focused numeric tests and retain bidirectional compile-time equality assertions |
| T-12-BOOT | Tampering / Availability | ABI 1.3 source bootstrapping a published ABI 1.2 artifact, or Phase 12 pretending final 0.2.0 already exists | mitigate | Source uses the explicit unreleased 0.2.0-dev pin and compile-time ABI 1.3 canary. Documentation requires a freshly built local override; no publication or tag occurs, and Phase 16 retains final 0.2.0 release ownership |
| T-12-SC | Tampering | Package/toolchain supply chain | accept | No package installation, dependency change, release upload, or tag operation occurs in this plan |
</threat_model>

<verification>
- Focused numeric/bootstrap tests and documentation checks pass without requiring plan 12-11 files
- Plan 12-11 consumes the completed ABI/bootstrap contract and owns the Wave 7 full Go integration gate
- docs/bootstrap.md truthfully requires the fresh local ABI 1.3 library for the unpublished source tree
</verification>

<success_criteria>
Go ABI/status pins, the bootstrap compile-time canary, and current source documentation agree on ABI
1.3 and unpublished 0.2.0-dev. The resulting five-file policy contract is ready for plan 12-11's
binding, release-checker, and loader integration without claiming a release has occurred.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-05-SUMMARY.md` when done
</output>
