---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified:
  - internal/ffi/types.go
  - internal/ffi/types_test.go
  - internal/ffi/bindings.go
  - internal/ffi/bindings_test.go
  - internal/bootstrap/version.go
  - internal/bootstrap/abi_assertion.go
  - scripts/release/check_bootstrap_abi_state.py
  - scripts/release/test_check_bootstrap_abi_state.py
  - library_loading_test.go
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03, DOM-04, UTIL-01, UTIL-02]

must_haves:
  truths:
    - "Go numeric tests, the bootstrap compile-time canary, the release policy checker, and loader fixtures all pin ABI 0x00010003 and error codes 11/12 in lockstep with native source (D-01)"
    - "Phase 12 uses an honest unreleased `bootstrap.Version = 0.2.0-dev` source pin with ABI 1.3 policy minimum 0.2.0; it neither downloads a published ABI 1.2 artifact nor publishes/tags the final 0.2.0 release reserved for Phase 16"
    - "internal/ffi/bindings.go registers all 9 new Phase 12 symbols as REQUIRED (not optional) via the mandatory symbols slice, since DOM-01..04/UTIL-01/02 are public documented API, not internal diagnostics"
    - "Binding tests require and exercise all 9 Phase 12 symbols, including wildcard copy/free and null/count handling, view-specific free warnings, and minify pointer/capacity marshaling"
  artifacts:
    - path: "internal/ffi/types.go"
      provides: "ErrInvalidPath=11, ErrIndexOutOfRange=12 ErrorCode consts; ABIVersion=0x00010003"
    - path: "internal/ffi/bindings.go"
      provides: "9 new required binding fields, registration entries, and typed wrapper methods: ElementAtPointer, ElementAtPath, ElementAtPathWildcard, ValueViewsFree, ArrayAt, ArrayLen, ObjectSize, Minify, ValidateUTF8"
    - path: "internal/bootstrap/version.go"
      provides: "Unreleased 0.2.0-dev source pin for the ABI 1.3 tree"
    - path: "internal/bootstrap/abi_assertion.go"
      provides: "Compile-time 0.2.0-dev -> ABI 1.3 equality canary"
    - path: "scripts/release/check_bootstrap_abi_state.py"
      provides: "ABI 1.3 minimum-version policy at 0.2.0"
    - path: "library_loading_test.go"
      provides: "ABI 1.2 rejection, ABI 1.3 complete/incomplete binding, and ABI 1.4 additive compatibility fixtures"
  key_links:
    - from: "internal/ffi/bindings.go Bindings.symbols"
      to: "the 9 new pure_simdjson_* exports landed in plans 12-01..12-04"
      via: "registerFuncWithRegistrar in the mandatory (non-optional) symbols slice"
      pattern: "pure_simdjson_element_at_pointer"
---

<objective>
Bind the 9 new native Phase 12 exports into the Go `internal/ffi` layer: 2 new error codes, the ABI
bump, and 9 required purego symbol registrations with typed wrapper methods. This plan is the
"contracts land before consumers" layer for the Go side — every subsequent Go-layer plan
(navigation, indexed access, minify, utf8) depends on this landing first and completely, since
`ffi.Bind` fails closed if any required symbol is missing from the loaded artifact.

Purpose: isolate the mechanical purego-binding work from the public API/doc-comment/behavior work in
later plans, so those plans can focus entirely on Go-idiomatic surface design without re-deriving
FFI marshaling.
Output: Go ABI/binding sources and tests, bootstrap/release policy canaries, and loader compatibility
fixtures updated coherently; no public `purejson` package API changes yet.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-PATTERNS.md

<interfaces>
<!-- The native exports this plan binds. Signatures from plans 12-01..12-04 (already landed by wave
5; read src/lib.rs directly to confirm the exact final parameter order before writing Go bindings). -->

Native signatures to bind (from include/pure_simdjson.h after `make generate-header` in plans
12-01..12-04):
- `pure_simdjson_element_at_pointer(const struct pure_simdjson_value_view_t *view, const uint8_t *pointer_ptr, size_t pointer_len, struct pure_simdjson_value_view_t *out_value)`
- `pure_simdjson_element_at_path(const struct pure_simdjson_value_view_t *view, const uint8_t *path_ptr, size_t path_len, struct pure_simdjson_value_view_t *out_value)`
- `pure_simdjson_element_at_path_wildcard(const struct pure_simdjson_value_view_t *view, const uint8_t *path_ptr, size_t path_len, struct pure_simdjson_value_view_t **out_views, size_t *out_count)`
- `pure_simdjson_value_views_free(struct pure_simdjson_value_view_t *ptr, size_t len)`
- `pure_simdjson_array_at(const struct pure_simdjson_value_view_t *array_view, uint64_t index, struct pure_simdjson_value_view_t *out_value)`
- `pure_simdjson_array_len(const struct pure_simdjson_value_view_t *array_view, uint64_t *out_len)`
- `pure_simdjson_object_size(const struct pure_simdjson_value_view_t *object_view, uint64_t *out_size)`
- `pure_simdjson_minify(const uint8_t *src_ptr, size_t src_len, uint8_t *dst_ptr, size_t dst_cap, size_t *out_written)`
- `pure_simdjson_validate_utf8(const uint8_t *data_ptr, size_t data_len, uint8_t *out_valid)`

From internal/ffi/bindings.go (ObjectGetField, ~line 500-516 — the exact string-key marshaling shape
ElementAtPointer/ElementAtPath copy):
```go
func (b *Bindings) ObjectGetField(view *ValueView, key string) (ValueView, int32) {
	var keyBytes []byte
	if key != "" { keyBytes = []byte(key) }
	var keyPtr *byte
	if len(keyBytes) != 0 { keyPtr = unsafe.SliceData(keyBytes) }
	var value ValueView
	rc := b.objectGetField(view, keyPtr, uintptr(len(keyBytes)), &value)
	runtime.KeepAlive(keyBytes)
	runtime.KeepAlive(view)
	runtime.KeepAlive(b)
	return value, rc
}
```

From internal/ffi/bindings.go (copyElementBytes, ~line 385-409 — the copy-then-free-native-buffer
shape ElementAtPathWildcard's Go wrapper follows, but for a []ValueView instead of a string):
```go
func (b *Bindings) copyElementBytes(view *ValueView, getter func(*ValueView, **byte, *uintptr) int32) (string, int32) {
	var ptr *byte
	var length uintptr
	rc := getter(view, &ptr, &length)
	runtime.KeepAlive(view)
	runtime.KeepAlive(b)
	if rc != int32(OK) { return "", rc }
	defer func() {
		if ptr == nil { return }
		if freeRC := b.BytesFree(ptr, length); freeRC != int32(OK) {
			emitBytesFreeFailureWarning(freeRC, length)
		}
	}()
	if ptr == nil && length == 0 { return "", int32(OK) }
	return string(unsafe.Slice(ptr, length)), int32(OK)
}
```

From internal/ffi/bindings.go (the required `symbols` slice, ~line 87-120, and the required-vs-
optional registration split ~line 127-145): all 9 new symbols go in the mandatory `symbols` slice
(registerFuncWithRegistrar), NEVER through `registerOptionalFuncWithRegistrar` (that path is
reserved for internal diagnostics allowed to be missing from released artifacts, e.g.
psdj_internal_materialize_build / native_alloc_stats_* — DOM-01..04/UTIL-01/02 are public API and a
Phase 12-claiming artifact missing any of them is corrupt per docs/ffi-contract.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Go ABI 1.3 and the unreleased 0.2.0-dev bootstrap canary</name>
  <files>internal/ffi/types.go, internal/ffi/types_test.go, internal/bootstrap/version.go, internal/bootstrap/abi_assertion.go</files>
  <read_first>
    .planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md (D-01)
    .planning/ROADMAP.md (Phase 16 owns final v0.2 release publication)
    .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md (published 0.1.7/ABI 1.2 compatibility baseline)
    internal/ffi/types.go (full file — small)
    internal/ffi/types_test.go (TestABINumericContract)
    internal/bootstrap/version.go (current published 0.1.7 pin and comment)
    internal/bootstrap/abi_assertion.go (compile-time 0.1.7 -> ABI 1.2 equality canary)
  </read_first>
  <action>
    Add `ErrInvalidPath ErrorCode = 11` and `ErrIndexOutOfRange ErrorCode = 12` to the `ErrorCode`
    const block, immediately after `ErrKernelLocked ErrorCode = 10`. Change `ABIVersion uint32 =
    0x00010002` to `0x00010003`. Do not touch `ValueView`, `InternalFrame`, `ArrayIter`,
    `ObjectIter`, or `NativeAllocStats` — none of the new Phase 12 exports change any struct layout.
    Update TestABINumericContract to assert ABI 0x00010003 and add exact numeric rows for
    ErrInvalidPath=11 and ErrIndexOutOfRange=12.
    As planner discretion, set `internal/bootstrap.Version` to `"0.2.0-dev"` and revise its comment
    to say this is the unreleased source-tree pin for ABI 1.3. This avoids asking the bootstrap
    loader for published v0.1.7 (ABI 1.2) while Phase 12 source requires ABI 1.3. Do not create a tag,
    upload an artifact, set Version to final `"0.2.0"`, or move Phase 16's release work here.
    Rename the bootstrap canary constant to
    `abiVersionForBootstrapVersion_0_2_0_dev`, set it to `0x00010003`, update the comment to the
    0.2.0-dev source state, and retain both compile-time array equality assertions against
    `ffi.ABIVersion`.
  </action>
  <acceptance_criteria>
    - internal/ffi/types.go contains `ErrInvalidPath ErrorCode = 11` and `ErrIndexOutOfRange ErrorCode = 12`
    - internal/ffi/types.go contains `ABIVersion             uint32 = 0x00010003`
    - TestABINumericContract pins ABI 1.3 and both new error-code numbers
    - internal/bootstrap/version.go is exactly `Version = "0.2.0-dev"` and abi_assertion.go pins that source state to ABI 1.3
    - No release tag, publication command, or final `Version = "0.2.0"` is introduced
    - `cargo build --release && go test ./internal/ffi ./internal/bootstrap` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && cargo build --release && go test ./internal/ffi ./internal/bootstrap && grep -n "ErrInvalidPath\\|ErrIndexOutOfRange\\|ABIVersion" internal/ffi/types.go && grep -n "0.2.0-dev\\|0x00010003" internal/bootstrap/version.go internal/bootstrap/abi_assertion.go</automated>
  </verify>
  <done>Go numeric pins and the unreleased bootstrap source canary agree on ABI 1.3 without prematurely publishing or tagging v0.2.0.</done>
</task>

<task type="auto">
  <name>Task 2: bindings.go — 9 required symbols and typed wrapper methods</name>
  <files>internal/ffi/bindings.go</files>
  <read_first>
    .planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md (D-01 through D-15)
    internal/ffi/bindings.go (full file — the Bindings struct fields ~line 15-54, the required
    `symbols` slice ~line 87-120, ObjectGetField ~line 500-516, copyElementBytes ~line 385-409,
    ArrayIterNew ~line 465-471 for a simple view-in/struct-out shape)
  </read_first>
  <action>
    Add 9 new unexported func fields to the `Bindings` struct: `elementAtPointer func(*ValueView,
    *byte, uintptr, *ValueView) int32`, `elementAtPath func(*ValueView, *byte, uintptr, *ValueView)
    int32`, `elementAtPathWildcard func(*ValueView, *byte, uintptr, **ValueView, *uintptr) int32`,
    `valueViewsFree func(*ValueView, uintptr) int32`, `arrayAt func(*ValueView, uint64, *ValueView)
    int32`, `arrayLen func(*ValueView, *uint64) int32`, `objectSize func(*ValueView, *uint64) int32`,
    `minify func(*byte, uintptr, *byte, uintptr, *uintptr) int32`, `validateUTF8 func(*byte,
    uintptr, *byte) int32`.
    Add 9 entries to the mandatory `symbols` slice (NOT the optional registration path) with names
    exactly `pure_simdjson_element_at_pointer`, `pure_simdjson_element_at_path`,
    `pure_simdjson_element_at_path_wildcard`, `pure_simdjson_value_views_free`,
    `pure_simdjson_array_at`, `pure_simdjson_array_len`, `pure_simdjson_object_size`,
    `pure_simdjson_minify`, `pure_simdjson_validate_utf8`, targeting the fields above.
    Add typed wrapper methods:
    `ElementAtPointer(view *ValueView, pointer string) (ValueView, int32)` and
    `ElementAtPath(view *ValueView, path string) (ValueView, int32)` — both copy ObjectGetField's
    exact string-to-byte-slice-to-pointer marshaling shape (empty string -> nil pointer, KeepAlive
    on the byte slice, view, and b).
    `ElementAtPathWildcard(view *ValueView, path string) ([]ValueView, int32)` — marshal `path` the
    same way, call `b.elementAtPathWildcard(view, pathPtr, uintptr(len(pathBytes)), &ptr, &count)`;
    on `rc != int32(OK)` return `nil, rc`; on `count == 0` return `[]ValueView{}, int32(OK)` (non-nil
    empty slice, matching D-02); on `ptr == nil` with `count != 0` return `nil, int32(ErrInternal)`;
    otherwise copy `unsafe.Slice(ptr, count)` into a freshly allocated `make([]ValueView, count)` Go
    slice, then free the native array via `b.ValueViewsFree(ptr, count)` (warn via
    a new `emitValueViewsFreeFailureWarning` on a non-OK free result, matching
    copyElementBytes's defer-free discipline without mislabeling a ValueView-array failure as a
    byte-buffer failure), and return the copied slice. Handle all output pairs explicitly:
    `(nil,0)` is a non-nil empty Go slice; `(nil,N)` and `(nonnull,0)` are internal-contract
    failures. On `(nonnull,0)`, attempt `ValueViewsFree(ptr,0)` and emit the view-specific warning
    before returning ErrInternal; never pass the pointer to `unsafe.Slice`.
    `ValueViewsFree(ptr *ValueView, count uintptr) int32` — one-line wrapper calling
    `b.valueViewsFree(ptr, count)` with `runtime.KeepAlive(b)`.
    `ArrayAt(view *ValueView, index uint64) (ValueView, int32)` — copies ArrayIterNew's simple
    view-in/struct-out shape.
    `ArrayLen(view *ValueView) (uint64, int32)` and `ObjectSize(view *ValueView) (uint64, int32)` —
    both copy ElementType's simple scalar-out-param shape (KeepAlive view and b).
    `Minify(dst, src []byte) (int, int32)` — nil-safe pointer construction for both `dst` and `src`
    via `unsafe.SliceData` when non-empty (matching ParserParse's inputPtr pattern), call
    `b.minify(srcPtr, uintptr(len(src)), dstPtr, uintptr(len(dst)), &written)`, KeepAlive both
    slices and b, return `int(written), rc`.
    `ValidateUTF8(data []byte) (bool, int32)` — same nil-safe pointer construction for `data`, call
    `b.validateUTF8(dataPtr, uintptr(len(data)), &valid)`, KeepAlive data and b, return `valid != 0,
    rc`.
  </action>
  <acceptance_criteria>
    - internal/ffi/bindings.go's mandatory `symbols` slice contains all 9 new entries (grep -c on the 9 exact pure_simdjson_ names against the symbols slice literal returns 9)
    - internal/ffi/bindings.go exports ElementAtPointer, ElementAtPath, ElementAtPathWildcard, ValueViewsFree, ArrayAt, ArrayLen, ObjectSize, Minify, ValidateUTF8 methods on *Bindings
    - Wildcard copying uses `emitValueViewsFreeFailureWarning`; it never routes a ValueView-array free failure through `emitBytesFreeFailureWarning`
    - Wildcard output pairs nil/zero, nil/nonzero, and nonnil/zero are handled before unsafe.Slice
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go build ./... && go vet ./...</automated>
  </verify>
  <done>internal/ffi/bindings.go registers all 9 new Phase 12 native symbols as required and exposes typed Go wrapper methods for each, ready for the public purejson API plans to consume.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce ABI 1.3 bootstrap, loader, and binding regressions</name>
  <files>scripts/release/check_bootstrap_abi_state.py, scripts/release/test_check_bootstrap_abi_state.py, internal/ffi/bindings_test.go, library_loading_test.go</files>
  <read_first>
    .planning/ROADMAP.md (Phase 16 retains final v0.2 release ownership)
    scripts/release/check_bootstrap_abi_state.py (ABI_MINIMUM_VERSION and prerelease comparison)
    scripts/release/test_check_bootstrap_abi_state.py (fixture generator and accepted/rejected policy cases)
    internal/ffi/bindings_test.go (Phase 11-named required-symbol tests and registrar stubs)
    library_loading_test.go (phase11MandatoryFixtureSymbols; ABI 1.2 complete/incomplete tests; 0x00010003 later-additive fixture)
    internal/ffi/bindings.go (the 9 wrapper implementations completed in Task 2)
  </read_first>
  <action>
    Add `"0x00010003": "0.2.0"` to `ABI_MINIMUM_VERSION`, keeping all older ABI history. Extend the
    Python tests to accept requested/bootstrap version `0.2.0-dev` with Go/Rust ABI 1.3, reject
    0.1.7 paired with ABI 1.3 as a stale bootstrap version, and move the unknown-future-ABI case
    from 1.3 to 1.4. Keep the exact requested-version check: this is a source-state canary, not
    permission to publish `0.2.0-dev` or final 0.2.0.
    In internal/ffi/bindings_test.go update ProbeABI's numeric fixture to 0x00010003. Replace stale
    Phase-11-only test naming/coverage with ABI-1.3 required-surface coverage that retains all
    earlier mandatory symbols and explicitly enumerates all 9 Phase 12 names. For each Phase 12
    symbol, make a subtest omit that one lookup and prove Bind fails closed naming it; add a complete
    lookup test proving all nine are requested. Add typed wrapper tests for string pointer/length
    marshaling (including empty), ArrayAt index, ArrayLen/ObjectSize outputs, Minify source/dest
    pointer order and lengths/capacity, ValidateUTF8's byte result, and wildcard transport:
    valid pointer/count copies to Go then frees exactly once; nil/zero returns non-nil empty;
    nil/nonzero and nonnil/zero return ErrInternal without unsafe slicing; and a simulated free
    error is reported through the ValueView-specific warning text, never the bytes-free warning.
    In library_loading_test.go rename `phase11MandatoryFixtureSymbols` to an ABI-1.3 name and include
    all earlier mandatory symbols plus all 9 Phase 12 symbols. Rename/update loader tests so ABI 1.2
    is rejected before any ABI-1.3-only lookup; complete ABI 1.3 binds and caches; incomplete ABI
    1.3 fails closed without caching (use a Phase 12 symbol as the missing symbol); and the later
    additive-minor compatibility fixture reports 0x00010004, not 0x00010003. Update assertion
    messages and test names from “ABI 1.2/Phase 11” to their actual ABI 1.3 meaning.
  </action>
  <acceptance_criteria>
    - ABI_MINIMUM_VERSION contains `"0x00010003": "0.2.0"` and policy tests accept 0.2.0-dev/ABI 1.3 while rejecting 0.1.7/ABI 1.3
    - `internal/ffi/bindings_test.go` requires every one of the 9 Phase 12 symbols and exercises every new typed wrapper
    - Wildcard binding tests prove copy/free, all three null/count boundary pairs, and ValueView-specific free warnings
    - Loader tests reject ABI 1.2, require complete ABI 1.3, and use 0x00010004 for later additive compatibility
    - `cargo build --release && go test ./internal/ffi ./...` and the release checker unit tests exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && cargo build --release && python3 -m unittest scripts/release/test_check_bootstrap_abi_state.py && go test ./internal/ffi ./...</automated>
  </verify>
  <done>ABI 1.3 source, bootstrap policy, mandatory lookup behavior, and every new binding wrapper are regression-pinned without consuming Phase 16's final release step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go purego layer -> native library | A loaded artifact claiming ABI 1.3 but missing any of the 9 new symbols must fail closed at bind time, not surface a nil-pointer crash later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Denial of Service | A Phase-12-incomplete artifact (old cached library predating this release) loaded against the new Go bindings | mitigate | All 9 symbols registered via the MANDATORY (non-optional) registerFuncWithRegistrar path, so ffi.Bind fails closed with a named missing-symbol error at load time rather than a runtime nil-function-pointer crash on first use |
| T-12-13 | Tampering | ElementAtPathWildcard's native-owned array not freed on an early Go-side error return path | mitigate | Wrapper method frees via ValueViewsFree in the same code path that consumes the copy, mirroring copyElementBytes's defer-based discipline; a free failure is warned, not silently ignored |
| T-12-BOOT | Tampering / Availability | ABI 1.3 source bootstrapping a published ABI 1.2 artifact, or Phase 12 pretending final 0.2.0 already exists | mitigate | Source uses the explicit unreleased 0.2.0-dev pin, compile-time ABI 1.3 canary, release-policy minimum 0.2.0, and loader tests. No publication or tag occurs; Phase 16 retains final 0.2.0 release ownership |
</threat_model>

<verification>
- `cargo build --release`, Go build/vet/tests, and release checker unit tests pass from a fresh checkout
- All 9 native symbols are registered as required, not optional
- ABI 1.2 is rejected; complete ABI 1.3 binds; ABI 1.4 remains additive-compatible
</verification>

<success_criteria>
All Go/native numeric pins, bootstrap policy, loader fixtures, and mandatory bindings agree on ABI
1.3. The 0.2.0-dev source tree cannot accidentally load ABI 1.2, all 9 new functions are required
and wrapper-tested, and plans 12-06/12-07/12-08 can consume them without new marshaling work.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-05-SUMMARY.md` when done
</output>
