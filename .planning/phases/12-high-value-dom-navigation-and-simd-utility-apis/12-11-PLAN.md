---
phase: 12-high-value-dom-navigation-and-simd-utility-apis
plan: 11
type: execute
wave: 7
depends_on: ["12-05"]
files_modified:
  - internal/ffi/bindings.go
  - internal/ffi/bindings_test.go
  - scripts/release/check_bootstrap_abi_state.py
  - scripts/release/test_check_bootstrap_abi_state.py
  - library_loading_test.go
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03, DOM-04, UTIL-01, UTIL-02]

must_haves:
  truths:
    - "internal/ffi/bindings.go registers all 9 Phase 12 symbols as required, never optional, because ABI 1.3 public artifacts must expose the complete DOM/utility surface (R-07)"
    - "All binding wrappers preserve pointer/count/capacity ordering, runtime.KeepAlive discipline, and caller-owned Go copies; wildcard native arrays are freed exactly once with a view-specific warning path (D-02, R-02)"
    - "Binding tests require and exercise every Phase 12 symbol, including wildcard null/count states, exact source/destination minify marshaling, and standalone UTF-8 results (D-08, D-09, D-11)"
    - "Release policy accepts the plan 12-05 0.2.0-dev/ABI 1.3 source identity while rejecting the published 0.1.7/ABI 1.2 identity as stale for this source tree"
    - "Loader tests reject ABI 1.2, bind and cache only a complete ABI 1.3 surface, fail closed on an incomplete ABI 1.3 artifact, and preserve later additive ABI 1.4 compatibility"
  artifacts:
    - path: "internal/ffi/bindings.go"
      provides: "9 required binding fields, registration entries, and typed wrapper methods"
    - path: "internal/ffi/bindings_test.go"
      provides: "Required-symbol, marshaling, wildcard ownership, and free-warning regressions"
    - path: "scripts/release/check_bootstrap_abi_state.py"
      provides: "ABI 1.3 minimum-version policy at 0.2.0"
    - path: "scripts/release/test_check_bootstrap_abi_state.py"
      provides: "0.2.0-dev/ABI 1.3 acceptance and stale/later-ABI policy tests"
    - path: "library_loading_test.go"
      provides: "ABI 1.2 rejection, complete/incomplete ABI 1.3 binding, and ABI 1.4 additive compatibility fixtures"
  key_links:
    - from: "internal/ffi/bindings.go mandatory symbols slice"
      to: "the 9 pure_simdjson_* exports landed in plans 12-01 through 12-04"
      via: "registerFuncWithRegistrar for every public ABI 1.3 symbol"
      pattern: "pure_simdjson_element_at_pointer"
    - from: "scripts/release/check_bootstrap_abi_state.py ABI_MINIMUM_VERSION"
      to: "plan 12-05 internal/bootstrap/version.go and internal/ffi/types.go"
      via: "0x00010003 -> 0.2.0 policy plus exact requested-version validation"
      pattern: "0x00010003"
    - from: "library_loading_test.go ABI 1.3 fixture"
      to: "internal/ffi/bindings.go mandatory symbols slice"
      via: "complete and one-symbol-missing registrar fixtures"
      pattern: "pure_simdjson_validate_utf8"
---

<objective>
Consume plan 12-05's ABI/bootstrap identity and integrate the complete Phase 12 native surface into
Go bindings, release-policy enforcement, and loader compatibility.

Purpose: keep bindings and loader integration in one dependency-linked five-file plan after the
ABI/bootstrap policy and documentation are stable.
Output: all 9 required typed bindings with focused ownership/marshaling tests, ABI 1.3 release-state
policy tests, and loader fixtures that fail closed on old or incomplete artifacts.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-PATTERNS.md
@.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-05-SUMMARY.md
@docs/releases.md

<interfaces>
Native signatures to bind from include/pure_simdjson.h after plans 12-01 through 12-04:
- `pure_simdjson_element_at_pointer(const struct pure_simdjson_value_view_t *view, const uint8_t *pointer_ptr, size_t pointer_len, struct pure_simdjson_value_view_t *out_value)`
- `pure_simdjson_element_at_path(const struct pure_simdjson_value_view_t *view, const uint8_t *path_ptr, size_t path_len, struct pure_simdjson_value_view_t *out_value)`
- `pure_simdjson_element_at_path_wildcard(const struct pure_simdjson_value_view_t *view, const uint8_t *path_ptr, size_t path_len, struct pure_simdjson_value_view_t **out_views, size_t *out_count)`
- `pure_simdjson_value_views_free(struct pure_simdjson_value_view_t *ptr, size_t len)`
- `pure_simdjson_array_at(const struct pure_simdjson_value_view_t *array_view, uint64_t index, struct pure_simdjson_value_view_t *out_value)`
- `pure_simdjson_array_len(const struct pure_simdjson_value_view_t *array_view, uint64_t *out_len)`
- `pure_simdjson_object_size(const struct pure_simdjson_value_view_t *object_view, uint64_t *out_size)`
- `pure_simdjson_minify(const uint8_t *src_ptr, size_t src_len, uint8_t *dst_ptr, size_t dst_cap, size_t *out_written)`
- `pure_simdjson_validate_utf8(const uint8_t *data_ptr, size_t data_len, uint8_t *out_valid)`

From internal/ffi/bindings.go:
- `ObjectGetField` is the exact string-to-bounded-pointer plus `runtime.KeepAlive` analog for
  `ElementAtPointer` and `ElementAtPath`.
- `copyElementBytes` is the copy-before-free analog for wildcard `[]ValueView` transport; use a
  distinct `emitValueViewsFreeFailureWarning`, not the byte-buffer warning.
- The `symbols` slice uses `registerFuncWithRegistrar` for required public exports. Do not use
  `registerOptionalFuncWithRegistrar`, which is reserved for internal diagnostics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind all 9 ABI 1.3 symbols with typed wrapper methods</name>
  <files>internal/ffi/bindings.go</files>
  <read_first>
    .planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-CONTEXT.md (D-02,
    D-08 through D-12, and D-15)
    internal/ffi/bindings.go (Bindings fields, mandatory symbols slice, ObjectGetField,
    copyElementBytes, ArrayIterNew, ParserParse, and ElementType)
    include/pure_simdjson.h (the exact generated ABI 1.3 signatures listed in this plan's
    interfaces block)
  </read_first>
  <action>
    Add 9 unexported function fields to `Bindings`: `elementAtPointer func(*ValueView, *byte,
    uintptr, *ValueView) int32`, `elementAtPath func(*ValueView, *byte, uintptr, *ValueView) int32`,
    `elementAtPathWildcard func(*ValueView, *byte, uintptr, **ValueView, *uintptr) int32`,
    `valueViewsFree func(*ValueView, uintptr) int32`, `arrayAt func(*ValueView, uint64, *ValueView)
    int32`, `arrayLen func(*ValueView, *uint64) int32`, `objectSize func(*ValueView, *uint64) int32`,
    `minify func(*byte, uintptr, *byte, uintptr, *uintptr) int32`, and `validateUTF8 func(*byte,
    uintptr, *byte) int32`.
    Register the exact 9 `pure_simdjson_*` names listed in the interfaces block through the
    mandatory `symbols` slice, not the optional path, per R-07.
    Add `ElementAtPointer` and `ElementAtPath` by cloning ObjectGetField's string marshaling,
    nil-for-empty pointer behavior, and KeepAlive calls. Add `ElementAtPathWildcard` so a successful
    `(nil,0)` becomes a non-nil empty `[]ValueView` per D-02; `(nil,N)` and `(nonnull,0)` return
    ErrInternal before `unsafe.Slice`. For `(nonnull,0)`, attempt `ValueViewsFree(ptr,0)` and emit a
    view-specific warning. For a valid pointer/count, copy into Go memory before freeing exactly
    once; a free failure uses `emitValueViewsFreeFailureWarning`, never the bytes-free warning.
    Add `ValueViewsFree`, `ArrayAt`, `ArrayLen`, and `ObjectSize` using the existing view/scalar
    wrapper shapes and KeepAlive discipline.
    Add `Minify(dst, src []byte) (int, int32)` with nil-safe SliceData pointers and call order
    `(srcPtr, srcLen, dstPtr, dstCap, &written)` per D-08/D-09. Add
    `ValidateUTF8(data []byte) (bool, int32)` with nil-safe input and a one-byte result per D-11.
  </action>
  <acceptance_criteria>
    - The mandatory symbols slice contains all 9 exact ABI 1.3 names and none uses optional registration
    - Bindings exposes ElementAtPointer, ElementAtPath, ElementAtPathWildcard, ValueViewsFree, ArrayAt, ArrayLen, ObjectSize, Minify, and ValidateUTF8
    - Wildcard output pairs nil/zero, nil/nonzero, and nonnil/zero are handled before unsafe.Slice
    - Wildcard copying frees through ValueViewsFree and reports failures through the view-specific warning
    - Minify preserves exact source/destination pointer order and capacity; ValidateUTF8 returns native validity separately from status
    - `go build ./...` and `go vet ./...` exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && go build ./... && go vet ./...</automated>
  </verify>
  <done>All 9 ABI 1.3 exports are mandatory typed Go bindings with bounded marshaling, ownership-safe wildcard copying, and utility signatures matching the generated header.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce ABI 1.3 release policy, binding behavior, and loader compatibility</name>
  <files>scripts/release/check_bootstrap_abi_state.py, scripts/release/test_check_bootstrap_abi_state.py, internal/ffi/bindings_test.go, library_loading_test.go</files>
  <read_first>
    .planning/ROADMAP.md (Phase 16 retains final v0.2 publication)
    .planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-05-SUMMARY.md
    scripts/release/check_bootstrap_abi_state.py (ABI_MINIMUM_VERSION and prerelease comparison)
    scripts/release/test_check_bootstrap_abi_state.py (fixture generator and accepted/rejected cases)
    internal/ffi/bindings_test.go (required-symbol registrar stubs and wrapper-test conventions)
    library_loading_test.go (mandatory fixture symbols, ABI 1.2 fixtures, and later-minor case)
    internal/ffi/bindings.go (the 9 wrappers completed in Task 1)
  </read_first>
  <action>
    Add `"0x00010003": "0.2.0"` to `ABI_MINIMUM_VERSION` while preserving all older ABI history.
    Extend policy tests to accept requested/bootstrap `0.2.0-dev` with Go/Rust ABI 1.3, reject
    0.1.7 with ABI 1.3, and use ABI 1.4 for the unknown-later-ABI case. Retain exact
    requested-version validation; do not create a tag, upload an artifact, or claim final 0.2.0.
    In `internal/ffi/bindings_test.go`, update ProbeABI to 0x00010003 and replace Phase-11-only
    naming with ABI-1.3 required-surface coverage that retains every earlier mandatory symbol and
    explicitly enumerates all 9 Phase 12 names. For each new symbol, omit exactly that lookup and
    prove Bind fails closed naming it; add a complete lookup test proving all nine are requested.
    Test string pointer/length marshaling including empty input, ArrayAt index, ArrayLen/ObjectSize
    outputs, exact Minify source/destination pointer order and lengths/capacity, ValidateUTF8's byte
    result, and wildcard transport. Wildcard cases must prove valid copy plus exactly one free,
    non-nil empty for `(nil,0)`, ErrInternal for `(nil,N)` and `(nonnull,0)` without unsafe slicing,
    and view-specific warning text on simulated free failure.
    In `library_loading_test.go`, rename the Phase 11 mandatory fixture for ABI 1.3 and include all
    earlier symbols plus all 9 Phase 12 symbols. Prove ABI 1.2 rejection happens before any
    ABI-1.3-only lookup; complete ABI 1.3 binds and caches; incomplete ABI 1.3 fails closed without
    caching using one missing Phase 12 symbol; and the later additive fixture reports 0x00010004.
  </action>
  <acceptance_criteria>
    - ABI_MINIMUM_VERSION contains `"0x00010003": "0.2.0"` and policy tests accept 0.2.0-dev/ABI 1.3 while rejecting 0.1.7/ABI 1.3
    - bindings_test.go requires and behavior-tests every one of the 9 Phase 12 symbols
    - Wildcard tests prove copy/free, all three null/count states, and view-specific free warnings
    - Loader tests reject ABI 1.2, require complete ABI 1.3, and use 0x00010004 for later additive compatibility
    - Focused release-policy, binding, and loader tests exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/pure-simdjson && python3 -m unittest scripts/release/test_check_bootstrap_abi_state.py && go test ./internal/ffi && go test . -run 'Test.*(ABI|Bind|Binding|Library|Load)' -count=1</automated>
  </verify>
  <done>Release policy, mandatory lookup behavior, typed wrappers, and loader compatibility are regression-pinned to the plan 12-05 ABI 1.3 source identity without consuming Phase 16 release work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go purego layer -> native library | A loaded artifact claiming ABI 1.3 but missing a required symbol must fail closed at bind time |
| Native wildcard allocation -> Go-owned slice | Raw pointer/count output must be validated, copied, and freed exactly once before it escapes the binding layer |
| Bootstrap source identity -> release policy checker | The unpublished 0.2.0-dev source pin must map to ABI 1.3 without authorizing publication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Denial of Service | Old or incomplete native artifact loaded against ABI 1.3 bindings | mitigate | Register all 9 symbols through the mandatory path and test one-symbol-at-a-time failures before caching |
| T-12-13 | Tampering | ElementAtPathWildcard native-owned array and pointer/count pairs | mitigate | Validate all output pairs before unsafe slicing, copy into Go memory, free exactly once, and emit a view-specific warning on free failure |
| T-12-BOOT | Tampering / Availability | ABI 1.3 source paired with published ABI 1.2 or treated as a final release | mitigate | Enforce 0.2.0-dev/ABI 1.3 and minimum 0.2.0 in policy tests; no tag or upload action occurs |
| T-12-SC | Tampering | Package/toolchain supply chain | accept | No dependency or package installation is introduced; tests use repository-owned code and existing toolchains |
</threat_model>

<verification>
- Each task runs its focused automated command
- Wave 7 gate runs `make verify-contract && make verify-docs && cargo build --release && go test ./... -race`
- All 9 native symbols are mandatory and wrapper-tested
- ABI 1.2 rejects; complete ABI 1.3 binds; incomplete ABI 1.3 fails closed; ABI 1.4 remains additive-compatible
</verification>

<success_criteria>
The five binding/loader/enforcement files consume plan 12-05's ABI 1.3 identity coherently. Every
Phase 12 export is mandatory and wrapper-tested, release policy recognizes the unpublished source
state without publishing it, and downstream public API plans receive a complete cached binding.
</success_criteria>

<output>
Create `.planning/phases/12-high-value-dom-navigation-and-simd-utility-apis/12-11-SUMMARY.md` when done
</output>
