---
phase: 07-benchmarks-v0.1-release
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/lib.rs
  - src/runtime/mod.rs
  - include/pure_simdjson.h
  - internal/ffi/bindings.go
  - tests/rust_shim_minimal.rs
  - tests/smoke/ffi_export_surface.c
  - benchmark_typed_test.go
  - benchmark_selective_test.go
  - benchmark_native_alloc_test.go
autonomous: true
requirements:
  - BENCH-01
  - BENCH-03
  - BENCH-05
user_setup: []
must_haves:
  truths:
    - "Tier 2 and Tier 3 benchmark families are runnable on the current DOM API, with Tier 3 explicitly labeled as a placeholder."
    - "Native allocator stats come from explicit Rust-side counters exposed through diagnostic FFI hooks, not from Go-only guesses."
    - "The new allocator telemetry does not alter existing parse semantics or release-path behavior outside benchmarks."
  artifacts:
    - path: "include/pure_simdjson.h"
      provides: "diagnostic native allocator stats struct and export declarations"
      contains: "pure_simdjson_native_alloc_stats_t"
    - path: "benchmark_native_alloc_test.go"
      provides: "benchmark helper that resets/snapshots native allocator counters and reports custom metrics"
      contains: "native-bytes/op"
    - path: "benchmark_typed_test.go"
      provides: "Tier 2 typed extraction benchmark family"
      contains: "BenchmarkTier2Typed_"
  key_links:
    - from: "src/lib.rs"
      to: "include/pure_simdjson.h"
      via: "the committed header must match the allocator stats exports and struct layout"
      pattern: "pure_simdjson_native_alloc_stats_snapshot"
    - from: "internal/ffi/bindings.go"
      to: "benchmark_native_alloc_test.go"
      via: "the Go benchmark helper consumes the new reset/snapshot binding methods"
      pattern: "NativeAllocStats"
    - from: "benchmark_selective_test.go"
      to: "iterator_test.go"
      via: "the placeholder benchmark must use the existing DOM traversal model rather than invent an On-Demand API"
      pattern: "GetField"
---

<objective>
Add Tier 2 and Tier 3 benchmark families plus the native allocator telemetry required to interpret them honestly.

Purpose: complete BENCH-01 and BENCH-05 without inventing a new public parser API.
Output: diagnostic FFI stats hooks, typed/selective benchmark families, and benchmark metrics that report native allocator activity alongside Go alloc counts.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@.planning/phases/07-benchmarks-v0.1-release/07-PATTERNS.md
@include/pure_simdjson.h
@src/lib.rs
@src/runtime/mod.rs
@internal/ffi/bindings.go
@iterator_test.go
@example_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnostic native allocator stats to the FFI surface</name>
  <files>
    src/lib.rs
    src/runtime/mod.rs
    include/pure_simdjson.h
    internal/ffi/bindings.go
    tests/rust_shim_minimal.rs
    tests/smoke/ffi_export_surface.c
  </files>
  <read_first>
    - src/lib.rs
    - src/runtime/mod.rs
    - include/pure_simdjson.h
    - internal/ffi/bindings.go
    - tests/rust_shim_minimal.rs
    - tests/smoke/ffi_export_surface.c
    - .planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
  </read_first>
  <action>
Add diagnostic-only allocator telemetry to the native shim. Commit this exact C ABI shape:

- a header struct named `pure_simdjson_native_alloc_stats_t` with these exact `uint64_t` fields in order:
  - `live_bytes`
  - `total_alloc_bytes`
  - `alloc_count`
  - `free_count`
- a reset export named `pure_simdjson_native_alloc_stats_reset(void)`
- a snapshot export named `pure_simdjson_native_alloc_stats_snapshot(pure_simdjson_native_alloc_stats_t *out_stats)`

Implement the counters in Rust so they measure native allocations performed by the shim path and can be reset between benchmark runs. Keep the surface diagnostic-only: no existing parse or accessor function may change signature or meaning.

Update `internal/ffi/bindings.go` with binding helpers named:
- `NativeAllocStatsReset()`
- `NativeAllocStatsSnapshot()`

Update `tests/rust_shim_minimal.rs` and `tests/smoke/ffi_export_surface.c` so the new exports are part of the committed smoke surface and are called successfully with safe inputs.
  </action>
  <verify>
    <automated>cargo test --release native_alloc && rg 'pure_simdjson_native_alloc_stats_t|pure_simdjson_native_alloc_stats_reset|pure_simdjson_native_alloc_stats_snapshot|NativeAllocStatsReset|NativeAllocStatsSnapshot' src/lib.rs src/runtime/mod.rs include/pure_simdjson.h internal/ffi/bindings.go tests/rust_shim_minimal.rs tests/smoke/ffi_export_surface.c</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pure_simdjson_native_alloc_stats_t' include/pure_simdjson.h` succeeds.
    - `rg 'pure_simdjson_native_alloc_stats_reset|pure_simdjson_native_alloc_stats_snapshot' src/lib.rs include/pure_simdjson.h tests/smoke/ffi_export_surface.c` succeeds.
    - `rg 'NativeAllocStatsReset|NativeAllocStatsSnapshot' internal/ffi/bindings.go` succeeds.
    - `cargo test --release native_alloc` exits 0.
  </acceptance_criteria>
  <done>The native shim exposes resettable allocator telemetry that later benchmark code can consume honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tier 2 and Tier 3 benchmark families plus custom native allocator metrics</name>
  <files>
    benchmark_typed_test.go
    benchmark_selective_test.go
    benchmark_native_alloc_test.go
  </files>
  <read_first>
    - benchmark_fixtures_test.go
    - benchmark_schema_test.go
    - benchmark_comparators_test.go
    - iterator_test.go
    - example_test.go
    - internal/ffi/bindings.go
  </read_first>
  <action>
Add `benchmark_native_alloc_test.go` with the benchmark helper surface that resets/snapshots the new allocator counters and reports these exact custom metrics:
- `native-bytes/op`
- `native-allocs/op`
- `native-live-bytes`

Add `benchmark_typed_test.go` with benchmark names matching `BenchmarkTier2Typed_<Fixture>/<Comparator>`. Use these exact extraction workloads:
- `Twitter`: read `search_metadata.max_id`, then for the first 10 `statuses` entries read `id`, `favorited`, `retweeted`, `user.id`, and `user.name`
- `CITMCatalog`: read the counts of `areaNames` and `topicNames`, then the first 20 event ids from `events`
- `Canada`: read top-level `type`, then sum the first 256 numeric coordinate values encountered while traversing the nested coordinate arrays

Add `benchmark_selective_test.go` with benchmark names matching `BenchmarkTier3SelectivePlaceholder_<Fixture>/<Comparator>`. Keep the implementation on the current DOM API only and label it as a placeholder in code comments and benchmark descriptions. Use these exact selective workloads:
- `Twitter`: read only the first 10 `statuses[*].user.id` and `statuses[*].retweeted`
- `CITMCatalog`: read only the first 20 event ids and one top-level count

Both files must reuse the comparator surface from Plan 07-02 and the allocator helper from `benchmark_native_alloc_test.go`. Do not add or imply any new On-Demand or path-query public API.
  </action>
  <verify>
    <automated>go test ./... -run '^$' -bench 'Benchmark(Tier2|Tier3)_' -benchtime=1x -count=1 && rg 'BenchmarkTier2Typed_|BenchmarkTier3SelectivePlaceholder_|native-bytes/op|native-allocs/op|native-live-bytes|placeholder' benchmark_typed_test.go benchmark_selective_test.go benchmark_native_alloc_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'BenchmarkTier2Typed_' benchmark_typed_test.go` succeeds.
    - `rg 'BenchmarkTier3SelectivePlaceholder_' benchmark_selective_test.go` succeeds.
    - `rg 'native-bytes/op|native-allocs/op|native-live-bytes' benchmark_native_alloc_test.go` succeeds.
    - `rg 'placeholder' benchmark_selective_test.go` succeeds.
    - `go test ./... -run '^$' -bench 'Benchmark(Tier2|Tier3)_' -benchtime=1x -count=1` exits 0.
  </acceptance_criteria>
  <done>The typed and selective benchmark families run on the current DOM API and expose native allocator metrics beside the standard Go benchmark numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native allocator counters -> published benchmark metrics | incorrect reset/snapshot behavior would make BENCH-05 misleading |
| current DOM API -> Tier 3 placeholder benchmark | accidental preview of a future On-Demand surface would overstate shipped capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | I | native allocator metrics | mitigate | explicit reset/snapshot exports plus benchmark helper metrics with stable names |
| T-07-03-02 | T | placeholder selective benchmark | mitigate | force code/comments/benchmark names to use `SelectivePlaceholder` and DOM traversal only |
| T-07-03-03 | T | FFI diagnostics surface | mitigate | update header, Rust tests, and export smoke in the same task so the ABI stays auditable |
</threat_model>

<verification>
- [ ] `cargo test --release native_alloc`
- [ ] `rg 'pure_simdjson_native_alloc_stats_reset|pure_simdjson_native_alloc_stats_snapshot' src/lib.rs include/pure_simdjson.h`
- [ ] `rg 'BenchmarkTier2Typed_|BenchmarkTier3SelectivePlaceholder_' benchmark_typed_test.go benchmark_selective_test.go`
- [ ] `go test ./... -run '^$' -bench 'Benchmark(Tier2|Tier3)_' -benchtime=1x -count=1`
</verification>

<success_criteria>
- BENCH-05 is backed by explicit native telemetry rather than Go-only counters
- Tier 2 and Tier 3 benchmark families are runnable through normal `go test -bench`
- Tier 3 remains clearly marked as a DOM-era placeholder and not a new API claim
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-03-SUMMARY.md`
</output>
