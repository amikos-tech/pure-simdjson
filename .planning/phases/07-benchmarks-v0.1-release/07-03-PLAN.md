---
phase: 07-benchmarks-v0.1-release
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - build.rs
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/native/native_alloc_telemetry.h
  - src/native/native_alloc_telemetry.cpp
  - src/lib.rs
  - src/runtime/mod.rs
  - include/pure_simdjson.h
  - internal/ffi/bindings.go
  - docs/ffi-contract.md
  - cbindgen.toml
  - tests/abi/check_header.py
  - tests/rust_shim_minimal.rs
  - tests/smoke/ffi_export_surface.c
autonomous: true
requirements:
  - BENCH-05
user_setup: []
must_haves:
  truths:
    - "Native allocator stats come from a concrete C++-side telemetry path compiled into the cdylib, not from Go-only guesses or a Rust global allocator."
    - "The telemetry surface is diagnostic-only and does not alter existing parse semantics or release-path behavior outside benchmarks."
    - "The allocator exports are part of the committed ABI contract, smoke surface, and header-audit rules."
  artifacts:
    - path: "src/native/native_alloc_telemetry.cpp"
      provides: "the concrete C++ allocation-counter implementation compiled into the cdylib"
      contains: "operator new"
    - path: "include/pure_simdjson.h"
      provides: "diagnostic native allocator stats struct and export declarations"
      contains: "pure_simdjson_native_alloc_stats_t"
    - path: "docs/ffi-contract.md"
      provides: "the normative ABI description for the new diagnostic surface"
      contains: "pure_simdjson_native_alloc_stats_reset"
  key_links:
    - from: "build.rs"
      to: "src/native/native_alloc_telemetry.cpp"
      via: "the new telemetry translation unit must be compiled into the same cdylib as simdjson and the bridge"
      pattern: "native_alloc_telemetry.cpp"
    - from: "src/lib.rs"
      to: "include/pure_simdjson.h"
      via: "the committed header must match the allocator stats exports and struct layout"
      pattern: "pure_simdjson_native_alloc_stats_snapshot"
    - from: "docs/ffi-contract.md"
      to: "tests/abi/check_header.py"
      via: "the new diagnostic exports must be part of the normative ABI and header-audit rules"
      pattern: "pure_simdjson_native_alloc_stats_reset"
---

<objective>
Add the native allocator telemetry surface required for BENCH-05.

Purpose: make the native allocation story concrete before any benchmark code consumes it.
Output: a C++-side telemetry implementation, bridge/Rust/Go bindings, and ABI docs/tests that expose reset/snapshot allocator stats safely.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@.planning/phases/07-benchmarks-v0.1-release/07-PATTERNS.md
@.planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
@build.rs
@src/native/simdjson_bridge.h
@src/native/simdjson_bridge.cpp
@include/pure_simdjson.h
@src/lib.rs
@src/runtime/mod.rs
@internal/ffi/bindings.go
@docs/ffi-contract.md
@tests/abi/check_header.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the C++ telemetry implementation and bridge hooks</name>
  <files>
    build.rs
    src/native/simdjson_bridge.h
    src/native/simdjson_bridge.cpp
    src/native/native_alloc_telemetry.h
    src/native/native_alloc_telemetry.cpp
  </files>
  <read_first>
    - build.rs
    - src/native/simdjson_bridge.h
    - src/native/simdjson_bridge.cpp
    - .planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
    - .planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
  </read_first>
  <action>
Add a concrete C++ telemetry implementation instead of a Rust-only allocator wrapper.

Create `src/native/native_alloc_telemetry.h` and `src/native/native_alloc_telemetry.cpp`, then update `build.rs` so the new translation unit is compiled into the same cdylib as `third_party/simdjson/singleheader/simdjson.cpp` and `src/native/simdjson_bridge.cpp`.

Implement process-local shim telemetry in the new C++ file:
- override `operator new(size_t)` and `operator delete(void *)`
- override `operator new[](size_t)` and `operator delete[](void *)`
- override the `std::nothrow` and aligned `operator new/delete` variants needed by this C++17 build
- maintain `std::atomic<uint64_t>` counters for `total_alloc_bytes`, `alloc_count`, and `free_count`
- maintain a mutex-protected pointer-size registry so `live_bytes` is exact on snapshot and reset

Scope the counters to heap allocations routed through the shim/simdjson cdylib path. Do not claim process-wide Go heap totals.

Expose bridge-level C++ functions named:
- `psimdjson_native_alloc_stats_reset()`
- `psimdjson_native_alloc_stats_snapshot(pure_simdjson_native_alloc_stats_t *out_stats)`
  </action>
  <verify>
    <automated>rg 'native_alloc_telemetry.cpp' build.rs && rg 'operator new|operator delete|psimdjson_native_alloc_stats_reset|psimdjson_native_alloc_stats_snapshot' src/native/simdjson_bridge.h src/native/simdjson_bridge.cpp src/native/native_alloc_telemetry.h src/native/native_alloc_telemetry.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'native_alloc_telemetry.cpp' build.rs` succeeds.
    - `rg 'operator new|operator delete' src/native/native_alloc_telemetry.cpp` succeeds.
    - `rg 'psimdjson_native_alloc_stats_reset|psimdjson_native_alloc_stats_snapshot' src/native/simdjson_bridge.h src/native/simdjson_bridge.cpp` succeeds.
  </acceptance_criteria>
  <done>The cdylib has a concrete C++ allocation-counter implementation and bridge hooks for reset/snapshot telemetry.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the telemetry through the ABI, bindings, docs, and smoke tests</name>
  <files>
    src/lib.rs
    src/runtime/mod.rs
    include/pure_simdjson.h
    internal/ffi/bindings.go
    docs/ffi-contract.md
    cbindgen.toml
    tests/abi/check_header.py
    tests/rust_shim_minimal.rs
    tests/smoke/ffi_export_surface.c
  </files>
  <read_first>
    - src/lib.rs
    - src/runtime/mod.rs
    - include/pure_simdjson.h
    - internal/ffi/bindings.go
    - docs/ffi-contract.md
    - cbindgen.toml
    - tests/abi/check_header.py
    - tests/rust_shim_minimal.rs
    - tests/smoke/ffi_export_surface.c
    - src/native/simdjson_bridge.h
  </read_first>
  <action>
Thread the new bridge hooks through Rust and Go as this exact public ABI surface:
- a header struct named `pure_simdjson_native_alloc_stats_t` with these exact `uint64_t` fields in order:
  - `live_bytes`
  - `total_alloc_bytes`
  - `alloc_count`
  - `free_count`
- a reset export named `pure_simdjson_native_alloc_stats_reset(void)`
- a snapshot export named `pure_simdjson_native_alloc_stats_snapshot(pure_simdjson_native_alloc_stats_t *out_stats)`

Update `src/runtime/mod.rs` and `internal/ffi/bindings.go` with helpers named:
- `NativeAllocStatsReset()`
- `NativeAllocStatsSnapshot()`

Update the ABI and audit surfaces in the same task:
- `include/pure_simdjson.h`
- `docs/ffi-contract.md`
- `cbindgen.toml`
- `tests/abi/check_header.py`
- `tests/rust_shim_minimal.rs`
- `tests/smoke/ffi_export_surface.c`

The smoke and Rust tests must round-trip the reset/snapshot APIs with safe inputs, verify counters are callable before and after parse activity, and keep existing public exports intact.
  </action>
  <verify>
    <automated>cargo test --release native_alloc_stats && python3 tests/abi/check_header.py include/pure_simdjson.h && rg 'pure_simdjson_native_alloc_stats_t|pure_simdjson_native_alloc_stats_reset|pure_simdjson_native_alloc_stats_snapshot|NativeAllocStatsReset|NativeAllocStatsSnapshot' src/lib.rs src/runtime/mod.rs include/pure_simdjson.h internal/ffi/bindings.go docs/ffi-contract.md tests/abi/check_header.py tests/rust_shim_minimal.rs tests/smoke/ffi_export_surface.c</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pure_simdjson_native_alloc_stats_t' include/pure_simdjson.h docs/ffi-contract.md` succeeds.
    - `rg 'pure_simdjson_native_alloc_stats_reset|pure_simdjson_native_alloc_stats_snapshot' src/lib.rs include/pure_simdjson.h tests/smoke/ffi_export_surface.c tests/abi/check_header.py` succeeds.
    - `rg 'NativeAllocStatsReset|NativeAllocStatsSnapshot' internal/ffi/bindings.go` succeeds.
    - `cargo test --release native_alloc_stats` exits 0.
    - `python3 tests/abi/check_header.py include/pure_simdjson.h` exits 0.
  </acceptance_criteria>
  <done>The native shim exposes resettable C++-side allocator telemetry through a documented, audited ABI surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native allocator counters -> published benchmark metrics | incorrect reset/snapshot behavior would make BENCH-05 misleading |
| bridge/Rust/Go binding chain -> public ABI | missing one link would leave the telemetry non-consumable or undocumented |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | I | native allocator metrics | mitigate | explicit C++-side reset/snapshot exports plus bridge/binding coverage with stable names |
| T-07-03-02 | T | ABI diagnostics surface | mitigate | update header, contract docs, header audit, and export smoke in the same plan so the ABI stays auditable |
</threat_model>

<verification>
- [ ] `rg 'native_alloc_telemetry.cpp' build.rs`
- [ ] `cargo test --release native_alloc_stats`
- [ ] `python3 tests/abi/check_header.py include/pure_simdjson.h`
- [ ] `rg 'pure_simdjson_native_alloc_stats_reset|pure_simdjson_native_alloc_stats_snapshot' src/lib.rs include/pure_simdjson.h docs/ffi-contract.md`
</verification>

<success_criteria>
- BENCH-05 has a concrete C++-side telemetry surface compiled into the cdylib
- The telemetry exports are documented, header-audited, and smoke-tested before benchmark code consumes them
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-03-SUMMARY.md`
</output>
