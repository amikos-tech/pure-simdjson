---
phase: 07-benchmarks-v0.1-release
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/bench/README.md
  - testdata/bench/twitter.json
  - testdata/bench/citm_catalog.json
  - testdata/bench/canada.json
  - testdata/bench/mesh.json
  - testdata/bench/numbers.json
  - testdata/jsontestsuite/README.md
  - testdata/jsontestsuite/expectations.tsv
  - testdata/jsontestsuite/cases/
  - benchmark_fixtures_test.go
  - benchmark_oracle_test.go
autonomous: true
requirements:
  - BENCH-02
  - BENCH-06
user_setup: []
must_haves:
  truths:
    - "All five benchmark corpus files live under `testdata/bench/` with repo-local provenance; benchmark execution does not read from `third_party/` or the network."
    - "The correctness oracle is driven by a committed expectation manifest plus committed JSON case files, with no network or git access at test runtime."
    - "`go test ./... -run TestJSONTestSuiteOracle -count=1` is the canonical BENCH-06 entrypoint, and it fails on corpus-floor or manifest drift."
  artifacts:
    - path: "testdata/bench/README.md"
      provides: "provenance, pinned upstream refs, and checksum inventory for the benchmark corpus"
      contains: "19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448"
    - path: "testdata/jsontestsuite/README.md"
      provides: "the pinned JSONTestSuite snapshot ref, archive URL, and vendored case-count floor"
      contains: "1ef36fa01286573e846ac449e8683f8833c5b26a"
    - path: "benchmark_oracle_test.go"
      provides: "normal Go test that enforces BENCH-06 from the vendored manifest"
      contains: "TestJSONTestSuiteOracle"
  key_links:
    - from: "benchmark_fixtures_test.go"
      to: "testdata/bench/README.md"
      via: "the fixture loader must use the exact vendored filenames documented in the provenance file"
      pattern: "testdata/bench"
    - from: "benchmark_oracle_test.go"
      to: "testdata/jsontestsuite/expectations.tsv"
      via: "oracle outcomes come from the committed manifest, not filename heuristics hidden in code"
      pattern: "expectations.tsv"
    - from: "testdata/jsontestsuite/README.md"
      to: "testdata/jsontestsuite/cases/"
      via: "the vendored snapshot, extraction source, and case count stay auditable together"
      pattern: "cases/"
---

<objective>
Create the committed benchmark and correctness inputs for Phase 7.

Purpose: make BENCH-02 and BENCH-06 deterministic before any benchmark numbers or README claims exist.
Output: vendored benchmark corpus, vendored correctness corpus + expectation manifest, and repo-local Go helpers/tests that use those committed assets only.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@.planning/phases/07-benchmarks-v0.1-release/07-PATTERNS.md
@.planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
@third_party/simdjson/jsonexamples/twitter.json
@third_party/simdjson/jsonexamples/citm_catalog.json
@parser_test.go
@helpers_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the benchmark corpus and record deterministic provenance</name>
  <files>
    testdata/bench/README.md
    testdata/bench/twitter.json
    testdata/bench/citm_catalog.json
    testdata/bench/canada.json
    testdata/bench/mesh.json
    testdata/bench/numbers.json
    testdata/jsontestsuite/README.md
    testdata/jsontestsuite/expectations.tsv
    testdata/jsontestsuite/cases/
  </files>
  <read_first>
    - .planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
    - .planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
    - .planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
    - third_party/simdjson/jsonexamples/twitter.json
    - third_party/simdjson/jsonexamples/citm_catalog.json
    - third_party/simdjson/LICENSE
    - third_party/simdjson/LICENSE-MIT
  </read_first>
  <action>
Create `testdata/bench/` as the only runtime source for Phase 7 benchmark fixtures.

Populate all five corpus files from one pinned simdjson snapshot that still contains the full historical benchmark set:
- `https://raw.githubusercontent.com/simdjson/simdjson/19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448/jsonexamples/twitter.json`
- `https://raw.githubusercontent.com/simdjson/simdjson/19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448/jsonexamples/citm_catalog.json`
- `https://raw.githubusercontent.com/simdjson/simdjson/19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448/jsonexamples/canada.json`
- `https://raw.githubusercontent.com/simdjson/simdjson/19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448/jsonexamples/mesh.json`
- `https://raw.githubusercontent.com/simdjson/simdjson/19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448/jsonexamples/numbers.json`

Do not use unpinned refs such as `master`, web HTML pages, or a later commit. Write `testdata/bench/README.md` with a concrete table using these exact columns: `filename`, `source`, `upstream_ref`, `sha256`, and `notes`. The table must explain that:
- all five corpus files come from `simdjson/simdjson@19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448`,
- the commit is intentionally pre-`714f0ba2226cf996fdd575f8c0e7a3c092c194bd`, which deleted most data files from the simdjson repo,
- benchmark execution must read only `testdata/bench/*.json`, never `third_party/simdjson/*` or the network.

Create `testdata/jsontestsuite/README.md`, `testdata/jsontestsuite/expectations.tsv`, and `testdata/jsontestsuite/cases/` from the exact archive `https://github.com/nst/JSONTestSuite/archive/1ef36fa01286573e846ac449e8683f8833c5b26a.tar.gz`. Extract only `test_parsing/` into `testdata/jsontestsuite/cases/`, record the pinned commit, archive URL, extraction command, and the expected current snapshot size (`318` files) in `testdata/jsontestsuite/README.md`.

The TSV must use exactly these tab-separated columns on the header row: `relative_path`, `expect`, `note`. Every vendored case file must have one manifest row, and every manifest row must resolve to a real file under `testdata/jsontestsuite/cases/`.

Generate `expectations.tsv` deterministically at vendoring time:
- `y_*.json` rows must be recorded as `accept`
- `n_*.json` rows must be recorded as `reject`
- `i_*.json` rows must be classified once by running the vendored `simdjson v4.6.1` parser against the pinned snapshot and recording the observed result as either `accept` or `reject`

After vendoring, the committed TSV becomes the only runtime source of truth. The vendored corpus must contain at least `300` files, and the manifest row count must exactly equal the case-file count.
  </action>
  <verify>
    <automated>test -f testdata/bench/twitter.json && test -f testdata/bench/citm_catalog.json && test -f testdata/bench/canada.json && test -f testdata/bench/mesh.json && test -f testdata/bench/numbers.json && test -f testdata/bench/README.md && rg '^\| filename \| source \| upstream_ref \| sha256 \| notes \|$' testdata/bench/README.md && rg -F '19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448' testdata/bench/README.md && test -f testdata/jsontestsuite/README.md && test -f testdata/jsontestsuite/expectations.tsv && rg -F '1ef36fa01286573e846ac449e8683f8833c5b26a' testdata/jsontestsuite/README.md && header="$(head -n 1 testdata/jsontestsuite/expectations.tsv)" && [ "$header" = $'relative_path\texpect\tnote' ] && case_count="$(find testdata/jsontestsuite/cases -type f | wc -l | tr -d ' ')" && [ "$case_count" -ge 300 ] && manifest_count="$(( $(wc -l < testdata/jsontestsuite/expectations.tsv) - 1 ))" && [ "$manifest_count" -eq "$case_count" ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f testdata/bench/twitter.json` succeeds.
    - `test -f testdata/bench/citm_catalog.json` succeeds.
    - `test -f testdata/bench/canada.json` succeeds.
    - `test -f testdata/bench/mesh.json` succeeds.
    - `test -f testdata/bench/numbers.json` succeeds.
    - `rg '^\| filename \| source \| upstream_ref \| sha256 \| notes \|$' testdata/bench/README.md` succeeds.
    - `rg -F '19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448' testdata/bench/README.md` succeeds.
    - `rg -F '1ef36fa01286573e846ac449e8683f8833c5b26a' testdata/jsontestsuite/README.md` succeeds.
    - `test "$(head -n 1 testdata/jsontestsuite/expectations.tsv)" = $'relative_path\texpect\tnote'` succeeds.
    - `test "$(find testdata/jsontestsuite/cases -type f | wc -l | tr -d ' ')" -ge 300` succeeds.
    - `test "$(( $(wc -l < testdata/jsontestsuite/expectations.tsv) - 1 ))" -eq "$(find testdata/jsontestsuite/cases -type f | wc -l | tr -d ' ')"` succeeds.
  </acceptance_criteria>
  <done>The full benchmark corpus and the correctness-oracle corpus are committed, auditable, and available without any mutable upstream dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Add fixture helpers and the committed correctness oracle</name>
  <files>
    benchmark_fixtures_test.go
    benchmark_oracle_test.go
  </files>
  <read_first>
    - helpers_test.go
    - parser_test.go
    - testdata/bench/README.md
    - testdata/jsontestsuite/expectations.tsv
    - testdata/jsontestsuite/README.md
  </read_first>
  <action>
Add `benchmark_fixtures_test.go` in the root `purejson` package. It must define the canonical benchmark fixture loader surface used by later plans:
- `loadBenchmarkFixture(tb testing.TB, name string) []byte`
- `loadOracleManifest(tb testing.TB) []oracleCase`
- any small caching/helper types needed to avoid re-reading files per iteration

The fixture loader must resolve exact paths under `testdata/bench/` and fail with the missing filename in the error message.

Add `benchmark_oracle_test.go` with `TestJSONTestSuiteOracle`. The test must:
- read `testdata/jsontestsuite/expectations.tsv`,
- iterate every listed file under `testdata/jsontestsuite/cases/`,
- call `NewParser()`, `Parse(data)`, and close every `Parser`/`Doc`,
- treat `accept` rows as "parse must succeed",
- treat `reject` rows as "parse must fail",
- fail if the manifest references a missing file or if an unlisted file exists in the vendored cases directory,
- fail if the vendored case count is below the floor `300`,
- log the final accepted and rejected case counts.

Do not shell out, do not read git metadata, and do not infer expectations from filename prefixes inside the runtime test logic; the manifest is the only runtime source of truth.
  </action>
  <verify>
    <automated>go test ./... -run TestJSONTestSuiteOracle -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'func loadBenchmarkFixture\\(tb testing.TB, name string\\) \\[\\]byte|func loadOracleManifest\\(tb testing.TB\\)' benchmark_fixtures_test.go` succeeds.
    - `rg 'TestJSONTestSuiteOracle|expectations.tsv|testdata/jsontestsuite/cases|minOracleCaseCount|300' benchmark_oracle_test.go` succeeds.
    - `go test ./... -run TestJSONTestSuiteOracle -count=1` exits 0.
  </acceptance_criteria>
  <done>The repo has one committed BENCH-06 oracle entrypoint and one committed fixture-loader surface that the later benchmark plans can build on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream benchmark files -> vendored `testdata/bench/` snapshot | missing provenance or mutable reads would make benchmark results non-reproducible |
| vendored oracle files -> `expectations.tsv` | a mismatch here invalidates BENCH-06 silently unless it is checked structurally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | T | benchmark corpus provenance | mitigate | commit a local README with exact file list, pinned refs, and checksums |
| T-07-01-02 | T | oracle expectations | mitigate | make the Go test fail on any manifest/file drift in either direction |
| T-07-01-03 | D | benchmark runtime input path | mitigate | force the loader to read only `testdata/bench/` so submodule or network drift cannot break runs |
</threat_model>

<verification>
- [ ] `test -f testdata/bench/twitter.json`
- [ ] `test -f testdata/bench/citm_catalog.json`
- [ ] `test -f testdata/bench/canada.json`
- [ ] `test -f testdata/bench/mesh.json`
- [ ] `test -f testdata/bench/numbers.json`
- [ ] `rg -F '19c3b1315a2a6b8ab0a6b7335bb97269cbd0a448' testdata/bench/README.md`
- [ ] `rg -F '1ef36fa01286573e846ac449e8683f8833c5b26a' testdata/jsontestsuite/README.md`
- [ ] `go test ./... -run TestJSONTestSuiteOracle -count=1`
</verification>

<success_criteria>
- All five BENCH-02 corpus files are committed under `testdata/bench/`
- The correctness oracle runs entirely from committed files and a committed manifest
- Later benchmark plans can reuse one fixture-loader surface instead of duplicating path logic
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-01-SUMMARY.md`
</output>
