---
phase: 07-benchmarks-v0.1-release
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - benchmark_fullparse_test.go
  - benchmark_coldstart_test.go
  - benchmark_typed_test.go
  - benchmark_selective_test.go
  - benchmark_native_alloc_test.go
autonomous: true
requirements:
  - BENCH-01
  - BENCH-03
  - BENCH-05
user_setup: []
must_haves:
  truths:
    - "Tier 2 and Tier 3 benchmark families are runnable on the current DOM API, with Tier 3 explicitly labeled as a placeholder."
    - "Native allocator stats are consumed through the reset/snapshot telemetry surface added in Plan 07-03."
    - "Tier 2 comparator fairness is locked to the shared schema definitions from Plan 07-02."
  artifacts:
    - path: "benchmark_native_alloc_test.go"
      provides: "benchmark helper that resets/snapshots native allocator counters and reports custom metrics"
      contains: "native-bytes/op"
    - path: "benchmark_fullparse_test.go"
      provides: "Tier 1 benchmark family wired to the native-metric helper that feeds the public snapshot"
      contains: "native-bytes/op"
    - path: "benchmark_typed_test.go"
      provides: "Tier 2 typed extraction benchmark family"
      contains: "BenchmarkTier2Typed_"
    - path: "benchmark_selective_test.go"
      provides: "Tier 3 selective placeholder benchmark family"
      contains: "BenchmarkTier3SelectivePlaceholder_"
  key_links:
    - from: "benchmark_native_alloc_test.go"
      to: "internal/ffi/bindings.go"
      via: "the benchmark helper consumes the reset/snapshot binding methods from Plan 07-03"
      pattern: "NativeAllocStats"
    - from: "benchmark_fullparse_test.go"
      to: "benchmark_native_alloc_test.go"
      via: "the public headline Tier 1 path must report native allocator stats alongside Go alloc counts"
      pattern: "native-bytes/op"
    - from: "benchmark_typed_test.go"
      to: "benchmark_schema_test.go"
      via: "all Tier 2 comparators must decode into the shared schema types"
      pattern: "benchTwitterRow"
    - from: "benchmark_selective_test.go"
      to: "iterator_test.go"
      via: "the placeholder benchmark must use the existing DOM traversal model rather than invent an On-Demand API"
      pattern: "GetField"
---

<objective>
Add Tier 2 and Tier 3 benchmark families plus the benchmark-side native allocator metrics required to interpret them honestly.

Purpose: complete the benchmark-consumer half of BENCH-01 and BENCH-05 after the telemetry surface exists.
Output: typed/selective benchmark families and benchmark metrics that report native allocator activity alongside Go alloc counts.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@.planning/phases/07-benchmarks-v0.1-release/07-PATTERNS.md
@.planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
@benchmark_fixtures_test.go
@benchmark_schema_test.go
@benchmark_comparators_test.go
@benchmark_comparators_minio_amd64_test.go
@benchmark_comparators_sonic_supported_test.go
@iterator_test.go
@example_test.go
@internal/ffi/bindings.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the native-metric helper and Tier 2 typed benchmark family</name>
  <files>
    benchmark_fullparse_test.go
    benchmark_coldstart_test.go
    benchmark_typed_test.go
    benchmark_native_alloc_test.go
  </files>
  <read_first>
    - benchmark_fullparse_test.go
    - benchmark_coldstart_test.go
    - benchmark_fixtures_test.go
    - benchmark_schema_test.go
    - benchmark_comparators_test.go
    - benchmark_comparators_minio_amd64_test.go
    - benchmark_comparators_sonic_supported_test.go
    - example_test.go
    - internal/ffi/bindings.go
  </read_first>
  <action>
Add `benchmark_native_alloc_test.go` with the benchmark helper surface that resets/snapshots the allocator counters from Plan 07-03 and reports these exact custom metrics:
- `native-bytes/op`
- `native-allocs/op`
- `native-live-bytes`

The helper must call `NativeAllocStatsReset()` immediately before each measured run and `NativeAllocStatsSnapshot()` immediately after the measured work, then feed the custom metrics through `b.ReportMetric`.

Update `benchmark_fullparse_test.go` so the Tier 1 benchmark family that feeds the public README/results snapshot also uses the native-metric helper. Tier 1 output must report the native metrics alongside the standard Go `-benchmem` numbers for every published comparator row. Update `benchmark_coldstart_test.go` the same way for `BenchmarkColdStart_*` and `BenchmarkWarm_*`, because those numbers are published in `coldwarm.bench.txt`.

Add `benchmark_typed_test.go` with benchmark names matching `BenchmarkTier2Typed_<Fixture>/<Comparator>`. Every comparator in Tier 2 must decode into the shared schema types from `benchmark_schema_test.go`; do not allow comparator-specific struct shapes. Use these exact extraction workloads:
- `Twitter`: read `search_metadata.max_id`, then for the first 10 `statuses` entries read `id`, `favorited`, `retweeted`, `user.id`, and `user.name`
- `CITMCatalog`: read the counts of `areaNames` and `topicNames`, then the first 20 event ids from `events`
- `Canada`: read top-level `type`, then sum the first 256 numeric coordinate values encountered while traversing the nested coordinate arrays
  </action>
  <verify>
    <automated>go test ./... -run '^$' -bench 'BenchmarkTier2Typed_' -benchtime=1x -count=1 && rg 'NativeAllocStatsReset|NativeAllocStatsSnapshot|native-bytes/op|native-allocs/op|native-live-bytes' benchmark_native_alloc_test.go benchmark_fullparse_test.go benchmark_coldstart_test.go && rg 'BenchmarkTier2Typed_|benchTwitterRow|benchCITMRow|benchCanadaRow' benchmark_typed_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'NativeAllocStatsReset|NativeAllocStatsSnapshot|native-bytes/op|native-allocs/op|native-live-bytes' benchmark_native_alloc_test.go` succeeds.
    - `rg 'native-bytes/op|native-allocs/op|native-live-bytes' benchmark_fullparse_test.go` succeeds.
    - `rg 'native-bytes/op|native-allocs/op|native-live-bytes' benchmark_coldstart_test.go` succeeds.
    - `rg 'BenchmarkTier2Typed_' benchmark_typed_test.go` succeeds.
    - `rg 'benchTwitterRow|benchCITMRow|benchCanadaRow' benchmark_typed_test.go` succeeds.
    - `go test ./... -run '^$' -bench 'BenchmarkTier2Typed_' -benchtime=1x -count=1` exits 0.
  </acceptance_criteria>
  <done>The repo has benchmark-side native metrics and a shared-schema Tier 2 benchmark family.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Tier 3 selective placeholder benchmark family</name>
  <files>
    benchmark_selective_test.go
  </files>
  <read_first>
    - benchmark_fixtures_test.go
    - benchmark_comparators_test.go
    - iterator_test.go
    - benchmark_native_alloc_test.go
  </read_first>
  <action>
Add `benchmark_selective_test.go` with benchmark names matching `BenchmarkTier3SelectivePlaceholder_<Fixture>/<Comparator>`. Keep the implementation on the current DOM API only and label it as a placeholder in code comments and benchmark descriptions.

Use these exact selective workloads:
- `Twitter`: read only the first 10 `statuses[*].user.id` and `statuses[*].retweeted`
- `CITMCatalog`: read only the first 20 event ids and one top-level count

Reuse the comparator surface from Plan 07-02 and the allocator helper from `benchmark_native_alloc_test.go`. Do not add or imply any new On-Demand or path-query public API.
  </action>
  <verify>
    <automated>go test ./... -run '^$' -bench 'BenchmarkTier3SelectivePlaceholder_' -benchtime=1x -count=1 && rg 'BenchmarkTier3SelectivePlaceholder_|placeholder' benchmark_selective_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'BenchmarkTier3SelectivePlaceholder_' benchmark_selective_test.go` succeeds.
    - `rg 'placeholder' benchmark_selective_test.go` succeeds.
    - `go test ./... -run '^$' -bench 'BenchmarkTier3SelectivePlaceholder_' -benchtime=1x -count=1` exits 0.
  </acceptance_criteria>
  <done>The selective benchmark family is present, runnable, and explicitly scoped as a DOM-era placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native allocator counters -> published benchmark metrics | incorrect reset/snapshot behavior would make BENCH-05 misleading |
| current DOM API -> Tier 3 placeholder benchmark | accidental preview of a future On-Demand surface would overstate shipped capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | I | native allocator metrics | mitigate | explicit reset/snapshot helper metrics with stable names |
| T-07-04-02 | T | placeholder selective benchmark | mitigate | force code/comments/benchmark names to use `SelectivePlaceholder` and DOM traversal only |
</threat_model>

<verification>
- [ ] `go test ./... -run '^$' -bench 'BenchmarkTier2Typed_' -benchtime=1x -count=1`
- [ ] `go test ./... -run '^$' -bench 'BenchmarkTier3SelectivePlaceholder_' -benchtime=1x -count=1`
- [ ] `rg 'NativeAllocStatsReset|NativeAllocStatsSnapshot' benchmark_native_alloc_test.go`
- [ ] `rg 'native-bytes/op|native-allocs/op|native-live-bytes' benchmark_fullparse_test.go`
- [ ] `rg 'native-bytes/op|native-allocs/op|native-live-bytes' benchmark_coldstart_test.go`
- [ ] `rg 'BenchmarkTier3SelectivePlaceholder_' benchmark_selective_test.go`
</verification>

<success_criteria>
- BENCH-05's telemetry surface is consumed by benchmark code through stable native-metric helpers
- Tier 2 and Tier 3 benchmark families are runnable through normal `go test -bench`
- Tier 3 remains clearly marked as a DOM-era placeholder and not a new API claim
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-04-SUMMARY.md`
</output>
