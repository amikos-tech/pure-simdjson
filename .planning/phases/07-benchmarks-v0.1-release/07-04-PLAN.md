---
phase: 07-benchmarks-v0.1-release
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - README.md
  - docs/benchmarks.md
  - CHANGELOG.md
  - LICENSE
  - NOTICE
autonomous: true
requirements:
  - BENCH-07
  - DOC-01
  - DOC-06
  - DOC-07
user_setup: []
must_haves:
  truths:
    - "The README is consumer-facing and short: install path, quick-start example, supported platforms, and a benchmark snapshot backed by measured results."
    - "`docs/benchmarks.md` carries the fairness note, tier definitions, comparator omission rule, cold vs warm distinction, and native allocator caveat."
    - "The repo root contains both `LICENSE` and `NOTICE`, and the changelog remains in Keep-a-Changelog format."
  artifacts:
    - path: "README.md"
      provides: "public project entrypoint with install, quick start, platform matrix, and benchmark snapshot"
      contains: "## Benchmark Snapshot"
    - path: "docs/benchmarks.md"
      provides: "detailed benchmark methodology, caveats, and rerun commands"
      contains: "Tier 1"
    - path: "NOTICE"
      provides: "simdjson attribution for the vendored upstream dependency"
      contains: "simdjson"
  key_links:
    - from: "README.md"
      to: "docs/bootstrap.md"
      via: "installation/bootstrap guidance should link to the detailed bootstrap contract rather than duplicate it"
      pattern: "docs/bootstrap.md"
    - from: "README.md"
      to: "docs/benchmarks.md"
      via: "the short benchmark snapshot should defer methodology detail to the dedicated benchmark document"
      pattern: "docs/benchmarks.md"
    - from: "NOTICE"
      to: "third_party/simdjson/LICENSE"
      via: "the upstream Apache-2.0 attribution must point at the actual vendored source"
      pattern: "Apache License"
---

<objective>
Turn the benchmark work into public-facing docs and legal artifacts.

Purpose: satisfy the README, changelog, and licensing requirements only after the benchmark harness is real enough to support public claims.
Output: a repo-root README, a detailed benchmark methodology doc, updated changelog content, and the repo-root `LICENSE` / `NOTICE` pair.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@.planning/phases/07-benchmarks-v0.1-release/07-PATTERNS.md
@docs/bootstrap.md
@docs/releases.md
@tests/smoke/go_bootstrap_smoke.go
@example_test.go
@CHANGELOG.md
@third_party/simdjson/LICENSE
@third_party/simdjson/LICENSE-MIT
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the README and the detailed benchmark methodology doc</name>
  <files>
    README.md
    docs/benchmarks.md
  </files>
  <read_first>
    - docs/bootstrap.md
    - docs/releases.md
    - tests/smoke/go_bootstrap_smoke.go
    - example_test.go
    - benchmark_fullparse_test.go
    - benchmark_typed_test.go
    - benchmark_selective_test.go
  </read_first>
  <action>
Create repo-root `README.md` as the public entrypoint. It must contain these exact top-level sections:
- `## Installation`
- `## Quick Start`
- `## Supported Platforms`
- `## Benchmark Snapshot`

The quick-start example must use the real public API flow from this repo: create a parser, parse a JSON byte slice, obtain the root object, read one field, and close resources. Keep the install guidance version-agnostic by using `@latest` or an equivalent non-pinned form so the README does not need a second rewrite during the patch release.

The supported platform matrix must list exactly:
- `linux/amd64`
- `linux/arm64`
- `darwin/amd64`
- `darwin/arm64`
- `windows/amd64`

The benchmark snapshot section must:
- state the `>=3x vs encoding/json + any` headline only after measured evidence exists,
- state the `within 2x of minio/simdjson-go on x86_64` claim only if the captured results support it,
- mention that comparator tables omit unsupported libraries on that target,
- link to `docs/benchmarks.md`,
- link to `docs/bootstrap.md` for bootstrap details.

Create `docs/benchmarks.md` as the detailed methodology document. It must include:
- exact Tier 1, Tier 2, and Tier 3 definitions,
- the comparator omission rule from Phase 7 context D-04,
- the cold-start vs warm distinction,
- the native allocator metrics note,
- one exact rerun command using `go test -bench`,
- one exact comparison command using `scripts/bench/run_benchstat.sh`.
  </action>
  <verify>
    <automated>test -f README.md && test -f docs/benchmarks.md && rg '^# pure-simdjson$|## Installation|## Quick Start|## Supported Platforms|## Benchmark Snapshot|docs/bootstrap.md|docs/benchmarks.md' README.md && rg 'Tier 1|Tier 2|Tier 3|cold-start|warm|native allocator|run_benchstat.sh|go test ./... -run '\\''\\^\\$'\\'' -bench' docs/benchmarks.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` succeeds.
    - `test -f docs/benchmarks.md` succeeds.
    - `rg '^# pure-simdjson$|## Installation|## Quick Start|## Supported Platforms|## Benchmark Snapshot' README.md` succeeds.
    - `rg 'docs/bootstrap.md|docs/benchmarks.md' README.md` succeeds.
    - `rg 'Tier 1|Tier 2|Tier 3|cold-start|warm|native allocator|run_benchstat.sh' docs/benchmarks.md` succeeds.
  </acceptance_criteria>
  <done>The repo has a short consumer README and a deeper benchmark methodology doc that together support BENCH-07 and DOC-01.</done>
</task>

<task type="auto">
  <name>Task 2: Add repo-root licensing and keep-a-changelog Phase 7 content</name>
  <files>
    CHANGELOG.md
    LICENSE
    NOTICE
  </files>
  <read_first>
    - CHANGELOG.md
    - third_party/simdjson/LICENSE
    - third_party/simdjson/LICENSE-MIT
    - docs/releases.md
  </read_first>
  <action>
Create repo-root `LICENSE` with the full MIT license text. Create repo-root `NOTICE` that explicitly attributes the vendored `simdjson` dependency, points at `third_party/simdjson/LICENSE`, and explains that the upstream project is carried under Apache-2.0 terms alongside its upstream MIT text.

Update `CHANGELOG.md` without breaking Keep-a-Changelog structure. Keep `## [Unreleased]` at the top and add Phase 7 content under concrete subsections such as `Added`, `Changed`, and `Documentation`. The unreleased bullets must cover:
- the benchmark harness and correctness oracle,
- the README and benchmark methodology doc,
- the repo-root `LICENSE` and `NOTICE`,
- the planned release-close follow-up for the benchmark/docs work.

Do not move the unreleased notes into a tagged version yet; that is reserved for the release-close plan.
  </action>
  <verify>
    <automated>test -f LICENSE && test -f NOTICE && rg 'MIT License' LICENSE && rg 'simdjson|Apache License|third_party/simdjson/LICENSE' NOTICE && rg '^## \\[Unreleased\\]|Keep a Changelog|Benchmark|README|NOTICE|LICENSE' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSE` succeeds.
    - `test -f NOTICE` succeeds.
    - `rg 'MIT License' LICENSE` succeeds.
    - `rg 'simdjson|Apache License|third_party/simdjson/LICENSE' NOTICE` succeeds.
    - `rg '^## \\[Unreleased\\]|Keep a Changelog|Benchmark|README|NOTICE|LICENSE' CHANGELOG.md` succeeds.
  </acceptance_criteria>
  <done>The repo root now carries the expected legal artifacts, and the changelog is ready to be rolled into the next patch release entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured benchmark results -> public README claim | stale or overstated numbers would undermine BENCH-07 |
| vendored upstream dependency -> repo-root legal files | incomplete attribution would leave DOC-07 unfinished despite the code shipping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | T | README benchmark claim | mitigate | require measured evidence plus a deeper methodology doc linked from the snapshot |
| T-07-04-02 | I | legal attribution | mitigate | cite the exact vendored simdjson license paths in `NOTICE` |
| T-07-04-03 | T | changelog state | mitigate | keep `Unreleased` during implementation and defer the versioned entry to release-close |
</threat_model>

<verification>
- [ ] `test -f README.md`
- [ ] `test -f docs/benchmarks.md`
- [ ] `test -f LICENSE`
- [ ] `test -f NOTICE`
- [ ] `rg '^## \\[Unreleased\\]' CHANGELOG.md`
</verification>

<success_criteria>
- The README is ready for public consumption and links to deeper docs instead of duplicating them
- The repo root contains the legal files required for distribution
- The changelog carries Phase 7 work without prematurely minting a release entry
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-04-SUMMARY.md`
</output>
