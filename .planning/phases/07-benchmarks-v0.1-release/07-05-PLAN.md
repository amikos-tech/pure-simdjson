---
phase: 07-benchmarks-v0.1-release
plan: "05"
type: execute
wave: 5
depends_on:
  - "07-04"
files_modified:
  - README.md
  - docs/benchmarks.md
  - docs/benchmarks/results-v0.1.1.md
  - testdata/benchmark-results/v0.1.1/
  - CHANGELOG.md
  - LICENSE
  - NOTICE
autonomous: true
requirements:
  - BENCH-07
  - DOC-01
  - DOC-06
  - DOC-07
user_setup: []
must_haves:
  truths:
    - "The README is consumer-facing and short: install path, quick-start example, supported platforms, and a benchmark snapshot backed by committed evidence."
    - "`docs/benchmarks.md` carries the fairness note, tier definitions, comparator omission rule, cold-start definition, native allocator caveat, and links to raw result files."
    - "The repo root contains both `LICENSE` and `NOTICE`, and the changelog remains in Keep-a-Changelog format."
  artifacts:
    - path: "docs/benchmarks/results-v0.1.1.md"
      provides: "the committed evidence summary that feeds README benchmark claims"
      contains: "encoding/json + any"
    - path: "README.md"
      provides: "public project entrypoint with install, quick start, platform matrix, and benchmark snapshot"
      contains: "## Benchmark Snapshot"
    - path: "NOTICE"
      provides: "simdjson attribution for the vendored upstream dependency"
      contains: "LICENSE-MIT"
  key_links:
    - from: "README.md"
      to: "docs/benchmarks/results-v0.1.1.md"
      via: "public benchmark claims must point at the committed evidence snapshot"
      pattern: "results-v0.1.1.md"
    - from: "README.md"
      to: "docs/bootstrap.md"
      via: "installation/bootstrap guidance should link to the detailed bootstrap contract rather than duplicate it"
      pattern: "docs/bootstrap.md"
    - from: "README.md"
      to: "example_test.go"
      via: "the quick-start snippet must be derived from a tested example path instead of drifting as freehand prose"
      pattern: "NewParser"
    - from: "docs/benchmarks.md"
      to: "testdata/benchmark-results/v0.1.1/"
      via: "the methodology document must show readers where the raw result files live"
      pattern: "testdata/benchmark-results/v0.1.1"
---

<objective>
Turn the benchmark work into public-facing docs, committed benchmark evidence, and legal artifacts.

Purpose: satisfy the README, changelog, and licensing requirements only after the benchmark harness is real enough to support public claims.
Output: a repo-root README, a detailed benchmark methodology doc, a committed results snapshot plus raw benchmark outputs, updated changelog content, and the repo-root `LICENSE` / `NOTICE` pair.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@.planning/phases/07-benchmarks-v0.1-release/07-PATTERNS.md
@.planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
@docs/bootstrap.md
@docs/releases.md
@tests/smoke/go_bootstrap_smoke.go
@example_test.go
@Makefile
@scripts/bench/run_benchstat.sh
@CHANGELOG.md
@third_party/simdjson/LICENSE
@third_party/simdjson/LICENSE-MIT
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture benchmark evidence and write the README plus benchmark docs from it</name>
  <files>
    README.md
    docs/benchmarks.md
    docs/benchmarks/results-v0.1.1.md
    testdata/benchmark-results/v0.1.1/
  </files>
  <read_first>
    - docs/bootstrap.md
    - docs/releases.md
    - tests/smoke/go_bootstrap_smoke.go
    - example_test.go
    - Makefile
    - scripts/bench/run_benchstat.sh
    - benchmark_fullparse_test.go
    - benchmark_typed_test.go
    - benchmark_selective_test.go
    - benchmark_native_alloc_test.go
  </read_first>
  <action>
After Plans 07-02 through 07-04 are merged, capture the committed benchmark evidence before editing README claims.

Write raw benchmark outputs to `testdata/benchmark-results/v0.1.1/` using these exact commands:
- `go test ./... -run '^$' -bench 'Benchmark(Tier1|Tier2|Tier3)_' -benchmem -count=5 > testdata/benchmark-results/v0.1.1/phase7.bench.txt`
- `go test ./... -run '^$' -bench 'Benchmark(ColdStart|Warm)_' -benchmem -count=5 > testdata/benchmark-results/v0.1.1/coldwarm.bench.txt`

Create `docs/benchmarks/results-v0.1.1.md` from those committed raw outputs. It must identify:
- which corpus files satisfy `>=3x vs encoding/json + any`
- whether `within 2x of minio/simdjson-go` holds on x86_64
- the exact target/toolchain used for the snapshot
- the exact raw files in `testdata/benchmark-results/v0.1.1/` that back each claim

The results document must also include these exact gate lines:
- `BENCH-07 >=3x: PASS` or `BENCH-07 >=3x: FAIL`
- `BENCH-07 within-2x-minio-x86_64: PASS` or `BENCH-07 within-2x-minio-x86_64: FAIL`

Create repo-root `README.md` as the public entrypoint. It must contain these exact top-level sections:
- `## Installation`
- `## Quick Start`
- `## Supported Platforms`
- `## Benchmark Snapshot`

The quick-start example must use the real public API flow from this repo: create a parser, parse a JSON byte slice, obtain the root object, read one field, and close resources. Keep the install guidance version-agnostic by using `@latest` or an equivalent non-pinned form so the README does not need a second rewrite during the patch release.

The supported platform matrix must list exactly:
- `linux/amd64`
- `linux/arm64`
- `darwin/amd64`
- `darwin/arm64`
- `windows/amd64`

The benchmark snapshot section must:
- link to `docs/benchmarks/results-v0.1.1.md`
- link to `docs/bootstrap.md` for bootstrap details
- mention that comparator tables omit unsupported libraries on that target
- use the `>=3x vs encoding/json + any` headline only if `docs/benchmarks/results-v0.1.1.md` proves it
- use the `within 2x of minio/simdjson-go on x86_64` claim only if `docs/benchmarks/results-v0.1.1.md` proves it

The quick-start snippet must be copied from a tested source path in `example_test.go` or `tests/smoke/go_bootstrap_smoke.go`, preserving the same parser lifecycle (`NewParser` -> `Parse` -> field read -> `Close`). Do not hand-write a different API sequence in the README.

If either BENCH-07 gate line in `docs/benchmarks/results-v0.1.1.md` is `FAIL`, stop this plan after writing the evidence snapshot and escalate BENCH-07 as unmet. Do not treat README benchmark publication as complete and do not proceed to Plan `07-06` under a truthful-but-threshold-missed fallback.

Create `docs/benchmarks.md` as the detailed methodology document. It must include:
- exact Tier 1, Tier 2, and Tier 3 definitions
- the comparator omission rule from Phase 7 context D-04
- the cold-start definition `first Parse after NewParser inside an already loaded process`, explicitly distinguished from bootstrap/download time
- the native allocator metrics note
- one exact rerun command using `go test -bench`
- one exact comparison command using `scripts/bench/run_benchstat.sh`
- links to `docs/benchmarks/results-v0.1.1.md` and `testdata/benchmark-results/v0.1.1/`
  </action>
  <verify>
    <automated>test -f README.md && test -f docs/benchmarks.md && test -f docs/benchmarks/results-v0.1.1.md && test -f testdata/benchmark-results/v0.1.1/phase7.bench.txt && test -f testdata/benchmark-results/v0.1.1/coldwarm.bench.txt && rg '^# pure-simdjson$' README.md && rg '^## Installation$' README.md && rg '^## Quick Start$' README.md && rg '^## Supported Platforms$' README.md && rg '^## Benchmark Snapshot$' README.md && rg 'results-v0.1.1.md' README.md && rg 'docs/bootstrap.md' README.md && rg 'NewParser' README.md example_test.go tests/smoke/go_bootstrap_smoke.go && rg 'Parse' README.md example_test.go tests/smoke/go_bootstrap_smoke.go && rg 'Close' README.md example_test.go tests/smoke/go_bootstrap_smoke.go && go test ./... -run '^Example' -count=1 && rg 'Tier 1' docs/benchmarks.md && rg 'Tier 2' docs/benchmarks.md && rg 'Tier 3' docs/benchmarks.md && rg 'first Parse after NewParser' docs/benchmarks.md && rg 'native allocator' docs/benchmarks.md && rg 'run_benchstat.sh' docs/benchmarks.md && rg 'testdata/benchmark-results/v0.1.1' docs/benchmarks.md && rg '^BENCH-07 >=3x: (PASS|FAIL)$' docs/benchmarks/results-v0.1.1.md && rg '^BENCH-07 within-2x-minio-x86_64: (PASS|FAIL)$' docs/benchmarks/results-v0.1.1.md && rg 'encoding/json \\+ any' docs/benchmarks/results-v0.1.1.md && rg 'minio/simdjson-go' docs/benchmarks/results-v0.1.1.md && rg 'phase7.bench.txt' docs/benchmarks/results-v0.1.1.md && rg 'coldwarm.bench.txt' docs/benchmarks/results-v0.1.1.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/benchmarks/results-v0.1.1.md` succeeds.
    - `test -f testdata/benchmark-results/v0.1.1/phase7.bench.txt` succeeds.
    - `test -f testdata/benchmark-results/v0.1.1/coldwarm.bench.txt` succeeds.
    - `rg '^# pure-simdjson$' README.md` succeeds.
    - `rg '^## Installation$' README.md` succeeds.
    - `rg '^## Quick Start$' README.md` succeeds.
    - `rg '^## Supported Platforms$' README.md` succeeds.
    - `rg '^## Benchmark Snapshot$' README.md` succeeds.
    - `rg 'results-v0.1.1.md' README.md` succeeds.
    - `rg 'docs/bootstrap.md' README.md` succeeds.
    - `rg 'NewParser' README.md example_test.go tests/smoke/go_bootstrap_smoke.go` succeeds.
    - `rg 'Parse' README.md example_test.go tests/smoke/go_bootstrap_smoke.go` succeeds.
    - `rg 'Close' README.md example_test.go tests/smoke/go_bootstrap_smoke.go` succeeds.
    - `go test ./... -run '^Example' -count=1` exits 0.
    - `rg 'Tier 1' docs/benchmarks.md` succeeds.
    - `rg 'Tier 2' docs/benchmarks.md` succeeds.
    - `rg 'Tier 3' docs/benchmarks.md` succeeds.
    - `rg 'first Parse after NewParser' docs/benchmarks.md` succeeds.
    - `rg 'native allocator' docs/benchmarks.md` succeeds.
    - `rg 'run_benchstat.sh' docs/benchmarks.md` succeeds.
    - `rg 'testdata/benchmark-results/v0.1.1' docs/benchmarks.md` succeeds.
    - `rg '^BENCH-07 >=3x: (PASS|FAIL)$' docs/benchmarks/results-v0.1.1.md` succeeds.
    - `rg '^BENCH-07 within-2x-minio-x86_64: (PASS|FAIL)$' docs/benchmarks/results-v0.1.1.md` succeeds.
    - `rg 'encoding/json \\+ any' docs/benchmarks/results-v0.1.1.md` succeeds.
    - `rg 'minio/simdjson-go' docs/benchmarks/results-v0.1.1.md` succeeds.
    - `rg 'phase7.bench.txt' docs/benchmarks/results-v0.1.1.md` succeeds.
    - `rg 'coldwarm.bench.txt' docs/benchmarks/results-v0.1.1.md` succeeds.
  </acceptance_criteria>
  <done>The repo has a short consumer README, a deeper benchmark methodology doc, and a committed evidence snapshot; if either BENCH-07 gate is `FAIL`, the plan stops for escalation before release-close.</done>
</task>

<task type="auto">
  <name>Task 2: Add repo-root licensing and Keep-a-Changelog Phase 7 content</name>
  <files>
    CHANGELOG.md
    LICENSE
    NOTICE
  </files>
  <read_first>
    - CHANGELOG.md
    - third_party/simdjson/LICENSE
    - third_party/simdjson/LICENSE-MIT
    - docs/releases.md
    - docs/benchmarks/results-v0.1.1.md
  </read_first>
  <action>
Create repo-root `LICENSE` with the full MIT license text and use the copyright line `Copyright (c) 2026 Amikos Tech`.

Create repo-root `NOTICE` that:
- explicitly attributes the vendored `simdjson` dependency
- points at both `third_party/simdjson/LICENSE` and `third_party/simdjson/LICENSE-MIT`
- explains that the vendored upstream project carries Apache-2.0 and MIT license texts in the vendored tree

Update `CHANGELOG.md` without breaking Keep-a-Changelog structure. Keep `## [Unreleased]` at the top and add Phase 7 content under concrete subsections such as `Added`, `Changed`, and `Documentation`. The unreleased bullets must cover:
- the benchmark harness and correctness oracle
- the committed benchmark evidence snapshot and raw result files
- the README and benchmark methodology doc
- the repo-root `LICENSE` and `NOTICE`
- the planned `v0.1.1` release-close follow-up for the benchmark/docs work

Do not move the unreleased notes into a tagged version yet; that is reserved for the release-close plan.
  </action>
  <verify>
    <automated>test -f LICENSE && test -f NOTICE && rg 'MIT License' LICENSE && rg 'Amikos Tech' LICENSE && rg 'simdjson' NOTICE && rg 'Apache License' NOTICE && rg 'LICENSE-MIT' NOTICE && rg 'third_party/simdjson/LICENSE' NOTICE && rg '^## \\[Unreleased\\]$' CHANGELOG.md && rg 'Keep a Changelog' CHANGELOG.md && rg 'Benchmark' CHANGELOG.md && rg 'README' CHANGELOG.md && rg 'NOTICE' CHANGELOG.md && rg 'LICENSE' CHANGELOG.md && rg 'results-v0.1.1' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f LICENSE` succeeds.
    - `test -f NOTICE` succeeds.
    - `rg 'MIT License' LICENSE` succeeds.
    - `rg 'Amikos Tech' LICENSE` succeeds.
    - `rg 'simdjson' NOTICE` succeeds.
    - `rg 'Apache License' NOTICE` succeeds.
    - `rg 'LICENSE-MIT' NOTICE` succeeds.
    - `rg 'third_party/simdjson/LICENSE' NOTICE` succeeds.
    - `rg '^## \\[Unreleased\\]$' CHANGELOG.md` succeeds.
    - `rg 'Keep a Changelog' CHANGELOG.md` succeeds.
    - `rg 'Benchmark' CHANGELOG.md` succeeds.
    - `rg 'README' CHANGELOG.md` succeeds.
    - `rg 'NOTICE' CHANGELOG.md` succeeds.
    - `rg 'LICENSE' CHANGELOG.md` succeeds.
    - `rg 'results-v0.1.1' CHANGELOG.md` succeeds.
  </acceptance_criteria>
  <done>The repo root now carries the expected legal artifacts, and the changelog is ready to be rolled into the next patch release entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured benchmark results -> public README claim | stale or overstated numbers would undermine BENCH-07 |
| vendored upstream dependency -> repo-root legal files | incomplete attribution would leave DOC-07 unfinished despite the code shipping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | T | README benchmark claim | mitigate | require committed evidence plus a deeper methodology doc linked from the snapshot |
| T-07-05-02 | I | legal attribution | mitigate | cite the exact vendored simdjson license paths in `NOTICE` and include both upstream license files |
| T-07-05-03 | T | changelog state | mitigate | keep `Unreleased` during implementation and defer the versioned entry to release-close |
</threat_model>

<verification>
- [ ] `test -f docs/benchmarks/results-v0.1.1.md`
- [ ] `test -f testdata/benchmark-results/v0.1.1/phase7.bench.txt`
- [ ] `test -f testdata/benchmark-results/v0.1.1/coldwarm.bench.txt`
- [ ] `test -f LICENSE`
- [ ] `test -f NOTICE`
- [ ] `rg '^## \\[Unreleased\\]' CHANGELOG.md`
</verification>

<success_criteria>
- The README is ready for public consumption and links to deeper docs instead of duplicating them
- Public benchmark claims are backed by committed raw results and a committed evidence summary
- The repo root contains the legal files required for distribution
- The changelog carries Phase 7 work without prematurely minting a release entry
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-05-SUMMARY.md`
</output>
