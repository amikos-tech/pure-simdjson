---
phase: 07-benchmarks-v0.1-release
plan: "06"
type: execute
wave: 6
depends_on:
  - "07-05"
autonomous: false
files_modified:
  - internal/bootstrap/version.go
  - CHANGELOG.md
requirements:
  - BENCH-07
  - DOC-06
user_setup:
  - service: github
    why: "This plan creates and pushes an annotated release tag, watches the tag-driven release workflow, and dispatches the post-publish public bootstrap validation workflow."
    dashboard_config:
      - task: "Authenticate both `git` push access and `gh` against github.com for this repository."
        location: "Local git remote + GitHub CLI session"
      - task: "Confirm the release workflow secrets and variables already required by `docs/releases.md` exist in GitHub Actions."
        location: "Repository Actions settings"
must_haves:
  truths:
    - "The existing `v0.1.0` tag is immutable history and must not be moved or recreated."
    - "If the Phase 7 benchmark/docs/legal work needs a public tagged release, it ships via `v0.1.1` through the existing CI-only publish path."
    - "Release-close is `origin/main -> annotated tag -> release.yml -> public-bootstrap-validation.yml`, exactly as documented in `docs/releases.md` and the repo-local release skill."
  artifacts:
    - path: "internal/bootstrap/version.go"
      provides: "compile-time version constant for the next release tag"
      contains: "const Version = "
    - path: "CHANGELOG.md"
      provides: "versioned release notes entry for the Phase 7 public release"
      contains: "## [0.1.1]"
    - path: "docs/benchmarks/results-v0.1.1.md"
      provides: "the committed benchmark evidence that release-close must preserve"
      contains: "encoding/json + any"
  key_links:
    - from: "internal/bootstrap/version.go"
      to: "docs/releases.md"
      via: "the version constant must match the pushed annotated tag consumed by `release.yml`"
      pattern: "v0.1.1"
    - from: "README.md"
      to: "docs/benchmarks/results-v0.1.1.md"
      via: "the public benchmark snapshot must agree with the committed evidence before a release tag is created"
      pattern: "results-v0.1.1.md"
    - from: ".github/workflows/public-bootstrap-validation.yml"
      to: "tests/smoke/go_bootstrap_smoke.go"
      via: "post-publish validation must still exercise the real bootstrap smoke path for the released version"
      pattern: "go_bootstrap_smoke.go"
---

<objective>
Close Phase 7 through the existing CI-only release path without rewriting the already-published `v0.1.0`.

Purpose: make the benchmark/docs/legal work publicly releasable and verifiable after the implementation plans land.
Output: a release-ready `0.1.1` source state, an annotated `v0.1.1` tag, a completed `release.yml` run, and a completed `public-bootstrap-validation.yml` run for `0.1.1`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@docs/releases.md
@docs/benchmarks.md
@docs/benchmarks/results-v0.1.1.md
@README.md
@testdata/benchmark-results/v0.1.1/phase7.bench.txt
@testdata/benchmark-results/v0.1.1/coldwarm.bench.txt
@internal/bootstrap/version.go
@CHANGELOG.md
@.github/workflows/release.yml
@.github/workflows/public-bootstrap-validation.yml
@.agents/skills/pure-simdjson-release/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the `0.1.1` release source state on `origin/main`</name>
  <files>
    internal/bootstrap/version.go
    CHANGELOG.md
  </files>
  <read_first>
    - docs/releases.md
    - docs/benchmarks.md
    - docs/benchmarks/results-v0.1.1.md
    - README.md
    - testdata/benchmark-results/v0.1.1/phase7.bench.txt
    - testdata/benchmark-results/v0.1.1/coldwarm.bench.txt
    - internal/bootstrap/version.go
    - CHANGELOG.md
    - .agents/skills/pure-simdjson-release/SKILL.md
  </read_first>
  <action>
After Plans 07-01 through 07-05 are merged, switch to the exact release commit on `origin/main` that should become the public benchmark/docs release. Do not work from a stale local branch tip or an unmerged feature branch.

Before bumping the version, verify that the evidence and docs are aligned on `origin/main`:
- `docs/benchmarks/results-v0.1.1.md` exists
- `testdata/benchmark-results/v0.1.1/phase7.bench.txt` exists
- `testdata/benchmark-results/v0.1.1/coldwarm.bench.txt` exists
- `README.md` links to `docs/benchmarks/results-v0.1.1.md`
- `docs/benchmarks/results-v0.1.1.md` contains `BENCH-07 >=3x: PASS`
- `docs/benchmarks/results-v0.1.1.md` contains `BENCH-07 within-2x-minio-x86_64: PASS`

If either BENCH-07 gate line is `FAIL`, stop this plan immediately and escalate. Do not bump the version, do not update the changelog into a release entry, and do not proceed to tagging under Phase 7. BENCH-07 is a hard gate for release-close, not optional release copy.

On that release commit, update `internal/bootstrap/version.go` from `0.1.0` to `0.1.1`. Then move the Phase 7 unreleased changelog bullets into a concrete `## [0.1.1] - YYYY-MM-DD` entry while preserving an empty `## [Unreleased]` placeholder at the top. Do not edit old `0.1.0` release notes.

Run this exact readiness command and require it to pass before any tag is created:

`bash scripts/release/check_readiness.sh --strict --version 0.1.1`

If the readiness gate fails, stop and fix the source state. Do not create or move any release tag until the strict gate succeeds.
  </action>
  <verify>
    <automated>test -f docs/benchmarks/results-v0.1.1.md && test -f testdata/benchmark-results/v0.1.1/phase7.bench.txt && test -f testdata/benchmark-results/v0.1.1/coldwarm.bench.txt && rg 'results-v0.1.1.md' README.md && rg '^BENCH-07 >=3x: PASS$' docs/benchmarks/results-v0.1.1.md && rg '^BENCH-07 within-2x-minio-x86_64: PASS$' docs/benchmarks/results-v0.1.1.md && grep -F 'const Version = "0.1.1"' internal/bootstrap/version.go && rg '^## \\[Unreleased\\]$' CHANGELOG.md && rg '^## \\[0\\.1\\.1\\]' CHANGELOG.md && bash scripts/release/check_readiness.sh --strict --version 0.1.1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/benchmarks/results-v0.1.1.md` succeeds.
    - `test -f testdata/benchmark-results/v0.1.1/phase7.bench.txt` succeeds.
    - `test -f testdata/benchmark-results/v0.1.1/coldwarm.bench.txt` succeeds.
    - `rg 'results-v0.1.1.md' README.md` succeeds.
    - `rg '^BENCH-07 >=3x: PASS$' docs/benchmarks/results-v0.1.1.md` succeeds.
    - `rg '^BENCH-07 within-2x-minio-x86_64: PASS$' docs/benchmarks/results-v0.1.1.md` succeeds.
    - `grep -F 'const Version = "0.1.1"' internal/bootstrap/version.go` succeeds.
    - `rg '^## \\[Unreleased\\]$' CHANGELOG.md` succeeds.
    - `rg '^## \\[0\\.1\\.1\\]' CHANGELOG.md` succeeds.
    - `bash scripts/release/check_readiness.sh --strict --version 0.1.1` exits 0.
  </acceptance_criteria>
  <done>The source tree is ready for a new public patch release without mutating the existing `v0.1.0` history.</done>
</task>

<task type="auto">
  <name>Task 2: Tag `v0.1.1`, watch `release.yml`, and run post-publish public validation</name>
  <files>
    internal/bootstrap/version.go
    CHANGELOG.md
  </files>
  <read_first>
    - docs/releases.md
    - .github/workflows/release.yml
    - .github/workflows/public-bootstrap-validation.yml
    - internal/bootstrap/version.go
    - CHANGELOG.md
  </read_first>
  <action>
This task is intentionally non-autonomous because it pushes tags and watches live GitHub workflows.

After explicit user approval and after Task 1 passes on `origin/main`, create and push the new annotated tag from that exact `origin/main` commit:
- `git tag -a v0.1.1 -m "v0.1.1"`
- `git push origin v0.1.1`

Use GitHub CLI to watch the tag-triggered release workflow to completion. Require the repository's `.github/workflows/release.yml` run for `v0.1.1` to conclude `success`.

After the publish workflow succeeds, dispatch the public bootstrap validation workflow for the same version:
- `gh workflow run public-bootstrap-validation.yml -f version=0.1.1`

Wait for the resulting workflow run and require it to conclude `success`.

Do not hand-upload artifacts, do not bypass CI, do not recreate or move `v0.1.0`, and do not tag from a commit that is not anchored on `origin/main`.
  </action>
  <verify>
    <automated>gh auth status -h github.com</automated>
  </verify>
  <acceptance_criteria>
    - `gh auth status -h github.com` exits 0 before tagging.
    - After approval, `git tag --list | rg '^v0\\.1\\.1$'` succeeds.
    - After approval, the `release.yml` run for `v0.1.1` concludes `success`.
    - After approval, the `public-bootstrap-validation.yml` run for `version=0.1.1` concludes `success`.
    - `v0.1.0` is unchanged throughout the process.
  </acceptance_criteria>
  <done>The Phase 7 output is publicly released and post-publish validated through the existing CI-only release path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prepared release source -> annotated tag | creating the wrong tag from the wrong commit would publish stale or misleading benchmark/docs state |
| tag push -> CI publish -> post-publish validation | skipping one of these gates would leave the public benchmark release unverified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | T | release history | mitigate | keep `v0.1.0` immutable and use `v0.1.1` for the Phase 7 public release |
| T-07-06-02 | R | release-close workflow observation | mitigate | require explicit `gh` auth and wait for the exact `release.yml` and public-validation runs to conclude `success` |
| T-07-06-03 | T | source/tag mismatch | mitigate | run `check_readiness.sh --strict --version 0.1.1` only after evidence/docs alignment on `origin/main` is verified |
</threat_model>

<verification>
- [ ] `test -f docs/benchmarks/results-v0.1.1.md`
- [ ] `test -f testdata/benchmark-results/v0.1.1/phase7.bench.txt`
- [ ] `test -f testdata/benchmark-results/v0.1.1/coldwarm.bench.txt`
- [ ] `rg '^BENCH-07 >=3x: PASS$' docs/benchmarks/results-v0.1.1.md`
- [ ] `rg '^BENCH-07 within-2x-minio-x86_64: PASS$' docs/benchmarks/results-v0.1.1.md`
- [ ] `grep -F 'const Version = "0.1.1"' internal/bootstrap/version.go`
- [ ] `rg '^## \\[0\\.1\\.1\\]' CHANGELOG.md`
- [ ] `bash scripts/release/check_readiness.sh --strict --version 0.1.1`
- [ ] `gh auth status -h github.com`
- [ ] observed `release.yml` success for `v0.1.1`
- [ ] observed `public-bootstrap-validation.yml` success for `0.1.1`
</verification>

<success_criteria>
- Phase 7 does not rewrite `v0.1.0`
- The benchmark/docs/legal work can be published as a clean patch release through the locked CI path
- BENCH-07 is proven in the committed results artifact before release-close proceeds
- The committed results artifact, README wording, and release note entry stay aligned through release-close
- Post-publish bootstrap validation still runs against the released version before the phase is considered closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-06-SUMMARY.md`
</output>
