---
phase: 07-benchmarks-v0.1-release
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - go.mod
  - go.sum
  - Makefile
  - scripts/bench/run_benchstat.sh
  - benchmark_schema_test.go
  - benchmark_comparators_test.go
  - benchmark_comparators_minio_amd64_test.go
  - benchmark_comparators_minio_stub_test.go
  - benchmark_comparators_sonic_supported_test.go
  - benchmark_comparators_sonic_stub_test.go
  - benchmark_fullparse_test.go
  - benchmark_coldstart_test.go
autonomous: true
requirements:
  - BENCH-01
  - BENCH-03
  - BENCH-04
user_setup: []
must_haves:
  truths:
    - "Tier 1 benchmarks time equivalent full-materialization work for every comparator, including `pure-simdjson`."
    - "Comparator availability is compile-time safe and target-aware; unsupported libraries are omitted with explicit reason strings instead of breaking `go test ./...`."
    - "Cold-start and warm paths are separate benchmark families, and the repo has a `benchstat` helper surface with statistically meaningful sampling."
  artifacts:
    - path: "benchmark_comparators_test.go"
      provides: "the canonical comparator registry, keys, and omission-reason surface reused by later benchmark files"
      contains: "minio-simdjson-go"
    - path: "benchmark_fullparse_test.go"
      provides: "Tier 1 full-materialization benchmark family"
      contains: "BenchmarkTier1FullParse_"
    - path: "scripts/bench/run_benchstat.sh"
      provides: "strict benchstat wrapper with dependency checks"
      contains: "benchstat@latest"
  key_links:
    - from: "benchmark_fullparse_test.go"
      to: "benchmark_fixtures_test.go"
      via: "Tier 1 benches must use the committed fixture loader instead of ad-hoc file reads"
      pattern: "loadBenchmarkFixture"
    - from: "benchmark_comparators_test.go"
      to: "go.mod"
      via: "the comparator adapters must match the exact imported module paths"
      pattern: "github.com/minio/simdjson-go"
    - from: "Makefile"
      to: "scripts/bench/run_benchstat.sh"
      via: "benchmark comparison targets should call the shared benchstat helper instead of duplicating shell"
      pattern: "run_benchstat.sh"
---

<objective>
Build the Tier 1 benchmark harness, comparator adapter surface, and cold-start vs warm reporting path.

Purpose: satisfy the structural benchmark requirements before native allocator telemetry and README claims are layered on top.
Output: comparator modules, Tier 1 benchmark families, cold/warm benchmark families, and repo-local benchstat helper commands.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
@.planning/phases/07-benchmarks-v0.1-release/07-PATTERNS.md
@.planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
@.planning/phases/07-benchmarks-v0.1-release/07-VALIDATION.md
@go.mod
@Makefile
@benchmark_fixtures_test.go
@tests/smoke/go_bootstrap_smoke.go
@example_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comparator dependencies, shared schema definitions, and compile-time-safe availability gates</name>
  <files>
    go.mod
    go.sum
    benchmark_schema_test.go
    benchmark_comparators_test.go
    benchmark_comparators_minio_amd64_test.go
    benchmark_comparators_minio_stub_test.go
    benchmark_comparators_sonic_supported_test.go
    benchmark_comparators_sonic_stub_test.go
  </files>
  <read_first>
    - go.mod
    - go.sum
    - benchmark_fixtures_test.go
    - example_test.go
    - .planning/phases/07-benchmarks-v0.1-release/07-RESEARCH.md
    - .planning/phases/07-benchmarks-v0.1-release/07-REVIEWS.md
    - .planning/research/PITFALLS.md
  </read_first>
  <action>
Update `go.mod` and `go.sum` to add the exact comparator modules required by BENCH-03:
- `github.com/minio/simdjson-go`
- `github.com/bytedance/sonic`
- `github.com/goccy/go-json`

Add `benchmark_schema_test.go` with the concrete typed benchmark target shapes used by Phase 7. Define at least these exact schema types:
- `benchTwitterRow`
- `benchCITMRow`
- `benchCanadaRow`

Every Tier 2 comparator added in later plans must decode into these exact shared schema types. Do not create comparator-specific struct shapes.

Add `benchmark_comparators_test.go` as the common comparator registry used by later benchmark files. It must define canonical comparator keys exactly as:
- `pure-simdjson`
- `encoding-json-any`
- `encoding-json-struct`
- `minio-simdjson-go`
- `bytedance-sonic`
- `goccy-go-json`

Make comparator portability compile-time safe:
- add `benchmark_comparators_minio_amd64_test.go` with `//go:build amd64` and the real `github.com/minio/simdjson-go` adapter
- add `benchmark_comparators_minio_stub_test.go` with `//go:build !amd64` that registers `minio-simdjson-go` as omitted with the explicit reason `unsupported on this GOARCH`
- add `benchmark_comparators_sonic_supported_test.go` with the real `github.com/bytedance/sonic` adapter behind the dependency's supported build tags
- add `benchmark_comparators_sonic_stub_test.go` as the complementary fallback that compiles everywhere else, does not import `sonic`, and registers the omission reason

Keep `encoding/json`, `goccy/go-json`, and `pure-simdjson` in the common file because they do not need the same build-tag split. Any comparator rejected by target or toolchain constraints must be omitted with a human-readable reason string instead of producing fake table rows, zero values, or a package-level compile failure.
  </action>
  <verify>
    <automated>rg 'github.com/minio/simdjson-go|github.com/bytedance/sonic|github.com/goccy/go-json' go.mod go.sum && rg 'type benchTwitterRow struct|type benchCITMRow struct|type benchCanadaRow struct' benchmark_schema_test.go && rg 'pure-simdjson|encoding-json-any|encoding-json-struct|minio-simdjson-go|bytedance-sonic|goccy-go-json' benchmark_comparators_test.go && rg '^//go:build amd64$' benchmark_comparators_minio_amd64_test.go && rg '^//go:build !amd64$' benchmark_comparators_minio_stub_test.go && rg 'unsupported on this GOARCH|unsupported on this target|unsupported on this toolchain' benchmark_comparators_minio_stub_test.go benchmark_comparators_sonic_stub_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'github.com/minio/simdjson-go|github.com/bytedance/sonic|github.com/goccy/go-json' go.mod go.sum` succeeds.
    - `rg 'type benchTwitterRow struct|type benchCITMRow struct|type benchCanadaRow struct' benchmark_schema_test.go` succeeds.
    - `rg 'pure-simdjson|encoding-json-any|encoding-json-struct|minio-simdjson-go|bytedance-sonic|goccy-go-json' benchmark_comparators_test.go` succeeds.
    - `rg '^//go:build amd64$' benchmark_comparators_minio_amd64_test.go` succeeds.
    - `rg '^//go:build !amd64$' benchmark_comparators_minio_stub_test.go` succeeds.
    - `rg 'unsupported on this GOARCH|unsupported on this target|unsupported on this toolchain' benchmark_comparators_minio_stub_test.go benchmark_comparators_sonic_stub_test.go` succeeds.
  </acceptance_criteria>
  <done>The repo has one explicit comparator surface, one shared schema definition surface, and compile-time-safe omission paths for architecture-constrained comparators.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tier 1 full-materialization benchmarks, cold/warm families, and benchstat commands</name>
  <files>
    Makefile
    scripts/bench/run_benchstat.sh
    benchmark_fullparse_test.go
    benchmark_coldstart_test.go
  </files>
  <read_first>
    - Makefile
    - benchmark_fixtures_test.go
    - benchmark_schema_test.go
    - benchmark_comparators_test.go
    - benchmark_comparators_minio_amd64_test.go
    - benchmark_comparators_sonic_supported_test.go
    - parser_test.go
    - pool_test.go
  </read_first>
  <action>
Add `benchmark_fullparse_test.go` with benchmark names matching `BenchmarkTier1FullParse_<Fixture>/<Comparator>`. Every Tier 1 sub-benchmark must time equivalent full-materialization work. For the `pure-simdjson` path, keep the timer running through DOM parse plus conversion into ordinary Go values (`map[string]any`, `[]any`, `string`, `bool`, `nil`, `int64`, `uint64`, `float64`). Do not stop the timer after parse and then materialize out-of-band.

Add `benchmark_coldstart_test.go` with these exact benchmark-family prefixes:
- `BenchmarkColdStart_`
- `BenchmarkWarm_`

`BenchmarkColdStart_*` must include `NewParser()` plus the first parse. `BenchmarkWarm_*` must perform one warm-up parse before `b.ResetTimer()` and then reuse the parser in the timed loop. Document in code comments that `cold-start` here means `first Parse after NewParser inside an already loaded process`, not bootstrap/download time.

Update `Makefile` with these exact targets:
- `bench-phase7`
- `bench-phase7-cold`
- `bench-phase7-compare`

Set the targets to:
- `bench-phase7`: `go test ./... -run '^$' -bench 'Benchmark(Tier1|Tier2|Tier3)_' -benchmem -count=5`
- `bench-phase7-cold`: `go test ./... -run '^$' -bench 'Benchmark(ColdStart|Warm)_' -benchmem -count=5`
- `bench-phase7-compare`: call `scripts/bench/run_benchstat.sh`

Create `scripts/bench/run_benchstat.sh` as a strict shell helper that accepts `--old <path>` and `--new <path>`, validates both arguments, fails early with `go install golang.org/x/perf/cmd/benchstat@latest` guidance if `benchstat` is missing, and then runs `benchstat` on the two input files.
  </action>
  <verify>
    <automated>bash -n scripts/bench/run_benchstat.sh && rg 'bench-phase7|bench-phase7-cold|bench-phase7-compare' Makefile && rg -- '-count=5' Makefile && rg 'command -v benchstat|golang.org/x/perf/cmd/benchstat@latest' scripts/bench/run_benchstat.sh && rg 'BenchmarkTier1FullParse_|BenchmarkColdStart_|BenchmarkWarm_|ResetTimer|ReportAllocs|SetBytes|first Parse after NewParser' benchmark_fullparse_test.go benchmark_coldstart_test.go && go test ./... -run '^$' -bench 'Benchmark(Tier1|ColdStart|Warm)_' -benchtime=1x -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/bench/run_benchstat.sh` succeeds.
    - `bash -n scripts/bench/run_benchstat.sh` exits 0.
    - `rg 'bench-phase7|bench-phase7-cold|bench-phase7-compare' Makefile` succeeds.
    - `rg -- '-count=5' Makefile` succeeds.
    - `rg 'command -v benchstat|golang.org/x/perf/cmd/benchstat@latest' scripts/bench/run_benchstat.sh` succeeds.
    - `rg 'BenchmarkTier1FullParse_|BenchmarkColdStart_|BenchmarkWarm_' benchmark_fullparse_test.go benchmark_coldstart_test.go` succeeds.
    - `rg 'ResetTimer|ReportAllocs|SetBytes|first Parse after NewParser' benchmark_fullparse_test.go benchmark_coldstart_test.go` succeeds.
    - `go test ./... -run '^$' -bench 'Benchmark(Tier1|ColdStart|Warm)_' -benchtime=1x -count=1` exits 0.
  </acceptance_criteria>
  <done>The repo can run the Tier 1 and cold/warm benchmark families repeatably and compare result files with one shared benchstat helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark adapter surface -> public comparison table | hidden comparator skips or fake placeholders would make the benchmark story misleading |
| parser lifecycle -> cold/warm numbers | missing warm-up or timer placement mistakes would distort BENCH-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T | comparator reporting | mitigate | use explicit comparator keys plus compile-time-safe omission gates and omit unsupported comparators entirely |
| T-07-02-02 | T | Tier 1 fairness | mitigate | keep full materialization inside the timed section for `pure-simdjson` |
| T-07-02-03 | I | cold/warm benchmark meaning | mitigate | separate benchmark families and require `b.ResetTimer()` only after the warm-up parse |
</threat_model>

<verification>
- [ ] `rg 'github.com/minio/simdjson-go|github.com/bytedance/sonic|github.com/goccy/go-json' go.mod go.sum`
- [ ] `rg '^//go:build amd64$' benchmark_comparators_minio_amd64_test.go`
- [ ] `rg 'BenchmarkTier1FullParse_|BenchmarkColdStart_|BenchmarkWarm_' benchmark_fullparse_test.go benchmark_coldstart_test.go`
- [ ] `rg -- '-count=5' Makefile`
- [ ] `bash -n scripts/bench/run_benchstat.sh`
- [ ] `go test ./... -run '^$' -bench 'Benchmark(Tier1|ColdStart|Warm)_' -benchtime=1x -count=1`
</verification>

<success_criteria>
- Tier 1, cold-start, and warm benchmark families all exist and execute through `go test -bench`
- Comparator availability is explicit, omission-based, and compile-time safe
- Make targets and a benchstat helper give the repo one repeatable benchmark command surface
</success_criteria>

<output>
After completion, create `.planning/phases/07-benchmarks-v0.1-release/07-02-SUMMARY.md`
</output>
