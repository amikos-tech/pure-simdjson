---
phase: 03-go-public-api-purego-happy-path
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - parser.go
  - doc.go
  - element.go
  - parser_test.go
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-09
  - API-12
user_setup: []
must_haves:
  truths:
    - "The public package declares `Parser`, `Doc`, `Element`, `Array`, and `Object` in Phase 3 and preserves the happy path `NewParser() -> Parse(data) -> doc.Root().GetInt64()`."
    - "`NewParser()` checks the native ABI version before allocating a parser and returns `ErrABIVersionMismatch` immediately on mismatch."
    - "`Parser.Close()` and `Doc.Close()` are idempotent, while later operations return `ErrClosed` and parser-busy remains visible instead of being auto-healed."
    - "`Parser.Close()` returns `ErrParserBusy` while a live doc still belongs to the parser and must not invalidate that doc; only `Doc.Close()` clears busy ownership."
    - "Purego calls that depend on Go-owned buffers or receiver-owned native handles use `runtime.KeepAlive(...)` immediately after the call so finalizers and GC cannot race native execution."
  artifacts:
    - path: parser.go
      provides: "Public parser constructor, parse path, ABI handshake, and parser close semantics"
      contains: "func NewParser()"
    - path: doc.go
      provides: "Live doc wrapper with cached root view and idempotent close"
      contains: "func (d *Doc) Root() Element"
    - path: element.go
      provides: "Public Element/Array/Object types with the single real `GetInt64` accessor"
      contains: "func (e Element) GetInt64()"
    - path: parser_test.go
      provides: "Happy-path, ABI-mismatch, closed-state, and parser-busy coverage"
      contains: "TestABIMismatchAtNewParser"
  key_links:
    - from: parser.go
      to: internal/ffi/bindings.go
      via: "Parser lifecycle calls use only the real Phase 3 native exports"
      pattern: "pure_simdjson_parser_"
    - from: doc.go
      to: include/pure_simdjson.h
      via: "The cached root view mirrors `pure_simdjson_value_view_t` exactly"
      pattern: "pure_simdjson_value_view_t"
    - from: parser_test.go
      to: tests/rust_shim_minimal.rs
      via: "Go wrapper behavior stays aligned with already-proven native lifecycle semantics"
      pattern: "parser busy"
---

<objective>
Implement the first public Go API surface for Phase 3: parser/doc lifecycle, cached root access, and the single real `Element.GetInt64()` accessor.

Purpose: satisfy the user-facing Go semantics of the happy path without pulling in the broader Phase 4 accessor and iteration surface.
Output: a usable `purejson` package that can allocate a parser, parse a document, read `doc.Root().GetInt64()`, close deterministically, and surface typed errors.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md
@.planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md
@.planning/phases/03-go-public-api-purego-happy-path/03-VALIDATION.md
@.planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md
@docs/ffi-contract.md
@include/pure_simdjson.h
@src/lib.rs
@tests/rust_shim_minimal.rs
@errors.go
@library_loading.go
@library_unix.go
@library_windows.go
@internal/ffi/types.go
@internal/ffi/bindings.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement parser, doc, and element happy-path semantics with explicit liveness guards</name>
  <files>parser.go, doc.go, element.go</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md, .planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md, .planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, docs/ffi-contract.md, include/pure_simdjson.h, tests/rust_shim_minimal.rs, errors.go, library_loading.go, internal/ffi/types.go, internal/ffi/bindings.go, parser.go, doc.go, element.go</read_first>
  <action>Implement the first public API surface in `package purejson`. In `parser.go`, declare `type Parser struct` with the native parser handle, synchronization for Go-visible lifecycle state, a closed flag, and enough ownership state to remember whether a live doc is attached. Add `func NewParser() (*Parser, error)` that resolves and binds the library, calls the shared ABI helper before `pure_simdjson_parser_new`, and returns `ErrABIVersionMismatch` immediately when the native value is not `0x00010000`. Add a package-private ABI override hook so tests can force a mismatch against the real library without fabricating a fake DLL. Add `func (p *Parser) Parse(data []byte) (*Doc, error)` that rejects a closed parser with `ErrClosed`, calls `pure_simdjson_parser_parse`, immediately calls `pure_simdjson_doc_root` on success, stores the returned root `ValueView` on the `Doc`, and calls `runtime.KeepAlive(data)` plus `runtime.KeepAlive(p)` immediately after native calls that depend on the slice buffer or parser handle. Make `func (p *Parser) Close() error` idempotent, but require it to return `ErrParserBusy` while a live doc still belongs to the parser; it must not auto-close or invalidate that doc, and the doc must remain usable until `Doc.Close()` clears busy ownership. In `doc.go`, declare `type Doc struct` with the native doc handle, a back-reference to its parser, cached root view, and a closed flag; implement `func (d *Doc) Root() Element` by returning `Element{doc: d, view: d.root}` so root access itself stays allocation-free and error-free. Implement `func (d *Doc) Close() error` as idempotent and clear the parser's live-doc ownership only when native `doc_free` succeeds, then call `runtime.KeepAlive(d)` and `runtime.KeepAlive(d.parser)` after the native free. In `element.go`, declare `type Element struct`, plus Phase 3 skeleton wrappers `type Array struct{ Element }` and `type Object struct{ Element }`, and implement only `func (e Element) GetInt64() (int64, error)`. `GetInt64()` must return `ErrClosed` when the parent doc is closed, call `pure_simdjson_element_get_int64` through the shared error translator on live docs, and call `runtime.KeepAlive(e.doc)` immediately after the purego call so finalizers cannot race the accessor.</action>
  <verify>
    <automated>cargo build --release && go test ./... && rg 'runtime.KeepAlive|ErrABIVersionMismatch|ErrParserBusy' parser.go doc.go element.go</automated>
  </verify>
  <acceptance_criteria>
    - `test -f parser.go` succeeds.
    - `test -f doc.go` succeeds.
    - `test -f element.go` succeeds.
    - `rg 'type Parser struct|func NewParser\\(\\) \\(\\*Parser, error\\)|func \\(p \\*Parser\\) Parse\\(data \\[\\]byte\\) \\(\\*Doc, error\\)|func \\(p \\*Parser\\) Close\\(\\) error' parser.go` succeeds.
    - `rg 'type Doc struct|func \\(d \\*Doc\\) Root\\(\\) Element|func \\(d \\*Doc\\) Close\\(\\) error' doc.go` succeeds.
    - `rg 'type Element struct|type Array struct|type Object struct|func \\(e Element\\) GetInt64\\(\\) \\(int64, error\\)' element.go` succeeds.
    - `rg 'runtime.KeepAlive' parser.go doc.go element.go` succeeds.
    - `rg 'ErrABIVersionMismatch|ErrClosed|ErrParserBusy' parser.go doc.go element.go` succeeds.
    - `cargo build --release && go test ./...` exits 0.
    - `rg 'runtime.KeepAlive|ErrABIVersionMismatch|ErrParserBusy' parser.go doc.go element.go` succeeds.
  </acceptance_criteria>
  <done>The public parser/doc/element happy path exists with exact ABI-mismatch, close, closed-doc, and parser-busy semantics, plus explicit liveness guards for purego calls.</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic tests for the public happy path and lifecycle edge cases</name>
  <files>parser_test.go</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md, .planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md, .planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, tests/rust_shim_minimal.rs, parser.go, doc.go, element.go, parser_test.go</read_first>
  <action>Write focused Go tests that prove the Phase 3 contract semantically instead of only grepping syntax. Cover the happy path `NewParser() -> Parse(data) -> doc.Root().GetInt64()`, ABI mismatch at `NewParser()` using the package-private override hook, double-close idempotence for both parser and doc, `Parser.Close()` while a doc is still live returning `ErrParserBusy` without invalidating that doc, parse-after-close returning `ErrClosed`, accessor-after-close returning `ErrClosed`, and parser-busy on a second parse while the first doc is still live. When asserting structured errors, verify that `errors.Is` matches the sentinel and that the wrapper exposes non-zero code/message detail only for the failing call path.</action>
  <verify>
    <automated>cargo build --release && go test ./... -run 'Test(HappyPathGetInt64|ABIMismatchAtNewParser|ParserDoubleClose|DocDoubleClose|ParserCloseWhileDocLive|ParseAfterClose|AccessorAfterClose|ParserBusy|StructuredErrorDetails)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f parser_test.go` succeeds.
    - `rg 'TestHappyPathGetInt64|TestABIMismatchAtNewParser|TestParserDoubleClose|TestDocDoubleClose|TestParserCloseWhileDocLive|TestParseAfterClose|TestAccessorAfterClose|TestParserBusy|TestStructuredErrorDetails' parser_test.go` succeeds.
    - `rg 'errors.Is' parser_test.go` succeeds.
    - `cargo build --release && go test ./... -run 'Test(HappyPathGetInt64|ABIMismatchAtNewParser|ParserDoubleClose|DocDoubleClose|ParserCloseWhileDocLive|ParseAfterClose|AccessorAfterClose|ParserBusy|StructuredErrorDetails)'` exits 0.
  </acceptance_criteria>
  <done>The public API behavior is covered by semantic tests that directly prove the Phase 3 contract instead of only checking token-level implementation details.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go public API -> native parser/doc handles | Mis-handled close or busy state would turn wrapper misuse into hidden native-state corruption |
| Go-owned buffers and receiver objects -> purego calls | Missing liveness guards can let GC/finalizers race native execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T | parser/doc lifecycle | mitigate | Keep parser/doc close semantics explicit, reject busy parser frees, and surface `ErrClosed` without silently mutating state |
| T-03-02-02 | T | ABI mismatch handling | mitigate | Force the ABI-version probe before parser allocation and cover the mismatch path with a deterministic test hook |
| T-03-02-03 | T | purego liveness | mitigate | Call `runtime.KeepAlive(...)` immediately after native calls that depend on slice buffers or receiver-owned handles |
| T-03-02-04 | I | structured error behavior | mitigate | Validate `errors.Is` against the sentinel set and ensure detail fields reflect only the failing call path |
</threat_model>

<verification>
- [ ] `cargo build --release && go test ./... -run 'Test(HappyPathGetInt64|ABIMismatchAtNewParser|ParserDoubleClose|DocDoubleClose|ParserCloseWhileDocLive|ParseAfterClose|AccessorAfterClose|ParserBusy|StructuredErrorDetails)'`
- [ ] `rg 'runtime.KeepAlive' parser.go doc.go element.go`
- [ ] `rg 'func NewParser|func \\(p \\*Parser\\) Parse|func \\(d \\*Doc\\) Root|func \\(e Element\\) GetInt64' parser.go doc.go element.go`
</verification>

<success_criteria>
- The public package exposes the locked happy-path types and methods
- `NewParser()` enforces ABI compatibility before allocating a native parser
- Close semantics, closed-object behavior, and parser-busy behavior are deterministic and tested
- Purego calls that depend on Go-owned buffers or live handles have explicit liveness guards
</success_criteria>

<output>
After completion, create `.planning/phases/03-go-public-api-purego-happy-path/03-02-SUMMARY.md`
</output>
