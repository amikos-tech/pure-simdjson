---
phase: 03-go-public-api-purego-happy-path
plan: "05"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - purejson.go
  - errors.go
  - parser.go
  - doc.go
  - element.go
  - pool.go
  - docs/concurrency.md
autonomous: true
requirements:
  - DOC-03
  - DOC-04
user_setup: []
must_haves:
  truths:
    - "Source docs describe the exact Phase 3 API that exists now, not future Phase 4 behavior."
    - "Package docs make the import path `github.com/amikos-tech/pure-simdjson` vs package name `purejson` explicit for consumers."
    - "`docs/concurrency.md` explains the single-doc invariant, goroutine-per-parser model, `ParserPool.Put` rejection rules, and leak-warning behavior."
  artifacts:
    - path: purejson.go
      provides: "Package-level doc comment clarifying import path and package name"
      contains: "Package purejson"
    - path: docs/concurrency.md
      provides: "User-facing concurrency and leak-warning contract"
      contains: "single-doc invariant"
    - path: pool.go
      provides: "Exported symbol docs for `ParserPool` and its methods"
      contains: "// ParserPool"
  key_links:
    - from: docs/concurrency.md
      to: pool.go
      via: "The documented pool and leak-warning rules match the actual implementation"
      pattern: "ParserPool"
    - from: purejson.go
      to: .planning/PROJECT.md
      via: "The package-level doc keeps the fixed module path visible at the Go API boundary"
      pattern: "github.com/amikos-tech/pure-simdjson"
---

<objective>
Document the public Phase 3 API and concurrency contract in source comments and `docs/concurrency.md`.

Purpose: close the remaining user-facing doc requirements after the pool/finalizer behavior is implemented, while keeping the execution surface smaller than the checker warning budget.
Output: package docs, exported symbol comments, and a concurrency guide that matches the actual Phase 3 behavior.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md
@.planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md
@.planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md
@errors.go
@parser.go
@doc.go
@element.go
@pool.go
@docs/concurrency.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package docs and exported comments for the exact Phase 3 surface</name>
  <files>purejson.go, errors.go, parser.go, doc.go, element.go, pool.go</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md, .planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md, .planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, purejson.go, errors.go, parser.go, doc.go, element.go, pool.go</read_first>
  <action>Create `purejson.go` with a package-level doc comment that explicitly says the import path is `github.com/amikos-tech/pure-simdjson` while the package name is `purejson`. Add exported doc comments for every Phase 3 exported symbol in `errors.go`, `parser.go`, `doc.go`, `element.go`, and `pool.go`: `Error`, `Parser`, `Doc`, `Element`, `Array`, `Object`, `ParserPool`, `NewParser`, `(*Parser).Parse`, `(*Parser).Close`, `(*Doc).Root`, `(*Doc).Close`, `Element.GetInt64`, `NewParserPool`, `(*ParserPool).Get`, and `(*ParserPool).Put`. The comments must describe the actual Phase 3 semantics, including busy-close and pool rejection behavior where relevant, not future Phase 4 behavior.</action>
  <verify>
    <automated>rg '^// Package purejson|^// Error|^// Parser|^// Doc|^// Element|^// Array|^// Object|^// ParserPool|^// NewParser|^// Parse|^// Close|^// Root|^// GetInt64|^// NewParserPool|^// Get|^// Put' purejson.go errors.go parser.go doc.go element.go pool.go</automated>
  </verify>
  <acceptance_criteria>
    - `test -f purejson.go` succeeds.
    - `rg '^// Package purejson' purejson.go` succeeds.
    - `rg '^// Error|^// Parser|^// Doc|^// Element|^// Array|^// Object|^// ParserPool|^// NewParser|^// Parse|^// Close|^// Root|^// GetInt64|^// NewParserPool|^// Get|^// Put' errors.go parser.go doc.go element.go pool.go` succeeds.
  </acceptance_criteria>
  <done>The public Go API is documented in-source and the import-path/package-name split is explicit to consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Write `docs/concurrency.md` for the exact parser/doc/pool contract</name>
  <files>docs/concurrency.md</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md, .planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md, .planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, pool.go, finalizer_prod.go, finalizer_testbuild.go, docs/concurrency.md</read_first>
  <action>Write `docs/concurrency.md` with the exact sections `## Invariant`, `## Why Parsers Are Not Shareable`, `## ParserPool Pattern`, `## Put Rejection Rules`, and `## Leak Warnings`. The document must say explicitly that a parser may own only one live document at a time, that the intended model is goroutine-per-parser, that `ParserPool.Put` rejects parsers with a live doc or closed parser, and that production finalizers clean silently while `purejson_testbuild` emits the leak warning prefix. Include one minimal code example using `pool.Get()`, `Parse`, `doc.Close()`, and `pool.Put()`.</action>
  <verify>
    <automated>rg '## Invariant|## Why Parsers Are Not Shareable|## ParserPool Pattern|## Put Rejection Rules|## Leak Warnings|single-doc invariant|goroutine-per-parser|ParserPool.Put|purejson_testbuild' docs/concurrency.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/concurrency.md` succeeds.
    - `rg '## Invariant|## Why Parsers Are Not Shareable|## ParserPool Pattern|## Put Rejection Rules|## Leak Warnings' docs/concurrency.md` succeeds.
    - `rg 'single-doc invariant|goroutine-per-parser|ParserPool.Put|purejson_testbuild' docs/concurrency.md` succeeds.
    - `rg 'pool.Get\\(|doc.Close\\(|pool.Put\\(' docs/concurrency.md` succeeds.
  </acceptance_criteria>
  <done>The concurrency guide explains the exact parser/doc/pool contract shipped in Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing docs -> actual wrapper behavior | Wrong docs would teach an unsafe parser-sharing model or the wrong cleanup expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | I | package docs | mitigate | Make the import-path/package-name split explicit at the package root and in symbol comments |
| T-03-05-02 | I | concurrency docs | mitigate | State the single-doc invariant, pool rejection rules, and leak-warning behavior exactly as implemented |
</threat_model>

<verification>
- [ ] `rg '^// Package purejson|^// ParserPool|^// NewParserPool|^// Put' purejson.go pool.go`
- [ ] `rg '## Invariant|## ParserPool Pattern|## Leak Warnings' docs/concurrency.md`
</verification>

<success_criteria>
- Consumers can understand the public API from source docs alone
- The concurrency guide matches the actual pool/finalizer behavior implemented in Phase 3
- The package-name/module-path split is no longer a source of consumer confusion
</success_criteria>

<output>
After completion, create `.planning/phases/03-go-public-api-purego-happy-path/03-05-SUMMARY.md`
</output>
