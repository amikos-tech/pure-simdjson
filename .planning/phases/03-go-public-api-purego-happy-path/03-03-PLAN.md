---
phase: 03-go-public-api-purego-happy-path
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - parser.go
  - doc.go
  - pool.go
  - finalizer_prod.go
  - finalizer_testbuild.go
  - parser_test.go
  - pool_test.go
autonomous: true
requirements:
  - API-10
  - API-11
user_setup: []
must_haves:
  truths:
    - "`ParserPool` rejects nil, closed, and still-busy parsers instead of silently repairing misuse."
    - "Finalizer attachment is build-tag-specific: production finalizers clean leaked native resources silently, while `purejson_testbuild` adds the warning prefix `purejson leak:` before the same cleanup path."
    - "Parsers returned to `sync.Pool` remain protected against GC eviction leaks because the cleanup finalizer stays armed until explicit `Close()` clears it."
  artifacts:
    - path: pool.go
      provides: "sync.Pool-backed parser reuse primitive with deterministic rejection rules"
      contains: "func (p *ParserPool) Put"
    - path: finalizer_prod.go
      provides: "Production leak-cleanup finalizer helpers without warning text"
      contains: "attachParserFinalizer"
    - path: finalizer_testbuild.go
      provides: "Test-build warning finalizer helpers"
      contains: "purejson leak:"
  key_links:
    - from: pool.go
      to: .planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md
      via: "Pool behavior obeys the locked rule that busy parsers must be rejected, not auto-healed"
      pattern: "ParserPool.Put"
    - from: finalizer_prod.go
      to: finalizer_testbuild.go
      via: "Both build variants share cleanup semantics while only the test-build variant adds warnings"
      pattern: "attachParserFinalizer"
---

<objective>
Implement `ParserPool` and finalize the finalizer strategy.

Purpose: resolve the review gaps around `sync.Pool` eviction and finalizer behavior without changing the Phase 3 happy-path scope.
Output: a safe `ParserPool`, build-tag-specific finalizer helpers with explicit cleanup semantics, and semantic tests that prove the tricky lifetime cases.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md
@.planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md
@.planning/phases/03-go-public-api-purego-happy-path/03-VALIDATION.md
@.planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md
@parser.go
@doc.go
@pool.go
@parser_test.go
@pool_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `ParserPool` plus build-tag-specific cleanup finalizers</name>
  <files>pool.go, parser.go, doc.go, finalizer_prod.go, finalizer_testbuild.go</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md, .planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md, .planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, parser.go, doc.go, pool.go, finalizer_prod.go, finalizer_testbuild.go</read_first>
  <action>Implement the concurrency helper and finalizer strategy as one coherent design. In `pool.go`, declare `type ParserPool struct { pool sync.Pool }` plus `func NewParserPool() *ParserPool`, `func (p *ParserPool) Get() (*Parser, error)`, and `func (p *ParserPool) Put(parser *Parser) error`. `Get()` should create a new parser on a miss through `NewParser()`. `Put()` must reject `nil`, closed, or still-busy parsers and must not auto-close docs or replace the parser; return `ErrClosed` for closed parsers and `ErrParserBusy` when a live doc still belongs to the parser. In `finalizer_prod.go` and `finalizer_testbuild.go`, introduce the same helper surface, for example `attachParserFinalizer`, `attachDocFinalizer`, `clearParserFinalizer`, and `clearDocFinalizer`. The production build (`//go:build !purejson_testbuild`) must attach silent cleanup finalizers that release leaked native parser/doc handles without warning text. The test-build variant (`//go:build purejson_testbuild`) must attach the same cleanup behavior but log `purejson leak: parser` or `purejson leak: doc` before cleanup. Update `parser.go` and `doc.go` to attach these finalizers when a parser/doc becomes live and clear them on explicit `Close()`. Keep finalizers armed while parsers sit inside `sync.Pool` so a GC-evicted pooled parser cannot leak native resources.</action>
  <verify>
    <automated>cargo build --release && go test ./... && rg 'type ParserPool struct|func NewParserPool\\(|func \\(p \\*ParserPool\\) Put\\(' pool.go && rg 'attachParserFinalizer|attachDocFinalizer|clearParserFinalizer|clearDocFinalizer' parser.go doc.go finalizer_prod.go finalizer_testbuild.go</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pool.go` succeeds.
    - `test -f finalizer_prod.go` succeeds.
    - `test -f finalizer_testbuild.go` succeeds.
    - `rg 'type ParserPool struct|sync.Pool|func NewParserPool\\(|func \\(p \\*ParserPool\\) Get\\(|func \\(p \\*ParserPool\\) Put\\(' pool.go` succeeds.
    - `rg 'attachParserFinalizer|attachDocFinalizer|clearParserFinalizer|clearDocFinalizer' parser.go doc.go finalizer_prod.go finalizer_testbuild.go` succeeds.
    - `rg 'go:build purejson_testbuild|purejson leak:' finalizer_testbuild.go` succeeds.
    - `rg 'go:build !purejson_testbuild' finalizer_prod.go` succeeds.
    - `cargo build --release && go test ./...` exits 0.
    - `rg 'type ParserPool struct|func NewParserPool\\(|func \\(p \\*ParserPool\\) Put\\(' pool.go` succeeds.
    - `rg 'attachParserFinalizer|attachDocFinalizer|clearParserFinalizer|clearDocFinalizer' parser.go doc.go finalizer_prod.go finalizer_testbuild.go` succeeds.
  </acceptance_criteria>
  <done>`ParserPool` exists as the explicit goroutine-per-parser reuse primitive, and finalizer behavior is coherent with both the review feedback and the Phase 3 requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests that prove pool-eviction cleanup and warning split semantically</name>
  <files>pool_test.go, parser_test.go</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, parser.go, doc.go, pool.go, finalizer_prod.go, finalizer_testbuild.go, parser_test.go, pool_test.go</read_first>
  <action>Add tests that prove the new design rather than assuming it. In `pool_test.go`, cover parser pool round-trip across goroutines, rejection of a parser with a live doc, and rejection of a closed parser. Add one test-only hook, counter, or helper-process assertion that demonstrates a parser returned to the pool is still cleaned up if the `sync.Pool` entry is later dropped by GC, so the review concern about pool eviction is closed concretely. In `parser_test.go` or `pool_test.go`, add subprocess-based tests that run once with `-tags purejson_testbuild` and assert the `purejson leak:` prefix appears, and once without the tag and assert the same leak path is silent while cleanup still happens. Add one explicit stress-style test such as `TestLeakWarningMassLeak10000` that allocates 10,000 parsers without `Close()` under the test-build path and proves warning emission there, while the default build remains silent. Use GC/polling loops or helper processes rather than fixed sleeps.</action>
  <verify>
    <automated>cargo build --release && go test ./... -run 'Test(ParserPoolRoundTrip|ParserPoolRejectsBusy|ParserPoolRejectsClosed|PooledParserEvictionCleansUp|LeakWarningSilentProd)' && cargo build --release && go test ./... -race -run 'Test(ParserPoolRoundTrip|ParserPoolRejectsBusy)' && cargo build --release && go test ./... -tags purejson_testbuild -run 'Test(LeakWarningTestBuild|LeakWarningMassLeak10000)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pool_test.go` succeeds.
    - `rg 'TestParserPoolRoundTrip|TestParserPoolRejectsBusy|TestParserPoolRejectsClosed|TestPooledParserEvictionCleansUp|TestLeakWarningTestBuild|TestLeakWarningSilentProd|TestLeakWarningMassLeak10000' pool_test.go parser_test.go` succeeds.
    - `! rg 'sleep 10|time.Sleep\\(10' pool_test.go parser_test.go` succeeds.
    - `cargo build --release && go test ./... -run 'Test(ParserPoolRoundTrip|ParserPoolRejectsBusy|ParserPoolRejectsClosed|PooledParserEvictionCleansUp|LeakWarningSilentProd)'` exits 0.
    - `cargo build --release && go test ./... -race -run 'Test(ParserPoolRoundTrip|ParserPoolRejectsBusy)'` exits 0.
    - `cargo build --release && go test ./... -tags purejson_testbuild -run 'Test(LeakWarningTestBuild|LeakWarningMassLeak10000)'` exits 0.
  </acceptance_criteria>
  <done>The pool/finalizer behavior is proven semantically, including the tricky case where `sync.Pool` drops an idle parser under GC pressure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go wrapper lifecycle -> native parser/doc handles | Mis-handled pool or finalizer behavior would leak or corrupt native state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | `ParserPool` | mitigate | Reject busy or closed parsers at `Put()` instead of auto-closing or replacing them |
| T-03-03-02 | T | pool eviction | mitigate | Keep cleanup finalizers armed while parsers sit in `sync.Pool` and prove the behavior with an explicit eviction test |
| T-03-03-03 | I | finalizer instrumentation | mitigate | Build-tag the warning path so only `purejson_testbuild` emits `purejson leak:` while production remains silent |
</threat_model>

<verification>
- [ ] `cargo build --release && go test ./... -run 'Test(ParserPoolRoundTrip|ParserPoolRejectsBusy|ParserPoolRejectsClosed|PooledParserEvictionCleansUp|LeakWarningSilentProd)'`
- [ ] `cargo build --release && go test ./... -race -run 'Test(ParserPoolRoundTrip|ParserPoolRejectsBusy)'`
- [ ] `cargo build --release && go test ./... -tags purejson_testbuild -run 'TestLeakWarningTestBuild'`
- [ ] `cargo build --release && go test ./... -tags purejson_testbuild -run 'TestLeakWarningMassLeak10000'`
- [ ] `rg 'attachParserFinalizer|attachDocFinalizer|clearParserFinalizer|clearDocFinalizer' parser.go doc.go finalizer_prod.go finalizer_testbuild.go`
</verification>

<success_criteria>
- `ParserPool` embodies the goroutine-per-parser model without silently repairing misuse
- Finalizer behavior is explicit: silent cleanup in production, warning plus cleanup in `purejson_testbuild`
- `sync.Pool` eviction no longer leaves parser lifetime ambiguous
</success_criteria>

<output>
After completion, create `.planning/phases/03-go-public-api-purego-happy-path/03-03-SUMMARY.md`
</output>
