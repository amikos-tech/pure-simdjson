---
phase: 03-go-public-api-purego-happy-path
plan: "04"
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - Makefile
  - .github/workflows/phase3-go-wrapper-smoke.yml
  - scripts/phase3-go-wrapper-smoke.sh
autonomous: false
requirements:
  - API-03
  - API-09
  - API-10
user_setup:
  - service: github
    why: "This plan pushes the working branch, dispatches a GitHub Actions workflow, and watches the observed five-target wrapper-smoke run."
    dashboard_config:
      - task: "Authenticate `gh` against github.com before remote verification begins."
        location: "Local GitHub CLI session"
      - task: "Ensure the working branch can be pushed to `origin` and the account can view Actions runs for this repository."
        location: "Repository remote permissions and GitHub Actions access"
must_haves:
  truths:
    - "Remote wrapper proof is split from local code changes and is not an unattended autonomous step."
    - "The wrapper-smoke workflow remains narrow: local shim build plus `go test ./... -race`, with no bootstrap, release uploads, signing, or broader Phase 6 automation."
    - "The workflow proves all five Phase 3 targets: `linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, and `windows/amd64`."
    - "The macOS proofs use pinned `macos-15-intel` and `macos-15` labels, the Linux arm64 proof uses `ubuntu-24.04-arm`, and the Windows proof enters an MSVC developer shell before building the shim."
    - "Workflow observation is branch-scoped and run-id-based, not a brittle `sleep 10` plus `gh run list --limit 1` heuristic."
  artifacts:
    - path: Makefile
      provides: "Local verification targets that mirror the workflow's wrapper-smoke commands"
      contains: "phase3-go-race"
    - path: .github/workflows/phase3-go-wrapper-smoke.yml
      provides: "Narrow race-enabled wrapper-smoke workflow for all five Phase 3 targets"
      contains: "linux-amd64-go-race"
    - path: scripts/phase3-go-wrapper-smoke.sh
      provides: "Robust dispatch-and-watch helper for the remote wrapper proof"
      contains: "gh workflow run"
  key_links:
    - from: .github/workflows/phase3-go-wrapper-smoke.yml
      to: .github/workflows/phase2-rust-shim-smoke.yml
      via: "Windows MSVC setup and repo-local build/test conventions stay consistent with the existing smoke workflow"
      pattern: "ilammy/msvc-dev-cmd@v1"
    - from: scripts/phase3-go-wrapper-smoke.sh
      to: .github/workflows/phase3-go-wrapper-smoke.yml
      via: "The observer watches the specific workflow and checks the named job conclusions"
      pattern: "phase3-go-wrapper-smoke.yml"
    - from: .github/workflows/phase3-go-wrapper-smoke.yml
      to: library_loading.go
      via: "The workflow relies on deterministic repo-local loader behavior instead of bootstrap/download"
      pattern: "target/release"
---

<objective>
Add reproducible local verification targets and a robust, observed five-target GitHub workflow that proves the Go happy path under `-race`.

Purpose: satisfy the roadmap's wrapper-proof requirement without folding remote GitHub activity into an unattended autonomous plan or prematurely expanding into release automation.
Output: local make targets, a narrow workflow file, and a branch-scoped dispatch-and-watch helper that captures observed `success` results for `linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, and `windows/amd64`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-go-public-api-purego-happy-path/03-CONTEXT.md
@.planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md
@.planning/phases/03-go-public-api-purego-happy-path/03-VALIDATION.md
@.planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md
@Makefile
@.github/workflows/phase2-rust-shim-smoke.yml
@library_loading.go
@parser_test.go
@pool_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local verification targets and the narrow wrapper-smoke workflow</name>
  <files>Makefile, .github/workflows/phase3-go-wrapper-smoke.yml</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-RESEARCH.md, .planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, Makefile, .github/workflows/phase2-rust-shim-smoke.yml, library_loading.go, parser_test.go, pool_test.go, .github/workflows/phase3-go-wrapper-smoke.yml</read_first>
  <action>Add local verification targets in `Makefile` named `phase3-go-test`, `phase3-go-race`, and `phase3-go-wrapper-remote`. `phase3-go-test` must run `cargo build --release && go test ./...`; `phase3-go-race` must run `cargo build --release && go test ./... -race`; `phase3-go-wrapper-remote` must invoke `scripts/phase3-go-wrapper-smoke.sh`. Then add `.github/workflows/phase3-go-wrapper-smoke.yml` with `workflow_dispatch` and exactly five wrapper-smoke jobs: `linux-amd64-go-race` on `ubuntu-latest`, `linux-arm64-go-race` on `ubuntu-24.04-arm`, `darwin-amd64-go-race` on `macos-15-intel`, `darwin-arm64-go-race` on `macos-15`, and `windows-amd64-go-race` on `windows-latest`. Each job must check out submodules, install Rust stable and Go 1.24, build the local shim with `cargo build --release`, and run `go test ./... -race` from the repo root without invoking bootstrap/download logic. The Windows job must run `ilammy/msvc-dev-cmd@v1` before build/test. Do not add release uploads, artifact publishing, cosign, bootstrap commands, or broader Phase 6 matrix behavior.</action>
  <verify>
    <automated>test -f .github/workflows/phase3-go-wrapper-smoke.yml && rg '^phase3-go-test|^phase3-go-race|^phase3-go-wrapper-remote' Makefile && rg 'workflow_dispatch|linux-amd64-go-race|linux-arm64-go-race|darwin-amd64-go-race|darwin-arm64-go-race|windows-amd64-go-race|ubuntu-24.04-arm|macos-15-intel|macos-15|windows-latest|cargo build --release|go test ./\\.\\.\\. -race|ilammy/msvc-dev-cmd@v1' .github/workflows/phase3-go-wrapper-smoke.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/phase3-go-wrapper-smoke.yml` succeeds.
    - `rg '^phase3-go-test|^phase3-go-race|^phase3-go-wrapper-remote' Makefile` succeeds.
    - `rg 'linux-amd64-go-race|linux-arm64-go-race|darwin-amd64-go-race|darwin-arm64-go-race|windows-amd64-go-race' .github/workflows/phase3-go-wrapper-smoke.yml` succeeds.
    - `rg 'ubuntu-latest|ubuntu-24.04-arm|macos-15-intel|macos-15|windows-latest|ilammy/msvc-dev-cmd@v1' .github/workflows/phase3-go-wrapper-smoke.yml` succeeds.
    - `rg 'cargo build --release|go test ./\\.\\.\\. -race' .github/workflows/phase3-go-wrapper-smoke.yml` succeeds.
    - `! rg 'upload-artifact|cosign|PURE_SIMDJSON_BINARY_MIRROR|releases\\.amikos\\.tech|cmd/pure-simdjson-bootstrap|BootstrapSync' .github/workflows/phase3-go-wrapper-smoke.yml Makefile` succeeds.
  </acceptance_criteria>
  <done>The repo has local verification targets and a narrow workflow that proves exactly the Phase 3 wrapper surface without expanding into release automation.</done>
</task>

<task type="auto">
  <name>Task 2: Add a robust branch-scoped dispatch-and-watch helper, then run it with human approval</name>
  <files>scripts/phase3-go-wrapper-smoke.sh, Makefile</files>
  <read_first>.planning/phases/03-go-public-api-purego-happy-path/03-REVIEWS.md, Makefile, scripts/phase3-go-wrapper-smoke.sh, .github/workflows/phase3-go-wrapper-smoke.yml</read_first>
  <action>Create `scripts/phase3-go-wrapper-smoke.sh` as a non-interactive helper that performs the remote proof robustly. The script must: 1) capture the current branch name and a pre-dispatch UTC timestamp, 2) verify `gh auth status -h github.com` and `git remote get-url origin` before doing anything destructive, 3) push the current branch to `origin`, 4) dispatch `phase3-go-wrapper-smoke.yml` for that branch, 5) poll `gh run list` for the newest `workflow_dispatch` run on the same branch created after the captured timestamp with bounded retries until a specific run id appears, 6) watch that exact run id to completion, and 7) assert that `linux-amd64-go-race`, `linux-arm64-go-race`, `darwin-amd64-go-race`, `darwin-arm64-go-race`, and `windows-amd64-go-race` all conclude `success`. Do not use a fixed `sleep 10` plus `--limit 1` shortcut. Wire `make phase3-go-wrapper-remote` to this script. Because this task pushes to the remote and dispatches GitHub Actions, stop for approval/user checkpoint before running the helper; after approval, execute it and fail the plan if any required job is not `success`.</action>
  <verify>
    <automated>gh auth status -h github.com && scripts/phase3-go-wrapper-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/phase3-go-wrapper-smoke.sh` succeeds.
    - `rg 'gh auth status -h github.com|gh workflow run|gh run list|gh run watch' scripts/phase3-go-wrapper-smoke.sh` succeeds.
    - `rg 'linux-amd64-go-race|linux-arm64-go-race|darwin-amd64-go-race|darwin-arm64-go-race|windows-amd64-go-race' scripts/phase3-go-wrapper-smoke.sh` succeeds.
    - `! rg 'sleep 10|--limit 1' scripts/phase3-go-wrapper-smoke.sh` succeeds.
    - `gh auth status -h github.com` exits 0 before dispatch.
    - After user approval, `scripts/phase3-go-wrapper-smoke.sh` exits 0 and the observed `linux-amd64-go-race`, `linux-arm64-go-race`, `darwin-amd64-go-race`, `darwin-arm64-go-race`, and `windows-amd64-go-race` job conclusions are all `success`.
  </acceptance_criteria>
  <done>The remote wrapper proof is observed through a branch-scoped run-id workflow and remains an explicit user-approved step rather than unattended automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch -> GitHub remote | Push and workflow dispatch require authenticated access and should not happen unattended |
| workflow YAML -> actual proof | YAML presence alone is not proof; the named five target jobs must be observed green on a specific run id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | R | remote verification | mitigate | Split remote proof into its own non-autonomous plan and require explicit `gh` auth plus push access before dispatch |
| T-03-04-02 | T | workflow targeting | mitigate | Pin the macOS arm64 runner label and configure the Windows job with `ilammy/msvc-dev-cmd@v1` before build/test |
| T-03-04-03 | T | run observation | mitigate | Capture a pre-dispatch timestamp, retrieve the matching run id for the same branch, and watch that exact run instead of using a brittle latest-run heuristic |
</threat_model>

<verification>
- [ ] `test -f .github/workflows/phase3-go-wrapper-smoke.yml`
- [ ] `rg '^phase3-go-test|^phase3-go-race|^phase3-go-wrapper-remote' Makefile`
- [ ] `rg 'ubuntu-24.04-arm|macos-15-intel|macos-15|ilammy/msvc-dev-cmd@v1|go test ./\\.\\.\\. -race' .github/workflows/phase3-go-wrapper-smoke.yml`
- [ ] `test -f scripts/phase3-go-wrapper-smoke.sh`
- [ ] `gh auth status -h github.com && scripts/phase3-go-wrapper-smoke.sh`
</verification>

<success_criteria>
- The local verification targets and the remote wrapper-smoke workflow both use the same simple `cargo build --release` + `go test ./... -race` pattern
- The workflow proves all five Phase 3 targets with pinned cross-arch runner labels and explicit Windows MSVC setup
- The remote proof is no longer bundled into an unattended autonomous plan
- Observed wrapper proof is tied to a specific branch-scoped run id, not a timing heuristic
</success_criteria>

<output>
After completion, create `.planning/phases/03-go-public-api-purego-happy-path/03-04-SUMMARY.md`
</output>
