---
phase: 02-rust-shim-minimal-parse-path
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - build.rs
  - .gitmodules
  - third_party/simdjson
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
autonomous: true
requirements:
  - SHIM-02
  - SHIM-03
  - SHIM-04
user_setup: []
must_haves:
  truths:
    - "The repository builds simdjson from vendored, pinned sources via Rust `build.rs` and the `cc` crate."
    - "Runtime implementation selection remains simdjson-owned; the build does not hard-code CPU tuning such as `-march=native`."
    - "A release build can produce the native library artifacts needed by the later runtime and smoke-test plans."
  artifacts:
    - path: build.rs
      provides: "Deterministic native build pipeline for vendored simdjson and the local bridge layer"
      contains: "cc::Build"
    - path: .gitmodules
      provides: "Pinned vendoring path for simdjson source"
      contains: "third_party/simdjson"
    - path: src/native/simdjson_bridge.cpp
      provides: "Narrow C++ bridge compiled into the Rust crate"
      contains: "simdjson"
  key_links:
    - from: build.rs
      to: third_party/simdjson
      via: "cc compile inputs and rerun-if-changed wiring"
      pattern: "third_party/simdjson"
    - from: src/native/simdjson_bridge.h
      to: src/native/simdjson_bridge.cpp
      via: "declared narrow bridge surface"
      pattern: "psimdjson_"
---

<objective>
Establish the native build substrate for Phase 2: vendored simdjson, `build.rs`, and a compiled bridge layer that Rust can call without guessing at toolchain or source layout.

Purpose: satisfy `SHIM-02`, `SHIM-03`, and `SHIM-04` before runtime logic lands.
Output: a buildable crate that compiles pinned simdjson sources and the local bridge into the Phase 2 native library.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-RESEARCH.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-VALIDATION.md
@.planning/phases/01-ffi-contract-design/01-CONTEXT.md
@docs/ffi-contract.md
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor simdjson and wire the Rust build to compile it via `cc`</name>
  <files>Cargo.toml, build.rs, .gitmodules, third_party/simdjson</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-RESEARCH.md, Cargo.toml, build.rs, .gitmodules</read_first>
  <action>Add the `cc` crate as a build dependency in `Cargo.toml` and create `build.rs` to compile vendored simdjson plus the local bridge layer. Vendor simdjson as a pinned submodule at `third_party/simdjson` and record it in `.gitmodules`. In `build.rs`, compile the simdjson amalgamation and the bridge source with `cpp(true)`, `-std=c++17`, include paths pointing at `third_party/simdjson`, and `cargo:rerun-if-changed` lines for `build.rs`, the bridge files, and the vendored simdjson source files. On GNU Linux targets add `-static-libstdc++` and `-static-libgcc`; do not add `-march=native`, manual kernel-selection flags, or cmake-based build logic.</action>
  <verify>
    <automated>cargo build --release</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'cc *= ' Cargo.toml` succeeds.
    - `test -f build.rs` succeeds.
    - `rg 'cpp\\(true\\)|-std=c\\+\\+17|third_party/simdjson' build.rs` succeeds.
    - `! rg '\\-march=native|cmake' build.rs` succeeds.
    - `test -f .gitmodules` succeeds.
    - `rg 'third_party/simdjson' .gitmodules` succeeds.
    - `cargo build --release` exits 0.
  </acceptance_criteria>
  <done>The crate compiles vendored simdjson through `build.rs` and the `cc` crate with the locked toolchain assumptions for Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Add the narrow native bridge that Rust will call in later plans</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, build.rs</files>
  <read_first>.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-RESEARCH.md, docs/ffi-contract.md, build.rs, src/lib.rs, src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp</read_first>
  <action>Create `src/native/simdjson_bridge.h` and `src/native/simdjson_bridge.cpp` with a narrow, Phase-2-specific bridge surface only: raw parser/doc lifecycle helpers, implementation-name/fallback inspection, last-error text/offset accessors, root-kind lookup, and int64 extraction. Give every bridge symbol a stable `psimdjson_` prefix. Keep bridge outputs on pointer/out-param patterns so Rust can preserve the Phase 1 ABI rules at the public boundary. Do not expose the full future accessor/iterator surface from the bridge in this plan; only add the functions needed by the real minimal path, diagnostics spine, and fallback gate decisions recorded in `02-CONTEXT.md`.</action>
  <verify>
    <automated>cargo build --release</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/native/simdjson_bridge.h` succeeds.
    - `test -f src/native/simdjson_bridge.cpp` succeeds.
    - `rg 'psimdjson_' src/native/simdjson_bridge.h src/native/simdjson_bridge.cpp` succeeds.
    - `rg 'implementation|fallback|last_error|offset|int64|root' src/native/simdjson_bridge.h src/native/simdjson_bridge.cpp` succeeds.
    - `cargo build --release` exits 0 after the bridge files are added to the build.
  </acceptance_criteria>
  <done>A compiled native bridge exists for the minimal runtime path, diagnostics spine, and CPU-gate logic without dragging Phase 4 surface area into Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored C++ source -> Rust build | Incorrect or drifting native inputs would invalidate every later runtime assumption |
| bridge layer -> future Rust public ABI | Over-broad bridge scope would pull later-phase semantics into Phase 2 and increase native-risk surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | build pipeline | mitigate | Pin simdjson vendoring under `third_party/simdjson` and make `build.rs` reference explicit source paths |
| T-02-01-02 | D | runtime dispatch policy | mitigate | Keep kernel dispatch inside simdjson and ban `-march=native` / manual dispatch flags from `build.rs` |
| T-02-01-03 | T | bridge scope | mitigate | Restrict the bridge to the minimal parse path, diagnostics, and implementation-name helpers needed by Phase 2 |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `cargo build --release`
- [ ] `rg 'third_party/simdjson|cpp\\(true\\)|-std=c\\+\\+17' build.rs`
- [ ] `! rg '\\-march=native|cmake' build.rs`
</verification>

<success_criteria>

- Vendored simdjson is present and wired through a deterministic Rust build pipeline
- The crate builds a native library artifact in release mode
- The native bridge surface exists and is intentionally limited to the Phase 2 decisions
- Later plans can focus on runtime behavior and smoke proof instead of native build discovery
</success_criteria>

<output>
After completion, create `.planning/phases/02-rust-shim-minimal-parse-path/02-01-SUMMARY.md`
</output>
