---
phase: 02-rust-shim-minimal-parse-path
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - tests/smoke/minimal_parse.c
  - tests/smoke/README.md
  - .github/workflows/phase2-rust-shim-smoke.yml
  - Makefile
autonomous: true
requirements:
  - SHIM-01
  - SHIM-02
  - SHIM-06
user_setup:
  - service: github
    why: "Task 2 must push the working branch and dispatch/watch `phase2-rust-shim-smoke.yml` on GitHub Actions."
    dashboard_config:
      - task: "Authenticate the local GitHub CLI session before execution so `gh auth status` succeeds."
        location: "Local `gh` session for the repository host"
      - task: "Ensure `origin` is writable for the working branch and the authenticated account can dispatch and view GitHub Actions runs for this repository."
        location: "Repository remote permissions and GitHub repository Settings -> Actions"
must_haves:
  truths:
    - "A native C harness proves the committed public header and built library agree on the minimal Phase 2 ABI."
    - "Linux and Windows/MSVC both have a concrete smoke path, with Windows treated as the second proof target and a green `windows-smoke` result observed before the plan is considered complete."
    - "The smoke proof checks ABI version, `element_type`, int64 extraction, one invalid-JSON error path, and normal resource release."
    - "Darwin gets a compile-only verification path so the Phase 2 plan still covers the roadmap's one-arch macOS build expectation without widening the smoke scope."
  artifacts:
    - path: tests/smoke/minimal_parse.c
      provides: "Native literal-`42` harness using the committed public ABI plus one invalid-JSON negative case"
      contains: "PURE_SIMDJSON_ERR_INVALID_JSON"
    - path: tests/smoke/README.md
      provides: "Exact local Linux and Windows smoke commands"
      contains: "cl /nologo"
    - path: .github/workflows/phase2-rust-shim-smoke.yml
      provides: "Dedicated linux/windows smoke workflow plus Darwin build-only verification"
      contains: "macos-"
  key_links:
    - from: tests/smoke/minimal_parse.c
      to: include/pure_simdjson.h
      via: "compiled harness against the committed header"
      pattern: "pure_simdjson_"
    - from: .github/workflows/phase2-rust-shim-smoke.yml
      to: tests/smoke/minimal_parse.c
      via: "linux and MSVC smoke execution"
      pattern: "minimal_parse"
---

<objective>
Prove the Phase 2 native surface externally: compile and run a public-header-driven C smoke harness on Linux and Windows/MSVC, and capture that proof in a repeatable workflow.

Purpose: make the Phase 2 exit gate real instead of implied by Rust tests alone.
Output: a native C smoke harness, exact local Linux/MSVC commands, and a dedicated Linux/Windows smoke workflow plus Darwin build-only verification.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-REVIEWS.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-VALIDATION.md
@docs/ffi-contract.md
@include/pure_simdjson.h
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a public-header smoke harness with exact Linux and MSVC commands</name>
  <files>tests/smoke/minimal_parse.c, tests/smoke/README.md, Makefile</files>
  <read_first>.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-REVIEWS.md, include/pure_simdjson.h, docs/ffi-contract.md, Makefile, tests/smoke/minimal_parse.c, tests/smoke/README.md</read_first>
  <action>Addresses review concern: concrete Windows smoke strategy, `get_abi_version`, `element_type`, error propagation through the public C ABI, and resource-release checks. Create `tests/smoke/minimal_parse.c` that includes `include/pure_simdjson.h`, parses the literal JSON `42`, and checks this exact sequence: `pure_simdjson_get_abi_version` returns `PURE_SIMDJSON_ABI_VERSION`; `pure_simdjson_parser_new` succeeds; `pure_simdjson_parser_parse` succeeds; `pure_simdjson_doc_root` succeeds; `pure_simdjson_element_type` returns `PURE_SIMDJSON_VALUE_KIND_INT64`; `pure_simdjson_element_get_int64` returns `42`; `pure_simdjson_doc_free` succeeds; a second `pure_simdjson_parser_parse` after `doc_free` succeeds; final `pure_simdjson_doc_free` and `pure_simdjson_parser_free` both succeed. Then add one negative case in the same harness that parses the exact invalid payload `{\"x\":}` and asserts `pure_simdjson_parser_parse` returns `PURE_SIMDJSON_ERR_INVALID_JSON`, `pure_simdjson_parser_get_last_error_len` reports a non-zero length, and `pure_simdjson_parser_copy_last_error` copies a non-empty diagnostic buffer. Document exact local commands in `tests/smoke/README.md`: on Linux compile with `cc -Iinclude tests/smoke/minimal_parse.c -Ltarget/release -lpure_simdjson -Wl,-rpath,$PWD/target/release -o target/phase2-smoke/minimal_parse` and run `target/phase2-smoke/minimal_parse`; on Windows/MSVC compile with `cl /nologo /Iinclude tests\\smoke\\minimal_parse.c /link /LIBPATH:target\\release pure_simdjson.dll.lib /OUT:target\\phase2-smoke\\minimal_parse.exe` and run it with `target\\release` prepended to `PATH`. Add `phase2-smoke-linux` and `phase2-smoke-windows` targets to `Makefile`. Also add optional symbol-export verification commands: Linux via `nm -D --defined-only target/release/libpure_simdjson.so` and Windows via `dumpbin /EXPORTS target\\release\\pure_simdjson.dll`.</action>
  <verify>
    <automated>make verify-contract && cargo build --release && make phase2-smoke-linux</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/smoke/minimal_parse.c` succeeds.
    - `test -f tests/smoke/README.md` succeeds.
    - `rg 'pure_simdjson_(get_abi_version|parser_new|parser_parse|doc_root|element_type|element_get_int64|doc_free|parser_free|parser_get_last_error_len|parser_copy_last_error)' tests/smoke/minimal_parse.c` succeeds.
    - `rg 'PURE_SIMDJSON_ABI_VERSION|PURE_SIMDJSON_VALUE_KIND_INT64|PURE_SIMDJSON_ERR_INVALID_JSON|42' tests/smoke/minimal_parse.c` succeeds.
    - `rg 'cc -Iinclude .*minimal_parse.c|cl /nologo /Iinclude' tests/smoke/README.md` succeeds.
    - `rg '^phase2-smoke-linux|^phase2-smoke-windows|phase2-verify-exports' Makefile` succeeds.
    - `make phase2-smoke-linux` exits 0.
  </acceptance_criteria>
  <done>The repository contains an executable native harness and documented exact Linux/MSVC smoke commands for the committed public ABI, including one invalid-JSON error-path assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated linux/windows smoke workflow plus Darwin build-only verification</name>
  <files>.github/workflows/phase2-rust-shim-smoke.yml, Makefile</files>
  <read_first>.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-REVIEWS.md, Makefile, tests/smoke/minimal_parse.c, tests/smoke/README.md, .github/workflows/phase2-rust-shim-smoke.yml</read_first>
  <action>Addresses review concern: Windows/MSVC workflow concreteness, Darwin roadmap alignment, brittle verification patterns, and the missing observed Windows proof. This task assumes the `github` `user_setup` prerequisites are satisfied before the remote dispatch/watch steps begin. Create `.github/workflows/phase2-rust-shim-smoke.yml` with `workflow_dispatch` enabled and three jobs only: `linux-smoke` on `ubuntu-latest`, `windows-smoke` on `windows-latest`, and `darwin-build-only` on a macOS runner (`macos-latest` or a pinned `macos-*` label available in the repo CI policy). The Linux job runs `make verify-contract`, `cargo build --release`, `make phase2-smoke-linux`, and an export check `nm -D --defined-only target/release/libpure_simdjson.so | rg 'pure_simdjson_(get_abi_version|parser_new|parser_parse|doc_root|element_type|element_get_int64)'`. The Windows job uses `ilammy/msvc-dev-cmd@v1` (or an equally explicit MSVC environment step) before invoking `cl`, then runs `cargo build --release`, compiles the harness with the exact `cl /nologo /Iinclude ... /LIBPATH:target\\release pure_simdjson.dll.lib` command from the README, executes the harness with `target\\release` on `PATH`, and runs `dumpbin /EXPORTS target\\release\\pure_simdjson.dll | findstr pure_simdjson_` as the symbol-export check. The Darwin job is build-only on purpose: it runs `cargo build --release` and verifies a `libpure_simdjson.dylib` artifact exists, but does not add a third smoke harness target. After the workflow file is committed and pushed on the working branch, dispatch this workflow with `gh workflow run phase2-rust-shim-smoke.yml --ref "$(git rev-parse --abbrev-ref HEAD)"`, wait for the run to finish with `gh run watch --exit-status`, and fail the plan unless the observed `windows-smoke` job conclusion is `success`; treat YAML-only verification as insufficient. Do not add release publishing, bootstrap download, signing, or extra non-Darwin platforms to this Phase 2 workflow.</action>
  <verify>
    <automated>gh auth status && git remote get-url origin >/dev/null && test -f .github/workflows/phase2-rust-shim-smoke.yml && rg 'workflow_dispatch|ubuntu-latest|windows-latest|macos-|ilammy/msvc-dev-cmd|cl /nologo|nm -D|dumpbin /EXPORTS|phase2-smoke-linux|minimal_parse|libpure_simdjson.dylib' .github/workflows/phase2-rust-shim-smoke.yml && git push -u origin HEAD && gh workflow run phase2-rust-shim-smoke.yml --ref "$(git rev-parse --abbrev-ref HEAD)" && sleep 10 && RUN_ID="$(gh run list --workflow phase2-rust-shim-smoke.yml --branch "$(git rev-parse --abbrev-ref HEAD)" --limit 1 --json databaseId --jq '.[0].databaseId')" && gh run watch "$RUN_ID" --exit-status && gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name=="windows-smoke") | .conclusion' | grep -x success</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - `rg 'workflow_dispatch|ubuntu-latest|windows-latest|macos-' .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - `rg 'ilammy/msvc-dev-cmd|cl /nologo|pure_simdjson\\.dll\\.lib' .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - `rg 'nm -D|dumpbin /EXPORTS|libpure_simdjson.dylib|pure_simdjson_(get_abi_version|parser_new|parser_parse|doc_root|element_type|element_get_int64)' .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - The verify command's dispatch/watch sequence succeeds on the pushed working branch and the observed `windows-smoke` job concludes `success`.
    - `! rg 'actions/upload-artifact|cosign|PURE_SIMDJSON_BINARY_MIRROR|releases\\.amikos\\.tech|cmd/pure-simdjson-bootstrap' .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
  </acceptance_criteria>
  <done>The repo contains a dedicated Linux and Windows/MSVC smoke workflow plus a Darwin build-only verification job, and plan completion now depends on an observed green `windows-smoke` run instead of YAML inspection alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed header -> external smoke harness | If the header and built library drift, later Go bindings inherit a false contract |
| Linux success -> Windows confidence | A Linux-only proof would leave the explicit MSVC risk unresolved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T | smoke harness | mitigate | Compile the harness from `include/pure_simdjson.h` and assert ABI version, `element_type`, int64 result, and release calls explicitly |
| T-02-03-02 | D | Windows/MSVC path | mitigate | Use `ilammy/msvc-dev-cmd@v1`, `cl`, and the import library `pure_simdjson.dll.lib` in a dedicated `windows-latest` smoke job |
| T-02-03-03 | T | symbol visibility | mitigate | Add Linux `nm -D` and Windows `dumpbin /EXPORTS` checks for the required Phase 2 exports |
</threat_model>

<verification>
- [ ] `make verify-contract && cargo build --release`
- [ ] `make phase2-smoke-linux`
- [ ] `rg 'PURE_SIMDJSON_ABI_VERSION|PURE_SIMDJSON_VALUE_KIND_INT64|pure_simdjson_doc_free|pure_simdjson_parser_free' tests/smoke/minimal_parse.c`
- [ ] `rg 'workflow_dispatch|cl /nologo|pure_simdjson\\.dll\\.lib|ilammy/msvc-dev-cmd|nm -D|dumpbin /EXPORTS' tests/smoke/README.md .github/workflows/phase2-rust-shim-smoke.yml`
- [ ] Push the working branch, dispatch `phase2-rust-shim-smoke.yml`, and observe `windows-smoke` conclude `success`
</verification>

<success_criteria>
- A native C harness proves the committed header against the built library on the minimal `42` path
- Windows/MSVC is the second explicit smoke target, with a concrete compiler/linker strategy and an observed green `windows-smoke` CI result
- ABI version, `element_type`, one invalid-JSON error path, and normal resource release are part of the smoke proof
- Darwin has a compile-only verification path that keeps Phase 2 aligned with the roadmap's one-arch macOS build expectation
- Optional symbol-export verification is documented and wired where the Phase 2 proof runs
</success_criteria>

<output>
After completion, create `.planning/phases/02-rust-shim-minimal-parse-path/02-03-SUMMARY.md`
</output>
