---
phase: 02-rust-shim-minimal-parse-path
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - tests/smoke/minimal_parse.c
  - tests/smoke/README.md
  - .github/workflows/phase2-rust-shim-smoke.yml
  - Makefile
autonomous: true
requirements:
  - SHIM-06
  - SHIM-07
user_setup: []
must_haves:
  truths:
    - "A native C smoke harness proves the public header and built library work together on the minimal parse path."
    - "Phase 2 burns down the Windows/MSVC native risk explicitly instead of deferring it."
    - "The contract/header verification loop remains part of the smoke path."
  artifacts:
    - path: tests/smoke/minimal_parse.c
      provides: "Native harness for the literal `42` public-ABI proof"
      contains: "pure_simdjson_parser_new"
    - path: .github/workflows/phase2-rust-shim-smoke.yml
      provides: "Phase 2 linux/windows native smoke gate"
      contains: "windows"
    - path: Makefile
      provides: "Convenient local smoke commands"
      contains: "phase2-smoke"
  key_links:
    - from: tests/smoke/minimal_parse.c
      to: include/pure_simdjson.h
      via: "compiled harness uses the committed public ABI header"
      pattern: "pure_simdjson_"
    - from: .github/workflows/phase2-rust-shim-smoke.yml
      to: Cargo.toml and built artifacts
      via: "linux/windows build and harness execution"
      pattern: "cargo build --release"
---

<objective>
Prove the Phase 2 native surface externally: run the public header against the built library on Linux and Windows, and capture that proof in a repeatable smoke/CI path.

Purpose: make the Phase 2 exit gate real rather than implied by Rust unit tests alone.
Output: a native C smoke harness, local smoke commands, and a linux/windows CI workflow for the minimal parse path.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-RESEARCH.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-VALIDATION.md
@docs/ffi-contract.md
@include/pure_simdjson.h
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the native C smoke harness and local smoke commands</name>
  <files>tests/smoke/minimal_parse.c, tests/smoke/README.md, Makefile</files>
  <read_first>.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-RESEARCH.md, include/pure_simdjson.h, docs/ffi-contract.md, Makefile, tests/smoke/minimal_parse.c, tests/smoke/README.md</read_first>
  <action>Create `tests/smoke/minimal_parse.c` that includes `include/pure_simdjson.h`, loads the built library through normal platform linking/loading, calls `pure_simdjson_get_abi_version`, `pure_simdjson_parser_new`, `pure_simdjson_parser_parse`, `pure_simdjson_doc_root`, and `pure_simdjson_element_get_int64` on a literal `42` document, and exits non-zero on any unexpected return code or value. Add `tests/smoke/README.md` documenting the expected artifact path and local invocation on Linux and Windows. Extend `Makefile` with explicit Phase 2 smoke commands such as `phase2-smoke-linux` and `phase2-smoke-windows` (or a single `phase2-smoke` plus platform branches) that compile the harness against the committed header and the just-built library.</action>
  <verify>
    <automated>make verify-contract && cargo build --release</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/smoke/minimal_parse.c` succeeds.
    - `test -f tests/smoke/README.md` succeeds.
    - `rg 'pure_simdjson_(get_abi_version|parser_new|parser_parse|doc_root|element_get_int64)' tests/smoke/minimal_parse.c` succeeds.
    - `rg '^phase2-smoke' Makefile` succeeds.
    - `make verify-contract && cargo build --release` exits 0.
  </acceptance_criteria>
  <done>A public-header-driven native smoke harness exists and can be invoked locally against the Phase 2 build outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add the linux/windows smoke workflow and make Windows the second proof target</name>
  <files>.github/workflows/phase2-rust-shim-smoke.yml, Makefile</files>
  <read_first>.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-RESEARCH.md, .github/workflows/claude.yml, Makefile, .github/workflows/phase2-rust-shim-smoke.yml, tests/smoke/minimal_parse.c, tests/smoke/README.md</read_first>
  <action>Create `.github/workflows/phase2-rust-shim-smoke.yml` with jobs for `linux/amd64` and `windows/amd64` that: check out the repo, install the Rust toolchain, run `make verify-contract`, run `cargo build --release`, compile the native smoke harness, and execute it against the produced library artifact. Make the Windows job use the native MSVC environment already present on the runner and keep the Linux job on `ubuntu-latest`. Do not broaden the workflow into a full release matrix or bootstrap/download path; this workflow exists only to prove the Phase 2 native path and the chosen Windows risk-burn-down target.</action>
  <verify>
    <automated>test -f .github/workflows/phase2-rust-shim-smoke.yml && rg 'windows|ubuntu|cargo build --release|verify-contract|minimal_parse' .github/workflows/phase2-rust-shim-smoke.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - `rg 'ubuntu|windows' .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - `rg 'cargo build --release|make verify-contract' .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - `rg 'minimal_parse' .github/workflows/phase2-rust-shim-smoke.yml` succeeds.
    - The workflow does not contain release-publishing, signing, or bootstrap-download steps.
  </acceptance_criteria>
  <done>The repo contains a dedicated Phase 2 smoke workflow that proves the minimal path on Linux and Windows without prematurely becoming the full Phase 6 release matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed header -> external smoke harness | If the public header and built library drift, later Go bindings will inherit a false contract |
| Linux-only proof -> release confidence | A passing Linux build alone would leave the known MSVC native risk unresolved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | T | smoke harness | mitigate | Compile the harness from the committed public header and fail on any unexpected return code or int64 value |
| T-02-03-02 | D | Windows native path | mitigate | Make `windows/amd64` the second Phase 2 smoke job instead of deferring it to later phases |
| T-02-03-03 | T | CI scope | mitigate | Keep the workflow strictly on verify/build/smoke steps and exclude release/distribution logic |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `make verify-contract && cargo build --release`
- [ ] `rg 'pure_simdjson_(parser_new|parser_parse|doc_root|element_get_int64)' tests/smoke/minimal_parse.c`
- [ ] `rg 'ubuntu|windows|cargo build --release|make verify-contract' .github/workflows/phase2-rust-shim-smoke.yml`
</verification>

<success_criteria>

- The minimal public ABI is proven by a native C harness, not just Rust unit tests
- Linux and Windows both have an explicit Phase 2 smoke path
- The contract/header verification loop remains part of the smoke proof
- The workflow de-risks MSVC early without pulling in Phase 5/6 release concerns
</success_criteria>

<output>
After completion, create `.planning/phases/02-rust-shim-minimal-parse-path/02-03-SUMMARY.md`
</output>
