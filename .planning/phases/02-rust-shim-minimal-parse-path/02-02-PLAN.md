---
phase: 02-rust-shim-minimal-parse-path
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib.rs
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - tests/rust_shim_minimal.rs
  - tests/rust_shim_fallback_gate.rs
autonomous: true
requirements:
  - SHIM-05
  - SHIM-06
  - SHIM-07
user_setup: []
must_haves:
  truths:
    - "The public ABI implements the real generation-checked parser/doc lifecycle instead of a temporary pointer shim."
    - "Every live FFI export is mechanically wrapped so Rust panics cannot cross the C ABI boundary."
    - "The minimal real path `parser_new -> parser_parse -> doc_root -> element_type -> element_get_int64` works on a literal `42` document with thin diagnostics and CPU gating."
    - "`element_type` maps every Phase 2 root discriminant to the exact public `PURE_SIMDJSON_VALUE_KIND_*` constant even though only `element_get_int64` is real."
    - "A deterministic test proves the C++ bridge converts a thrown exception into `PURE_SIMDJSON_ERR_CPP_EXCEPTION`."
    - "A dedicated deterministic fallback-gate test path proves `parser_new` returns `PURE_SIMDJSON_ERR_CPU_UNSUPPORTED` on `fallback` unless the hidden test-only bypass is enabled."
  artifacts:
    - path: src/runtime/registry.rs
      provides: "Generation-checked parser/doc registry with explicit lifecycle states"
      contains: "ParserState"
    - path: src/lib.rs
      provides: "Real Phase 2 exports and mechanical panic containment"
      contains: "ffi_wrap"
    - path: tests/rust_shim_minimal.rs
      provides: "Lifecycle, diagnostics, padding, and minimal parse-path tests"
      contains: "parser_free_while_doc_live"
    - path: tests/rust_shim_fallback_gate.rs
      provides: "Deterministic internal-only fallback CPU gate coverage"
      contains: "PURE_SIMDJSON_ERR_CPU_UNSUPPORTED"
  key_links:
    - from: src/lib.rs
      to: src/native/simdjson_bridge.h
      via: "bridge calls for implementation name, padding, parse, root, type, and int64"
      pattern: "psimdjson_"
    - from: src/runtime/registry.rs
      to: src/lib.rs
      via: "packed handle validation and parser/doc state transitions"
      pattern: "generation"
    - from: tests/rust_shim_fallback_gate.rs
      to: src/lib.rs
      via: "hidden internal-only forced-fallback selector evaluated in `parser_new`"
      pattern: "PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION"
---

<objective>
Implement the real Phase 2 runtime core: the precise parser/doc lifecycle, Rust-owned padded input handling, the minimal real data path, thin parser diagnostics, and the fallback CPU gate.

Purpose: satisfy `SHIM-01`, `SHIM-05`, `SHIM-06`, and `SHIM-07` without dragging the full accessor surface into Phase 2.
Output: a real native library that parses literal `42`, reports its implementation name, exposes `element_type`, preserves last-error helpers, rejects fallback CPUs, and enforces the locked lifecycle rules.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-RESEARCH.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-REVIEWS.md
@.planning/phases/02-rust-shim-minimal-parse-path/02-VALIDATION.md
@docs/ffi-contract.md
@src/lib.rs
@build.rs
@src/native/simdjson_bridge.h
@src/native/simdjson_bridge.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the precise parser/doc state machine and mechanical Rust panic containment</name>
  <files>src/lib.rs, src/runtime/mod.rs, src/runtime/registry.rs</files>
  <read_first>.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-REVIEWS.md, docs/ffi-contract.md, src/lib.rs, src/runtime/mod.rs, src/runtime/registry.rs, src/native/simdjson_bridge.h</read_first>
  <action>Addresses review concern: Rust panic containment and lifecycle precision. Add `src/runtime/mod.rs` and `src/runtime/registry.rs` with a custom registry that maps directly to the locked packed handle format (`slot:u32 | generation:u32`) per D-05. Define the parser lifecycle explicitly: `parser_new` creates `ParserState::Idle`; `parser_parse` on `Idle` transitions to `ParserState::Busy { doc_slot, doc_generation }`; `parser_parse` on `Busy` returns `PURE_SIMDJSON_ERR_PARSER_BUSY`; `doc_free` on the live doc transitions the owning parser back to `Idle`; `parser_free` on `Idle` frees the slot and bumps generation; `parser_free` while a doc is live returns `PURE_SIMDJSON_ERR_PARSER_BUSY`; any stale, double-freed, zero, or forged parser/doc handle returns `PURE_SIMDJSON_ERR_INVALID_HANDLE`. Keep the remaining Phase 4 accessors and iterators as explicit stubs. Replace any open-coded export bodies with a single mechanical wrapper policy in `src/lib.rs`: every public `extern "C"` export, including real exports and retained stubs, must route through `ffi_wrap` (or a macro that expands to it) so `catch_unwind` is never optional.</action>
  <verify>
    <automated>cargo test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/runtime/mod.rs` succeeds.
    - `test -f src/runtime/registry.rs` succeeds.
    - `rg 'ParserState|Idle|Busy' src/runtime/mod.rs src/runtime/registry.rs` succeeds.
    - `rg 'PURE_SIMDJSON_ERR_INVALID_HANDLE|PURE_SIMDJSON_ERR_PARSER_BUSY' src/lib.rs src/runtime/mod.rs src/runtime/registry.rs` succeeds.
    - `sh -c 'exports=$(rg "^pub unsafe extern \"C\" fn pure_simdjson_" -c src/lib.rs || true); wraps=$(rg "ffi_wrap\\(\"pure_simdjson_" -c src/lib.rs || true); if [ "$exports" -gt 0 ] && [ "$exports" -eq "$wraps" ]; then exit 0; fi; rg "macro_rules!" src/lib.rs >/dev/null && rg "ffi_wrap" src/lib.rs >/dev/null && rg "pure_simdjson_" src/lib.rs >/dev/null'` succeeds.
    - `cargo test` exits 0.
  </acceptance_criteria>
  <done>The runtime has an explicit parser/doc state machine and every public export is mechanically protected by the Rust panic wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the minimal parse path, real diagnostics spine, and padding contract</name>
  <files>src/lib.rs, src/runtime/mod.rs, src/runtime/registry.rs, tests/rust_shim_minimal.rs, tests/rust_shim_fallback_gate.rs</files>
  <read_first>.planning/phases/02-rust-shim-minimal-parse-path/02-CONTEXT.md, .planning/phases/02-rust-shim-minimal-parse-path/02-REVIEWS.md, docs/ffi-contract.md, src/lib.rs, src/runtime/mod.rs, src/runtime/registry.rs, tests/rust_shim_minimal.rs, src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp</read_first>
  <action>Addresses review concern: real padding constant usage, zero-initialized tail bytes, diagnostics, full root-kind `element_type` mapping, deterministic C++ exception containment coverage, and CPU gating. Make `pure_simdjson_get_implementation_name_len` and `pure_simdjson_copy_implementation_name` read the real simdjson implementation name through the bridge. In `pure_simdjson_parser_new`, return `PURE_SIMDJSON_ERR_CPU_UNSUPPORTED` when the selected implementation name is `fallback`, unless the hidden internal-only bypass `PURE_SIMDJSON_ALLOW_FALLBACK_FOR_TESTS=1` is set; evaluate that env var at `parser_new` call time only, and do not add this bypass to the public header or docs. Add a second hidden internal-only selector `PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback` that is honored only inside Rust tests/CI so the fallback gate can be exercised deterministically on non-fallback hosts; do not expose this selector in the public header or docs, and serialize env-mutating tests in the dedicated fallback test file so they do not race. In `pure_simdjson_parser_parse`, call `psimdjson_padding_bytes()` to get the real simdjson padding constant, allocate `vec![0u8; input_len + padding_bytes]`, copy the caller bytes into the prefix only, and keep the zero-initialized tail intact. Store parser last-error text and offset as advisory data; use `u64::MAX` as the pinned unknown-offset sentinel when simdjson does not provide a precise location. Implement the real minimal exports only: parser create/free/parse, parser last-error length/copy/offset, doc free/root, `element_type`, and `element_get_int64`. Make `element_type` fully functional for all root values Phase 2 can parse: `null -> PURE_SIMDJSON_VALUE_KIND_NULL`, `true/false -> PURE_SIMDJSON_VALUE_KIND_BOOL`, signed integers -> `PURE_SIMDJSON_VALUE_KIND_INT64`, unsigned integers -> `PURE_SIMDJSON_VALUE_KIND_UINT64`, floating-point values -> `PURE_SIMDJSON_VALUE_KIND_FLOAT64`, strings -> `PURE_SIMDJSON_VALUE_KIND_STRING`, arrays -> `PURE_SIMDJSON_VALUE_KIND_ARRAY`, and objects -> `PURE_SIMDJSON_VALUE_KIND_OBJECT`. Keep the broad lifecycle/data-path coverage in `tests/rust_shim_minimal.rs`, and add a dedicated `tests/rust_shim_fallback_gate.rs` path that asserts `PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback` makes `parser_new` return `PURE_SIMDJSON_ERR_CPU_UNSUPPORTED` when the bypass is absent and succeed when `PURE_SIMDJSON_ALLOW_FALLBACK_FOR_TESTS=1` is also set. Also add focused tests covering: ABI version helper, implementation-name round-trip, literal `42` parse and int64 extraction, `element_type` across the exact root literals `null`, `true`, `-42`, `42`, `1.5`, `\"x\"`, `[1]`, and `{\"k\":1}`, invalid JSON diagnostics, parser-busy on second parse, `parser_free` while doc live returning `PURE_SIMDJSON_ERR_PARSER_BUSY`, stale handle after `doc_free`, double-free returning `PURE_SIMDJSON_ERR_INVALID_HANDLE`, and a deterministic `PURE_SIMDJSON_ERR_CPP_EXCEPTION` assertion by calling the internal bridge hook `psimdjson_test_force_cpp_exception` introduced in Plan 01.</action>
  <verify>
    <automated>cargo test --test rust_shim_minimal && PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback cargo test --test rust_shim_fallback_gate parser_new_rejects_fallback_without_bypass && PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback PURE_SIMDJSON_ALLOW_FALLBACK_FOR_TESTS=1 cargo test --test rust_shim_fallback_gate parser_new_allows_fallback_with_hidden_bypass</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/rust_shim_minimal.rs` succeeds.
    - `test -f tests/rust_shim_fallback_gate.rs` succeeds.
    - `rg 'psimdjson_padding_bytes|vec!\\[0u8; .*padding' src/lib.rs src/runtime/mod.rs src/runtime/registry.rs` succeeds.
    - `rg 'PURE_SIMDJSON_ALLOW_FALLBACK_FOR_TESTS|PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION|CPU_UNSUPPORTED|fallback' src/lib.rs tests/rust_shim_minimal.rs tests/rust_shim_fallback_gate.rs` succeeds.
    - `rg 'u64::MAX|last_error|offset|implementation_name' src/lib.rs tests/rust_shim_minimal.rs` succeeds.
    - `rg 'PURE_SIMDJSON_VALUE_KIND_(NULL|BOOL|INT64|UINT64|FLOAT64|STRING|ARRAY|OBJECT)' src/lib.rs tests/rust_shim_minimal.rs` succeeds.
    - `rg 'get_abi_version|element_type|element_get_int64|parser_busy|parser_free_while_doc_live|double_free|INVALID_HANDLE' tests/rust_shim_minimal.rs` succeeds.
    - `rg 'parser_new_rejects_fallback_without_bypass|parser_new_allows_fallback_with_hidden_bypass|PURE_SIMDJSON_ERR_CPU_UNSUPPORTED' tests/rust_shim_fallback_gate.rs` succeeds.
    - `rg 'psimdjson_test_force_cpp_exception|PURE_SIMDJSON_ERR_CPP_EXCEPTION' tests/rust_shim_minimal.rs` succeeds.
    - `cargo test --test rust_shim_minimal && PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback cargo test --test rust_shim_fallback_gate parser_new_rejects_fallback_without_bypass && PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback PURE_SIMDJSON_ALLOW_FALLBACK_FOR_TESTS=1 cargo test --test rust_shim_fallback_gate parser_new_allows_fallback_with_hidden_bypass` exits 0.
  </acceptance_criteria>
  <done>The Phase 2 runtime proves the minimal parse path, thin diagnostics spine, real padding contract, full root-kind `element_type` mapping, deterministic `PURE_SIMDJSON_ERR_CPP_EXCEPTION` containment, and the hidden fallback CPU gate behavior in executable tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public C ABI -> Rust runtime | Panics, forged handles, or stale views at this boundary would become memory safety failures if not trapped |
| parser-owned input buffer -> simdjson | Missing or uninitialized padding would let native code read beyond the logical JSON payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | D | Rust FFI exports | mitigate | Route every `pure_simdjson_*` export through `ffi_wrap` so `catch_unwind` converts panics to `PURE_SIMDJSON_ERR_PANIC` |
| T-02-02-02 | T | parser/doc registry | mitigate | Enforce generation-checked handles plus explicit `Idle`/`Busy` transitions and test stale-handle, double-free, and parser-busy cases |
| T-02-02-03 | I | padded input arena | mitigate | Query `psimdjson_padding_bytes()` and allocate `vec![0u8; input_len + padding_bytes]` so tail bytes are zero-initialized |
| T-02-02-04 | D | unsupported CPU path | mitigate | Reject `fallback` in `parser_new` by default and keep the bypass hidden to internal tests only |
</threat_model>

<verification>
- [ ] `cargo test`
- [ ] `cargo test --test rust_shim_minimal`
- [ ] `PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback cargo test --test rust_shim_fallback_gate parser_new_rejects_fallback_without_bypass`
- [ ] `PURE_SIMDJSON_TEST_FORCE_IMPLEMENTATION=fallback PURE_SIMDJSON_ALLOW_FALLBACK_FOR_TESTS=1 cargo test --test rust_shim_fallback_gate parser_new_allows_fallback_with_hidden_bypass`
- [ ] `rg 'ffi_wrap\\(\"pure_simdjson_|psimdjson_padding_bytes|PURE_SIMDJSON_ALLOW_FALLBACK_FOR_TESTS|u64::MAX' src/lib.rs src/runtime/mod.rs src/runtime/registry.rs tests/rust_shim_minimal.rs`
</verification>

<success_criteria>
- The minimal Phase 2 public ABI is real, not stubbed
- Parser/doc lifecycle edge cases are explicitly implemented and tested
- The input-copy path uses simdjson's real padding constant with zero-initialized tail bytes
- `get_implementation_name`, parser last-error helpers, full root-kind `element_type`, and `element_get_int64` are real for the minimal parse path
- Deterministic internal-only tests prove both the bridge exception path and the fallback CPU gate without exposing new public controls
</success_criteria>

<output>
After completion, create `.planning/phases/02-rust-shim-minimal-parse-path/02-02-SUMMARY.md`
</output>
