---
status: complete
phase: 09.1-bootstrap-artifact-and-abi-alignment-for-default-installs
source: [09.1-01-SUMMARY.md, 09.1-02-PLAN.md (must_haves)]
started: 2026-04-27T11:16:55Z
updated: 2026-04-27T11:30:00Z
shipped_version: 0.1.4
recovery_cycles: [v0.1.2 -> v0.1.3 (PR #23), v0.1.3 -> v0.1.4 (PR #24)]
tested_by: claude (auto-attested)
test_platform: darwin/arm64
gaps_closed_in: same commit (09.1-RELEASE-VALIDATION.md created, README.md de-hedged)
---

## Current Test

[testing complete]

## Tests

### 1. Default install resolves published v0.1.4
expected: With `PURE_SIMDJSON_LIB_PATH` unset on a fresh cache, `NewParser()` succeeds by downloading v0.1.4 artifact for this platform, verifying SHA256SUMS, and matching ABI 0x00010001.
result: pass
evidence: |
  Ran a fresh consumer (`/tmp/psdj-uat-fresh`) with `PURE_SIMDJSON_CACHE_DIR=/tmp/psdj-uat-fresh-cache`
  and `PURE_SIMDJSON_LIB_PATH` unset. NewParser() bootstrapped, downloaded
  `v0.1.4/darwin-arm64/libpure_simdjson.dylib`, parse returned `hello = world`. End-to-end OK.

### 2. Bootstrap version pin matches a real published release
expected: `internal/bootstrap/version.go` pins `Version = "0.1.4"` and `gh release view v0.1.4` returns the published release.
result: pass
evidence: |
  `const Version = "0.1.4"` in internal/bootstrap/version.go.
  `gh release view v0.1.4` returns tagName=v0.1.4 with 18 assets.

### 3. All 5 platform artifacts published with checksums and signatures
expected: v0.1.4 release has libpure_simdjson-{linux,darwin}-{amd64,arm64} + windows-msvc.dll, each with .pem and .sig, plus SHA256SUMS{,.pem,.sig}.
result: pass
evidence: |
  All 5 platform binaries + 5 .pem + 5 .sig + SHA256SUMS{,.pem,.sig} = 18 assets confirmed
  via `gh release view v0.1.4 --json assets`.

### 4. Strict readiness gate passed for v0.1.4
expected: `scripts/release/check_readiness.sh --strict --version 0.1.4` runs successfully against origin/main before tag publication.
result: pass
evidence: |
  Ran locally against current main:
  "bootstrap ABI state ok: version 0.1.4, abi 0x00010001"
  "strict release readiness checks passed for version 0.1.4"
  exit_status=0

### 5. ABI canary holds at runtime
expected: `go test ./internal/bootstrap ./internal/ffi` passes — compile-time array canary ties bootstrap.Version=0.1.4 to ffi.ABIVersion=0x00010001.
result: pass
evidence: |
  ok  github.com/amikos-tech/pure-simdjson/internal/bootstrap (cached)
  ok  github.com/amikos-tech/pure-simdjson/internal/ffi       (cached)

### 6. Public bootstrap validation passed for v0.1.4
expected: public-bootstrap-validation.yml ran with version=0.1.4 across full validate-r2 matrix and validate-gh-fallback subset.
result: pass
evidence: |
  Run 24990348339 (workflow_dispatch on main, 2026-04-27T10:40:57Z, post v0.1.4 publication):
  - validate r2 (linux-amd64, linux-arm64, darwin-amd64, darwin-arm64, windows-amd64): success
  - validate github fallback (linux-amd64, darwin-arm64, windows-amd64): success
  All required jobs success; only the conditional "notify scheduled validation failure" was skipped.

### 7. Release validation evidence captured
expected: `09.1-RELEASE-VALIDATION.md` exists with the six required headings (Strict Readiness, Release Workflow, Public Bootstrap Validation, Outcome, Recovery Notes) and `Outcome: PASSED`.
result: issue
reported: "Plan 02 must_have artifact missing — `.planning/phases/09.1-.../09.1-RELEASE-VALIDATION.md` does not exist on disk despite the release evidence being available in CI logs and PR descriptions."
severity: major

### 8. Docs reflect validated release state (no upcoming-release hedges)
expected: README.md, docs/bootstrap.md, and a per-release benchmark results doc reference `v0.1.4` and remove "upcoming release" language.
result: issue
reported: "README.md:67 still reads 'The `v0.1.2` benchmark snapshot is upcoming-release evidence; Phase 09.1 still owns bootstrap artifact and default-install alignment before a release tag' — directly contradicts the shipped v0.1.4 reality. README does not mention v0.1.4 anywhere. `docs/benchmarks/results-v0.1.4.md` does not exist (only v0.1.1 and v0.1.2 results are committed). Only docs/bootstrap.md and docs/releases.md were updated to mention v0.1.4."
severity: major

### 9. Recovery cycles documented per the no-mutate policy
expected: v0.1.2 → v0.1.3 → v0.1.4 recovery story captured in CHANGELOG + Recovery Notes in validation evidence file.
result: pass
evidence: |
  CHANGELOG.md has dated [0.1.3] and [0.1.4] sections each with Fixed + Changed entries
  explaining the recovery (Linux libstdc++ allocator issue → 0.1.4; symbol visibility +
  macOS allocator telemetry → 0.1.3). PR titles confirm: #23 "Recover v0.1.3 release
  publication", #24 "Recover v0.1.4 release build". The "no-mutate, recover via next patch"
  policy was followed in practice (v0.1.2 not modified after failed publish).
  NOTE: Recovery Notes in 09.1-RELEASE-VALIDATION.md are also missing — that gap is
  already covered by Test 7 (subsumed).

## Summary

total: 9
passed: 7
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Release validation evidence file `09.1-RELEASE-VALIDATION.md` exists with the six required headings and `Outcome: PASSED` for the validated version (v0.1.4)"
  status: failed
  reason: "User reported: Plan 02 must_have artifact missing — `.planning/phases/09.1-.../09.1-RELEASE-VALIDATION.md` does not exist on disk despite the release evidence being available in CI logs and PR descriptions."
  severity: major
  test: 7
  artifacts:
    - path: ".planning/phases/09.1-bootstrap-artifact-and-abi-alignment-for-default-installs/09.1-RELEASE-VALIDATION.md"
      issue: "file does not exist"
  missing:
    - "Create 09.1-RELEASE-VALIDATION.md with required headings: # Phase 09.1 Release Validation, ## Strict Readiness, ## Release Workflow, ## Public Bootstrap Validation, ## Outcome, ## Recovery Notes"
    - "Record evidence lines: 'Strict readiness: PASSED', 'Release workflow: PASSED', 'Public bootstrap validation: PASSED', 'validated version: 0.1.4', 'Outcome: PASSED'"
    - "Record release.yml run identifier and public-bootstrap-validation.yml run identifier (24990348339)"
    - "In Recovery Notes: capture the v0.1.2 → v0.1.3 → v0.1.4 recovery cycle and the literal sentence: 'If post-publish validation fails after v0.1.4 exists, recover with a corrected v0.1.5; do not mutate or reuse v0.1.4.'"
  root_cause: "Plan 02 was fulfilled in spirit (release published, validation passed) via two recovery PRs (#23, #24), but the literal evidence-capture artifact was never produced. The recovery cycles required operator focus on shipping v0.1.4, and the post-ship documentation step was skipped — a process gap, not a code gap."
  debug_session: ""

- truth: "README.md, benchmark docs, and bootstrap docs reference the validated release (v0.1.4) and remove 'upcoming release' / pre-tag hedges"
  status: failed
  reason: "User reported: README.md:67 still says 'The v0.1.2 benchmark snapshot is upcoming-release evidence; Phase 09.1 still owns bootstrap artifact and default-install alignment before a release tag' — directly contradicts shipped v0.1.4 reality. README has zero v0.1.4 mentions. docs/benchmarks/results-v0.1.4.md missing (only v0.1.1, v0.1.2 exist). Only docs/bootstrap.md and docs/releases.md updated to v0.1.4."
  severity: major
  test: 8
  artifacts:
    - path: "README.md"
      issue: "line 67 hedges the v0.1.2 benchmark as 'upcoming-release evidence' and says Phase 09.1 still owns alignment 'before a release tag'; needs rewrite to reflect validated v0.1.4"
    - path: "docs/benchmarks/results-v0.1.4.md"
      issue: "file does not exist; either re-anchor README to results-v0.1.2.md as the validated baseline or produce a v0.1.4-scoped results doc"
  missing:
    - "Update README.md:63 and :67 to remove the 'upcoming-release evidence' framing and reflect that v0.1.4 is the published default-install version"
    - "Either commit docs/benchmarks/results-v0.1.4.md (mirroring results-v0.1.2.md format) OR amend README to explicitly state that results-v0.1.2.md remains the validated baseline since v0.1.3/v0.1.4 are recovery-only releases with no benchmark drift"
    - "Sweep README and docs/benchmarks.md for any other 'upcoming' / 'pre-tag' / 'before tag' hedges and update accordingly"
  root_cause: "Plan 02 originally targeted v0.1.2 as the published version. When recovery shifted the published tag to v0.1.4, the README/benchmark-docs update step (also a Plan 02 must_have) was not redone against v0.1.4. Recovery PRs (#23, #24) focused on the build/publish failure modes, not consumer-facing doc state."
  debug_session: ""
