---
phase: "06.1"
slug: fresh-machine-end-to-end-bootstrap-uat-against-live-r2-githu
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-22
audited: 2026-04-22
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from `06.1-RESEARCH.md` §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | GitHub Actions workflow + bash wrapper + Python `unittest` contract checks |
| **Config file** | workflow YAML + repo scripts |
| **Quick run command** | `bash scripts/release/run_public_bootstrap_smoke.sh --help && python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` |
| **Full suite command** | `python3 -m unittest scripts/release/test_release_workflow_contracts.py scripts/release/test_public_bootstrap_validation_contracts.py` |
| **Estimated runtime** | ~5-20 seconds locally for contract checks; live workflow runtime depends on GitHub runner queue time |

---

## Sampling Rate

- **After every task commit:** `bash scripts/release/run_public_bootstrap_smoke.sh --help`
- **After every plan wave:** `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py`
- **Before `$gsd-verify-work`:** contract checks must be green and the public workflow must have been dispatched against a published tag
- **Max feedback latency:** under 20 seconds for local contract checks; external GitHub-runner latency accepted for the live validation gate

---

## Per-Requirement Verification Map

| Req | Behavior | Test Type | Automated Command | File Exists | Status |
|-----|----------|-----------|-------------------|-------------|--------|
| UAT-06.1-01 | rerunnable workflow keyed by published version and target-tag checkout | workflow + unittest | `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` | ✅ `test_workflow_exists_and_is_dispatchable`, `test_workflow_pins_actions_and_checks_out_target_src`, `test_jobs_force_bash_timeout_and_target_validation` | ✅ green |
| UAT-06.1-02 | five-target R2-only validation with GitHub fallback disabled | workflow + shell contract | `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` | ✅ `test_validate_r2_job_covers_full_matrix_in_r2_mode`, `test_wrapper_proves_fallback_404s_and_unix_permissions` | ✅ green |
| UAT-06.1-03 | representative GitHub fallback subset with forced mirror failure | workflow + shell contract | `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` | ✅ `test_validate_gh_fallback_job_uses_representative_subset`, `test_wrapper_proves_fallback_404s_and_unix_permissions` | ✅ green |
| UAT-06.1-04 | deterministic empty-cache start and exact cached artifact path proof | shell contract | `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` | ✅ `test_wrapper_clears_cache_and_proves_expected_path` | ✅ green |
| UAT-06.1-05 | unix cache directories stay `0700` after live bootstrap | shell contract | `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` | ✅ `test_wrapper_proves_fallback_404s_and_unix_permissions` | ✅ green |
| UAT-06.1-06 | docs identify the Phase `06.1` workflow as the canonical post-publish entrypoint | docs contract | `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` | ✅ `test_docs_define_phase_06_1_boundary_and_entrypoint` | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Fault Injection Test Matrix

| Fault | Test Pattern | Expected Behavior | Requirement |
|-------|--------------|-------------------|-------------|
| requested version does not match the checked-out target tag source | workflow checks `target-src/internal/bootstrap/version.go` | job fails before smoke | UAT-06.1-01 |
| stale cache makes the bootstrap path look green | wrapper removes `PURE_SIMDJSON_CACHE_DIR` before `go run` | job fails unless the artifact is downloaded again | UAT-06.1-04 |
| R2 is down but the full matrix still passes through fallback | `PURE_SIMDJSON_DISABLE_GH_FALLBACK=1` in `r2` mode | job fails instead of silently masking an R2 outage | UAT-06.1-02 |
| GitHub fallback subset accidentally still uses R2 | broken mirror base `https://releases.amikos.tech/pure-simdjson-does-not-exist` | job only passes if GitHub fallback works | UAT-06.1-03 |
| unix perms drift from `0700` while parse still succeeds | wrapper stats cache dirs after smoke | unix jobs fail on wrong perms | UAT-06.1-05 |

---

## Wave 0 Requirements

- [x] `.github/workflows/public-bootstrap-validation.yml`
- [x] `scripts/release/run_public_bootstrap_smoke.sh`
- [x] `scripts/release/test_public_bootstrap_validation_contracts.py`
- [x] `docs/releases.md` post-publish validation section
- [x] `docs/bootstrap.md` live public validation wording update

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Dispatch the new workflow against a real published tag and inspect the job summaries | UAT-06.1-01..05 | requires GitHub-hosted runners and public release artifacts | run `public-bootstrap-validation.yml` with `version=<published-semver>`, confirm all five `r2` jobs pass, confirm the three fallback-subset jobs pass, and inspect the summary lines for cache path + unix perms |
| Confirm the docs describe the same operator sequence the workflow implements | UAT-06.1-06 | wording quality and operator ergonomics need human review | read the new `docs/releases.md` / `docs/bootstrap.md` sections after implementation and check that they do not merge Phase 6 publish with Phase 06.1 validation |

---

## Security Domain

### Applicable ASVS L1 Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|------------------|
| V1 Architecture | yes | separate post-publish workflow; do not fold into `release.yml` |
| V5 Input Validation | yes | version input, target-tag checkout, and explicit target-source verification |
| V6 Cryptography | yes | live validation depends on published `SHA256SUMS`, not local overrides |
| V14 Build / Deploy | yes | fresh-runner jobs validate the actual released bytes and cache installation path |

### Known Threat Patterns

| Pattern | STRIDE | Standard Mitigation | Test |
|---------|--------|---------------------|------|
| stale cache produces false-positive success | Tampering / Quality gap | always clear `PURE_SIMDJSON_CACHE_DIR` | UAT-06.1-04 |
| wrong tag/source tree validated after the repo advances | Tampering | dual checkout with `target-src` version guard | UAT-06.1-01 |
| GitHub fallback masks an R2 outage in the full matrix | Availability gap | disable fallback for `r2` mode | UAT-06.1-02 |
| local checksum overrides hide live metadata regressions | Tampering | use published `SHA256SUMS` path only | UAT-06.1-02 / UAT-06.1-03 |

---

## Validation Sign-Off

- [x] All requirements have local automated verification plus explicit manual-only follow-up where the environment cannot be reproduced locally
- [x] Sampling continuity: every plan has local contract checks before the external workflow gate
- [x] Wave 0 gaps are closed by the current workflow / script / docs infrastructure
- [x] No watch-mode flags
- [x] Feedback latency < 20s for local contract checks
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** refreshed 2026-04-22

---

## Validation Audit 2026-04-22

| Metric | Count |
|--------|-------|
| Requirements audited | 6 |
| Gaps found | 5 |
| Resolved | 5 |
| Escalated | 0 |
| Tests green | 16 contract checks |

Fresh audit evidence:

- `bash scripts/release/run_public_bootstrap_smoke.sh --help` passed.
- `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py` passed (`8` tests).
- `python3 -m unittest scripts/release/test_release_workflow_contracts.py scripts/release/test_public_bootstrap_validation_contracts.py` passed (`16` tests total).
- The audit tightened `scripts/release/test_public_bootstrap_validation_contracts.py` so it now locks the exact `validate-r2` matrix, the exact `validate-gh-fallback` subset, cache clearing plus expected cache-path proof, unix `0700` assertions, and the Phase `06.1` docs contract.
- The audit corrected the stale placeholder `pending` statuses in this file. Remaining manual-only work is environmental: dispatching the hosted-runner workflow against a published tag and doing the final human docs-readability pass.
