---
phase: 06.1-fresh-machine-end-to-end-bootstrap-uat-against-live-r2-githu
verified: 2026-04-23T12:29:00Z
status: verified
score: 6/6 requirements verified; hosted-runner R2 and GitHub fallback matrices passed
overrides_applied: 0
human_verification: []
---

# Phase 06.1 Verification Report

**Phase Goal:** Add the reusable wrapper, dedicated rerunnable workflow, and operator docs needed to validate the live public bootstrap path against published artifacts on fresh hosted runners.
**Verified:** 2026-04-23T12:29:00Z
**Status:** verified
**Re-verification:** Yes — hosted validation run completed successfully

## Requirement Status

| Requirement | Status | Evidence |
| --- | --- | --- |
| UAT-06.1-01 — Dedicated rerunnable workflow keyed by published version / tag | ✓ VERIFIED | `.github/workflows/public-bootstrap-validation.yml` is `workflow_dispatch`, requires `version`, uses `public-bootstrap-validation-${{ inputs.version }}`, and validates `target-src` before setup. |
| UAT-06.1-02 — R2-only validation on all five supported targets | ✓ VERIFIED | GitHub Actions run `24835017953` completed `validate-r2` successfully for `linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, and `windows/amd64`. |
| UAT-06.1-03 — Forced GitHub fallback subset validation | ✓ VERIFIED | GitHub Actions run `24835017953` completed `validate-gh-fallback` successfully for `linux/amd64`, `darwin/arm64`, and `windows/amd64`; the wrapper's fallback mode requires broken-mirror `404` proof before smoke. |
| UAT-06.1-04 — Empty-cache start and exact cached artifact path proof | ✓ VERIFIED | `run_public_bootstrap_smoke.sh` clears the requested cache dir, runs `go run ./tests/smoke/go_bootstrap_smoke.go`, and fails unless `v<version>/<goos>-<goarch>/<libname>` exists afterward; both local live spot-checks passed. |
| UAT-06.1-05 — Unix `0700` cache-directory contract | ✓ VERIFIED | The wrapper enforces `0700` on the cache root, version dir, and platform dir via portable `stat` handling; the local `darwin/arm64` live runs passed these checks. |
| UAT-06.1-06 — Docs identify the workflow as the canonical Phase `06.1` entrypoint | ✓ VERIFIED | `docs/releases.md` and `docs/bootstrap.md` now document `public-bootstrap-validation.yml`, `target-src`, the fallback subset, and the live `SHA256SUMS` validation boundary. |

## Automated Checks Run

- `bash -n scripts/release/run_public_bootstrap_smoke.sh`
- `bash scripts/release/run_public_bootstrap_smoke.sh --help`
- `python3 -m unittest scripts/release/test_public_bootstrap_validation_contracts.py scripts/release/test_release_workflow_contracts.py`
- `rg -e 'workflow_dispatch|public-bootstrap-validation-\\$\\{\\{ inputs\\.version \\}\\}|actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683|actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff|shell: bash|timeout-minutes: 20|target-src|refs/tags/v\\$\\{\\{ inputs\\.version \\}\\}|target-src/go\\.mod|target-src/internal/bootstrap/version\\.go|target-src/tests/smoke/go_bootstrap_smoke\\.go|linux-amd64|linux-arm64|darwin-amd64|darwin-arm64|windows-amd64|--mode r2|--mode github-fallback' .github/workflows/public-bootstrap-validation.yml`
- `rg -e '--repo-root|--version|--goos|--goarch|--mode|--cache-dir|refusing unsafe cache dir|PURE_SIMDJSON_DISABLE_GH_FALLBACK=1|PURE_SIMDJSON_BINARY_MIRROR=\"https://releases\\.amikos\\.tech/pure-simdjson-does-not-exist\"|SHA256SUMS|stat -c|stat -f|go run ./tests/smoke/go_bootstrap_smoke\\.go|v\\$\\{version\\}/\\$\\{goos\\}-\\$\\{goarch\\}|GITHUB_STEP_SUMMARY|public bootstrap validation' scripts/release/run_public_bootstrap_smoke.sh`
- `bash scripts/release/run_public_bootstrap_smoke.sh --repo-root "$PWD" --version 0.1.0 --goos "$(go env GOOS)" --goarch "$(go env GOARCH)" --mode r2 --cache-dir "$(mktemp -d "${TMPDIR:-/tmp}/pure-simdjson-public-bootstrap.XXXXXX")"`
- `bash scripts/release/run_public_bootstrap_smoke.sh --repo-root "$PWD" --version 0.1.0 --goos "$(go env GOOS)" --goarch "$(go env GOARCH)" --mode github-fallback --cache-dir "$(mktemp -d "${TMPDIR:-/tmp}/pure-simdjson-public-bootstrap-fallback.XXXXXX")"`
- `gh workflow run public-bootstrap-validation.yml --repo amikos-tech/pure-simdjson --ref main -f version=0.1.0`
- `gh run view 24835017953 --repo amikos-tech/pure-simdjson --json status,conclusion,jobs` returned overall `status=completed`, `conclusion=success`, five successful `validate r2` jobs, and three successful `validate github fallback` jobs.

## Hosted Verification Completed

1. Dispatched `public-bootstrap-validation.yml` with `version=0.1.0`.
2. Confirmed all five `validate-r2` jobs passed.
3. Confirmed all three `validate-gh-fallback` jobs passed.
4. Confirmed the smoke wrapper success path validates the expected cache path, published `SHA256SUMS` digest, unix `0700` permissions where applicable, and broken-mirror fallback proof.

## Conclusion

The phase implementation is complete and verified: the wrapper, workflow, contract tests, docs, local live spot-checks, and hosted-runner public bootstrap validation have all passed for `v0.1.0`.
