# Phase 06.1: Fresh-machine end-to-end bootstrap UAT against live R2 + GitHub Releases - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `06.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-22
**Phase:** 06.1-fresh-machine-end-to-end-bootstrap-uat-against-live-r2-githu
**Areas discussed:** Execution venue, Source ladder coverage, Pass evidence, Trigger model

---

## Execution venue

| Option | Description | Selected |
|--------|-------------|----------|
| CI-only fresh-runner matrix | Use hosted runners for all 5 targets; each job clears cache, hits the live release, and runs the real bootstrap smoke. | ✓ |
| Hybrid | CI covers most targets, but 1-2 platforms also require manual workstation validation. | |
| Manual only | Treat `06.1` as a human-run UAT document/runbook, not a CI workflow. | |
| You decide | Let the agent pick the default. | |

**User's choice:** CI-only fresh-runner matrix
**Notes:** The phase should rely on fresh CI runners rather than workstation-only validation.

---

## Source ladder coverage

| Option | Description | Selected |
|--------|-------------|----------|
| R2 on all 5 + forced GitHub fallback on a smaller subset | Proves the normal path everywhere and still validates the GitHub mirror path without doubling the full matrix cost. | ✓ |
| R2 only on all 5 | Fastest and simplest, but leaves GitHub fallback unproven in `06.1`. | |
| Both R2 and GitHub fallback on all 5 | Strongest coverage, but much slower and more operationally noisy. | |
| You decide | Let the agent pick the default. | |

**User's choice:** R2 on all 5 + forced GitHub fallback on a smaller subset
**Notes:** Normal live R2 coverage stays full-matrix; forced GitHub fallback stays narrower.

---

## Pass evidence

| Option | Description | Selected |
|--------|-------------|----------|
| Bootstrap works + cache state is asserted | Require empty-cache start, real `NewParser() -> Parse(42)` success, expected cache file created, and `0700` perms checked on unix. | ✓ |
| Bootstrap works only | Only require the parse smoke to pass after download; skip cache/perms assertions. | |
| Bootstrap + cache state + explicit CLI re-verify | Everything in the recommended option, plus `pure-simdjson-bootstrap verify` against the downloaded artifact. | |
| You decide | Let the agent pick the default. | |

**User's choice:** Bootstrap works + cache state is asserted
**Notes:** Parse success alone is not enough; the workflow should also prove cache placement and unix permission behavior.

---

## Trigger model

| Option | Description | Selected |
|--------|-------------|----------|
| Separate rerunnable workflow keyed by tag/version | Keep it outside `release.yml`, but make it easy to rerun against an already-published tag. | ✓ |
| Auto-follow from publish | Trigger it automatically after release publish completes. | |
| Manual script/runbook only | No dedicated workflow; operators run scripts from the docs. | |
| You decide | Let the agent pick the default. | |

**User's choice:** Separate rerunnable workflow keyed by tag/version
**Notes:** The validation phase should remain independent from publish so it can be rerun without republishing.

---

## the agent's Discretion

- Pick the exact representative subset for forced GitHub Releases fallback validation.
- Choose the exact rerunnable workflow trigger shape, as long as it keys off a published tag/version.
- Choose the exact implementation used to assert cache paths and unix permissions in CI logs.

## Deferred Ideas

- Live corporate-firewall validation remains separate backlog work.
- Optional CLI `verify` participation is deferred unless planning finds a strong reason to add it.
- Auto-triggering public validation directly from `release.yml` is deferred in favor of a separate workflow.
