---
phase: 06.1-fresh-machine-end-to-end-bootstrap-uat-against-live-r2-githu
plan: "03"
subsystem: documentation
tags: [docs, bootstrap, release, runbook]
requirements-completed: [UAT-06.1-06]
duration: 1 session
completed: 2026-04-22T11:56:03Z
---

# Phase 06.1 Plan 03 Summary

**Updated the release and bootstrap docs so operators can discover the new public-validation workflow, understand the current-branch plus `target-src` checkout model, and keep the publish path separate from the post-publish validation path**

## Accomplishments

- Added a dedicated Phase `06.1` section to `docs/releases.md` that points operators at `public-bootstrap-validation.yml` after `release.yml` succeeds.
- Documented the full five-target R2 matrix, the smaller GitHub fallback subset, the `target-src/` dual-checkout model, and the expected workflow-summary evidence.
- Updated `docs/bootstrap.md` so the testing boundary now explicitly distinguishes local `httptest` plus optional `bootstrap.Checksums` overrides from live public validation against published `SHA256SUMS` and public artifacts.
- Made the docs say that live validation is owned by Phase `06.1` and follows the published tag source, not the moving branch tip.

## Verification

- `rg -e 'public-bootstrap-validation\\.yml|target-src|linux/amd64|linux/arm64|darwin/amd64|darwin/arm64|windows/amd64|GitHub fallback|404|06\\.1|SHA256SUMS' docs/releases.md docs/bootstrap.md`

## Decisions

- Kept `docs/releases.md` focused on publish and operator follow-up rather than merging the hosted-runner validation steps into `release.yml`.
- Updated `docs/bootstrap.md` in the scope section instead of rewording the whole document so the existing bootstrap contract stays stable.

## Task Commits

No plan-specific commit was created. The phase branch already contained local in-progress changes, so execution stayed in the working tree instead of sweeping unrelated edits into a task commit.

## Self-Check: PASSED

- The docs now name the workflow, target-source checkout model, fallback subset, and live `SHA256SUMS` boundary explicitly.
