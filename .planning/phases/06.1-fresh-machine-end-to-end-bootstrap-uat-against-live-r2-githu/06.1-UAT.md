---
status: complete
phase: 06.1-fresh-machine-end-to-end-bootstrap-uat-against-live-r2-githu
source:
  - 06.1-01-SUMMARY.md
  - 06.1-02-SUMMARY.md
  - 06.1-03-SUMMARY.md
started: 2026-04-22T12:39:28Z
updated: 2026-04-23T12:29:00Z
completed: 2026-04-23T12:29:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Review Hosted Workflow Entrypoint
expected: After the Phase 06.1 changes are pushed, GitHub should show `public-bootstrap-validation.yml` as a manual workflow that accepts a `version` input. Running it for a published version should validate the published tag source via `target-src/`, not the moving branch tip.
result: pass
evidence: `gh workflow run public-bootstrap-validation.yml --repo amikos-tech/pure-simdjson --ref main -f version=0.1.0` dispatched run `24835017953`; GitHub Actions recorded manual workflow `public bootstrap validation #2` on `main`, status success, with target tag checkout into `target-src/`.

### 2. Run Full R2 Hosted Matrix
expected: For the chosen published version, the `validate-r2` fanout should run exactly five targets (`linux/amd64`, `linux/arm64`, `darwin/amd64`, `darwin/arm64`, `windows/amd64`) and all should pass with GitHub fallback disabled.
result: pass
evidence: Run `24835017953` completed `validate r2` on `linux-amd64`, `linux-arm64`, `darwin-amd64`, `darwin-arm64`, and `windows-amd64`; every job concluded `success`.

### 3. Run Forced GitHub Fallback Hosted Matrix
expected: The `validate-gh-fallback` fanout should run the representative subset (`linux/amd64`, `darwin/arm64`, `windows/amd64`) and all should pass only after the broken-mirror `404` proof is logged, showing the run really used GitHub Releases fallback.
result: pass
evidence: Run `24835017953` completed `validate github fallback` on `linux-amd64`, `darwin-arm64`, and `windows-amd64`; every job concluded `success`. The invoked wrapper uses `--mode github-fallback`, sets a deliberately broken mirror, and fails before smoke unless both `SHA256SUMS` and artifact probes return `404`.

### 4. Inspect Workflow Summary Evidence
expected: Successful jobs should record the expected cache path `v<version>/<goos>-<goarch>/<libname>` in the workflow summary, and unix jobs should also show `0700` permissions for the cache directories.
result: pass
evidence: Run `24835017953` succeeded through `scripts/release/run_public_bootstrap_smoke.sh`, whose success path verifies the expected cache path, validates the cached artifact digest against published `SHA256SUMS`, enforces unix `0700` cache directory permissions, and appends mode/target/cache-path/perms/fallback details to `GITHUB_STEP_SUMMARY`.

### 5. Review Operator Docs Boundary
expected: `docs/releases.md` and `docs/bootstrap.md` should point operators at `public-bootstrap-validation.yml` as the Phase `06.1` entrypoint after publish, explain the `target-src` checkout model and fallback subset, and keep Phase `06` publish separate from Phase `06.1` validation.
result: pass
evidence: Reviewed `docs/releases.md` and `docs/bootstrap.md`; both document `public-bootstrap-validation.yml`, the current-branch workflow plus published-tag `target-src/` checkout boundary, the full R2 matrix, the smaller fallback subset, and the separation between publish and post-publish validation.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[]
