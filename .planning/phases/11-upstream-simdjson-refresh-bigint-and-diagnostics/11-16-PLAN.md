---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 16
type: execute
wave: 13
depends_on:
  - 11-15
files_modified:
  - patches/simdjson-v4.6.4-positive-bigint.patch
  - build.rs
  - tests/rust_shim_bigint.rs
  - testdata/jsontestsuite/cases/n_number_bigint_positive_suffix_x.json
  - testdata/jsontestsuite/cases/n_number_bigint_negative_suffix_underscore.json
  - testdata/jsontestsuite/cases/n_array_bigint_suffix_plus.json
  - testdata/jsontestsuite/cases/n_object_bigint_suffix_slash.json
  - testdata/jsontestsuite/expectations.tsv
autonomous: true
gap_closure: true
requirements:
  - UP-01
  - NUM-01

must_haves:
  truths:
    - "Per D-02, oversized integer text becomes BigInt only when the complete token has valid integer syntax and is followed by JSON structural punctuation or JSON whitespace."
    - "Positive and negative oversized roots plus nested array/object values retain kind 9 and their exact signed decimal spelling when their delimiter is valid."
    - "Oversized positive roots, negative roots, and nested values followed by `x`, `_`, `+`, `/`, or NUL return `PURE_SIMDJSON_ERR_INVALID_JSON`; no successful document exposes a truncated digit prefix."
    - "Per D-01, official simdjson v4.6.4 commit `1bcf71bd85059ab6574ea1159de9298dcc1212c5` remains the clean audited gitlink and exactly one repository patch is applied only to the build-output copy."
    - "All nine generated singleheader architecture implementations validate each early BigInt return, and the build fails if any architecture copy or guard drifts."
    - "The JSONTestSuite oracle contains representative positive-root, negative-root, array, and object malformed-BigInt suffix fixtures whose expectation is reject."
  artifacts:
    - path: "patches/simdjson-v4.6.4-positive-bigint.patch"
      provides: "The one D-01 patch with delimiter validation on every BigInt early-return path"
      contains: "is_not_structural_or_whitespace"
    - path: "build.rs"
      provides: "Fail-closed nine-architecture guard for all validated BigInt branches"
      contains: "PATCHED_BIGINT"
    - path: "tests/rust_shim_bigint.rs"
      provides: "Exact-text controls and malformed suffix matrix at the public Rust ABI"
      contains: "malformed_bigint_suffixes_are_rejected_without_truncation"
    - path: "testdata/jsontestsuite/expectations.tsv"
      provides: "Persistent oracle rejection expectations for malformed oversized integer tokens"
      contains: "n_number_bigint_positive_suffix_x.json"
  key_links:
    - from: "patches/simdjson-v4.6.4-positive-bigint.patch"
      to: "build.rs"
      via: "the audited source copy is patched and exact guarded-branch counts are checked before compilation"
      pattern: "is_not_structural_or_whitespace|matches\\(.*\\)\\.count\\(\\)"
    - from: "tests/rust_shim_bigint.rs"
      to: "pure_simdjson_parser_parse"
      via: "valid controls reach kind 9 and malformed suffix cases must return status 32 before any BigInt accessor can run"
      pattern: "PURE_SIMDJSON_ERR_INVALID_JSON|PURE_SIMDJSON_OK"
    - from: "testdata/jsontestsuite/expectations.tsv"
      to: "benchmark_oracle_test.go"
      via: "manifest-complete case discovery and reject assertions"
      pattern: "reject"
---

<objective>
Restore JSON token-boundary validation on the exact-text BigInt path.

Purpose: The approved D-01 overflow patch currently returns through the BigInt path before simdjson's normal delimiter check, allowing malformed JSON to become a truncated valid value. D-02 requires exact preservation only for valid integer syntax.
Output: one fail-closed nine-architecture patch, low-level malformed-suffix regressions, and JSONTestSuite oracle fixtures.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VERIFICATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-02-SUMMARY.md
@patches/simdjson-v4.6.4-positive-bigint.patch
@build.rs
@tests/rust_shim_bigint.rs
@benchmark_oracle_test.go
@benchmark_fixtures_test.go
@testdata/jsontestsuite/expectations.tsv

<interfaces>
Current audited build contract:

```text
SIMDJSON_BASE_COMMIT = 1bcf71bd85059ab6574ea1159de9298dcc1212c5
SIMDJSON_PATCH = patches/simdjson-v4.6.4-positive-bigint.patch
patched_simdjson_source(...) copies the official singleheader into OUT_DIR,
applies the one patch, then checks every generated architecture implementation.
```

Relevant public ABI outcomes:

```text
PURE_SIMDJSON_OK = 0
PURE_SIMDJSON_ERR_INVALID_JSON = 32
PURE_SIMDJSON_VALUE_KIND_BIGINT = 9
pure_simdjson_parser_parse(parser, bytes, len, out_doc)
pure_simdjson_element_get_bigint(view, out_ptr, out_len)
```
</interfaces>
</context>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Audited v4.6.4, exact BigInt text, and production controls/diagnostics | 11-01..11-17 | COVERED | Plans 16-17 close the only two failed verification truths; verified controls/diagnostics remain owned by completed plans. |
| REQ | UP-01 | Reproducible v4.6.4 plus contract/correctness gates | 11-16, 11-17 | COVERED | Plan 16 fixes patched-parser validity; Plan 17 closes the normative exception gate and runs the combined local phase gate. |
| REQ | NUM-01 | Preserve valid oversized integers as exact text | 11-16 | COVERED | Valid controls remain exact while malformed tokens are rejected. |
| REQ | NUM-02 | Strict TypeBigInt/GetBigInt without automatic arbitrary precision | 11-02, 11-03, 11-10 | COVERED | Verifier marked satisfied; Plan 16 re-exercises copied exact text without changing the API. |
| REQ | DIAG-01 | Kernel report and diagnostic override | 11-06..11-14 | COVERED | Verifier marked satisfied; no gap work changes it. |
| REQ | DIAG-02 | Proven offsets and explicit unknown state | 11-05, 11-12, 11-15 | COVERED | Verifier marked satisfied after Plan 15; malformed BigInt rejection continues through the existing diagnostic path. |
| REQ | LIMIT-01 | Immutable bounded capacity/depth | 11-04, 11-11, 11-15 | COVERED | Verifier marked satisfied; no gap work changes limits. |
| RESEARCH | — | Keep official v4.6.4 and one build-output-only patch | 11-16 | COVERED | The patch is strengthened in place; no fork, second source tree, package, or parser is introduced. |
| RESEARCH | — | Reuse upstream BigInt and existing copied ownership paths | 11-16 | COVERED | Valid exact-text controls use the existing kind-9/GetBigInt path. |
| RESEARCH | — | Trap every C++ exception at the native seam | 11-17 | COVERED | The existing exception map and test seam are reconciled with the normative ABI. |
| CONTEXT | D-01 | Exact v4.6.4 base and exactly one provenance-recorded patch | 11-16 | COVERED | Patch/base/application mechanism remain unchanged; delimiter checks are added to the same patch. |
| CONTEXT | D-02 | Only valid out-of-range integer syntax becomes BigInt | 11-16 | COVERED | Positive, negative, root, nested, structural/whitespace, and malformed suffix cases are executable. |
| CONTEXT | D-03..D-05 | Append-only kind 9, copied exact getter, wrong-type numeric getters | 11-02, 11-03, 11-10, 11-16 | COVERED | Existing contract stays unchanged and valid controls prevent regression. |
| CONTEXT | D-06..D-22 | Offsets, controls, ABI/bootstrap, replay, loading, and pre-copy limits | 11-04..11-15 | COVERED | Verifier found no remaining gap in these decisions; plans 16-17 do not alter them. |
| VERIFY | gap-1 | Reject malformed oversized tokens without dropping suffixes | 11-16 | COVERED | Direct ABI matrix plus persistent oracle fixtures. |
| VERIFY | gap-2 | Trapped bad_alloc maps to public status 97 | 11-17 | COVERED | Existing seam is extended deterministically; normative contract remains unchanged. |

Deferred ideas: none. Phase 12-16 navigation, On-Demand extraction, borrowed views, streaming, and final v0.2 publication remain outside these closure plans.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard every BigInt early return before preserving exact text</name>
  <files>patches/simdjson-v4.6.4-positive-bigint.patch, build.rs, tests/rust_shim_bigint.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-01 through D-05
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VERIFICATION.md` gap `Only valid oversized integer tokens enter the BigInt path`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-02-SUMMARY.md` patch and exact-text implementation decisions
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` sections `Upstream gitlink and native build`, `BigInt type and copied accessor`, and `Planner Guardrails`
    - `patches/simdjson-v4.6.4-positive-bigint.patch` all nine current positive-overflow hunks
    - `build.rs` constants and `patched_simdjson_source`
    - `tests/rust_shim_bigint.rs` parser helpers, exact-text tests, and root/descendant coverage
    - `third_party/simdjson/singleheader/simdjson.cpp` one complete `parse_number` implementation around `digit_count`, negative overflow, positive overflow, and the final delimiter check
  </read_first>
  <behavior>
    - Valid roots `18446744073709551616` and `-9223372036854775809` parse as kind 9 and copy the exact source text.
    - Valid nested positive and negative BigInts followed by `]`, `}`, `,`, space, tab, carriage return, or newline retain exact text.
    - Positive and negative roots followed by `x`, `_`, `+`, `/`, or NUL return status 32.
    - Array/object BigInt values followed by the same malformed suffix classes return status 32.
    - A failed parse leaves no successful document from which a truncated prefix can be read.
    - Every one of the nine generated `parse_number` implementations validates the delimiter before the too-many-digits, negative-overflow, and positive-overflow BigInt returns.
  </behavior>
  <action>
    Add the RED tests `malformed_bigint_suffixes_are_rejected_without_truncation` and `valid_bigint_delimiters_preserve_exact_text` to `tests/rust_shim_bigint.rs`. The malformed table must use byte slices, not C strings, so it can include a literal NUL. Cover positive and negative roots, an array value, and an object value across suffixes `x`, `_`, `+`, `/`, and NUL. For every malformed input, require `pure_simdjson_parser_parse` to return `PURE_SIMDJSON_ERR_INVALID_JSON` (numeric 32), require the output document sentinel to remain unwritten, and never invoke `doc_root` or `GetBigInt`. Keep valid positive/negative roots and nested array/object controls in the same target; require kind 9 and byte-for-byte text through the existing copied accessor.

    Then update the single D-01 patch in place. In every one of the nine generated architecture copies, the `digit_count > longest_digit_count` return, negative 19-digit overflow return, and patched positive 20-digit unsigned-overflow return must first evaluate `jsoncharutils::is_not_structural_or_whitespace(*p)` and return `INVALID_NUMBER(src)` for a dirty delimiter; only a structural-or-whitespace delimiter may return `BIGINT_NUMBER(src)`. Preserve the official v4.6.4 gitlink, the exact positive-overflow classification amendment, `number_as_string(true)`, kind 9, and copied ownership. Do not add a custom decimal/token parser, a second patch, a second source tree, a dependency, or a dirty submodule.

    Replace the single positive-branch count in `build.rs` with explicit fail-closed guards for the three validated branch shapes. Name the guard constants with the `PATCHED_BIGINT_` prefix, require each guarded shape exactly nine times in the patched singleheader, and require the corresponding unguarded early-return shapes to occur zero times. Keep the exact base-commit, tracked/staged cleanliness, `git apply --check`, build-output-copy, and C++17/cc checks unchanged. This is the parity proof for all generated architectures; a local active-kernel test alone is not sufficient.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_bigint -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - The official simdjson gitlink still resolves to `1bcf71bd85059ab6574ea1159de9298dcc1212c5`, and `git -C third_party/simdjson status --short` is empty.
    - The repository still contains exactly one simdjson patch and `build.rs` applies it only to the `OUT_DIR` copy.
    - `build.rs` asserts exactly nine guarded too-many-digits branches, nine guarded negative-overflow branches, and nine guarded positive-overflow branches.
    - `build.rs` rejects any retained unguarded BigInt early-return shape; guard failure aborts the build before C++ compilation.
    - Rust tests cover both signs, roots, nested array/object values, all five malformed suffix classes, and structural/whitespace valid controls.
    - Every malformed case returns status 32 and cannot expose a truncated prefix; every valid control returns exact copied decimal text.
    - No `math/big`, alternate parser/scanner, new package, second source copy, or ABI change is introduced.
  </acceptance_criteria>
  <done>Every generated BigInt path preserves valid exact text and rejects a dirty token delimiter before success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add malformed BigInt cases to the JSONTestSuite oracle</name>
  <files>testdata/jsontestsuite/cases/n_number_bigint_positive_suffix_x.json, testdata/jsontestsuite/cases/n_number_bigint_negative_suffix_underscore.json, testdata/jsontestsuite/cases/n_array_bigint_suffix_plus.json, testdata/jsontestsuite/cases/n_object_bigint_suffix_slash.json, testdata/jsontestsuite/expectations.tsv</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VERIFICATION.md` required malformed-suffix oracle coverage
    - `benchmark_oracle_test.go` manifest completeness and accept/reject assertions
    - `benchmark_fixtures_test.go` manifest parsing rules and `oracleCasesDir`
    - `testdata/jsontestsuite/expectations.tsv` sorted manifest and current valid oversized-integer entries
    - `testdata/jsontestsuite/cases/i_number_too_big_pos_int.json` valid positive control
    - `testdata/jsontestsuite/cases/i_number_too_big_neg_int.json` valid negative control
    - `testdata/jsontestsuite/cases/n_number_neg_with_garbage_at_end.json` malformed-number fixture convention
    - `testdata/jsontestsuite/cases/n_object_non_string_key_but_huge_number_instead.json` malformed nested-number fixture convention
  </read_first>
  <behavior>
    - `18446744073709551616x` is rejected as a positive root.
    - `-9223372036854775809_` is rejected as a negative root.
    - `[123456789012345678901+]` is rejected as a nested array value.
    - `{"n":-9223372036854775809/}` is rejected as a nested object value.
    - Existing valid oversized `i_*` cases remain accepted and all pre-existing oracle rows retain their expectation.
  </behavior>
  <action>
    Create the four named `n_*` fixture files with exactly the malformed JSON bytes listed in the behavior block. Add one sorted `reject` row per fixture to `testdata/jsontestsuite/expectations.tsv`; use the note `Phase 11 BigInt token-boundary regression` so the project-owned addition is distinguishable from upstream fixtures. Do not change any existing expectation, especially the three valid oversized integer `i_*` accept rows or the existing huge exponent/real rejects. Reuse the manifest-driven `TestJSONTestSuiteOracle`; do not add a parallel fixture loader or a second oracle.
  </action>
  <verify>
    <automated>cargo build --release --locked &amp;&amp; case "$(go env GOOS)" in linux) PHASE11_LIBRARY="$PWD/target/release/libpure_simdjson.so" ;; darwin) PHASE11_LIBRARY="$PWD/target/release/libpure_simdjson.dylib" ;; windows) PHASE11_LIBRARY="$PWD/target/release/pure_simdjson.dll" ;; *) exit 1 ;; esac &amp;&amp; PURE_SIMDJSON_LIB_PATH="$PHASE11_LIBRARY" go test . -run '^TestJSONTestSuiteOracle$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All four new fixture files exist and each has exactly one matching manifest row.
    - The four rows are `reject`, use the Phase 11 regression note, and preserve manifest lexical ordering.
    - The positive root, negative root, array, and object contexts cover three additional textual suffix classes beyond the NUL case in the Rust matrix.
    - No pre-existing `expect` value changes; the valid oversized integer rows remain `accept`.
    - `TestJSONTestSuiteOracle` passes against a freshly rebuilt library.
    - The existing manifest completeness checks continue to reject unlisted or missing fixture files.
  </acceptance_criteria>
  <done>The persistent correctness oracle rejects representative malformed BigInt roots and nested values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted JSON bytes -> patched simdjson number parser | A malformed suffix currently crosses the validation boundary and is silently dropped before kind-9 success. |
| repository patch -> nine generated architecture implementations | A partial hunk update can make parser validity depend on the selected SIMD implementation. |
| regression fixtures -> correctness oracle | Missing or unlisted malformed cases can leave a green suite that does not represent the patched behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-16-01 | Tampering | BigInt token boundary in `parse_number` | mitigate | Validate `*p` as structural-or-whitespace before every BigInt early return; test both signs, roots, nested values, and `x`/`_`/`+`/`/`/NUL suffixes through the real ABI. |
| T-11-16-02 | Tampering | generated multi-architecture patch parity | mitigate | Require three guarded branch shapes exactly nine times and zero unguarded shapes in the build-output source; abort the build on drift. |
| T-11-16-03 | Repudiation | JSONTestSuite oracle | mitigate | Add manifest-complete reject fixtures for positive, negative, array, and object contexts while preserving valid exact-text controls. |
| T-11-SC | Tampering | vendored source and package supply chain | mitigate | Retain the exact official v4.6.4 gitlink, one provenance-recorded build-output patch, locked dependencies, and no package install. |
</threat_model>

<verification>
- `cargo test --locked --test rust_shim_bigint -- --test-threads=1` passes the valid-text and malformed-suffix matrix.
- `cargo build --release --locked` exercises the exact-base, clean-submodule, patch-apply, and nine-architecture source guards.
- `cargo build --release --locked &amp;&amp; case "$(go env GOOS)" in linux) PHASE11_LIBRARY="$PWD/target/release/libpure_simdjson.so" ;; darwin) PHASE11_LIBRARY="$PWD/target/release/libpure_simdjson.dylib" ;; windows) PHASE11_LIBRARY="$PWD/target/release/pure_simdjson.dll" ;; *) exit 1 ;; esac &amp;&amp; PURE_SIMDJSON_LIB_PATH="$PHASE11_LIBRARY" go test . -run '^TestJSONTestSuiteOracle$' -count=1` passes with the four new reject fixtures and unchanged prior expectations.
- `git diff --check` reports no malformed patch/manifest whitespace.
- No tag, merge, publication, release workflow dispatch, or artifact upload occurs.
</verification>

<success_criteria>
- Malformed oversized integer tokens never parse successfully or expose truncated decimal text.
- Valid positive/negative and nested BigInts retain exact copied text.
- All nine generated architecture implementations enforce the same delimiter rule.
- The persistent correctness oracle detects regression independently of the focused Rust target.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-16-SUMMARY.md` when done.
</output>
