---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 04
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - src/lib.rs
  - tests/rust_shim_limits.rs
autonomous: true
requirements:
  - LIMIT-01

must_haves:
  truths:
    - "A configured parser stores immutable max-capacity/max-depth values; the legacy C constructor remains and delegates to the current defaults."
    - "Input longer than configured capacity returns dedicated status 9 before Rust detaches, resizes, or copies the padded arena."
    - "Capacity 0 means 0xFFFFFFFF, depth 0 means 1024, capacity 32 is valid, and capacity 1..31 or values above 0xFFFFFFFF are rejected rather than clamped."
    - "Configured depth N accepts N-1 nested arrays and rejects N with the existing depth-limit status."
    - "A Rust-side capacity rejection clears prior native error text/location instead of leaking stale diagnostics."
  artifacts:
    - path: "tests/rust_shim_limits.rs"
      provides: "Configured construction, pre-copy capacity, exact depth boundary, and stale-detail tests"
      contains: "PURE_SIMDJSON_ERR_CAPACITY_LIMIT"
    - path: "src/runtime/registry.rs"
      provides: "Stored parser config and the authoritative pre-copy capacity gate"
      contains: "max_capacity"
    - path: "src/lib.rs"
      provides: "Additive configured constructor and capacity-limit status 9"
      exports: ["pure_simdjson_parser_new_configured"]
  key_links:
    - from: "src/lib.rs"
      to: "src/runtime/registry.rs"
      via: "configured constructor passes normalized capacity/depth into ParserEntry"
      pattern: "parser_new_configured"
    - from: "src/runtime/registry.rs"
      to: "src/native/simdjson_bridge.cpp"
      via: "capacity preflight precedes arena resize/copy; depth config reaches DOM parser"
      pattern: "max_capacity|max_depth"
---

<objective>
Add immutable native parser bounds with a real pre-allocation capacity limit and exact upstream depth semantics.

Purpose: D-13 only protects production memory when Rust rejects before its padded arena allocation; the C++ limit alone is too late.
Output: configured constructor, stored registry config, dedicated capacity status, depth configuration, and native tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@.planning/spikes/003-pre-copy-capacity-proof/README.md
@src/native/simdjson_bridge.h
@src/runtime/mod.rs
@src/runtime/registry.rs
@src/lib.rs

<interfaces>
Additive constructor chosen for this phase:

```c
pure_simdjson_error_code_t pure_simdjson_parser_new_configured(
    uint64_t max_capacity,
    uint32_t max_depth,
    pure_simdjson_parser_t *out_parser);
```

Constants/status:

```text
default max_capacity = 0xFFFFFFFF
default max_depth = 1024
PURE_SIMDJSON_ERR_CAPACITY_LIMIT = 9
PURE_SIMDJSON_ERR_DEPTH_LIMIT = 8 (unchanged)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add configured native construction while retaining the legacy constructor</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-10 and D-13
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Pattern 1: Normalize Once, Store Comparable Configuration`, `Pitfall 2`, and `Pitfall 3`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Immutable parser options and homogeneous pools`
    - `src/native/simdjson_bridge.h` existing parser constructor declaration
    - `src/native/simdjson_bridge.cpp` `psimdjson_parser` and constructor/parser setup
    - `src/runtime/mod.rs` `native_parser_new` wrapper
    - `third_party/simdjson/include/simdjson/dom/parser.h` and `parser-inl.h` allocation/depth behavior
  </read_first>
  <action>
    Add private bridge constructor `psimdjson_parser_new_configured(uint64_t max_capacity, uint32_t max_depth, ...)` and make existing `psimdjson_parser_new` call it with `0xFFFFFFFF` and `1024`. Reject capacity `1..31`, capacity above `0xFFFFFFFF`, and depth values that cannot cross the selected `uint32_t` ABI; zero means the stated defaults. Configure `number_as_string(true)`, maximum capacity, and maximum depth before the parser's first parse. Add `native_parser_new_configured` in Rust and retain `native_parser_new` as the default wrapper. Add a native diagnostic-reset helper used at parse-attempt start so later Rust-side early returns cannot expose stale details. Preserve every existing constructor signature per D-16.
  </action>
  <verify>
    <automated>cargo check --locked &amp;&amp; cargo test --locked --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Existing `psimdjson_parser_new` still exists with its original signature.
    - The configured constructor receives `uint64_t` capacity and `uint32_t` depth and sets both before parse.
    - Zero maps to exact defaults; capacity 32 is accepted; 1 and 31 are rejected.
    - `number_as_string(true)` remains enabled for both old and configured constructors.
    - Diagnostic reset is callable before any registry capacity return.
  </acceptance_criteria>
  <done>The native parser can be constructed with validated immutable bounds while the old constructor retains current defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce capacity before Rust arena growth and pin depth boundaries</name>
  <files>src/runtime/registry.rs, src/lib.rs, tests/rust_shim_limits.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` tasks `11-W0-05` and `11-W0-06`, threat T-11-01
    - `.planning/spikes/003-pre-copy-capacity-proof/README.md` validated ordering, unchanged-arena proof, and avoided-copy comparison
    - `src/runtime/registry.rs` `ParserEntry`, `parser_new`, and `parser_parse` arena resize/copy sequence
    - `src/lib.rs` error enum, `pure_simdjson_parser_new`, and parser export tests
    - `materializer_fastpath_test.go` existing default-depth 1023/1024 boundary fixture
    - `tests/rust_shim_minimal.rs` parser lifecycle helpers
  </read_first>
  <behavior>
    - Capacity-sized input succeeds; capacity+1 returns status 9.
    - Rejected input does not increase reusable input length/capacity and is never copied.
    - Depth N-1 succeeds and N returns status 8 for a small configured N.
    - Default depth retains the existing 1023-success/1024-failure boundary.
    - A known parse failure followed by capacity rejection leaves no prior message or known offset.
  </behavior>
  <action>
    Write `tests/rust_shim_limits.rs` first with configured/default constructor, invalid-value, boundary, pre-copy buffer-state, and stale-detail cases. Add capacity status `PURE_SIMDJSON_ERR_CAPACITY_LIMIT = 9` to `src/lib.rs` without changing 0 through 8 or 32+. Store `max_capacity: u64` and `max_depth: u32` on every `ParserEntry`. Implement `registry::parser_new_configured`; keep `parser_new` delegating to defaults. At the start of `parser_parse`, validate the handle/busy state, clear native diagnostics, and compare `input.len()` to the stored capacity while `reusable_input` is still attached to `ParserEntry`; return status 9 on excess. Only an accepted input may proceed to `mem::take`, `checked_add(padding)`, `Vec::resize`, or `copy_from_slice`. Export `pure_simdjson_parser_new_configured` through `ffi_wrap`, while existing `pure_simdjson_parser_new` remains unchanged. Prove the rejected buffer's bytes, length, and capacity remain unchanged through a white-box registry test, preferring an existing registry unit-test module over a new public symbol.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_limits -- --test-threads=1 &amp;&amp; cargo test --locked registry::tests::capacity -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Status 9 is append-only and named capacity limit in Rust tests.
    - The capacity check textually precedes `mem::take`, padding `checked_add`, `resize`, and `copy_from_slice` in `parser_parse`.
    - A boundary test proves capacity+1 rejection without arena growth.
    - Small-N and default depth boundaries are both executable.
    - A capacity rejection after another parse error reports no inherited message/offset.
    - Existing parser lifecycle/busy behavior remains green.
  </acceptance_criteria>
  <done>Configured capacity is a real pre-copy safety bound, depth uses exact upstream semantics, and early rejection cannot leak stale diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller length/config -> Rust arena | Untrusted size values reach the first native-owned allocation. |
| configured depth -> recursive DOM/materializer work | Deep untrusted input can consume stack and parser resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-01 | Denial of Service | `registry::parser_parse` | mitigate | Compare input length before padding calculation, resize, and copy; return dedicated status 9; test capacity+1 and unchanged arena state. |
| T-11-05 | Repudiation | early capacity error details | mitigate | Clear diagnostic state before the Rust-side gate and test that prior text/location is not inherited. |
</threat_model>

<verification>
- Focused Rust limit tests pass serially.
- Registry white-box capacity test proves rejection precedes growth.
- Existing minimal/materializer depth tests remain green.
</verification>

<success_criteria>
- Maximum capacity is enforced before allocation/copy.
- Maximum depth has an executable N-1/N contract.
- Legacy constructors keep the current defaults.
- No existing status, symbol, or layout is renumbered.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-04-SUMMARY.md` when done.
</output>
