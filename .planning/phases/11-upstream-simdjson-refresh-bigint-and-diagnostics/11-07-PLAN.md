---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 07
type: execute
wave: 6
depends_on:
  - 11-01
  - 11-06
files_modified:
  - internal/bootstrap/version.go
  - internal/bootstrap/checksums.go
  - scripts/release/check_bootstrap_abi_state.py
  - scripts/release/test_check_bootstrap_abi_state.py
  - CHANGELOG.md
  - src/lib.rs
  - internal/ffi/types.go
  - internal/ffi/types_test.go
  - internal/bootstrap/abi_assertion.go
autonomous: true
requirements:
  - UP-01
  - NUM-02
  - DIAG-01
  - DIAG-02
  - LIMIT-01

must_haves:
  truths:
    - "Rust and Go both declare ABI `0x00010002`; value kind 9 and statuses 9/10 are append-only."
    - "D-17: The exact operator-approved version from 11-01 is the bootstrap pin and ABI-1.2 policy entry; no version is guessed."
    - "The compile-time canary binds that exact bootstrap version to Go ABI `0x00010002` in both subtraction directions."
    - "D-15: ABI 1.1 is below the Phase 11 minimum, while later additive ABI 1.x remains policy-compatible only if mandatory binding succeeds."
    - "The changelog names the intermediate compatibility artifact, ABI/source break, BigInt/options/diagnostics surface, and rebuild requirement without calling it the final v0.2 release."
    - "The approved bootstrap pin is source-preparation state only until Plan 11-14 publishes and validates that version; every pre-publication runtime smoke uses a freshly built explicit local library."
    - "Every retained checksum example key uses the exact `approved_abi12_version`; no `v0.1.4/` example remains after the bootstrap pin changes."
  artifacts:
    - path: "internal/bootstrap/version.go"
      provides: "Approved intermediate artifact pin"
      contains: "Version"
    - path: "internal/bootstrap/checksums.go"
      provides: "Retained five-platform checksum example keys coordinated with the approved artifact version"
      contains: "<sha256>"
    - path: "internal/bootstrap/abi_assertion.go"
      provides: "Exact compile-time version/ABI canary"
      contains: "0x00010002"
    - path: "scripts/release/check_bootstrap_abi_state.py"
      provides: "ABI 1.2 minimum-version readiness policy"
      contains: "0x00010002"
    - path: "internal/ffi/types.go"
      provides: "Go ABI/status/kind mirrors"
      contains: "ValueKindBigInt"
  key_links:
    - from: ".planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md"
      to: "internal/bootstrap/version.go"
      via: "exact `approved_abi12_version` substitution"
      pattern: "approved_abi12_version"
    - from: ".planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md"
      to: "internal/bootstrap/checksums.go"
      via: "the same approved version replaces the previous version component in all retained example keys"
      pattern: "approved_abi12_version"
    - from: "internal/bootstrap/abi_assertion.go"
      to: "internal/ffi/types.go"
      via: "bidirectional compile-time equality"
      pattern: "ffi\.ABIVersion"
---

<objective>
Commit one coherent ABI 1.2 and approved bootstrap-version source identity after every native symbol exists.

Purpose: D-15 through D-17 require Go, Rust, bootstrap, readiness policy, and release notes to move together; an artifact cannot honestly claim ABI 1.2 otherwise.
Output: approved version pin, ABI policy/tests, exact canary, ABI constants, append-only mirrors, and changelog entry.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md
@internal/bootstrap/version.go
@internal/bootstrap/checksums.go
@internal/bootstrap/abi_assertion.go
@scripts/release/check_bootstrap_abi_state.py
@internal/ffi/types.go
@src/lib.rs

<interfaces>
Locked ABI values:

```text
PURE_SIMDJSON_ABI_VERSION / ffi.ABIVersion = 0x00010002
ValueKindBigInt = 9
ErrCapacityLimit = 9
ErrKernelLocked = 10
```

The semantic version is not written in this plan: read the exact `approved_abi12_version` from `11-01-SUMMARY.md` and use that same value everywhere.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind the approved semantic version to the ABI 1.2 readiness policy</name>
  <files>internal/bootstrap/version.go, internal/bootstrap/checksums.go, scripts/release/check_bootstrap_abi_state.py, scripts/release/test_check_bootstrap_abi_state.py, CHANGELOG.md</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md` exact `approved_abi12_version`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-15 through D-17
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Binding Resolutions` and `Pattern 6`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` `File Classification` row for `internal/bootstrap/checksums.go` and section `Bootstrap ABI policy and release source state`
    - `internal/bootstrap/version.go` current 0.1.4 pin
    - `internal/bootstrap/checksums.go` retained five-platform `v0.1.4/...` example keys and empty override-map contract
    - `scripts/release/check_bootstrap_abi_state.py` `ABI_MINIMUM_VERSION` and source parsing
    - `scripts/release/test_check_bootstrap_abi_state.py` synthetic source fixtures
    - `CHANGELOG.md` current Keep-a-Changelog headings and ABI notes
  </read_first>
  <behavior>
    - Approved version + Go/Rust ABI 1.2 passes.
    - Current 0.1.4 pin + ABI 1.2 fails stale-version policy.
    - ABI 1.1 with approved version fails mismatch/minimum policy.
    - Unknown ABI, Go/Rust mismatch, and requested-version mismatch fail closed.
    - All five retained checksum example keys use `v<approved_abi12_version>/...`; the previous `v0.1.4/...` examples are absent while the override map remains data-empty.
  </behavior>
  <action>
    Read `approved_abi12_version` from `11-01-SUMMARY.md`; abort if missing, malformed, or already tagged. Add synthetic tests first for accepted ABI 1.2 plus stale/mismatch/unknown cases. Set `bootstrap.Version` to that exact value and add `0x00010002: <approved-version>` to `ABI_MINIMUM_VERSION` without changing the 1.0/1.1 history. Per the `11-PATTERNS.md` checksum-file assignment, retain the empty `Checksums` override map and its five commented platform examples, but replace the `v0.1.4` component in every example key with the exact `approved_abi12_version` value. Keep the `<sha256>` example values and all five platform filenames unchanged; do not insert a literal `approved_abi12_version` placeholder or real digest into the map. Add a dated Keep-a-Changelog section for the approved intermediate artifact describing v4.6.4, ABI 1.2, TypeBigInt/GetBigInt, parser options/pool source break, kernel/offset diagnostics, strict ABI 1.1 rejection, and native rebuild requirement. Label it an intermediate compatibility artifact and state that default-bootstrap availability remains unproven until Plan 11-14; do not claim Phase 16 final release or publication success. Addresses review concern: the bootstrap pin, retained checksum examples, and readiness policy now consume one recorded version, while changing source state is not mistaken for a usable public artifact.
  </action>
  <verify>
    <automated>APPROVED_ABI12_VERSION="$(sed -nE 's/.*approved_abi12_version:[[:space:]]*`?([0-9]+\.[0-9]+\.[0-9]+)`?.*/\1/p' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md | head -n 1)" &amp;&amp; export APPROVED_ABI12_VERSION &amp;&amp; test -n "$APPROVED_ABI12_VERSION" &amp;&amp; python3 scripts/release/test_check_bootstrap_abi_state.py &amp;&amp; ! rg -Fq '"v0.1.4/' internal/bootstrap/checksums.go &amp;&amp; (for path in linux-amd64/libpure_simdjson.so linux-arm64/libpure_simdjson.so darwin-amd64/libpure_simdjson.dylib darwin-arm64/libpure_simdjson.dylib windows-amd64/pure_simdjson-msvc.dll; do rg -Fq "\"v${APPROVED_ABI12_VERSION}/${path}\": \"&lt;sha256&gt;\"" internal/bootstrap/checksums.go || exit 1; done)</automated>
  </verify>
  <acceptance_criteria>
    - Every version-specific edit equals `approved_abi12_version`; no guessed literal appears.
    - `internal/bootstrap/checksums.go` retains exactly the five documented platform example keys with the approved `v<version>` prefix and `<sha256>` values, while `v0.1.4/` is absent.
    - `Checksums` remains an empty runtime override map: the coordinated examples stay comments and no digest is invented.
    - Policy retains prior ABI entries and adds exactly `0x00010002`.
    - Tests cover approved, stale 0.1.4, ABI mismatch, unknown ABI, and requested-version mismatch.
    - CHANGELOG documents the deliberate `NewParserPool` source break and does not say the artifact is published.
  </acceptance_criteria>
  <done>The approved version is the sole Phase 11 ABI 1.2 bootstrap/readiness identity and its source-level release note is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize Rust/Go ABI mirrors and the exact bootstrap canary</name>
  <files>src/lib.rs, internal/ffi/types.go, internal/ffi/types_test.go, internal/bootstrap/abi_assertion.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-03, D-15, D-16, and D-17
    - `src/lib.rs` ABI/error/kind enums and ABI unit assertion
    - `internal/ffi/types.go` ABI, error, kind, and frame mirrors
    - `internal/ffi/types_test.go` layout and numeric assertions
    - `internal/bootstrap/abi_assertion.go` bidirectional current-version canary
    - `scripts/release/check_bootstrap_abi_state.py` parser syntax expectations
  </read_first>
  <behavior>
    - Rust ABI and Go ABI both equal `0x00010002`.
    - Kinds 0..8 and statuses 0..8 remain unchanged; the Wave-1 `TypeBigInt=9` classification is mirrored by Rust/ffi BigInt=9, capacity=9, and kernel-locked=10.
    - ValueView, iterator, native allocator stats, and InternalFrame sizes/offsets remain unchanged.
    - The approved version canary compiles only when ffi.ABIVersion equals 1.2.
  </behavior>
  <action>
    Change Rust `PURE_SIMDJSON_ABI_VERSION` and Go `ffi.ABIVersion` to `0x00010002`. Append Rust `PURE_SIMDJSON_VALUE_KIND_BIGINT = 9` and the Go `ValueKindBigInt = 9` ABI mirror, then add `ErrCapacityLimit = 9` and `ErrKernelLocked = 10`; do not renumber any existing value. `element.go` already established `TypeBigInt = 9` in Plan 11-02 so the behavior-changing wave stayed green; do not redefine that public constant here. Extend numeric/layout tests to pin every value and unchanged struct offset; the later public BigInt tests must also assert that the established TypeBigInt and `ElementType(ffi.ValueKindBigInt)` are both numeric 9. Replace the 0.1.4 canary constant with an identifier derived from the approved semver (dots converted to underscores), value `0x00010002`, and retain both zero-length-array subtraction directions. Run the real source-state checker with the approved version after all four files agree.
  </action>
  <verify>
    <automated>APPROVED_ABI12_VERSION="$(sed -nE 's/.*approved_abi12_version:[[:space:]]*`?([0-9]+\.[0-9]+\.[0-9]+)`?.*/\1/p' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md | head -n 1)" &amp;&amp; export APPROVED_ABI12_VERSION &amp;&amp; test -n "$APPROVED_ABI12_VERSION" &amp;&amp; git fetch origin --tags &amp;&amp; test -z "$(git tag -l "v${APPROVED_ABI12_VERSION}")" &amp;&amp; go test ./internal/ffi ./internal/bootstrap -count=1 &amp;&amp; python3 scripts/release/check_bootstrap_abi_state.py --version "$APPROVED_ABI12_VERSION" &amp;&amp; cargo test --locked --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Go and Rust print/parse as ABI `0x00010002`.
    - Numeric tests pin BigInt 9 and status 9/10 while preserving all earlier values.
    - Every existing public/internal struct size and offset assertion still passes.
    - Compile-time canary references the approved version and has both subtraction directions.
    - Actual source-state checker succeeds for the approved version.
  </acceptance_criteria>
  <done>All source-level ABI/version mirrors are coherent at strict minimum 1.2 and remain append-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| version/ABI source -> release artifact identity | A mismatched pin can download a library with a different callable contract. |
| Rust enums/layouts -> Go mirrors | Numeric or layout drift can corrupt FFI calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-03 | Tampering | ABI 1.2 source identity | mitigate | Exact policy entry, bidirectional canary, numeric/layout tests, and source-state checker fail closed. |
| T-11-SC | Tampering | artifact version pin | mitigate | Consume only the operator-approved unused version; publication remains CI-only and separately gated. |
</threat_model>

<verification>
- Python policy tests and actual checker pass for the approved version.
- A source assertion rejects any retained `v0.1.4/` checksum example and requires all five exact approved-version example keys.
- Go bootstrap/FFI tests and Rust ABI tests pass.
- Wave 6 uses the rebuilt local ABI 1.2 library explicitly for runtime tests: `cargo build --release --locked` then `PURE_SIMDJSON_LIB_PATH=<fresh local release library> go test ./... -race`; no pre-publication test may depend on the approved default-bootstrap URL.
- `make verify-contract` is expected to remain red only for the pending generated ABI macro/kind/status/function synchronization. Plan 11-09 is the required closure; no header hand-edit is allowed.
- No existing numeric value or layout changes.
</verification>

<success_criteria>
- ABI 1.2 is the exact Go/Rust minimum identity.
- Approved semantic version, retained checksum examples, canary, policy, and changelog agree.
- ABI 1.1 cannot satisfy Phase 11 source readiness.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-07-SUMMARY.md` when done.
</output>
