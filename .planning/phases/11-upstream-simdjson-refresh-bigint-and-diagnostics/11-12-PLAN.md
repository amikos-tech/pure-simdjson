---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 12
type: execute
wave: 9
depends_on:
  - 11-10
  - 11-11
files_modified:
  - errors.go
  - errors_test.go
  - parser_test.go
  - kernel.go
  - kernel_test.go
  - parser.go
  - pool.go
  - library_loading.go
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02

must_haves:
  truths:
    - "D-06: `Error.Offset()` keeps its signature; `Error.HasOffset()` preserves known zero independently from unknown."
    - "D-07: Error formatting includes offset 0 when known and omits all offset text when unknown."
    - "`Kernel()` is package-level, read-only, returns empty before a native library is cached, and never triggers bootstrap/download."
    - "D-12: `SetKernel(name)` is package-level, diagnostic-only, exact-name, supports empty auto selection, and returns typed invalid/unsupported/locked errors."
    - "Native status 10 maps through the centralized status table to public `ErrKernelLocked`; a real post-construction native return is tested independently of the complementary Go pre-lock guard."
    - "Go serializes SetKernel/NewParser/NewParserPool; successful pool construction locks Go selection without loading native code, and the first parser construction locks native state."
  artifacts:
    - path: "errors.go"
      provides: "Explicit known-offset state, HasOffset, public ErrKernelLocked, and centralized native status-10 mapping"
      contains: "func (e *Error) HasOffset() bool"
    - path: "kernel.go"
      provides: "Process-global Kernel/SetKernel lifecycle"
      exports: ["Kernel", "SetKernel"]
    - path: "kernel_test.go"
      provides: "Subprocess-isolated kernel lifecycle and race coverage"
      contains: "TestKernel"
    - path: "parser_test.go"
      provides: "Known zero/nonzero/unknown error-offset integration tests"
      contains: "TestErrorOffsetKnownZero"
  key_links:
    - from: "errors.go"
      to: "internal/ffi/bindings.go"
      via: "wrapParserStatus reads both raw offset and explicit native known flag"
      pattern: "ParserLastErrorHasOffset"
    - from: "kernel.go"
      to: "parser.go"
      via: "one Go mutex linearizes selection and constructor lock"
      pattern: "kernel.*Mu|lockKernelSelection"
    - from: "kernel.go"
      to: "library_loading.go"
      via: "Kernel reads cached library only; SetKernel may call activeLibrary"
      pattern: "cachedLibrary"
---

<objective>
Complete the public production diagnostics: truthful error-location state and safe process-global kernel control.

Purpose: D-06 through D-12 require honest known/unknown reporting and a diagnostic override that cannot race first parser/pool creation or select unsupported instructions.
Output: Error.HasOffset, corrected formatting/transport, Kernel/SetKernel, lifecycle integration, and isolated tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@errors.go
@parser.go
@pool.go
@library_loading.go
@internal/ffi/bindings.go
@parser_test.go

<interfaces>
Public additions:

```go
func (e *Error) HasOffset() bool
func Kernel() string
func SetKernel(name string) error
```

Selected error behavior:

```text
invalid kernel name -> error wrapping ErrInvalidOption
compiled but unsupported -> ErrCPUUnsupported
post-creation Go pre-lock guard -> ErrKernelLocked
native ffi.ErrKernelLocked / status 10 -> centralized wrapStatus mapping -> ErrKernelLocked
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve known zero and expose Error.HasOffset</name>
  <files>errors.go, errors_test.go, parser_test.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-06 through D-09
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Pattern 4` and `Pitfall 5`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Error offsets: known zero versus unknown`
    - `errors.go` Error/nativeDetails, formatting, wrapParserStatus, normalizeOffset, and hasOffset heuristic
    - `errors_test.go` sentinel and detail formatting tables
    - `parser_test.go` current parser diagnostic and ABI tests
    - `internal/ffi/bindings.go` `ParserLastErrorOffset` and `ParserLastErrorHasOffset`
    - `tests/rust_shim_diagnostics.rs` characterized native corpus from Plan 11-05
  </read_first>
  <behavior>
    - Known nonzero returns Offset=N/HasOffset=true and formats offset=N.
    - Known zero returns Offset=0/HasOffset=true and formats offset=0.
    - Unknown returns Offset=0/HasOffset=false and omits offset text.
    - Success/capacity rejection after another failure carries no stale details.
    - Nil Error returns Offset=0/HasOffset=false.
  </behavior>
  <action>
    Per D-06, preserve `Offset() uint64`, add `hasOffset bool` to `Error` and `nativeDetails`, and expose `HasOffset() bool`. Per D-07, in `wrapParserStatus`, read `ParserLastErrorHasOffset` and retain raw zero when the native bool is true. Per D-09, normalize failures without a trustworthy upstream location to Offset zero plus false. Replace formatting branches that call the old `offset != 0` heuristic with the explicit field, so known zero prints. Keep native message advisory and errors.Is sentinel behavior unchanged. Add unit/integration tests named `TestErrorHasOffset`, `TestErrorOffsetKnownZero`, `TestErrorOffsetUnknown`, and `TestErrorOffsetCorpus`, plus the stale-detail capacity case.
  </action>
  <verify>
    <automated>go test . -run '^TestError(HasOffset|OffsetKnownZero|OffsetUnknown|OffsetCorpus|OffsetStaleDetails)' -count=1 &amp;&amp; go test . -run '^TestSentinelMapping$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Offset method signature is unchanged and HasOffset is exported/documented.
    - No helper infers known state from `offset != 0`.
    - Known zero formatting includes `offset=0`; unknown formatting has no offset clause.
    - Characterized known/unknown corpus agrees with native tests.
    - Capacity status after prior error has false/zero/empty details.
  </acceptance_criteria>
  <done>Go callers can distinguish known byte zero from unknown without breaking existing Offset callers or inventing locations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add package-global Kernel/SetKernel and lock on parser or pool creation</name>
  <files>errors.go, errors_test.go, kernel.go, kernel_test.go, parser.go, pool.go, library_loading.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decision D-12 and the approved pool constructor D-11
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` `Pattern 5` and security threats T-11-02/T-11-06
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Process-global kernel selection`
    - `library_loading.go` active/cached library state and implementation name field
    - `parser.go` option normalization and `newParserWithConfig`
    - `pool.go` fallible constructor and stored config
    - `errors.go` public sentinel declarations, `wrapStatus`, and centralized `sentinelForStatus` switch
    - `errors_test.go` table-driven native-status-to-public-sentinel assertions
    - `internal/ffi/types.go` append-only `ErrKernelLocked = 10` status mirror
    - `internal/ffi/bindings.go` setter/lock/implementation-name methods
    - `tests/rust_shim_kernel.rs` native isolated behavior
    - `parser_test.go` existing subprocess test helper pattern
  </read_first>
  <behavior>
    - Kernel before library load returns empty and leaves cache/files/network untouched.
    - SetKernel empty/valid before creation succeeds and Kernel reports selected name.
    - Invalid name wraps ErrInvalidOption; unsupported wraps ErrCPUUnsupported; neither changes active name.
    - A native `SetImplementation` call made after parser construction returns raw status 10; `wrapStatus` preserves code 10 and `errors.Is(..., ErrKernelLocked)` succeeds.
    - NewParser permanently locks Go/native selection only after options validate.
    - NewParserPool permanently locks Go selection after options validate but leaves cachedLibrary nil and performs no bootstrap; its first Get locks native selection through parser construction.
    - Explicit fallback permits parser creation; automatic fallback remains ErrCPUUnsupported.
    - Concurrent SetKernel/constructor has one linearized winner and passes race detection.
  </behavior>
  <action>
    Per D-12, declare exported `ErrKernelLocked = errors.New("kernel selection locked")` beside the established public sentinels in `errors.go`, then map `ffi.ErrKernelLocked` to it in the single `sentinelForStatus` switch. Do not special-case status 10 only inside `SetKernel`; every native status path must receive the same errors.Is identity and retain `Error.Code()==10`. Create `kernel.go` with package-level `Kernel()`/`SetKernel(name string) error`, one package mutex, and irreversible `kernelSelectionLocked` state. Implement `Kernel()` by taking a read snapshot of `cachedLibrary` under `libraryMu`; return `""` without calling `resolveLibraryPath`/`activeLibrary` when nil, otherwise query current `bindings.ImplementationName()`. Implement `SetKernel(name)` under the lifecycle mutex: reject if locked with `ErrKernelLocked`, resolve/load only here, call `SetImplementation`, pass its native return through the centralized status wrapper, translate invalid-arg to an error wrapping `ErrInvalidOption`, preserve CPU unsupported and kernel-locked sentinels, and update the cached diagnostic name only on success. In `newParserWithConfig`, after option validation, take the same mutex through native configured construction and mark locked on successful first construction; the native constructor takes the native state-machine lock. In `NewParserPool`, after option validation, mark only the Go lifecycle state locked and return the configured pool without calling `activeLibrary`, `LockImplementationSelection`, or any native function. On the first pool miss, `newParserWithConfig` creates the parser and locks native state. This is sufficient because every supported Go mutation flows through `SetKernel`, while the native public setter remains independently protected by native parser construction/explicit-lock semantics. Addresses review concern: an empty pool deterministically blocks later Go `SetKernel` without triggering a five-minute bootstrap path.

    Add the ordinary table-driven status-10 assertion to `errors_test.go`, then add `TestKernelNativeLockedStatusMapsToSentinel` in `kernel_test.go` as a fresh subprocess case. In that child, use the freshly built local ABI 1.2 library, successfully construct a parser to lock the native state, obtain the already-loaded package-internal bindings without resetting the Go lifecycle guard, invoke `bindings.SetImplementation("")` directly as a test-only observation of the native ABI, require the raw return to equal `int32(ffi.ErrKernelLocked)`/10, pass that raw value to `wrapStatus`, and assert both `errors.Is(err, ErrKernelLocked)` and `errors.As(err, *Error)` with `Code()==10`. This proves the native-path mapping rather than only the Go-side pre-lock check; retain a separate public `SetKernel`-after-parser assertion because the Go guard is complementary. Write every setter/parser/pool/race case in a fresh subprocess; only the unloaded `Kernel()` read may be tested without mutation. Do not add a production bypass, unlock API, or per-parser kernel field.
  </action>
  <verify>
    <automated>go test . -run '^TestKernelNativeLockedStatusMapsToSentinel$' -count=1 &amp;&amp; go test . -run '^TestKernel' -race -count=1 &amp;&amp; go test . -run '^TestParserPool.*(KernelLock|LazyNativeLock)' -race -count=1 &amp;&amp; go test . -run '^TestSentinelMapping$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `errors.go` exports ErrKernelLocked using the established sentinel pattern, and `sentinelForStatus` maps ffi.ErrKernelLocked/status 10 to it.
    - A fresh-process test observes raw status 10 from the real native setter after parser construction, then proves `errors.Is(..., ErrKernelLocked)` and `Error.Code()==10`; a Go-only pre-lock assertion is not accepted as a substitute.
    - Kernel contains no call to activeLibrary/resolveLibraryPath on the unloaded path.
    - SetKernel exact-name errors are typed and preserve prior selection.
    - Invalid parser/pool options fail before locking selection.
    - Successful parser construction locks Go and native selection.
    - Successful empty-pool construction locks Go selection, leaves `cachedLibrary` nil, performs no resolver/bootstrap call, and its first Get locks native selection.
    - Every irreversible scenario is subprocess-isolated; race tests pass.
    - No public reset/unlock or per-parser kernel field exists.
  </acceptance_criteria>
  <done>Kernel diagnostics are reproducible and safe: selection is validated, process-global, and immutable after first parser/pool creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| native offset state -> operator logs | Wrong known state can misdirect incident investigation. |
| kernel name -> native CPU dispatch | Unsupported implementation may crash the process. |
| concurrent setter -> constructor/pool | A race could create a parser under a different implementation than reported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-02 | Denial of Service | SetKernel | mitigate | Native compiled-name/support check plus typed Go mapping and subprocess tests. |
| T-11-05 | Repudiation | Error.HasOffset/formatting | mitigate | Carry explicit native bool, test known zero/nonzero/unknown, and prohibit inference. |
| T-11-06 | Tampering / Denial of Service | Go/native kernel lifecycle and status mapping | mitigate | One mutex across setter/parser/pool, native lock defense in depth, centralized status-10 mapping, and a real post-construction native-return test plus irreversible/race tests. |
| T-11-SC | Tampering | dependency and package supply chain | mitigate | Introduce no package installs; retain locked Cargo/Go inputs and the audited vendored v4.6.4 source pin. |
</threat_model>

<verification>
- Focused diagnostics and kernel subprocess/race tests pass.
- Native status 10 is observed after parser construction and maps to public ErrKernelLocked with code 10; the Go pre-lock guard remains separately covered.
- Kernel unloaded test proves no cache/network side effect.
- Known zero and unknown format differently as specified.
</verification>

<success_criteria>
- Error locations are truthful and backwards-compatible.
- Kernel control is safe, diagnostic-only, process-global, and locked at first creation, with one public ErrKernelLocked identity for both Go-guard and native status-10 paths.
- Typed error behavior is documented by tests, not message parsing.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-12-SUMMARY.md` when done.
</output>
