---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 10
type: execute
wave: 8
depends_on:
  - 11-08
  - 11-09
files_modified:
  - element.go
  - bigint_test.go
  - element_scalar_test.go
  - materializer_fastpath.go
  - materializer_fastpath_test.go
  - element_fuzz_test.go
autonomous: true
requirements:
  - NUM-01
  - NUM-02

must_haves:
  truths:
    - "`TypeBigInt` numerically equals native/ffi kind 9 and existing `ElementType` values remain unchanged."
    - "`Element.GetBigInt() (string,error)` accepts only TypeBigInt, returns exact copied text, and remains valid after Doc.Close/GC."
    - "GetInt64/GetUint64/GetFloat64 on TypeBigInt return ErrWrongType; boundaries and float syntax retain their earlier types/getter behavior."
    - "Root, descendant lookup, array iteration, object iteration, fuzz traversal, and fast materialization all carry BigInt without precision loss or parse rejection."
  artifacts:
    - path: "element.go"
      provides: "Public TypeBigInt and strict copied GetBigInt"
      contains: "TypeBigInt"
    - path: "bigint_test.go"
      provides: "Boundary, strict getter, copied lifetime, root/descendant/iteration tests"
      contains: "TestBigInt"
    - path: "materializer_fastpath.go"
      provides: "Kind-9 exact text in Go-owned materialized trees"
      contains: "ValueKindBigInt"
    - path: "element_fuzz_test.go"
      provides: "BigInt-aware successful DOM walk"
      contains: "TypeBigInt"
  key_links:
    - from: "element.go"
      to: "internal/ffi/bindings.go"
      via: "GetBigInt calls copied ElementGetBigInt and keeps the document alive"
      pattern: "ElementGetBigInt"
    - from: "materializer_fastpath.go"
      to: "ffi.InternalFrame.StringPtr/StringLen"
      via: "kind 9 reuses `copyFrameString` before frame lifetime ends"
      pattern: "ValueKindBigInt.*copyFrameString"
---

<objective>
Expose strict copied BigInt text throughout the safe Go DOM and internal materializer surface.

Purpose: D-02 through D-05 require exact preservation without widening existing getters, automatic `math/big`, or a borrowed lifetime.
Output: TypeBigInt/GetBigInt, complete traversal/materializer propagation, fuzz handling, and public semantic tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@element.go
@materializer_fastpath.go
@element_scalar_test.go
@materializer_fastpath_test.go
@element_fuzz_test.go
@internal/ffi/types.go
@internal/ffi/bindings.go

<interfaces>
Public addition:

```go
const TypeBigInt ElementType = ElementType(ffi.ValueKindBigInt) // numeric 9
func (e Element) GetBigInt() (string, error)
```

`GetBigInt` is not a generic integer formatter: TypeInt64 and TypeUint64 return ErrWrongType.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict TypeBigInt/GetBigInt with exact boundary and lifetime tests</name>
  <files>element.go, bigint_test.go, element_scalar_test.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-02 through D-05
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` `Phase Requirements` rows NUM-01/NUM-02 and `Pitfall 4`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `BigInt type and copied accessor`
    - `element.go` ElementType constants, Type switch, numeric getter comments, and GetString ownership pattern
    - `element_scalar_test.go` current integer/float boundary tables and copied-string-after-close test
    - `internal/ffi/types.go` ValueKindBigInt numeric mirror
    - `internal/ffi/bindings.go` ElementGetBigInt copied/free helper
  </read_first>
  <behavior>
    - Signed/unsigned overflow integer syntax returns TypeBigInt and exact spelling.
    - MinInt64/MaxUint64 keep int64/uint64; 1.0/1e20 keep float64.
    - GetBigInt rejects every non-BigInt kind with ErrWrongType.
    - Existing numeric getters reject BigInt with ErrWrongType.
    - Returned BigInt string survives document close and GC.
  </behavior>
  <action>
    Write `bigint_test.go` first with boundary classification, positive/negative exact spelling, root, object field, array/object iterator, strict wrong-type matrix, numeric getter matrix, and copied lifetime after `Doc.Close()` plus `runtime.GC()`. Append `TypeBigInt` after `TypeObject` as numeric 9 and add it to `Element.Type`. Implement `Element.GetBigInt` parallel to `GetString`: validate the document-tied view, call `bindings.ElementGetBigInt`, `runtime.KeepAlive(e.doc)`, and wrap status. Update anticipatory BigInt comments on `GetInt64`/`GetUint64`/`GetFloat64` to state ErrWrongType per D-05. Replace old valid-oversized-literal ErrInvalidJSON expectations in `element_scalar_test.go`; retain truly invalid numeric syntax tests.
  </action>
  <verify>
    <automated>cargo build --release --locked &amp;&amp; go test . -run '^TestBigInt(Classification|Boundaries|Getter|WrongType|CopyLifetime|Traversal)$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TypeBigInt and ffi.ValueKindBigInt both assert numeric 9.
    - Boundary matrix exactly names `-9223372036854775808`, `-9223372036854775809`, `18446744073709551615`, `18446744073709551616`, `1.0`, and `1e20`.
    - All non-BigInt kinds return ErrWrongType from GetBigInt.
    - All three existing numeric getters return ErrWrongType on BigInt.
    - Copy remains exact after close/GC.
    - `go list -deps .` contains no `math/big` dependency introduced by the package.
  </acceptance_criteria>
  <done>Callers can distinguish TypeBigInt and retrieve only exact copied BigInt text with strict typed behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate BigInt through fast materialization and fuzz traversal</name>
  <files>materializer_fastpath.go, materializer_fastpath_test.go, element_fuzz_test.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-02 through D-05
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-02`, threat T-11-04
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `BigInt in the fast materializer`
    - `materializer_fastpath.go` frame switch and `copyFrameString`
    - `materializer_fastpath_test.go` parity, ownership, adversarial frame, and old oversized rejection cases
    - `element_fuzz_test.go` successful type walk and accepted-error set
    - `internal/ffi/types.go` unchanged InternalFrame layout
  </read_first>
  <behavior>
    - Root and nested kind-9 frames materialize to exact Go strings.
    - BigInt materialized text is copied before doc/frame lifetime ends.
    - Adversarial kind-9 frame with nil/nonzero span fails ErrInternal; empty valid span follows established string-span rules.
    - Fuzz walker calls GetBigInt for TypeBigInt and no longer treats valid oversized integer parse rejection as acceptable.
  </behavior>
  <action>
    Add `ffi.ValueKindBigInt` to `buildAnyFromFrame` and reuse `copyFrameString` exactly; do not grow `InternalFrame` or return a borrowed byte/string. Replace old fast-materializer oversized-literal rejection tests with exact root/nested BigInt parity and ownership-after-close cases. Add adversarial kind-9 span coverage mirroring TypeString's pointer/length safety. Extend the fuzz type switch with `TypeBigInt -> GetBigInt` and remove `ErrPrecisionLoss`/valid oversized `ErrInvalidJSON` from accepted outcomes where Phase 11 now parses successfully. Preserve full-frame-consumption, doc mutex, `runtime.KeepAlive`, and duplicate-key behavior.
  </action>
  <verify>
    <automated>go test . -run '^TestBigIntMaterializer|^TestFastMaterializer.*BigInt' -count=1 &amp;&amp; go test . -run '^FuzzParseThenGetString/' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Root and nested BigInt fast-materializer tests compare exact decimal strings.
    - Materialized BigInt survives Doc.Close/GC.
    - No InternalFrame field, size, or offset changes.
    - Fuzz walker has an explicit TypeBigInt branch.
    - Valid oversized integer syntax is no longer listed as an accepted parse error.
  </acceptance_criteria>
  <done>Every existing Go DOM traversal/materialization path preserves BigInt exactly and safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| native copied bytes -> Go string | Go must own text independently from native/doc lifetime. |
| borrowed internal frames -> Go materialized tree | Frame spans expire when the document scratch is reused or released. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-04 | Information Disclosure / Tampering | GetBigInt and fast materializer | mitigate | Copy through existing byte-free binding and frame string copier; assert close/GC lifetime and adversarial spans. |
</threat_model>

<verification>
- Focused BigInt API/materializer tests pass against rebuilt ABI 1.2 native library.
- Existing scalar and fuzz smoke tests remain green.
- No `math/big` or new dependency appears.
</verification>

<success_criteria>
- BigInt is exact copied text everywhere.
- Strict getters never silently coerce kinds.
- Materializer and fuzz paths understand kind 9 without layout growth.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-10-SUMMARY.md` when done.
</output>
