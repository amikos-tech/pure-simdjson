---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - UP-01

must_haves:
  truths:
    - "An operator explicitly chooses the intermediate ABI 1.2 artifact semantic version before any bootstrap pin, changelog heading, tag, or publication action."
    - "The chosen version is recorded as an intermediate Phase 11 compatibility artifact and is not silently treated as Phase 16's final v0.2 release."
    - "The chosen `v<version>` tag is unused when checked against fetched repository tags."
    - "This checkpoint creates no tag, publishes no artifact, and changes no source file."
  artifacts:
    - path: ".planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md"
      provides: "The approved `approved_abi12_version` value consumed by Plan 11-07"
      contains: "approved_abi12_version"
  key_links:
    - from: ".planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md"
      to: "internal/bootstrap/version.go"
      via: "Plan 11-07 reads the approved version before changing the bootstrap source pin"
      pattern: "approved_abi12_version"
---

<objective>
Obtain the required operator decision for the semantic version of the intermediate ABI `0x00010002` bootstrap artifact.

Purpose: D-17 requires a matching published artifact, but neither CONTEXT.md nor research authorizes the planner or executor to invent its version. This decision gate keeps every version-specific source and release action parameterized and preserves Phase 16's final-release boundary.
Output: one approved, unused semver-without-`v` recorded in the plan summary; no repository or external-state mutation.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@docs/releases.md
@.agents/skills/pure-simdjson-release/SKILL.md

<interfaces>
Decision output contract consumed by Plan 11-07:

```text
approved_abi12_version: <MAJOR.MINOR.PATCH without a leading v>
artifact_role: intermediate Phase 11 ABI 1.2 compatibility artifact
tag_preflight: refs/tags/v<version> absent after fetching tags
phase16_boundary: not the final v0.2 release unless the user explicitly changes Phase 16 scope
```
</interfaces>
</context>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan(s) | Status |
|---|---|---|---|---|
| GOAL | — | v4.6.4 + exact BigInt + production diagnostics/controls + matching artifact | 02-14 | COVERED |
| REQ | UP-01 | Audited v4.6.4 pin and all regression/platform gates | 02, 13, 14 | COVERED |
| REQ | NUM-01 | Preserve out-of-range integer syntax as exact text | 02, 03, 10 | COVERED |
| REQ | NUM-02 | Strict TypeBigInt/GetBigInt copied string API | 03, 08-10 | COVERED |
| REQ | DIAG-01 | Process-global Kernel/SetKernel with lifecycle lock | 06, 08, 09, 12 | COVERED |
| REQ | DIAG-02 | Truthful known/unknown offsets | 05, 08, 09, 12 | COVERED |
| REQ | LIMIT-01 | Immutable capacity/depth options and homogeneous pools | 04, 08, 09, 11 | COVERED |
| RESEARCH | — | ABI-first binding; pre-copy capacity; copied BigInt; upstream-only locations | 03-12 | COVERED |
| RESEARCH | — | main -> strict readiness -> annotated tag -> release.yml -> Phase 06.1 | 01, 07, 13, 14 | COVERED |
| CONTEXT | D-01..D-05 | Upstream and BigInt contract | 02, 03, 10 | COVERED |
| CONTEXT | D-06..D-09 | Error offset contract | 05, 12 | COVERED |
| CONTEXT | D-10..D-14 | Options, pool, and kernel lifecycle | 04, 06, 11, 12 | COVERED |
| CONTEXT | D-15..D-18 | ABI 1.2, bootstrap, and five-platform proof | 01, 07-09, 13, 14 | COVERED |

Deferred ideas: none. Phase 12-16 navigation, On-Demand, zero-copy, streaming, and final v0.2 release work do not appear in these plans.
</source_audit>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the intermediate ABI 1.2 artifact version</name>
  <files>None — record the decision only in `11-01-SUMMARY.md`.</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-15 through D-18
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Binding Resolutions` and `Pattern 6: Treat Artifact Publication as a Phase Gate`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` section `Manual-Only Verifications`
    - `docs/releases.md` sections `Org-Standard Flow`, `Pre-Tag Readiness Gate`, and `Post-Publish Public Bootstrap Validation`
    - `.agents/skills/pure-simdjson-release/SKILL.md`
  </read_first>
  <decision>Select one semver in `MAJOR.MINOR.PATCH` form for the intermediate artifact carrying ABI `0x00010002`; do not infer it from Phase 16 or the current `0.1.4` bootstrap pin.</decision>
  <action>
    Ask the operator for `VERSION=<semver-without-v>`. After receiving it, assign that response to `APPROVED_ABI12_VERSION`, export it, validate the syntax with the Python `packaging`-free regular expression `^[0-9]+\.[0-9]+\.[0-9]+$`, run `git fetch origin --tags`, and only then require `git tag -l "v${APPROVED_ABI12_VERSION}"` to return no line. Record the exact value under `approved_abi12_version` in `11-01-SUMMARY.md`, together with the statements `artifact_role: intermediate Phase 11 ABI 1.2 compatibility artifact` and `publication_authorized: false`. Per D-17, this decision authorizes Plan 11-07 to prepare source state only; it does not authorize a tag, a push, a release, or a manual upload. If the operator selects the Phase 16 final v0.2 label, stop and require an explicit Phase 16 scope change rather than silently consuming it.
  </action>
  <verify>
    <automated>APPROVED_ABI12_VERSION="${VERSION:?set VERSION from the approved checkpoint response}" &amp;&amp; export APPROVED_ABI12_VERSION &amp;&amp; printf '%s\n' "$APPROVED_ABI12_VERSION" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$' &amp;&amp; git fetch origin --tags &amp;&amp; test -z "$(git tag -l "v${APPROVED_ABI12_VERSION}")"</automated>
  </verify>
  <acceptance_criteria>
    - The response supplies exactly one semver without a leading `v`.
    - The corresponding `v<version>` tag is absent after a read-only tag fetch.
    - The decision explicitly labels the version as Phase 11's intermediate ABI 1.2 artifact.
    - The decision does not silently claim Phase 16's final v0.2 release label.
    - No tracked file, tag, release, or remote ref changes during this checkpoint.
  </acceptance_criteria>
  <resume-signal>Reply `VERSION=x.y.z` with the approved intermediate artifact version.</resume-signal>
  <done>The exact version is recorded for Plan 11-07, its tag is unused, and no publication authority has been inferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator version input -> bootstrap/release source | A human-selected string later controls the public artifact path and tag name. |
| Phase 11 intermediate artifact -> Phase 16 final release | An ambiguous version label could falsely collapse two separate roadmap release boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-03 | Tampering | ABI/version identity | mitigate | Require an explicit semver, verify the tag is unused, and pass the recorded value to the ABI/bootstrap policy plan; no artifact may claim ABI 1.2 under an implicit version. |
| T-11-SC | Tampering | Release supply chain | mitigate | This plan performs no install or publication; the official CI path and immutable tag checks remain mandatory in Plan 14. |
</threat_model>

<verification>
- `11-01-SUMMARY.md` records `approved_abi12_version`, artifact role, unused-tag evidence, and `publication_authorized: false`.
- `git status --short` shows no new change caused by the checkpoint other than the executor-generated summary.
</verification>

<success_criteria>
- One operator-approved intermediate version exists before any version-specific source change.
- The version is syntactically valid and not already tagged.
- No tag or artifact is created.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md` when done.
</output>
