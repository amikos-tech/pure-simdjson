---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 09
type: execute
wave: 7
depends_on:
  - 11-07
files_modified:
  - cbindgen.toml
  - include/pure_simdjson.h
  - tests/abi/check_header.py
  - tests/abi/test_check_header.py
  - tests/abi/handle_layout.c
  - tests/smoke/ffi_export_surface.c
  - docs/ffi-contract.md
autonomous: true
requirements:
  - NUM-02
  - DIAG-01
  - DIAG-02
  - LIMIT-01

must_haves:
  truths:
    - "D-16: The generated header advertises ABI 1.2, kind 9, statuses 9/10, and every mandatory Phase 11 export while retaining all old signatures/layouts."
    - "Internal bridge/test symbols remain excluded from the public header."
    - "A real C smoke resolves and calls configured construction, BigInt copy/free, known-offset, and implementation setter/lock without platform-specific duplicate code."
    - "The normative FFI document defines append-only ABI 1.2 ownership, compatibility, limit, kernel, and diagnostic contracts."
  artifacts:
    - path: "include/pure_simdjson.h"
      provides: "Generated public ABI 1.2 header"
      contains: "PURE_SIMDJSON_ABI_VERSION 0x00010002"
    - path: "tests/abi/handle_layout.c"
      provides: "Numeric and unchanged-layout static assertions"
      contains: "PURE_SIMDJSON_VALUE_KIND_BIGINT"
    - path: "tests/smoke/ffi_export_surface.c"
      provides: "Five-platform-consumed ABI 1.2 symbol/call smoke"
      contains: "pure_simdjson_element_get_bigint"
    - path: "docs/ffi-contract.md"
      provides: "Normative ABI 1.2 contract"
      contains: "0x00010002"
  key_links:
    - from: "src/lib.rs"
      to: "include/pure_simdjson.h"
      via: "cbindgen generation only"
      pattern: "generate-header"
    - from: "include/pure_simdjson.h"
      to: "tests/smoke/ffi_export_surface.c"
      via: "one C typedef/resolve/call table compiled on every release target"
      pattern: "pure_simdjson_"
---

<objective>
Freeze the complete additive ABI 1.2 contract in the generated header, static audits, C smoke, and normative documentation.

Purpose: D-16/D-18 require old layouts and symbols to remain intact while every Phase 11 function becomes mandatory on all five platform artifacts.
Output: regenerated header, append-only audits, executable C export smoke, and updated FFI contract.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@src/lib.rs
@cbindgen.toml
@include/pure_simdjson.h
@tests/abi/check_header.py
@tests/abi/handle_layout.c
@tests/smoke/ffi_export_surface.c
@docs/ffi-contract.md

<interfaces>
Public ABI additions expected from `src/lib.rs`:

```text
pure_simdjson_parser_new_configured
pure_simdjson_parser_get_last_error_has_offset
pure_simdjson_element_get_bigint
pure_simdjson_set_implementation
pure_simdjson_lock_implementation_selection
```

Pinned layouts remain `pure_simdjson_value_view_t` and `psdj_internal_frame_t` at their existing sizes/offsets.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ABI audits and regenerate the complete public header</name>
  <files>cbindgen.toml, include/pure_simdjson.h, tests/abi/check_header.py, tests/abi/test_check_header.py, tests/abi/handle_layout.c</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-03, D-15, and D-16
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-07`, threat T-11-03
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` sections `Additive ABI propagation` and `ABI and native smoke tests`
    - `src/lib.rs` complete Phase 11 Rust declarations
    - `cbindgen.toml` header prelude and internal exclusion list
    - `tests/abi/check_header.py` current version/symbol/signature/ownership rules
    - `tests/abi/test_check_header.py` synthetic header fixtures
    - `tests/abi/handle_layout.c` current enum/layout static assertions
    - `Makefile` generate-header and verify-contract targets
  </read_first>
  <behavior>
    - Header version is exactly 0x00010002.
    - Required symbol audit fails when any Phase 11 public export is absent.
    - BigInt accessor has the copied-byte ownership/free rule.
    - Kinds 0..8, statuses 0..8, and all struct sizes/offsets remain pinned; new values append.
    - Internal `psimdjson_*` and `psdj_internal_*` symbols fail the public-header audit.
  </behavior>
  <action>
    Extend header audit tests before generation for ABI 1.2, the five mandatory functions, exact signatures, BigInt copy/free ownership, kind 9, status 9/10, and unchanged old symbols/layouts. Add any Phase 11 test-only/internal helper names to `cbindgen.toml` exclusions. Update the cbindgen ABI prelude to `0x00010002`, then generate `include/pure_simdjson.h` from `src/lib.rs`; never hand-edit the generated header. Extend `handle_layout.c` with append-only numeric assertions and leave all prior size/offset assertions intact.
  </action>
  <verify>
    <automated>python3 tests/abi/test_check_header.py &amp;&amp; make generate-header &amp;&amp; make verify-contract</automated>
  </verify>
  <acceptance_criteria>
    - Generated header contains all five public functions and no private/test helper.
    - Header audit has a failing synthetic case for each missing Phase 11 function.
    - BigInt ownership rule requires `pure_simdjson_bytes_free`.
    - Static C assertions pin new numeric values and preserve old layouts.
    - A second `make generate-header` produces no diff.
  </acceptance_criteria>
  <done>The committed header is a reproducible, append-only ABI 1.2 contract with complete automated audits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise every ABI 1.2 addition in the shared C smoke and contract</name>
  <files>tests/smoke/ffi_export_surface.c, docs/ffi-contract.md</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-02 through D-18
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` sections `ABI and native smoke tests`, `Ownership`, and `Release automation`
    - `include/pure_simdjson.h` generated ABI 1.2 declarations
    - `tests/smoke/ffi_export_surface.c` typedef, export table, resolver, and call-all sections
    - `scripts/release/run_native_smoke.sh` staged-library argv, platform tuple, and OS-specific linker handling
    - `docs/ffi-contract.md` ownership, error, diagnostics, parser lifecycle, and versioning sections
    - `.github/workflows/release.yml` shared smoke invocations on all five targets
  </read_first>
  <behavior>
    - C smoke resolves every Phase 11 function.
    - It creates a configured parser, parses a BigInt, copies/frees exact text, checks strict wrong-type behavior, and reads explicit offset-known state.
    - It sets automatic implementation selection before construction, reads the selected name, locks through parser construction, and verifies later set returns status 10.
    - Old constructor and at least one old scalar path still execute.
  </behavior>
  <action>
    Add the five function typedefs, export-table fields, resolver calls, and active call assertions to `ffi_export_surface.c`. Exercise `pure_simdjson_parser_new_configured` with safe nondefault limits, parse `18446744073709551616`, assert kind 9, copy exact text through `pure_simdjson_element_get_bigint`, free it, and assert all three existing numeric getters return wrong-type. Exercise offset and known getters on an explicitly unknown case. Call `pure_simdjson_set_implementation` with empty input before parser construction, read the active implementation through existing APIs, and assert a later setter returns kernel-locked. Update `docs/ffi-contract.md` with exact status/kind numbers, option defaults/bounds, ownership, process-global lock, known-zero/unknown semantics, strict 1.2 minimum, and mandatory-symbol fail-closed rule. Preserve old call sequences and layouts.
  </action>
  <verify>
    <automated>make verify-contract &amp;&amp; cargo build --release --locked &amp;&amp; case "$(go env GOOS)/$(go env GOARCH)" in linux/amd64) STAGED_LIBRARY="$PWD/target/release/libpure_simdjson.so"; PLATFORM_TUPLE=linux-amd64 ;; linux/arm64) STAGED_LIBRARY="$PWD/target/release/libpure_simdjson.so"; PLATFORM_TUPLE=linux-arm64 ;; darwin/amd64) STAGED_LIBRARY="$PWD/target/release/libpure_simdjson.dylib"; PLATFORM_TUPLE=darwin-amd64 ;; darwin/arm64) STAGED_LIBRARY="$PWD/target/release/libpure_simdjson.dylib"; PLATFORM_TUPLE=darwin-arm64 ;; windows/amd64) STAGED_LIBRARY="$PWD/target/release/pure_simdjson.dll"; PLATFORM_TUPLE=windows-amd64 ;; *) echo "unsupported smoke platform: $(go env GOOS)/$(go env GOARCH)" &gt;&amp;2; exit 1 ;; esac &amp;&amp; bash scripts/release/run_native_smoke.sh "$STAGED_LIBRARY" "$PLATFORM_TUPLE"</automated>
  </verify>
  <acceptance_criteria>
    - All five new symbols occur in typedef, table, resolver, and call sections.
    - BigInt copied text and numeric wrong-type semantics execute in C.
    - Configured constructor and kernel post-lock status execute.
    - Contract explicitly states ABI 1.1 rejection and missing-1.2-symbol corruption.
    - The smoke remains the single cross-platform C source consumed by release jobs.
  </acceptance_criteria>
  <done>The same C source used by every release target executes the complete ABI 1.2 contract, and the normative document matches it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Rust declarations -> generated C header | Generation/exclusion drift can publish missing or unintended symbols. |
| public header -> platform artifacts | Every release compiler/loader consumes the declared widths and layouts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-03 | Tampering | public ABI 1.2 header/export surface | mitigate | Required-symbol/signature tests, static layout assertions, deterministic cbindgen diff, and call-all C smoke. |
| T-11-04 | Information Disclosure | C BigInt ownership | mitigate | Header ownership rule plus copy/free smoke; no borrowed pointer is public. |
| T-11-05 | Repudiation | C offset contract | mitigate | Smoke explicit known flag separately from numeric offset; contract forbids inferred locations. |
</threat_model>

<verification>
- `make verify-contract` passes with a clean generated-header diff.
- ABI Python/C checks pass.
- Shared C smoke loads the local release library and executes new plus old paths.
</verification>

<success_criteria>
- ABI 1.2 is generated, audited, documented, and executable.
- Existing symbols/layouts stay intact.
- All Phase 11 functions are mandatory public exports.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-09-SUMMARY.md` when done.
</output>
