---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - third_party/simdjson
  - patches/simdjson-v4.6.4-positive-bigint.patch
  - build.rs
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - tests/rust_shim_minimal.rs
  - tests/rust_shim_fast_materializer.rs
  - element.go
  - element_fuzz_test.go
  - element_scalar_test.go
  - materializer_fastpath_test.go
  - testdata/jsontestsuite/expectations.tsv
autonomous: true
requirements:
  - UP-01
  - NUM-01
  - NUM-02

must_haves:
  truths:
    - "D-01 amended: The simdjson gitlink is exactly official v4.6.4 commit `1bcf71bd85059ab6574ea1159de9298dcc1212c5`; one provenance-recorded patch changes positive 20-digit unsigned overflow from NUMBER_ERROR to the existing BIGINT_ERROR path on a build-output copy, with exact-base and clean-submodule checks that fail closed. The existing singleheader/cc/C++17 compile path remains in use."
    - "D-02: Only integer syntax below -9223372036854775808 or above 18446744073709551615 classifies as native kind 9; the two boundaries and decimal/exponent forms retain their existing kinds."
    - "D-03: BigInt reaches the internal frame stream as exact decimal bytes in the existing string span without changing `psdj_internal_frame_t`."
    - "D-05: The three existing numeric accessors return `PURE_SIMDJSON_ERR_WRONG_TYPE` for BigInt and no longer normalize valid BigInt input to invalid JSON."
    - "The Go wrapper classifies native kind 9 as `TypeBigInt` in this same wave, and the committed oversized-integer fuzz seeds walk that kind without falling through to `TypeInvalid`."
    - "The complete race-enabled Go suite and JSONTestSuite oracle remain green in the same wave that native BigInt acceptance changes; no old parse-rejection, parse-level precision-loss, or fuzz type-switch failure is left for a later plan to discover."
  artifacts:
    - path: "third_party/simdjson"
      provides: "Reproducible v4.6.4 upstream gitlink"
    - path: "patches/simdjson-v4.6.4-positive-bigint.patch"
      provides: "The sole approved downstream change, with official-base provenance"
    - path: "build.rs"
      provides: "Fail-closed application of the approved patch to a build-output source copy"
    - path: "src/native/simdjson_bridge.cpp"
      provides: "BigInt-enabled DOM parser, kind classification, strict numeric behavior, and frame text"
      contains: "number_as_string(true)"
    - path: "tests/rust_shim_minimal.rs"
      provides: "Native boundary classification coverage"
      contains: "18446744073709551616"
    - path: "tests/rust_shim_fast_materializer.rs"
      provides: "Root and nested kind-9 frame coverage with exact text"
      contains: "BIGINT"
    - path: "element.go"
      provides: "Wave-1 public kind-9 classification used by every Go traversal"
      contains: "TypeBigInt"
    - path: "element_fuzz_test.go"
      provides: "Successful TypeBigInt fuzz traversal and committed oversized-integer seeds"
      contains: "TypeBigInt"
    - path: "testdata/jsontestsuite/expectations.tsv"
      provides: "v4.6.4 oracle expectations for the three valid oversized-integer fixtures"
      contains: "i_number_too_big_pos_int.json"
  key_links:
    - from: "src/native/simdjson_bridge.cpp"
      to: "third_party/simdjson"
      via: "simdjson DOM `number_as_string(true)`, `element_type::BIGINT`, and `get_bigint()`"
      pattern: "number_as_string\(true\)|get_bigint"
    - from: "build.rs"
      to: "patches/simdjson-v4.6.4-positive-bigint.patch"
      via: "exact official-base verification followed by one build-output patch application"
      pattern: "simdjson-v4\\.6\\.4-positive-bigint\\.patch"
    - from: "src/native/simdjson_bridge.cpp"
      to: "tests/rust_shim_fast_materializer.rs"
      via: "kind 9 uses the existing frame `string_ptr`/`string_len` fields"
      pattern: "BIGINT"
    - from: "src/native/simdjson_bridge.cpp"
      to: "element.go"
      via: "raw kind 9 crosses the existing u32 element-type transport and maps to TypeBigInt"
      pattern: "TypeBigInt"
---

<objective>
Move the native engine to the audited v4.6.4 pin and make BigInt a real DOM/frame kind at the C++ boundary.

Purpose: D-01 through D-03 replace the Phase 8 parse-time rejection with upstream's exact-text model while preserving every existing kind number and frame layout.
Output: pinned upstream source, BigInt-enabled bridge classification/materialization, and native regression tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@build.rs
@src/native/simdjson_bridge.h
@src/native/simdjson_bridge.cpp
@tests/rust_shim_minimal.rs
@tests/rust_shim_fast_materializer.rs
@element.go
@element_fuzz_test.go

<interfaces>
Existing values that must remain stable:

```text
ValueKindInt64=3, ValueKindUint64=4, ValueKindFloat64=5,
ValueKindString=6, ValueKindArray=7, ValueKindObject=8
TypeBigInt=9 (Wave-1 Go classification; completed ffi/public ABI mirror lands in Plan 11-07)
psdj_internal_frame_t: existing fields and size only; BigInt uses string_ptr/string_len
native element-type out transport: raw u32, never a Rust enum value with an unknown discriminant
```

Locked v4.6.4 identity:

```text
tag: v4.6.4
commit: 1bcf71bd85059ab6574ea1159de9298dcc1212c5
```

Execution-time D-01 amendment:

```text
official base remains unchanged
one allowed patch: positive 20-digit uint64 overflow NUMBER_ERROR -> BIGINT_ERROR
patch destination: build-output copy only
failure policy: reject wrong commit, tracked submodule drift, or patch mismatch
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the audited v4.6.4 upstream without changing the build mechanism</name>
  <files>third_party/simdjson</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decision D-01
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Summary`, `Standard Stack`, and `Upstream gitlink and native build`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Upstream gitlink and native build`
    - `third_party/simdjson` current gitlink
    - `build.rs` existing singleheader/C++17 build path
    - `Cargo.lock` locked build dependencies
  </read_first>
  <action>
    Update only the `third_party/simdjson` gitlink from the current v4.6.1 revision to official v4.6.4 commit `1bcf71bd85059ab6574ea1159de9298dcc1212c5` per D-01. Verify that this commit is tagged `v4.6.4`. Keep `build.rs` on `third_party/simdjson/singleheader/simdjson.cpp` plus `src/native/simdjson_bridge.cpp`, C++17, and the existing `cc` crate; do not vendor a second upstream copy, add a package manager, or add `-march=native`. Run the existing native suite before changing bridge behavior so any patch-release incompatibility is separated from Phase 11 feature changes.
  </action>
  <verify>
    <automated>test "$(git -C third_party/simdjson rev-parse HEAD)" = "1bcf71bd85059ab6574ea1159de9298dcc1212c5" &amp;&amp; cargo test --locked --test rust_shim_minimal -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `git -C third_party/simdjson rev-parse HEAD` prints `1bcf71bd85059ab6574ea1159de9298dcc1212c5`.
    - `git -C third_party/simdjson tag --points-at HEAD` contains `v4.6.4`.
    - `git diff -- build.rs Cargo.toml Cargo.lock` contains no dependency/build-mechanism change caused by this task.
    - The existing `rust_shim_minimal` integration target passes against the new pin.
  </acceptance_criteria>
  <done>The repository reproducibly builds official simdjson v4.6.4 through the unchanged singleheader path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enable native BigInt classification and frame propagation</name>
  <files>patches/simdjson-v4.6.4-positive-bigint.patch, build.rs, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, tests/rust_shim_minimal.rs, tests/rust_shim_fast_materializer.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-02, D-03, and D-05
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Upstream BigInt Preservation`, `Pattern 3: Reuse the Existing Copy-Out Ownership Path`, and `Pitfall 4: Partial BigInt Propagation`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` sections `BigInt type and copied accessor` and `BigInt in the fast materializer`
    - `third_party/simdjson/tests/dom/big_integer_tests.cpp` upstream boundary and wrong-type tests
    - `third_party/simdjson/doc/dom.md` exact-text contract
    - `src/native/simdjson_bridge.h` parser and `psdj_internal_frame_t` declarations
    - `src/native/simdjson_bridge.cpp` `map_element_type`, parse-specific BIGINT normalization, both `psimdjson_element_type*` BIGINT branches, scalar getters, and frame builder
    - `src/runtime/mod.rs` `psimdjson_element_type*` declarations and `native_element_type*` result transport
    - `tests/rust_shim_minimal.rs` current oversized-literal rejection test
    - `tests/rust_shim_fast_materializer.rs` current BIGINT rejection/frame tests
  </read_first>
  <behavior>
    - `-9223372036854775808` remains kind 3 and `18446744073709551615` remains kind 4.
    - `-9223372036854775809` and `18446744073709551616` parse successfully as kind 9.
    - `1.0` and `1e20` remain kind 5.
    - int64, uint64, and double accessors on kind 9 return status 4 (`WRONG_TYPE`).
    - Root and nested BigInt frames use kind 9 and exact `string_ptr`/`string_len` text; no frame field or layout changes.
    - Native element-type results cross the Rust bridge as raw `u32`, so additive kind 9 is never read as an invalid Rust enum discriminant before the public ABI enum is synchronized.
  </behavior>
  <action>
    First replace the old oversized-literal rejection expectations in both Rust integration targets with the D-02 boundary matrix and confirm they fail against the pinned but unchanged bridge. Execution established that official v4.6.4 and upstream HEAD return `NUMBER_ERROR` for positive 20-digit unsigned overflow, so apply the user-approved D-01 amendment before bridge work: commit exactly one unified patch whose only semantic change routes that already-detected overflow to `BIGINT_ERROR`; keep the gitlink at official v4.6.4; and have `build.rs` verify the exact commit and clean tracked submodule, copy the singleheader source into the Cargo build output, apply that one patch there, and fail closed if the base or hunk does not match. Record the official commit and reason in the patch. Do not leave a dirty submodule or unpublished submodule commit, add a generic patch framework, introduce another upstream copy, or add a dependency.

    Then configure every production DOM parser in `src/native/simdjson_bridge.cpp` with `number_as_string(true)` before its first parse. Remove the parse-specific `BIGINT_ERROR -> PURE_SIMDJSON_ERR_INVALID_JSON` normalization. Append bridge kind `9` for `simdjson::dom::element_type::BIGINT` without renumbering 0 through 8 per D-03. Explicitly delete the `type == BIGINT -> PURE_SIMDJSON_ERR_PRECISION_LOSS` branches in both `psimdjson_element_type` and `psimdjson_element_type_at`; both must return kind 9. In `src/runtime/mod.rs`, declare both native element-type out-parameters as `*mut u32` and initialize/read raw `u32` values rather than materializing the C++ result as `pure_simdjson_value_kind_t`; this is the permanent additive-kind transport and prevents undefined behavior when C++ writes kind 9 before Plan 11-07 adds the public Rust enum variant. Use a test-local expected-kind constant with value 9 in the Wave-1 Rust assertions; Plan 11-07 exclusively adds the public Rust/Go ABI enum mirrors with the ABI 1.2 synchronization. The pre-existing registry precision-loss fallback is unreachable once these native calls return `Ok(9)`, so leave its removal to dependency-ordered Plan 11-03 Task 2, which already owns `src/runtime/registry.rs`; Wave 1 must nevertheless prove root and descendant kind 9 pass through it successfully. In each native int64/uint64/double accessor, preserve upstream `INCORRECT_TYPE -> PURE_SIMDJSON_ERR_WRONG_TYPE` per D-05. In `psimdjson_materialize_build`, fetch `get_bigint()` and place its exact borrowed decimal span in the existing `string_ptr` and `string_len` fields; preserve frame size, offsets, preorder traversal, reentrancy, and clear-on-error behavior. Addresses review concern: every behavior-affecting bridge BIGINT special case is removed in the introducing task, while the separable dead-registry cleanup is assigned to an already-dependent owner. Do not add a decimal parser or a second BigInt buffer.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_minimal --test rust_shim_fast_materializer -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - The four locked integer boundaries and the two float-syntax examples have explicit assertions.
    - Kind 9 is appended; source and tests still assert kinds 0 through 8 unchanged.
    - All three numeric getter tests assert status 4 for BigInt.
    - Root and nested BigInt materializer tests compare exact signed/unsigned decimal spelling.
    - Rust reads kind 9 through a raw `u32` out-parameter; no FFI path constructs an enum value with an undeclared discriminant.
    - C++ and Rust static size assertions for `psdj_internal_frame_t` remain unchanged and passing.
    - No `math/big`, Boost multiprecision, custom range parser, or second vendored simdjson copy is introduced.
    - Official v4.6.4 remains the clean gitlink; the sole patch is provenance-recorded, applied only to the build-output copy, and the build rejects an unexpected commit, tracked submodule drift, or patch mismatch.
  </acceptance_criteria>
  <done>v4.6.4 preserves valid oversized integer syntax as native kind 9 and exact frame text while every old kind/layout remains stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile Go and correctness-oracle expectations in the behavior-changing wave</name>
  <files>element.go, element_fuzz_test.go, element_scalar_test.go, materializer_fastpath_test.go, testdata/jsontestsuite/expectations.tsv</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-REVIEWS.md` Claude concerns `Cross-wave Go suite breakage` and `JSONTestSuite oracle expectations`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-02, D-03, and D-05
    - `element.go` `ElementType` constants, numeric-accessor comments, and `TypeErr` switch
    - `element_fuzz_test.go` seed list, parse-error allowance, and `fuzzWalkElement` type switch
    - `element_scalar_test.go` subtest `oversized literal rejected at parse`
    - `materializer_fastpath_test.go` test `TestFastMaterializerOversizedLiteralParseRejected`
    - `benchmark_oracle_test.go` `TestJSONTestSuiteOracle` manifest contract
    - `testdata/jsontestsuite/expectations.tsv` current `i_number_too_big_*` and `i_number_very_big_negative_int` rows
    - `testdata/jsontestsuite/cases/i_number_too_big_neg_int.json`
    - `testdata/jsontestsuite/cases/i_number_too_big_pos_int.json`
    - `testdata/jsontestsuite/cases/i_number_very_big_negative_int.json`
    - `testdata/jsontestsuite/cases/i_number_huge_exp.json`
    - `testdata/jsontestsuite/cases/n_object_non_string_key_but_huge_number_instead.json`
    - `testmain_test.go` local release-library override used by Go tests
  </read_first>
  <behavior>
    - `18446744073709551616` and the nested 23-digit integer parse successfully in the Go suite immediately after native BigInt is enabled.
    - `Element.Type()` and `TypeErr()` return `TypeBigInt` (numeric 9) for positive and negative oversized integer syntax rather than `TypeInvalid`.
    - `fuzzWalkElement` has an explicit TypeBigInt classification branch; committed positive/negative oversized seeds parse and complete the walk, while parse-level `ErrPrecisionLoss` is no longer accepted.
    - The three valid integer-only JSONTestSuite fixtures change from reject to accept with a v4.6.4 BigInt note.
    - Huge exponent/real fixtures and the non-string object-key fixture remain rejected.
    - Plan 11-10 owns copied `GetBigInt` and materializer semantics and enriches this established classification branch rather than redefining the kind or seeds.
  </behavior>
  <action>
    Addresses review concern: keep Wave 1 green at the same commit that changes native acceptance. Append `TypeBigInt ElementType = 9` after `TypeObject` per D-03 and add `ffi.ValueKind(TypeBigInt)` to `TypeErr`; the literal is deliberate because `ffi.ValueKindBigInt` is synchronized with the complete ABI mirrors in Plan 11-07, whose tests must prove equality. Update `GetInt64`, `GetUint64`, and `GetFloat64` comments so TypeBigInt explicitly reports `ErrWrongType` per D-05, and remove `ErrPrecisionLoss` from the `TypeErr` classification-failure comment because kind 9 is now returned successfully. In `element_fuzz_test.go`, add positive and negative oversized integer `f.Add` seeds, remove parse-level `ErrPrecisionLoss` from the accepted parse errors, and add an explicit TypeBigInt branch to `fuzzWalkElement`. The Wave-1 branch contract is successful classification/traversal without invoking the copied accessor; Plan 11-10 exclusively extends that branch with `GetBigInt` verification after its ABI-binding dependencies. Add a deterministic regression that parses both committed seeds, requires `TypeBigInt` rather than `TypeInvalid`, and calls `fuzzWalkElement` successfully.

    Replace `TestGetUint64/oversized literal rejected at parse` with a permanent parse-preservation assertion that requires a non-nil document, nil error, and `TypeBigInt` for `18446744073709551616`, then closes the document. Replace `TestFastMaterializerOversizedLiteralParseRejected` with `TestFastMaterializerOversizedLiteralParseAccepted`, requiring parse success for `{"ok":1,"big":99999999999999999999999}`; exact `GetBigInt` and materializer-result assertions remain in Plan 11-10. In `expectations.tsv`, change exactly `i_number_too_big_neg_int.json`, `i_number_too_big_pos_int.json`, and `i_number_very_big_negative_int.json` from `reject` to `accept`, with notes naming simdjson v4.6.4 BigInt preservation. Do not change exponent/real overflow cases or any `n_*` invalid-syntax fixture. Rebuild the release library and run the complete race-enabled Go suite now, rather than leaving any kind-switch, fuzz, or oracle reconciliation to the final gate.
  </action>
  <verify>
    <automated>cargo build --release --locked &amp;&amp; case "$(go env GOOS)" in linux) LOCAL_LIBRARY="$PWD/target/release/libpure_simdjson.so" ;; darwin) LOCAL_LIBRARY="$PWD/target/release/libpure_simdjson.dylib" ;; windows) LOCAL_LIBRARY="$PWD/target/release/pure_simdjson.dll" ;; *) exit 1 ;; esac &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" go test ./... -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Both superseded Go rejection tests are removed or renamed and now require parse success.
    - `TypeBigInt` is numeric 9, both oversized seed roots classify as TypeBigInt, and neither reaches the TypeInvalid failure branch.
    - `fuzzWalkElement` has an explicit TypeBigInt arm, both positive and negative oversized seeds are committed, and valid oversized integer syntax is not an accepted parse error.
    - Go numeric getter comments state ErrWrongType for TypeBigInt, and TypeErr no longer advertises ErrPrecisionLoss for classification.
    - Exactly the three valid integer-only `i_*` manifest rows change to `accept`; their notes identify v4.6.4 BigInt preservation.
    - `i_number_huge_exp.json`, `i_number_neg_int_huge_exp.json`, `i_number_pos_double_huge_exp.json`, and `n_object_non_string_key_but_huge_number_instead.json` remain `reject`.
    - `go test ./... -race` and `TestJSONTestSuiteOracle` pass against the rebuilt v4.6.4 library in this wave.
    - No `GetBigInt` stub, materializer shortcut, or extra public API is added.
  </acceptance_criteria>
  <done>The behavior-changing wave is fully green: Go classifies and fuzz-walks kind 9, no test pins the old rejection, and the committed oracle describes v4.6.4's deliberate BigInt acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| vendored gitlink -> native build | The pinned third-party source is compiled into every platform artifact. |
| repository patch -> build-output source | The sole downstream hunk changes upstream overflow classification before compilation. |
| C++ DOM/string_view -> internal frame | BigInt bytes are document-owned while the frame span is borrowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-04 | Information Disclosure / Tampering | `psimdjson_materialize_build` BigInt span | mitigate | Keep BigInt bytes inside the doc-owned frame lifetime and add root/nested exact-span tests; Plan 10 copies before Go escape. |
| T-11-SC | Tampering | `third_party/simdjson` and D-01 patch | mitigate | Pin official v4.6.4 commit exactly; verify the commit and clean tracked submodule before copying; apply exactly one provenance-recorded patch to the build-output copy; fail on base drift or hunk mismatch; introduce no package install, dirty submodule, unpublished submodule commit, or second vendored tree. |
</threat_model>

<verification>
- `git -C third_party/simdjson rev-parse HEAD` equals the locked commit.
- `git -C third_party/simdjson status --short` is empty, and a normal Cargo build consumes only the provenance-recorded D-01 patch through a fail-closed build-output copy.
- `cargo test --locked --test rust_shim_minimal --test rust_shim_fast_materializer -- --test-threads=1` passes.
- `make verify-contract` and `cargo build --release --locked && PURE_SIMDJSON_LIB_PATH=<fresh local release library> go test ./... -race` pass in Wave 1.
- `TestJSONTestSuiteOracle` passes with only the three valid integer-only expectation flips.
- Existing bridge/frame layout assertions remain green.
</verification>

<success_criteria>
- The audited official upstream base plus the sole approved fail-closed patch is exact and reproducible.
- BigInt classification matches D-02 at both signed/unsigned boundaries.
- Native frame materialization carries exact text without layout growth.
- Existing numeric getters return wrong-type, not precision loss.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-02-SUMMARY.md` when done.
</output>
