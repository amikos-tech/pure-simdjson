---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 11
type: execute
wave: 8
depends_on:
  - 11-08
  - 11-09
files_modified:
  - parser_options.go
  - parser_options_test.go
  - parser.go
  - errors.go
  - errors_test.go
  - pool.go
  - pool_test.go
  - example_test.go
  - docs/concurrency.md
autonomous: true
requirements:
  - LIMIT-01

must_haves:
  truths:
    - "D-10: `NewParser(opts ...ParserOption)` validates immutable options before native allocation; zero arguments retain current behavior."
    - "D-13: `WithMaxCapacity(int)` and `WithMaxDepth(int)` normalize zero to 0xFFFFFFFF/1024, reject invalid/clamped/overflow values, and use deterministic last-option-wins semantics."
    - "Configured capacity errors map to `ErrCapacityLimitExceeded`, and invalid options map to `ErrInvalidOption`."
    - "D-14: `NewParserPool(opts ...ParserOption) (*ParserPool,error)` stores one normalized config, applies it to every miss, and rejects mismatched Put values."
    - "D-11: Every repository call site is updated for the deliberate pool-constructor return-count break."
  artifacts:
    - path: "parser_options.go"
      provides: "Immutable comparable parser config and public option constructors"
      exports: ["ParserOption", "WithMaxCapacity", "WithMaxDepth"]
    - path: "parser.go"
      provides: "Variadic option constructor and stored effective config"
      contains: "func NewParser(opts ...ParserOption)"
    - path: "pool.go"
      provides: "Fallible configured homogeneous ParserPool"
      contains: "func NewParserPool(opts ...ParserOption) (*ParserPool, error)"
    - path: "parser_options_test.go"
      provides: "Defaults, duplicates, invalid values, capacity, and depth boundary tests"
  key_links:
    - from: "parser_options.go"
      to: "internal/ffi/bindings.go"
      via: "normalized config passed once to ParserNewConfigured"
      pattern: "ParserNewConfigured"
    - from: "pool.go"
      to: "parser.go"
      via: "pool miss calls internal constructor with stored comparable config"
      pattern: "newParserWithConfig"
---

<objective>
Expose immutable Go parser capacity/depth options and make pools configuration-homogeneous through the approved constructor break.

Purpose: D-10, D-11, D-13, and D-14 require one validated effective config for each parser and every parser a pool can return.
Output: ParserOption API, configured parser construction, truthful errors, fallible pool constructor, complete call-site/tests/docs update.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@parser.go
@pool.go
@errors.go
@internal/ffi/bindings.go
@pool_test.go
@example_test.go
@docs/concurrency.md

<interfaces>
Chosen public option surface:

```go
type ParserOption func(parserConfig) (parserConfig, error)
func WithMaxCapacity(bytes int) ParserOption
func WithMaxDepth(depth int) ParserOption
func NewParser(opts ...ParserOption) (*Parser, error)
func NewParserPool(opts ...ParserOption) (*ParserPool, error)
```

Private `parserConfig{maxCapacity uint64, maxDepth uint32}` is comparable and stores normalized effective values. Duplicate options use last-option-wins.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize immutable options and construct configured parsers</name>
  <files>parser_options.go, parser_options_test.go, parser.go, errors.go, errors_test.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-10 and D-13
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Pattern 1`, `Pitfall 1`, `Pitfall 2`, and `Pitfall 3`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Immutable parser options and homogeneous pools`
    - `parser.go` current constructor, locks, lifecycle, and expected ABI flow
    - `errors.go` sentinel/status map and native detail construction
    - `errors_test.go` table-driven status assertions
    - `internal/ffi/types.go` capacity status 9
    - `internal/ffi/bindings.go` ParserNewConfigured signature
    - `materializer_fastpath_test.go` existing default depth boundary
  </read_first>
  <behavior>
    - Omitted and explicit-zero options normalize equally to 0xFFFFFFFF/1024.
    - Duplicate max-capacity/max-depth use the last value.
    - Capacity -1, 1..31, >0xFFFFFFFF, conversion overflow, and negative depth fail before activeLibrary/native allocation.
    - Capacity 32 is valid; depth N-1/N and default 1023/1024 boundaries hold.
    - Capacity+1 maps through native status 9 to ErrCapacityLimitExceeded with no stale offset.
  </behavior>
  <action>
    Create `parser_options.go` with private comparable `parserConfig`, `defaultParserConfig`, immutable `ParserOption func(parserConfig) (parserConfig,error)`, `WithMaxCapacity(int)`, `WithMaxDepth(int)`, and `normalizeParserOptions`. Use last-option-wins. Add exported sentinels `ErrInvalidOption` and `ErrCapacityLimitExceeded`; map ffi status 9 to the latter without changing existing invalid-arg mapping. Change `NewParser` to variadic, normalize before calling `activeLibrary`, and delegate to private `newParserWithConfig(config)` which calls `ParserNewConfigured` and stores config on Parser. Write tests that use loader/native allocation counters or existing seams to prove invalid options fail before native allocation, plus exact capacity/depth/default/duplicate behavior. Preserve zero-argument calls and all parser lifecycle/finalizer semantics.
  </action>
  <verify>
    <automated>cargo build --release --locked &amp;&amp; go test . -run '^Test(ParserOption|ParserCapacity|ParserDepth)' -count=1 &amp;&amp; go test . -run '^TestSentinelMapping$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - ParserOption has the immutable input/output signature shown above.
    - Normalized config is comparable and stored on Parser.
    - Invalid values are rejected before library resolution/allocation.
    - Capacity 32, capacity+1, small depth N-1/N, and default depth are tested.
    - Status 9 matches ErrCapacityLimitExceeded through errors.Is.
    - Existing `NewParser()` call sites compile unchanged.
  </acceptance_criteria>
  <done>Every Parser is born with one validated immutable effective bound set, and invalid options allocate nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make ParserPool fallible, configured, and homogeneous</name>
  <files>pool.go, pool_test.go, example_test.go, docs/concurrency.md</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-11 and D-14
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-06`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` pool section and exact call-site list
    - `pool.go` current sync.Pool Get/Put lifecycle checks
    - `pool_test.go` reuse, busy/closed/nil, concurrency, and finalizer tests
    - `example_test.go` `ExampleParserPool_Get`
    - `docs/concurrency.md` constructor/example guidance
    - `parser_options.go` normalized config and equality contract
  </read_first>
  <behavior>
    - Constructor validation failure returns nil pool/error before any parser exists.
    - Every miss uses the pool's stored config.
    - Omitted and explicit defaults compare equal.
    - Put rejects a parser with different capacity or depth and does not stash it.
    - Existing nil/closed/busy and concurrent reuse behavior remains.
  </behavior>
  <action>
    Change `ParserPool` to store one normalized `parserConfig` alongside the existing `sync.Pool`. Implement the deliberate D-11 signature `NewParserPool(opts ...ParserOption) (*ParserPool,error)` and validate once. Make `Get` call `newParserWithConfig(p.config)` on a miss. Under `parser.mu`, extend `Put` to compare `parser.config == p.config`; return a wrapped `ErrInvalidOption` on mismatch and never insert it. Keep nil/closed/live-doc checks and finalizer behavior. Update every `NewParserPool` call in `pool_test.go`, `example_test.go`, and `docs/concurrency.md` to handle `(pool,error)`, and document immutable options, last-wins duplicates, and homogeneous Put behavior in plain language.
  </action>
  <verify>
    <automated>go test . -run '^TestParserPool.*(Option|Config|Reuse|Concurrent|Busy|Closed|Nil)' -race -count=1 &amp;&amp; go test . -run '^ExampleParserPool_Get$' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Constructor exact signature is `(*ParserPool,error)`.
    - Every pool miss uses the stored normalized config.
    - Mismatched Put returns an error matching ErrInvalidOption and later Get never returns that parser.
    - All repository `NewParserPool` call sites compile with the new return count.
    - Concurrency/race tests remain green.
  </acceptance_criteria>
  <done>ParserPool can never become heterogeneous, and the approved source break is fully applied and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public option values -> native allocation/copy | Caller-controlled limits must be validated before resource use. |
| arbitrary Parser -> ParserPool.Put | A parser from another policy could weaken future callers' limits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-01 | Denial of Service | parser capacity/depth options | mitigate | Normalize/reject before native allocation, retain Rust pre-copy gate, and test capacity/depth boundaries. |
| T-11-06 | Tampering | heterogeneous pool insertion | mitigate | Store comparable config and reject mismatched Put under parser lock with race coverage. |
</threat_model>

<verification>
- Focused option/pool tests pass with race detection.
- All examples and repository call sites compile.
- Invalid options never invoke native allocation.
</verification>

<success_criteria>
- Parser options are immutable, validated, and exact.
- Pools apply one config to every parser.
- The deliberate constructor break is complete, not partially migrated.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-11-SUMMARY.md` when done.
</output>
