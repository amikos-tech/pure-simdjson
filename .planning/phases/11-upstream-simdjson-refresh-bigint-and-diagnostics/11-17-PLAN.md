---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 17
type: execute
wave: 14
depends_on:
  - 11-16
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/lib.rs
  - tests/rust_shim_minimal.rs
autonomous: true
gap_closure: true
requirements:
  - UP-01

must_haves:
  truths:
    - "Every C++ exception trapped by the native catch boundary, including `std::runtime_error`, `std::bad_alloc`, and unknown exceptions, maps to normative public status `PURE_SIMDJSON_ERR_CPP_EXCEPTION` (97)."
    - "The existing internal exception seam deterministically selects both `std::runtime_error` and `std::bad_alloc`; both modes traverse the production catch macro and return status 97."
    - "Parser-aware and parser-independent catch macros continue to delegate to the same exception-status mapper, so diagnostic capture cannot change the public status."
    - "The explicit simdjson `MEMALLOC` error-code path remains distinct from a trapped C++ exception; no public enum value, ABI symbol, generated header, or normative contract text changes."
    - "The combined contract, focused Rust, JSON oracle, Go race, benchmark-signal, and non-strict readiness gates pass after both gap fixes."
  artifacts:
    - path: "src/native/simdjson_bridge.cpp"
      provides: "Normative status-97 mapping for every trapped C++ exception"
      contains: "PURE_SIMDJSON_ERR_CPP_EXCEPTION"
    - path: "src/native/simdjson_bridge.h"
      provides: "Extended internal deterministic exception selector seam"
      contains: "psimdjson_test_force_cpp_exception"
    - path: "src/runtime/mod.rs"
      provides: "Rust declaration and wrapper for the selector-based native seam"
      contains: "test_force_cpp_exception_for_tests"
    - path: "src/lib.rs"
      provides: "Hidden Rust test helper that reaches the existing native seam"
      contains: "pure_simdjson_test_force_cpp_exception_for_tests"
    - path: "tests/rust_shim_minimal.rs"
      provides: "Deterministic runtime_error and bad_alloc status assertions"
      contains: "force_bad_alloc"
  key_links:
    - from: "PSIMDJSON_CATCH_CPP_EXCEPTIONS"
      to: "map_cpp_exception"
      via: "all caught exception categories delegate to one public-status mapper"
      pattern: "catch.*bad_alloc|map_cpp_exception"
    - from: "tests/rust_shim_minimal.rs"
      to: "psimdjson_test_force_cpp_exception"
      via: "hidden Rust wrapper passes the test-only exception selector into the production catch macro"
      pattern: "test_force_cpp_exception_for_tests"
    - from: "src/native/simdjson_bridge.cpp"
      to: "docs/ffi-contract.md"
      via: "source behavior matches the unchanged normative status-97 rule"
      pattern: "PURE_SIMDJSON_ERR_CPP_EXCEPTION"
---

<objective>
Make trapped `std::bad_alloc` obey the normative C++ exception status contract.

Purpose: The public ABI already requires every trapped C++ exception to return status 97, but a special bad-allocation overload currently returns internal status 127 and the existing seam cannot exercise that branch.
Output: a selector-based extension of the existing test seam, status-97 bad-allocation mapping, and complete local Phase 11 closure evidence.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VERIFICATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-09-SUMMARY.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-16-SUMMARY.md
@docs/ffi-contract.md
@src/native/simdjson_bridge.h
@src/native/simdjson_bridge.cpp
@src/runtime/mod.rs
@src/lib.rs
@tests/rust_shim_minimal.rs
@cbindgen.toml

<interfaces>
Normative and current status contract:

```text
PURE_SIMDJSON_ERR_CPP_EXCEPTION = 97
PURE_SIMDJSON_ERR_INTERNAL = 127
docs/ffi-contract.md: C++ exceptions must be trapped and converted to status 97.
```

Existing internal seam:

```text
psimdjson_test_force_cpp_exception(void) noexcept
runtime::test_force_cpp_exception_for_tests()
pure_simdjson_test_force_cpp_exception_for_tests()
```

Current mapper split to reconcile:

```text
map_cpp_exception(const std::bad_alloc&amp;) -> status 127
map_cpp_exception(const std::exception&amp;) -> status 97
map_cpp_exception(unknown) -> status 97
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the exception seam and map bad_alloc to status 97</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, src/lib.rs, tests/rust_shim_minimal.rs</files>
  <read_first>
    - `.planning/PROJECT.md` constraint `FFI safety`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VERIFICATION.md` gap `Every C++ exception trapped at the Rust/C++ seam maps to ... 97`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-09-SUMMARY.md` normative ABI and generated-header decisions
    - `docs/ffi-contract.md` status table and `Panic and exception containment`
    - `src/native/simdjson_bridge.h` declaration of `psimdjson_test_force_cpp_exception`
    - `src/native/simdjson_bridge.cpp` `map_cpp_exception` overloads, both catch macros, parser diagnostic capture, `LastErrorBuffer::assign`, and the existing forced runtime-error seam
    - `src/runtime/mod.rs` native declaration and `test_force_cpp_exception_for_tests` wrapper
    - `src/lib.rs` hidden `pure_simdjson_test_force_cpp_exception_for_tests` helper and public error enum values
    - `tests/rust_shim_minimal.rs` existing forced runtime-error test and imported status constants
    - `cbindgen.toml` internal-symbol exclusion for `psimdjson_test_force_cpp_exception`
  </read_first>
  <behavior>
    - Test selector 0 throws `std::runtime_error` inside the existing native seam and returns status 97.
    - Test selector 1 throws `std::bad_alloc` inside the same seam and returns status 97.
    - An unsupported test selector returns `PURE_SIMDJSON_ERR_INVALID_ARGUMENT` without throwing.
    - Both production catch macros still call `map_cpp_exception`; parser diagnostic capture may retain best-effort text but cannot change status 97.
    - `map_error(simdjson::MEMALLOC)` retains its existing non-exception mapping; status 127 remains available for explicit internal failures.
    - The generated public header and normative document are byte-unchanged.
  </behavior>
  <action>
    Begin by replacing the single runtime-error-only assertion with two focused RED tests named `psimdjson_test_force_runtime_error_returns_err_cpp_exception` and `psimdjson_test_force_bad_alloc_returns_err_cpp_exception`. Extend the existing seam rather than adding a parallel export: change `psimdjson_test_force_cpp_exception` to accept a test-only `uint32_t exception_kind`, use selector 0 for `std::runtime_error`, selector 1 for `std::bad_alloc`, and return `PURE_SIMDJSON_ERR_INVALID_ARGUMENT` for any other selector. Thread that argument through the existing declaration in `src/native/simdjson_bridge.h`, the Rust `extern` declaration and wrapper in `src/runtime/mod.rs`, and the hidden helper in `src/lib.rs`. Keep the same symbol name so the existing cbindgen exclusion remains authoritative; do not add a public ABI export, status, or production fault-injection switch.

    Change the `std::bad_alloc` exception mapper to return `PURE_SIMDJSON_ERR_CPP_EXCEPTION`, matching the normative contract and the other overloads. Preserve stderr logging, parser-aware best-effort diagnostic capture, catch ordering, `noexcept`, and the rule that no exception crosses into Rust. Both `PSIMDJSON_CATCH_CPP_EXCEPTIONS` and `PSIMDJSON_CATCH_PARSER_CPP_EXCEPTIONS` must continue to call the same mapper. Do not change the separate `map_error(simdjson::MEMALLOC)` branch: a returned simdjson error code is not a trapped C++ exception. Do not edit `docs/ffi-contract.md`, the generated header, public enum values, or Go error mapping because the source is being corrected to the already-locked contract.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_minimal psimdjson_test_force_ -- --test-threads=1 &amp;&amp; make verify-contract</automated>
  </verify>
  <acceptance_criteria>
    - The existing native test seam, not a second symbol, deterministically throws both `std::runtime_error` and `std::bad_alloc`.
    - The two named Rust tests each assert exact status 97; the bad_alloc test fails against the pre-fix source and passes after the mapper correction.
    - The invalid-selector case returns status 1 and no exception escapes.
    - Both parser-aware and parser-independent macros route bad_alloc through the corrected mapper.
    - `simdjson::MEMALLOC` and explicit internal errors retain their existing mapping; status 127 is not globally redefined.
    - `git diff --exit-code -- include/pure_simdjson.h docs/ffi-contract.md` is clean after the task.
    - No public symbol, ABI number, error enum, package dependency, release identity, or production configuration surface changes.
  </acceptance_criteria>
  <done>Runtime errors and bad allocations trapped at the native boundary deterministically return the normative public C++ exception status 97.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| throwing C++ code -> `noexcept` native ABI | Exception categories must be contained and translated before Rust regains control. |
| native status -> Rust/Go callers | Returning 127 for one exception category makes caller diagnostics contradict the normative ABI. |
| hidden fault seam -> production/public surface | Deterministic testing must not create a bindable public fault-injection API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-17-01 | Tampering | C++ exception/status translation | mitigate | Route runtime_error, bad_alloc, and unknown catches through the shared status-97 mapper; retain `noexcept` and exact-status tests. |
| T-11-17-02 | Repudiation | FFI diagnostics and caller classification | mitigate | Keep the normative document unchanged, distinguish thrown exceptions from returned MEMALLOC, and run generated contract audits plus Rust assertions. |
| T-11-17-03 | Elevation of Privilege | deterministic exception seam | mitigate | Extend the existing hidden/internal symbol, keep it excluded from cbindgen and public bindings, and accept only fixed test selectors. |
| T-11-SC | Tampering | dependency and package supply chain | mitigate | Add no package install or dependency; retain locked Go/Rust inputs and the audited vendored source path. |
</threat_model>

<verification>
- `cargo test --locked --test rust_shim_bigint --test rust_shim_minimal -- --test-threads=1` passes both gap regressions.
- `make verify-contract` and `make verify-docs` pass without changing the generated header or normative contract.
- `cargo build --release --locked &amp;&amp; case "$(go env GOOS)" in linux) PHASE11_LIBRARY="$PWD/target/release/libpure_simdjson.so" ;; darwin) PHASE11_LIBRARY="$PWD/target/release/libpure_simdjson.dylib" ;; windows) PHASE11_LIBRARY="$PWD/target/release/pure_simdjson.dll" ;; *) exit 1 ;; esac &amp;&amp; PURE_SIMDJSON_LIB_PATH="$PHASE11_LIBRARY" go test ./... -race -count=1 -timeout=180s` passes.
- `PHASE11_BENCH_OUT="$(mktemp -d)" &amp;&amp; bash scripts/bench/run_pr_benchmark.sh --no-baseline --out-dir "$PHASE11_BENCH_OUT"` exits zero and produces non-empty signal artifacts.
- `bash scripts/release/check_readiness.sh` passes the non-strict source gate.
- No tag, merge, publication, release workflow dispatch, or artifact upload occurs.
</verification>

<success_criteria>
- All malformed BigInt suffix regressions and both deterministic exception variants pass.
- Every trapped C++ exception returns status 97 while returned internal/MEMALLOC errors keep their existing semantics.
- Contract, oracle, race, benchmark-signal, and readiness gates are green on the combined closure source.
- The public ABI and normative documentation remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-17-SUMMARY.md` when done.
</output>
