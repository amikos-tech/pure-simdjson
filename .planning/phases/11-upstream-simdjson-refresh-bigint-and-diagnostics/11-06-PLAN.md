---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 06
type: execute
wave: 5
depends_on:
  - 11-05
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/lib.rs
  - tests/rust_shim_kernel.rs
autonomous: true
requirements:
  - DIAG-01

must_haves:
  truths:
    - "Kernel selection is process-global, exact-name, case-sensitive, and serialized with parser construction."
    - "Empty selection restores upstream automatic detection before the lifecycle lock."
    - "An explicit implementation is assigned only after compiled-name lookup and `supported_by_runtime_system()` succeed."
    - "First parser construction or explicit pool-lock export permanently locks selection; later changes return status 10."
    - "Explicit fallback is distinguishable from silent automatic fallback; automatic fallback still reports CPU unsupported."
  artifacts:
    - path: "tests/rust_shim_kernel.rs"
      provides: "Subprocess-isolated auto, valid, invalid, unsupported, fallback, race, and post-lock tests"
      contains: "PURE_SIMDJSON_ERR_KERNEL_LOCKED"
    - path: "src/native/simdjson_bridge.cpp"
      provides: "Synchronized process-global implementation state"
      contains: "supported_by_runtime_system"
    - path: "src/lib.rs"
      provides: "Additive setter/lock exports and kernel-locked status 10"
      exports: ["pure_simdjson_set_implementation", "pure_simdjson_lock_implementation_selection"]
  key_links:
    - from: "src/lib.rs"
      to: "src/native/simdjson_bridge.cpp"
      via: "Rust exports delegate to one synchronized upstream implementation state machine"
      pattern: "set_implementation|lock_implementation"
    - from: "src/native/simdjson_bridge.cpp"
      to: "psimdjson_parser_new"
      via: "parser construction takes the same mutex and commits the irreversible lock"
      pattern: "kernel.*lock|implementation.*lock"
---

<objective>
Add the native half of diagnostic process-global kernel control with a creation-time lock and CPU-support validation.

Purpose: D-12 needs reproducible diagnostics without permitting an unsupported SIMD implementation or racing selection against parser creation.
Output: synchronized setter/lock exports, status 10, explicit-fallback tracking, and isolated native tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@src/native/simdjson_bridge.cpp
@src/runtime/mod.rs
@src/lib.rs
@tests/rust_shim_fallback_gate.rs

<interfaces>
Additive public functions/status:

```c
pure_simdjson_error_code_t pure_simdjson_set_implementation(
    const uint8_t *name, size_t name_len);
pure_simdjson_error_code_t pure_simdjson_lock_implementation_selection(void);
PURE_SIMDJSON_ERR_KERNEL_LOCKED = 10
```

Existing `pure_simdjson_get_implementation_name_len/copy` remains the read side.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement one synchronized native implementation-selection state machine</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decision D-12
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Pattern 5: Lock Process-Global Kernel State at Creation` and `Pitfall 7`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Process-global kernel selection`
    - `third_party/simdjson/doc/implementation-selection.md` official lookup/support/assignment sequence
    - `src/native/simdjson_bridge.cpp` parser construction and existing implementation-name read path
    - `src/runtime/mod.rs` existing fallback/test override state
    - `tests/rust_shim_fallback_gate.rs` isolated fallback behavior analog
  </read_first>
  <action>
    Create one poison-tolerant native mutex/state holding `locked` and `explicit_selection`. Implement exact, case-sensitive `psimdjson_set_implementation`: empty name calls `detect_best_supported()`; nonempty name indexes `get_available_implementations()`, rejects a missing/compiled-out name with invalid-arg, checks `supported_by_runtime_system()`, then assigns `get_active_implementation()`. Return CPU unsupported before assignment when support fails. Implement `psimdjson_lock_implementation_selection`. Take the same lock in both legacy and configured parser constructors and set `locked=true` before allocation. Track whether fallback was explicitly selected so explicit diagnostic fallback passes while automatically detected fallback retains the existing CPU-unsupported gate. Add Rust wrappers; do not add a per-parser kernel field or an unlock API.
  </action>
  <verify>
    <automated>cargo check --locked &amp;&amp; cargo test --locked --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Setter, explicit lock, and both parser constructors use the same native mutex.
    - Name lookup is exact and support is checked before assignment.
    - Empty selection invokes upstream automatic detection.
    - There is no production unlock/reset-after-lock function.
    - Existing implementation-name read exports remain unchanged.
  </acceptance_criteria>
  <done>Native selection and parser creation have one linearized, irreversible process-global lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose kernel setter/lock and prove irreversible behavior in subprocesses</name>
  <files>src/lib.rs, tests/rust_shim_kernel.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-03`, threats T-11-02 and T-11-06
    - `src/lib.rs` implementation-name exports, parser constructors, error enum, and `ffi_wrap`
    - `tests/rust_shim_fallback_gate.rs` child-process command/env pattern
    - `tests/rust_shim_kernel.rs` if created by an interrupted prior attempt; preserve valid unrelated work
  </read_first>
  <behavior>
    - Empty set before creation succeeds and reports the auto-selected implementation.
    - A compiled and supported name can be selected and read back exactly.
    - Invalid and unsupported names fail without changing the active implementation.
    - Explicit fallback is accepted; silent automatic fallback still returns CPU unsupported on parser creation.
    - Setter after parser creation or explicit lock returns status 10.
    - Concurrent setter/constructor has one winner and no data race.
  </behavior>
  <action>
    Append `PURE_SIMDJSON_ERR_KERNEL_LOCKED = 10` without renumbering earlier statuses. Add public `pure_simdjson_set_implementation` and `pure_simdjson_lock_implementation_selection` through `ffi_wrap`, validating null/name-length combinations and forwarding exact bytes. Write `tests/rust_shim_kernel.rs` as a parent harness that spawns one child process per irreversible scenario using a dedicated environment discriminator, mirroring `rust_shim_fallback_gate`; never rely on Rust test order or reset production state. Include a concurrent setter-versus-constructor child case and assert only success/locked outcomes, never partial state or crash.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_kernel -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Status 10 is append-only and exercised after parser creation and explicit lock.
    - Tests cover empty, valid, invalid, unsupported-when-observable, explicit fallback, automatic fallback, and race cases.
    - Every irreversible case runs in a fresh child process.
    - An invalid/unsupported call leaves the prior selected name unchanged.
  </acceptance_criteria>
  <done>The additive C ABI safely controls and locks process-global kernel selection with deterministic isolated proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller kernel name -> CPU instruction path | An unvalidated implementation can execute unsupported instructions. |
| setter thread -> parser-construction thread | Process-global mutable state can race first use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-02 | Denial of Service | implementation setter | mitigate | Require compiled-name lookup and runtime CPU support before assignment; test invalid/unsupported cases. |
| T-11-06 | Tampering / Denial of Service | kernel lifecycle mutex | mitigate | Serialize setter, explicit pool lock, and parser construction; permanently lock at first creation; run a concurrent race test. |
</threat_model>

<verification>
- `cargo test --locked --test rust_shim_kernel -- --test-threads=1` passes.
- Native concurrency tests show no crash/data race and only allowed outcomes.
</verification>

<success_criteria>
- Unsupported implementations cannot be selected.
- Selection is process-global and immutable after first parser/pool lock.
- Explicit fallback and silent fallback remain distinguishable.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-06-SUMMARY.md` when done.
</output>
