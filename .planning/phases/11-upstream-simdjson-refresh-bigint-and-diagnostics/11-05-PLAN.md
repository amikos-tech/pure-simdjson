---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - src/lib.rs
  - tests/rust_shim_diagnostics.rs
autonomous: true
requirements:
  - DIAG-02

must_haves:
  truths:
    - "D-08: Native diagnostics mark a location known only when upstream returns a concrete pointer inside `[input, input+len)`."
    - "Unknown remains `UINT64_MAX` plus false; byte-zero remains offset 0 plus true."
    - "D-09: Stage-one UTF-8, unclosed-string, resource, and internal failures remain unknown when upstream supplies no valid location."
    - "D-19: A primary DOM failure uses the locked two-fresh-parser On-Demand hybrid—raw_json/at_end first, then full recursive consumption only after a valid first pass—with no parser/document reuse after error."
    - "Both fresh On-Demand replay parsers receive the exact `psimdjson_parser::max_capacity` and `psimdjson_parser::max_depth` values stored by Plan 11-04; neither pass can fall back to upstream defaults."
    - "The recursive consumer enforces the same N-1-success/N-failure depth boundary explicitly, while primary or replay capacity/depth/resource exhaustion stays unknown and never triggers an unbounded or additional attempt."
    - "D-20: current_location is read only after successful iterate plus replay failure/trailing content, and only an in-range non-end pointer produces the locked known/unknown corpus offsets."
    - "Success and pre-copy capacity rejection clear prior message, offset, and known state."
  artifacts:
    - path: "tests/rust_shim_diagnostics.rs"
      provides: "v4.6.4 characterization corpus plus natural offsets, clear-state, exact replay-capacity, and exact replay-depth boundary tests"
      contains: "last_error_has_offset"
    - path: "src/native/simdjson_bridge.cpp"
      provides: "Upstream-only in-range diagnostic location capture"
      contains: "current_location"
    - path: "src/lib.rs"
      provides: "Additive known-offset getter"
      exports: ["pure_simdjson_parser_get_last_error_has_offset"]
  key_links:
    - from: "src/native/simdjson_bridge.cpp"
      to: "src/runtime/registry.rs"
      via: "numeric offset and explicit known flag are read while padded input is alive"
      pattern: "last_error_has_offset"
    - from: "src/lib.rs"
      to: "tests/rust_shim_diagnostics.rs"
      via: "public getter preserves zero independently from unknown"
      pattern: "get_last_error_has_offset"
    - from: "psimdjson_parser::max_capacity/max_depth"
      to: "both fresh On-Demand diagnostic replay parsers"
      via: "`DiagnosticReplayLimits` is passed to raw-json and recursive replay; each parser allocates with the same capacity/depth"
      pattern: "DiagnosticReplayLimits|max_capacity|max_depth"
---

<objective>
Make native parser locations truthful and able to distinguish a known byte zero from an unknown location.

Purpose: D-06 through D-09 prefer explicit unknown over a fabricated location; the only accepted source is upstream v4.6.4 state with an in-bounds pointer proof.
Output: characterized native location capture, explicit known flag, additive export, and stale-state tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@.planning/spikes/001-v464-error-location-replay/README.md
@src/native/simdjson_bridge.cpp
@src/runtime/registry.rs
@src/lib.rs
@tests/rust_shim_minimal.rs

<interfaces>
Additive public getter:

```c
pure_simdjson_error_code_t pure_simdjson_parser_get_last_error_has_offset(
    pure_simdjson_parser_t parser,
    uint8_t *out_has_offset);
```

Wire states:

```text
known byte zero: offset=0, has_offset=1
known nonzero:   offset=N, has_offset=1
unknown:         offset=UINT64_MAX, has_offset=0
```

Bounded replay identifiers:

```text
psimdjson_parser::max_capacity / psimdjson_parser::max_depth
DiagnosticReplayLimits{max_capacity, max_depth}
replay_raw_json_location(...)
replay_recursive_location(...)
consume_ondemand_value(value, current_depth, max_depth)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize v4.6.4 locations and capture only proven pointers</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, tests/rust_shim_diagnostics.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-07 through D-09 and D-19 through D-20
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Pattern 4: Prove, Do Not Guess, Error Locations`, `Binding Resolutions`, and `Pitfall 6`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Error offsets: known zero versus unknown`
    - `.planning/spikes/001-v464-error-location-replay/README.md` validated two-parser hybrid, false-negative investigation, and exact selected-corpus observations
    - `third_party/simdjson/doc/basics.md` `current_location()` guarantees and unavailable cases
    - `src/native/simdjson_bridge.cpp` parser error fields, clear helpers, and parse failure path
    - `src/runtime/mod.rs` current last-error offset wrapper
    - `tests/rust_shim_minimal.rs` current message/offset clearing cases
  </read_first>
  <behavior>
    - The named `characterize_v464_error_locations` test reproduces the Spike 001 malformed-input corpus against pinned v4.6.4 before production assertions are finalized.
    - Only successful upstream `current_location()` results inside the copied input become known.
    - End pointers, out-of-range pointers, iterate/current-location errors, and resource/internal errors remain unknown.
    - UTF8_ERROR, UNESCAPED_CHARS, UNCLOSED_STRING, and EMPTY cases remain unknown when upstream cannot create a valid diagnostic document.
    - Empty input, invalid UTF-8, and unclosed string remain unknown; `[1,]`, trailing content, missing object key, `x`, extra closing bracket, and mismatched container produce offsets 3, 8, 16, 0, 15, and 9 respectively.
    - Both fresh replay parsers are constructed with `DiagnosticReplayLimits{parser->max_capacity, parser->max_depth}` and call On-Demand allocation with the current input length plus that exact maximum depth before `iterate()`.
    - A malformed payload of length N is rejected before replay under capacity N-1 and reaches bounded replay under capacity N; no replay parser observes a default or larger capacity.
    - With configured depth N, N-1 nested containers may reach the ordinary malformed-input replay result, while N nested containers remain unknown with no replay amplification; the recursive consumer also rejects entry at the same boundary.
    - Primary `CAPACITY`, `DEPTH_ERROR`, `MEMALLOC`, and internal/exception failures skip replay and remain unknown; the same failures inside either replay pass terminate that path without another parser or retry.
    - Successful DOM parses perform no replay; a failed DOM parse performs at most two fresh On-Demand passes with no retry loop.
  </behavior>
  <action>
    First create `tests/rust_shim_diagnostics.rs` with a test named `characterize_v464_error_locations` and reproduce the full Spike 001 corpus: empty input, invalid UTF-8, unclosed string, `[1,]`, trailing content, missing object key, unexpected root token `x`, extra closing bracket, and mismatched container. The characterization path must report the primary DOM status, replay pass, upstream `current_location()` status, pointer-range result, and derived offset. Run `cargo test --locked --test rust_shim_diagnostics characterize_v464_error_locations -- --exact --nocapture --test-threads=1` against pinned v4.6.4 and compare it with Spike 001 before finalizing assertions; preserve the mapping in `11-05-SUMMARY.md`.

    Then implement the validated D-19 failure-only hybrid with exactly two fresh On-Demand parser instances and the explicit internal value `DiagnosticReplayLimits{parser->max_capacity, parser->max_depth}` sourced from the effective fields Plan 11-04 stores. Pass that value into both `replay_raw_json_location` and `replay_recursive_location`; each helper must construct its own `simdjson::ondemand::parser` with `limits.max_capacity` and call `allocate(input_len, limits.max_depth)` before `iterate()`. Never construct a replay parser with default limits, enlarge either limit after allocation failure, or retry with a different parser configuration. Before invoking either helper, classify primary `CAPACITY`, `DEPTH_ERROR`, `MEMALLOC`, and internal/exception failures as terminal unknown; these resource/limit failures perform zero replay. Within either helper, allocation, iterate, capacity, depth, memory, or internal failure is likewise terminal unknown for that path. Pass 2 runs only after Pass 1 is fully valid; a Pass-1 failure never falls through to recursive replay, and a Pass-2 failure has no third attempt.

    Pass 1 calls `raw_json()` and `at_end()` to retain explicit upstream errors and the useful trailing-content location. Only when Pass 1 reports valid does Pass 2 call `consume_ondemand_value(value, current_depth, max_depth)` to recursively consume every object key, array element, and scalar through public On-Demand accessors; this fallback is required because `raw_json()` skipped `[1,]`, the missing-object-key fixture, and `x` in Spike 001. Start the root at `current_depth=0`; before entering an object or array, checked-add one and require the resulting container depth to be strictly less than `max_depth`, so configured depth N accepts N-1 nested containers and rejects N exactly. A guard hit returns the replay depth-limit outcome, keeps offset unknown, and performs no further attempt. Never reuse a parser/document after an error. Call `current_location()` only after that pass's `iterate()` succeeded and the pass reports an ordinary syntax error or trailing content; resource/limit outcomes are never promoted to a location. Per D-08/D-20: do not call `encoding/json`, add a secondary non-simdjson scanner/parser, parse error-message text, or estimate token positions.

    Convert the input and location pointers to `uintptr_t`, reject address-addition overflow, and compute the offset only after integer-domain checks prove `input_addr <= location_addr < input_addr+len`; this avoids relational/subtraction operations on an out-of-range pointer. Treat the exact end address as unknown. Set `last_error_has_offset=true` only after that proof; keep `last_error_offset=UINT64_MAX` and false for all other outcomes. Extend reset/success paths to clear message, offset, and bool together, and add bridge/runtime getters. Add tests named `diagnostic_replay_respects_capacity_n_minus_1_and_n`, `diagnostic_replay_respects_depth_n_minus_1_and_n`, and `diagnostic_replay_resource_failures_stay_unknown`. The capacity test uses one malformed payload of exact byte length N: configured capacity N-1 must return status 9 before replay, while capacity N must enter only replay parsers configured with N. The depth test uses matched malformed fixtures with N-1 and N nested containers and asserts the N case is unknown with no extra pass. Use an internal test-only replay observation helper if needed to assert pass count and effective limits; keep it `psimdjson_test_*` and outside the public/cbindgen surface. Put replay solely in the ordinary primary DOM-syntax-failure branch, use no retry loop, and document beside it that successful input pays zero replay cost while eligible failures pay at most two additional O(input length) scans under the caller's exact configured capacity/depth. Addresses review concern: a low-depth DOM failure cannot trigger default-depth recursive work or any second unbounded attempt.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_diagnostics characterize_v464_error_locations -- --exact --nocapture --test-threads=1 &amp;&amp; cargo test --locked --test rust_shim_diagnostics diagnostic_replay_ -- --test-threads=1 &amp;&amp; cargo check --locked &amp;&amp; cargo test --locked --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Production code contains no `encoding/json`, custom scanner, guessed index, or error-message parsing.
    - `characterize_v464_error_locations` runs first; its per-fixture v4.6.4 output is captured before the known/unknown golden table is finalized and preserved in `11-05-SUMMARY.md`.
    - Pointer subtraction occurs only after explicit lower/upper bounds checks.
    - Address comparison/subtraction is performed in checked `uintptr_t` integer space; exact-end, below-start, overflow, and out-of-range values remain unknown.
    - Source structure has no replay on DOM success and no loop that can create more than the two D-19 fresh parsers on failure.
    - Source passes the same `DiagnosticReplayLimits` to both passes; both fresh parsers use `limits.max_capacity` and `allocate(input_len, limits.max_depth)` before iteration.
    - Capacity N-1/N and depth N-1/N tests assert the exact configured boundary, pass count, unknown state on exhaustion, and absence of a default-limit retry.
    - Primary or replay `CAPACITY`, `DEPTH_ERROR`, `MEMALLOC`, and internal/exception outcomes remain `(UINT64_MAX,false)` and never launch another attempt.
    - `consume_ondemand_value` has an explicit checked depth counter/guard matching the primary parser's N-1-success/N-failure rule.
    - The stable corpus asserts unknown/unknown/unknown and exact offsets `3,8,16,0,15,9` in the D-20 order.
    - Unknown uses both `UINT64_MAX` and false.
    - Clear/success resets all three diagnostic fields.
    - A comment names the upstream cases that cannot supply a location without claiming broader coverage.
  </acceptance_criteria>
  <done>The native parser records only upstream-proven in-range locations and otherwise remains explicitly unknown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose and lock known/nonzero, known/zero, unknown, and clearing semantics</name>
  <files>src/runtime/registry.rs, src/lib.rs, tests/rust_shim_diagnostics.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-04` and threat T-11-05
    - `src/runtime/registry.rs` current `parser_last_error_offset` and configured capacity early-return path
    - `src/lib.rs` current offset export and internal test-seam/cbindgen-exclusion conventions
    - `tests/rust_shim_minimal.rs` parse diagnostics and clear-after-success patterns
    - `cbindgen.toml` existing internal `psdj_internal_*` exclusions
  </read_first>
  <behavior>
    - At least one characterized production fixture asserts a stable known nonzero offset when v4.6.4 proves it.
    - The characterized unexpected-root-token fixture proves known zero through the production replay and Rust transport.
    - Documented unavailable cases assert offset UINT64_MAX and false.
    - Success and capacity rejection after an error assert empty message, UINT64_MAX, and false.
  </behavior>
  <action>
    Extend `tests/rust_shim_diagnostics.rs` from the recorded characterization results and replace observation-only output with the exact D-20 assertions, including `(0,true)` for unexpected root token `x`. Add registry function `parser_last_error_has_offset` and public `pure_simdjson_parser_get_last_error_has_offset`, using `uint8_t` 0/1 and `ffi_wrap`. Preserve the existing offset export and sentinel. Do not add a test-only offset injection seam unless the production characterization contradicts Spike 001; treat such a contradiction as a blocking investigation rather than silently replacing the natural known-zero proof. Add stale-detail coverage for success and status-9 capacity rejection.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_diagnostics -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - The test table separates observed known fixtures from explicitly unknown fixtures.
    - Known zero is asserted as `(0, true)`, not inferred from nonzero.
    - Unknown is asserted as `(UINT64_MAX, false)`.
    - A success and a capacity rejection clear prior details.
    - The existing `pure_simdjson_parser_get_last_error_offset` signature is unchanged.
  </acceptance_criteria>
  <done>The Rust/public ABI transports a truthful known bit independently of the numeric offset and proves stale state cannot leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| upstream parser pointer -> numeric incident diagnostic | A bad range assumption could fabricate an offset or invoke undefined pointer arithmetic. |
| previous parse state -> next error | Stale text/location could be attributed to a different input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-05 | Repudiation | native diagnostic replay and offset transport | mitigate | Use checked `uintptr_t` range proof before subtraction, pass the exact configured capacity/depth to both fresh parsers, guard recursive depth explicitly, keep resource/limit outcomes unknown without another attempt, test N-1/N boundaries and zero separately, and clear state on every attempt/success. |
| T-11-SC | Tampering | dependency and package supply chain | mitigate | Introduce no package installs; retain locked Cargo/Go inputs and the audited vendored v4.6.4 source pin. |
</threat_model>

<verification>
- `cargo test --locked --test rust_shim_diagnostics -- --test-threads=1` passes.
- Source scan finds no secondary JSON parser or estimator.
- Replay-bound tests prove both fresh parsers use the Plan 11-04 effective capacity/depth, the recursive consumer enforces N-1/N, and resource/limit failures stay unknown without amplification.
- Wave 4 stays green with `cargo build --release --locked` followed by `PURE_SIMDJSON_LIB_PATH=<fresh local release library> go test ./... -race`.
- `make verify-contract` remains deliberately deferred to Plan 11-09; the only allowed interim mismatch is the generated header/audits lacking the explicit known-offset export.
- Existing last-error offset signature remains unchanged.
</verification>

<success_criteria>
- Native known/unknown state is truthful and independently represented.
- Byte zero is representable.
- Unsupported error categories remain unknown.
- Stale details never cross parse attempts.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-05-SUMMARY.md` when done.
</output>
