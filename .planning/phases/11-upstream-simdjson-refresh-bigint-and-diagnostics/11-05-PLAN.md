---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - src/lib.rs
  - tests/rust_shim_diagnostics.rs
autonomous: true
requirements:
  - DIAG-02

must_haves:
  truths:
    - "D-08: Native diagnostics mark a location known only when upstream returns a concrete pointer inside `[input, input+len)`."
    - "Unknown remains `UINT64_MAX` plus false; byte-zero remains offset 0 plus true."
    - "D-09: Stage-one UTF-8, unclosed-string, resource, and internal failures remain unknown when upstream supplies no valid location."
    - "Success and pre-copy capacity rejection clear prior message, offset, and known state."
  artifacts:
    - path: "tests/rust_shim_diagnostics.rs"
      provides: "v4.6.4 characterization corpus and known/nonzero, known/zero-transport, unknown, clear-state tests"
      contains: "last_error_has_offset"
    - path: "src/native/simdjson_bridge.cpp"
      provides: "Upstream-only in-range diagnostic location capture"
      contains: "current_location"
    - path: "src/lib.rs"
      provides: "Additive known-offset getter"
      exports: ["pure_simdjson_parser_get_last_error_has_offset"]
  key_links:
    - from: "src/native/simdjson_bridge.cpp"
      to: "src/runtime/registry.rs"
      via: "numeric offset and explicit known flag are read while padded input is alive"
      pattern: "last_error_has_offset"
    - from: "src/lib.rs"
      to: "tests/rust_shim_diagnostics.rs"
      via: "public getter preserves zero independently from unknown"
      pattern: "get_last_error_has_offset"
---

<objective>
Make native parser locations truthful and able to distinguish a known byte zero from an unknown location.

Purpose: D-06 through D-09 prefer explicit unknown over a fabricated location; the only accepted source is upstream v4.6.4 state with an in-bounds pointer proof.
Output: characterized native location capture, explicit known flag, additive export, and stale-state tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@src/native/simdjson_bridge.cpp
@src/runtime/registry.rs
@src/lib.rs
@tests/rust_shim_minimal.rs

<interfaces>
Additive public getter:

```c
pure_simdjson_error_code_t pure_simdjson_parser_get_last_error_has_offset(
    pure_simdjson_parser_t parser,
    uint8_t *out_has_offset);
```

Wire states:

```text
known byte zero: offset=0, has_offset=1
known nonzero:   offset=N, has_offset=1
unknown:         offset=UINT64_MAX, has_offset=0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize v4.6.4 locations and capture only proven pointers</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, tests/rust_shim_diagnostics.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-07 through D-09
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` sections `Pattern 4: Prove, Do Not Guess, Error Locations`, `Binding Resolutions`, and `Pitfall 6`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `Error offsets: known zero versus unknown`
    - `third_party/simdjson/doc/basics.md` `current_location()` guarantees and unavailable cases
    - `src/native/simdjson_bridge.cpp` parser error fields, clear helpers, and parse failure path
    - `src/runtime/mod.rs` current last-error offset wrapper
    - `tests/rust_shim_minimal.rs` current message/offset clearing cases
  </read_first>
  <behavior>
    - The named `characterize_v464_error_locations` test probes a fixed malformed-input corpus against pinned v4.6.4 before golden expectations or production eligibility rules are selected.
    - Only successful upstream `current_location()` results inside the copied input become known.
    - End pointers, out-of-range pointers, iterate/current-location errors, and resource/internal errors remain unknown.
    - UTF8_ERROR, UNESCAPED_CHARS, UNCLOSED_STRING, and EMPTY cases remain unknown when upstream cannot create a valid diagnostic document.
  </behavior>
  <action>
    First create `tests/rust_shim_diagnostics.rs` with a test named `characterize_v464_error_locations` and a fixed corpus containing empty input, invalid UTF-8, an unclosed string, `[1,]`, and `{"a":1} trailing`. The characterization path must report the primary DOM status, upstream `current_location()` status, pointer-range result, and derived offset without a preselected known/unknown golden table. Run `cargo test --locked --test rust_shim_diagnostics characterize_v464_error_locations -- --exact --nocapture --test-threads=1` against the pinned v4.6.4 source and capture the mapping as task evidence before finalizing any fixture-specific production assumption; preserve that captured mapping in `11-05-SUMMARY.md` at plan completion. Then add a failure-only native diagnostic routine that replays solely through upstream On-Demand APIs and asks upstream `current_location()`. Per D-08: do not call `encoding/json`, add a secondary parser or scanner, parse error-message text, or estimate token positions. Set `last_error_has_offset=true` and compute the integer offset only after upstream returns success and the pointer lies in `[input, input+len)`; treat `input+len` as unknown. Keep `last_error_offset=UINT64_MAX` and false for all other outcomes. Extend `psimdjson_parser` reset/success paths to clear message, offset, and bool together, and add bridge/runtime getters for the bool. Promote only the recorded upstream-proven fixtures to known expectations; do not promise every syntax or UTF-8 failure.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_diagnostics characterize_v464_error_locations -- --exact --nocapture --test-threads=1 &amp;&amp; cargo check --locked &amp;&amp; cargo test --locked --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Production code contains no `encoding/json`, custom scanner, guessed index, or error-message parsing.
    - `characterize_v464_error_locations` runs first; its per-fixture v4.6.4 output is captured before the known/unknown golden table is finalized and preserved in `11-05-SUMMARY.md`.
    - Pointer subtraction occurs only after explicit lower/upper bounds checks.
    - Unknown uses both `UINT64_MAX` and false.
    - Clear/success resets all three diagnostic fields.
    - A comment names the upstream cases that cannot supply a location without claiming broader coverage.
  </acceptance_criteria>
  <done>The native parser records only upstream-proven in-range locations and otherwise remains explicitly unknown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose and lock known/nonzero, known/zero, unknown, and clearing semantics</name>
  <files>src/runtime/registry.rs, src/lib.rs, tests/rust_shim_diagnostics.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-04` and threat T-11-05
    - `src/runtime/registry.rs` current `parser_last_error_offset` and configured capacity early-return path
    - `src/lib.rs` current offset export and internal test-seam/cbindgen-exclusion conventions
    - `tests/rust_shim_minimal.rs` parse diagnostics and clear-after-success patterns
    - `cbindgen.toml` existing internal `psdj_internal_*` exclusions
  </read_first>
  <behavior>
    - At least one characterized production fixture asserts a stable known nonzero offset when v4.6.4 proves it.
    - Known zero is proven through the same Rust transport using a test-only injection seam if no stable production fixture naturally yields zero.
    - Documented unavailable cases assert offset UINT64_MAX and false.
    - Success and capacity rejection after an error assert empty message, UINT64_MAX, and false.
  </behavior>
  <action>
    Extend `tests/rust_shim_diagnostics.rs` from the recorded characterization results and replace observation-only output with stable assertions for the proven corpus. Add registry function `parser_last_error_has_offset` and public `pure_simdjson_parser_get_last_error_has_offset`, using `uint8_t` 0/1 and `ffi_wrap`. Preserve the existing offset export and sentinel. If characterization finds no reliable malformed input at byte zero, add test-only internal export `psdj_internal_test_set_last_error_location` that sets `(offset, known)` on a live parser solely to prove zero survives C++ -> Rust; exclude this symbol from the public generated header in Plan 11-09. Do not use the seam in production paths. Add stale-detail coverage for success and status-9 capacity rejection.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_diagnostics -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - The test table separates observed known fixtures from explicitly unknown fixtures.
    - Known zero is asserted as `(0, true)`, not inferred from nonzero.
    - Unknown is asserted as `(UINT64_MAX, false)`.
    - A success and a capacity rejection clear prior details.
    - The existing `pure_simdjson_parser_get_last_error_offset` signature is unchanged.
  </acceptance_criteria>
  <done>The Rust/public ABI transports a truthful known bit independently of the numeric offset and proves stale state cannot leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| upstream parser pointer -> numeric incident diagnostic | A bad range assumption could fabricate an offset or invoke undefined pointer arithmetic. |
| previous parse state -> next error | Stale text/location could be attributed to a different input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-05 | Repudiation | native diagnostic replay and offset transport | mitigate | Accept only upstream-success/in-range pointers, keep all other cases unknown, test zero separately, and clear state on every attempt/success. |
</threat_model>

<verification>
- `cargo test --locked --test rust_shim_diagnostics -- --test-threads=1` passes.
- Source scan finds no secondary JSON parser or estimator.
- Existing last-error offset signature remains unchanged.
</verification>

<success_criteria>
- Native known/unknown state is truthful and independently represented.
- Byte zero is representable.
- Unsupported error categories remain unknown.
- Stale details never cross parse attempts.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-05-SUMMARY.md` when done.
</output>
