---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 13
type: execute
wave: 10
depends_on:
  - 11-12
files_modified:
  - tests/smoke/go_bootstrap_smoke.go
  - scripts/release/test_release_workflow_contracts.py
  - scripts/release/test_public_bootstrap_validation_contracts.py
  - docs/bootstrap.md
  - purejson.go
  - .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
  - .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-ARTIFACT-READINESS.md
autonomous: true
requirements:
  - UP-01
  - NUM-01
  - NUM-02
  - DIAG-01
  - DIAG-02
  - LIMIT-01

must_haves:
  truths:
    - "The packaged Go smoke proves a downloaded artifact supplies ABI 1.2 behavior, not merely baseline parsing."
    - "Release contract tests retain tag-on-main, CI-only publication, five target artifacts, and Phase 06.1 R2/fallback validation."
    - "Go/Rust/C++ contract, correctness oracle, race suite, release-source tests, and representative Tier 1/2/3 benchmark signal are green before source integration."
    - "Readiness evidence labels the state `SOURCE READY — NOT RELEASED`; Phase 11 is not complete before Plan 14."
    - "Before publication, every local Go runtime gate uses the freshly built library through `PURE_SIMDJSON_LIB_PATH`; no source-readiness claim depends on the not-yet-published bootstrap version."
    - "`11-VALIDATION.md` names concrete plan/task/wave owners for every verification row and reaches source-gate approval only after the exact serial source gate exits zero; no `W0`, `TBD`, `draft`, or `pending` marker remains."
    - "Artifact-enabling source is prepared for a squash merge to main, with no tag or publication performed here."
  artifacts:
    - path: "tests/smoke/go_bootstrap_smoke.go"
      provides: "Public bootstrap ABI 1.2 BigInt/configured-parser smoke"
      contains: "GetBigInt"
    - path: ".planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-ARTIFACT-READINESS.md"
      provides: "Exact local gate results, version/ABI/upstream identity, and not-released boundary"
      contains: "SOURCE READY — NOT RELEASED"
    - path: ".planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md"
      provides: "Completed Wave 0/Nyquist status backed by command evidence"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "tests/smoke/go_bootstrap_smoke.go"
      via: "existing packaged-artifact smoke on each release artifact"
      pattern: "go_bootstrap_smoke"
    - from: ".github/workflows/public-bootstrap-validation.yml"
      to: "internal/bootstrap/version.go"
      via: "published version dispatch and target-source validation"
      pattern: "version"
    - from: "11-ARTIFACT-READINESS.md"
      to: "Plan 11-14"
      via: "operator receives exact version/source commit and pre-publication gate results"
      pattern: "NOT RELEASED"
    - from: "11-VALIDATION.md"
      to: "11-ARTIFACT-READINESS.md"
      via: "Nyquist/source-gate flags and sign-off are set only after the exact serial source commands produce zero exit statuses"
      pattern: "source-gate approved|nyquist_compliant"
---

<objective>
Close all local/source gates and prepare the exact artifact-enabling commit for the supported main/tag/CI sequence.

Purpose: D-01, D-17, and D-18 make existing correctness/performance/platform mechanisms mandatory regression fences; local success is necessary but explicitly not a substitute for live publication proof.
Output: stronger public bootstrap smoke, release workflow contract tests, public docs, completed validation contract, and a source-readiness evidence record.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md
@docs/releases.md
@.agents/skills/pure-simdjson-release/SKILL.md
@.github/workflows/release.yml
@.github/workflows/public-bootstrap-validation.yml
@tests/smoke/go_bootstrap_smoke.go
@scripts/release/check_readiness.sh

<interfaces>
Local/source readiness command set from 11-VALIDATION.md:

```text
make verify-contract
cargo build --release --locked
PURE_SIMDJSON_LIB_PATH=<fresh local release library> go test ./... -race
python3 scripts/release/test_check_bootstrap_abi_state.py
python3 scripts/release/test_release_workflow_contracts.py
python3 scripts/release/test_public_bootstrap_validation_contracts.py
python3 scripts/release/test_render_release_notes.py
PURE_SIMDJSON_LIB_PATH=<fresh local release library> go test . -run '^TestJSONTestSuiteOracle$' -count=1
bash scripts/bench/run_pr_benchmark.sh --no-baseline --out-dir <temporary-dir>
```

Strict readiness is deliberately absent here; it runs only from the approved source on `origin/main` in Plan 14.

Nyquist closeout state after successful local evidence:

```text
status: source-gate-approved
wave_0_complete: true
nyquist_compliant: true
Approval: source-gate approved; hosted release proof remains the blocking 11-14-T1 operator gate
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make packaged/public validation prove ABI 1.2 behavior</name>
  <files>tests/smoke/go_bootstrap_smoke.go, scripts/release/test_release_workflow_contracts.py, scripts/release/test_public_bootstrap_validation_contracts.py, docs/bootstrap.md, purejson.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-01, D-17, and D-18
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` `Pattern 6: Treat Artifact Publication as a Phase Gate`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` sections `Go API, fuzz, and public bootstrap tests` and `Tag-driven release path`
    - `tests/smoke/go_bootstrap_smoke.go` current tiny default-install parse
    - `.github/workflows/release.yml` five platform jobs and packaged smoke invocation
    - `.github/workflows/public-bootstrap-validation.yml` target-src checkout, R2 matrix, and fallback subset
    - `scripts/release/test_release_workflow_contracts.py` existing tag/main/five-target assertions
    - `scripts/release/test_public_bootstrap_validation_contracts.py` existing public validation assertions
    - `docs/bootstrap.md` current version/examples
    - `purejson.go` current package contract
  </read_first>
  <behavior>
    - Packaged smoke constructs a parser with nondefault safe limits, parses BigInt, checks TypeBigInt/exact GetBigInt, and closes cleanly.
    - Release workflow contract still proves linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64 and invokes the shared native plus packaged smoke.
    - Public validation contract still proves five-target R2 plus linux/amd64, darwin/arm64, windows/amd64 fallback from published tag source.
  </behavior>
  <action>
    Per D-18, extend `go_bootstrap_smoke.go` just enough to require ABI 1.2: call `NewParser(WithMaxCapacity(...), WithMaxDepth(...))`, parse an object containing `18446744073709551616`, assert `TypeBigInt`, exact `GetBigInt`, and normal scalar compatibility. Keep it a real default-bootstrap smoke with no library-path override logic inside the program. Because this file is a build-ignore runnable `main`, execute it with `go run` against the freshly built local shared library for task feedback; do not use `go test ./tests/smoke` and do not invoke it locally without `PURE_SIMDJSON_LIB_PATH` before publication. Keep `scripts/release/run_go_packaged_smoke.sh` as the release workflow's staged-artifact harness, and leave the real public/default-bootstrap proof to Plan 11-14. Extend existing Python contract tests to assert all five release targets still execute native and packaged smokes and that public validation still checks out the requested published tag, runs the full R2 matrix, and the documented three-target fallback subset. Update `docs/bootstrap.md` versioned examples to the approved version, explicitly label that version as source-prepared until hosted publication succeeds, and document explicit-path ABI 1.1 mismatch plus Phase 06.1 validation. Update package docs in `purejson.go` with small copied-BigInt/options/HasOffset examples and the process-global diagnostic kernel lock; do not preview Phase 12-16 APIs. Addresses review concern: local smoke and public default-bootstrap proof are separate, named gates.
  </action>
  <verify>
    <automated>cargo build --release --locked &amp;&amp; case "$(go env GOOS)" in linux) LOCAL_LIBRARY="$PWD/target/release/libpure_simdjson.so" ;; darwin) LOCAL_LIBRARY="$PWD/target/release/libpure_simdjson.dylib" ;; windows) LOCAL_LIBRARY="$PWD/target/release/pure_simdjson.dll" ;; *) echo "unsupported smoke OS: $(go env GOOS)" &gt;&amp;2; exit 1 ;; esac &amp;&amp; test -f "$LOCAL_LIBRARY" &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" go run ./tests/smoke/go_bootstrap_smoke.go &amp;&amp; python3 scripts/release/test_release_workflow_contracts.py &amp;&amp; python3 scripts/release/test_public_bootstrap_validation_contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - Go smoke contains WithMaxCapacity/WithMaxDepth, TypeBigInt, and GetBigInt.
    - Workflow contract tests name all five release targets and both public-validation bands.
    - No workflow is changed to support a manual/branch upload path.
    - Bootstrap docs use only the approved intermediate version and still point to docs/releases.md.
    - Every pre-publication local `go run`/`go test` runtime invocation names the freshly built `PURE_SIMDJSON_LIB_PATH`; only Plan 11-14 may prove the default URL.
    - Package docs explain copied text and HasOffset in plain language.
  </acceptance_criteria>
  <done>Every future published Phase 11 artifact must prove real ABI 1.2 behavior through the existing five-platform/public workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete source gate and record not-yet-released readiness</name>
  <files>.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md, .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-ARTIFACT-READINESS.md</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` every task/status/sign-off row
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md` approved version
    - `docs/releases.md` supported source-on-main and strict-readiness boundary
    - `scripts/release/check_readiness.sh` non-strict versus strict checks
    - `scripts/bench/run_pr_benchmark.sh` no-baseline temporary-output contract
    - `src/lib.rs` authoritative cbindgen comments for document root, element type, numeric getters, and BigInt
    - `include/pure_simdjson.h` generated ABI 1.2 documentation emitted from `src/lib.rs`
    - `.planning/phases/09.1-bootstrap-artifact-and-abi-alignment-for-default-installs/09.1-RELEASE-VALIDATION.md` evidence shape only; do not copy old version/run values
  </read_first>
  <action>
    Keep `11-VALIDATION.md` at false/false and non-approved while evidence is being collected. Run the exact full source suite from the interfaces block serially, with `cargo build --release --locked` before Go tests, the platform-correct release library exported as `PURE_SIMDJSON_LIB_PATH` for every Go runtime command, and a newly created temporary benchmark directory. The mandatory serial sequence is `make verify-contract`; native release build; full race-enabled Go suite; bootstrap-policy test; release-workflow contract test; public-bootstrap-workflow contract test; release-note renderer test; correctness oracle; no-baseline Tier 1/2/3 benchmark signal; then non-strict `scripts/release/check_readiness.sh`. Do not replace this sequence with a grep of readiness prose or manually entered exit codes. Reserve strict ancestry/version readiness for Plan 14 after squash merge to main. Before recording readiness, run `rg -n 'NewParserPool\(' --glob '*.go' --glob '*.md' --glob '!.planning/**' --glob '!third_party/**' .`, inspect every result, and require Go source to handle both returns while docs/examples show `(pool, err)` handling; the full Go suite is the compile gate for source call sites.

    Run one case-insensitive stale-contract scan across `element.go`, `element_fuzz_test.go`, `element_scalar_test.go`, `materializer_fastpath_test.go`, `src/native/simdjson_bridge.cpp`, `src/runtime/registry.rs`, `src/lib.rs`, and `include/pure_simdjson.h`. It must fail on any of these obsolete contracts: `oversized literal rejected at parse`; `TestFastMaterializerOversizedLiteralParseRejected`; parse errors described as `ErrInvalidJSON or ErrPrecisionLoss`; TypeErr listing `ErrPrecisionLoss, or ErrPanic`; `native BIGINT classifications report ErrPrecisionLoss`; `BIGINT roots are unreachable`; `err_precision_loss()` mapped to `KIND_HINT_INVALID`; `kind cannot be classified (for example, BIGINT)`; `canonical precision-loss error surfaces` for the type query; or `PRECISION_LOSS` returned for BigInt. This explicitly verifies both the Rust cbindgen source and generated ABI header per D-05 rather than scanning only Go/native implementation files.

    After and only after every command exits zero, replace the placeholder ownership in `11-VALIDATION.md` with this concrete map: pin/build row -> `11-02-T1/T3`, `11-09-T1`, `11-13-T2` in waves `1/7/10`; BigInt row -> `11-02-T2/T3`, `11-03-T2`, `11-10-T1/T2` in waves `1/2/8`; kernel row -> `11-06-T1/T2`, `11-12-T2` in waves `5/9`; diagnostic-offset row -> `11-05-T1/T2`, `11-12-T1` in waves `4/9`; parser-limit row -> `11-04-T1/T2`, `11-11-T1` in waves `3/8`; pool row -> `11-11-T2`, `11-12-T2` in waves `8/9`; ABI/binding row -> `11-07-T2`, `11-08-T1/T2`, `11-09-T1/T2` in waves `6/7`; bootstrap/release-source row -> `11-07-T1`, `11-13-T1/T2` in waves `6/10`; correctness-oracle row -> `11-02-T3`, `11-13-T2` in waves `1/10`; benchmark row -> `11-13-T2` in wave `10`. Replace every `File Exists` placeholder with the exact test/evidence files owned by those tasks and mark the automated row green only when its command appears with exit status zero in the readiness record. Check every completed Wave 0 requirement item.

    Record the exact command, exit status, exact v4.6.4 commit, ABI `0x00010002`, approved version, source commit, correctness-oracle result, and benchmark summary in `11-ARTIFACT-READINESS.md`. The first outcome line must be `Status: SOURCE READY — NOT RELEASED`; explicitly state that the default bootstrap URL, five-platform/public proof, and real no-override smoke remain the blocking Plan 11-14 operator gate. In `11-VALIDATION.md`, change frontmatter `status` from `draft` to `source-gate-approved`, set `wave_0_complete: true` and `nyquist_compliant: true`, replace all row-level `pending` values with evidence-backed `green`, and replace the sign-off with `**Approval:** source-gate approved — evidence: 11-ARTIFACT-READINESS.md; hosted release proof: 11-14-T1`. Rewrite the hosted-publication sign-off item as an unchecked, explicitly owned `11-14-T1 operator gate`, not as a passed local check. The completed validation file must contain no literal `W0`, `TBD`, `draft`, or `pending`. Keep the validation/readiness changes in their planning-only task commit and out of changelog/release-note prose; commit_docs is enabled, so they remain tracked project evidence without becoming a public compatibility claim. Do not create/push a tag, dispatch a release, or describe Phase 11 as complete. Prepare source changes for the required squash merge to main.
  </action>
  <verify>
    <automated>make verify-contract &amp;&amp; cargo build --release --locked &amp;&amp; case "$(go env GOOS)" in linux) LOCAL_LIBRARY="$PWD/target/release/libpure_simdjson.so" ;; darwin) LOCAL_LIBRARY="$PWD/target/release/libpure_simdjson.dylib" ;; windows) LOCAL_LIBRARY="$PWD/target/release/pure_simdjson.dll" ;; *) echo "unsupported source-gate OS: $(go env GOOS)" &gt;&amp;2; exit 1 ;; esac &amp;&amp; export LOCAL_LIBRARY &amp;&amp; test -f "$LOCAL_LIBRARY" &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" go test ./... -race -count=1 &amp;&amp; python3 scripts/release/test_check_bootstrap_abi_state.py &amp;&amp; python3 scripts/release/test_release_workflow_contracts.py &amp;&amp; python3 scripts/release/test_public_bootstrap_validation_contracts.py &amp;&amp; python3 scripts/release/test_render_release_notes.py &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" go test . -run '^TestJSONTestSuiteOracle$' -count=1 &amp;&amp; PHASE11_BENCH_DIR="$(mktemp -d)" &amp;&amp; export PHASE11_BENCH_DIR &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" bash scripts/bench/run_pr_benchmark.sh --no-baseline --out-dir "$PHASE11_BENCH_DIR" &amp;&amp; bash scripts/release/check_readiness.sh &amp;&amp; test "$(git -C third_party/simdjson rev-parse HEAD)" = "1bcf71bd85059ab6574ea1159de9298dcc1212c5" &amp;&amp; ! rg -ni 'oversized literal rejected at parse|TestFastMaterializerOversizedLiteralParseRejected|want ErrInvalidJSON or ErrPrecisionLoss|ErrPrecisionLoss, or ErrPanic|native BIGINT classifications report ErrPrecisionLoss|BIGINT roots are unreachable|err_precision_loss\(\).*KIND_HINT_INVALID|kind cannot be classified \(for example, BIGINT\)|canonical precision-loss error surfaces|PRECISION_LOSS.*BIGINT|BIGINT.*precision.loss' element.go element_fuzz_test.go element_scalar_test.go materializer_fastpath_test.go src/native/simdjson_bridge.cpp src/runtime/registry.rs src/lib.rs include/pure_simdjson.h &amp;&amp; grep -q '^Status: SOURCE READY — NOT RELEASED$' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-ARTIFACT-READINESS.md &amp;&amp; grep -q '^status: source-gate-approved$' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md &amp;&amp; grep -q '^nyquist_compliant: true$' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md &amp;&amp; grep -q '^wave_0_complete: true$' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md &amp;&amp; grep -q '^\*\*Approval:\*\* source-gate approved' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md &amp;&amp; ! rg -n '\b(W0|TBD|draft|pending)\b' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every full-suite command exits zero and appears with its result in the readiness record.
    - Readiness record contains exact upstream commit, ABI, approved version, and source commit.
    - First outcome is literally `Status: SOURCE READY — NOT RELEASED`.
    - The record states that local Go gates used an explicit rebuilt library and default-bootstrap/public validation is still pending.
    - VALIDATION frontmatter is true/true only after commands pass; every task row is green.
    - The automated verifier itself executes the complete serial source gate, including the race suite, correctness oracle, benchmark signal, and non-strict readiness; it does not trust self-authored readiness flags as evidence.
    - Every validation row names concrete plan/task/wave owners from the required map, exact owned files replace `File Exists` placeholders, and no `W0` or `TBD` remains.
    - Validation status/sign-off becomes source-gate-approved only after zero exits; no `draft` or `pending` remains, while the hosted proof is honestly assigned to the unchecked `11-14-T1 operator gate`.
    - `go test ./...` compiles every changed `NewParserPool` call; docs/examples handle both constructor returns.
    - The targeted stale-contract scan covers both `src/lib.rs` and `include/pure_simdjson.h` and reports no valid-BigInt parse rejection, unclassifiable-kind statement, or BigInt precision-loss expectation.
    - No tag, release, public object, or strict-readiness claim is created by this task.
    - Source integration instructions specify squash merge.
  </acceptance_criteria>
  <done>The artifact-enabling source is fully tested and evidence-backed for squash merge to main, while publication proof remains explicitly pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local source gates -> release recommendation | Local success can be mistaken for hosted five-platform/public success. |
| tagged artifact -> packaged Go smoke | The released binary must expose the same ABI/API that source tests exercised. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-01 | Denial of Service | packaged configured parse | mitigate | Public smoke exercises configured bounds; full option/capacity suite remains a source gate. |
| T-11-02 | Denial of Service | packaged implementation selection | mitigate | C/native and Go kernel tests run before source readiness; public smoke reads the actual implementation. |
| T-11-03 | Tampering | release/public workflow contract | mitigate | Contract tests require tag-on-main, all five artifacts, packaged smoke, and Phase 06.1 matrices. |
| T-11-04 | Information Disclosure | packaged BigInt | mitigate | Public smoke requires copied exact GetBigInt behavior. |
| T-11-05 | Repudiation | diagnostics evidence | mitigate | Full characterized/native/Go tests run before readiness; docs require HasOffset checks. |
| T-11-06 | Tampering / Denial of Service | kernel lifecycle | mitigate | Race/subprocess suites are part of `go test ./... -race`. |
| T-11-SC | Tampering | dependency and package supply chain | mitigate | Introduce no package installs; retain locked Cargo/Go inputs and the audited vendored v4.6.4 source pin. |
</threat_model>

<verification>
- Fast task feedback uses the focused bootstrap-policy/evidence command in Task 2 and remains below the per-task latency target.
- Final source evidence separately records this full gate after resolving `LOCAL_LIBRARY` for the host: `make verify-contract &amp;&amp; cargo build --release --locked &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" go test ./... -race &amp;&amp; python3 scripts/release/test_check_bootstrap_abi_state.py &amp;&amp; python3 scripts/release/test_release_workflow_contracts.py &amp;&amp; python3 scripts/release/test_public_bootstrap_validation_contracts.py &amp;&amp; python3 scripts/release/test_render_release_notes.py &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" go test . -run '^TestJSONTestSuiteOracle$' -count=1 &amp;&amp; PURE_SIMDJSON_LIB_PATH="$LOCAL_LIBRARY" bash scripts/bench/run_pr_benchmark.sh --no-baseline --out-dir "$(mktemp -d)/phase11-pr-bench" &amp;&amp; bash scripts/release/check_readiness.sh`.
- Full local/source suite and benchmark signal pass.
- Readiness record is explicit that publication is pending.
- Release/public workflow contract tests retain the required matrices and source ancestry.
</verification>

<success_criteria>
- Source is ready for the artifact-enabling squash merge.
- Every local regression fence is green and recorded.
- The published-artifact proof cannot be confused with local readiness.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-13-SUMMARY.md` when done.
</output>
