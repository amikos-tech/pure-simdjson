---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - src/lib.rs
  - tests/rust_shim_bigint.rs
autonomous: true
requirements:
  - NUM-01
  - NUM-02

must_haves:
  truths:
    - "D-04: The public Rust C export retrieves BigInt only from kind 9 and returns exact copied bytes owned by the existing allocation registry."
    - "BigInt bytes remain valid after the document is freed until `pure_simdjson_bytes_free` releases the copy."
    - "Calling the BigInt export on int64, uint64, float64, string, bool, null, array, or object returns `PURE_SIMDJSON_ERR_WRONG_TYPE`."
    - "Every output pointer follows the existing write-on-success and `ffi_wrap` panic/exception boundary."
  artifacts:
    - path: "tests/rust_shim_bigint.rs"
      provides: "Copied BigInt ownership, strict getter, and boundary integration tests"
      contains: "pure_simdjson_element_get_bigint"
    - path: "src/lib.rs"
      provides: "Additive `pure_simdjson_element_get_bigint` C export"
      exports: ["pure_simdjson_element_get_bigint"]
    - path: "src/runtime/registry.rs"
      provides: "Validated view resolution and tracked copied-byte allocation for BigInt"
  key_links:
    - from: "src/lib.rs"
      to: "src/runtime/registry.rs"
      via: "`ffi_wrap` export delegates to registry copy-out"
      pattern: "element_get_bigint"
    - from: "src/runtime/registry.rs"
      to: "src/native/simdjson_bridge.cpp"
      via: "validated view -> upstream `get_bigint()` view -> Rust-owned byte copy"
      pattern: "bigint"
---

<objective>
Carry native BigInt text through the Rust ownership boundary as a strict copied C ABI accessor.

Purpose: D-04 requires exact text without a borrowed-pointer escape or `math/big`; this plan reuses the proven string allocation/free path.
Output: C++ view helper, Rust registry copy, panic-safe public export, and native lifetime tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@src/native/simdjson_bridge.h
@src/native/simdjson_bridge.cpp
@src/runtime/mod.rs
@src/runtime/registry.rs
@src/lib.rs
@tests/rust_shim_accessors.rs

<interfaces>
New public ABI contract created here and generated into the header by Plan 11-09:

```c
pure_simdjson_error_code_t pure_simdjson_element_get_bigint(
    const pure_simdjson_value_view_t *view,
    uint8_t **out_ptr,
    size_t *out_len);
```

Ownership remains identical to `pure_simdjson_element_get_string`: the caller copies then invokes `pure_simdjson_bytes_free(ptr, len)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the native BigInt view seam without exposing borrowed memory</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-04 and D-05
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` section `Pattern 3: Reuse the Existing Copy-Out Ownership Path`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `BigInt type and copied accessor`
    - `src/native/simdjson_bridge.h` existing `psimdjson_element_get_string_view` declaration
    - `src/native/simdjson_bridge.cpp` existing string-view and resolved-index accessor implementations
    - `src/runtime/mod.rs` existing `native_element_get_string_view` wrapper
    - `third_party/simdjson/include/simdjson/dom/element-inl.h` `get_bigint()` contract
  </read_first>
  <action>
    Add private bridge function `psimdjson_element_get_bigint_view` beside `psimdjson_element_get_string_view`, accepting a resolved doc/index and returning a borrowed pointer plus length only when upstream `get_bigint()` succeeds. Map `INCORRECT_TYPE` to status 4 and preserve the existing C++ exception boundary. Add `native_element_get_bigint_view` in `src/runtime/mod.rs`; it may return the borrowed slice only to the registry call that immediately copies it. Do not add a new allocator, public borrowed pointer, or frame field. Keep all existing string helper signatures unchanged per D-16.
  </action>
  <verify>
    <automated>cargo check --locked &amp;&amp; cargo test --locked --lib -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - The bridge helper calls upstream `get_bigint()` rather than parsing decimal text.
    - The Rust wrapper names and types make the borrowed lifetime local to the call.
    - Non-BigInt kinds map to status 4.
    - Existing string, numeric, and frame declarations are byte-for-byte unchanged except adjacent comments required by the new seam.
  </acceptance_criteria>
  <done>The native/Rust private seam can expose a validated BigInt span only long enough for the registry to copy it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Copy BigInt through the existing allocation registry and public export</name>
  <files>src/runtime/registry.rs, src/lib.rs, tests/rust_shim_bigint.rs</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-02` and threat T-11-04
    - `src/runtime/registry.rs` existing string copy allocation and `bytes_free` registry path
    - `src/lib.rs` `pure_simdjson_element_get_string`, `pure_simdjson_bytes_free`, and `ffi_wrap` patterns
    - `tests/rust_shim_accessors.rs` copied string/free and document-lifetime tests
    - `tests/rust_shim_bigint.rs` if created by an interrupted prior attempt; preserve unrelated valid work
  </read_first>
  <behavior>
    - Positive and negative BigInt return byte-for-byte decimal text.
    - Returned text survives `pure_simdjson_doc_free` until `pure_simdjson_bytes_free`.
    - A second free fails under the same allocation-registry contract as copied strings.
    - Every non-BigInt JSON kind returns status 4 and leaves out-parameters untouched.
    - Null out-parameters return status 1 without panic.
  </behavior>
  <action>
    Write `tests/rust_shim_bigint.rs` first with the listed boundary, strict-type, ownership-after-doc-free, free, null-output, root, descendant, array-iterator, and object-lookup cases. Then add registry function `element_get_bigint_copy` parallel to the existing string copy path: validate `ValueView` once, call `native_element_get_bigint_view`, copy into the existing tracked byte allocation, and return pointer/length. Add `pure_simdjson_element_get_bigint` to `src/lib.rs` using `ffi_wrap`, pointer validation, and write-on-success out-parameters. Per D-04, do not accept int64/uint64 and do not add `math/big` or any arbitrary-precision dependency. Do not hand-edit `include/pure_simdjson.h`; Plan 11-09 performs the single complete cbindgen synchronization after all Phase 11 exports exist.
  </action>
  <verify>
    <automated>cargo test --locked --test rust_shim_bigint -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/rust_shim_bigint.rs` covers both signed and unsigned out-of-range literals and exact spelling.
    - The returned copy is asserted after document free and is released by `pure_simdjson_bytes_free`.
    - All non-BigInt kind cases assert `PURE_SIMDJSON_ERR_WRONG_TYPE`.
    - The export passes through `ffi_wrap` and writes outputs only after successful allocation.
    - `cargo tree --locked` shows no new dependency.
  </acceptance_criteria>
  <done>The C ABI exposes strict, copied, exact BigInt text with the same ownership/free contract as strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| C++ `string_view` -> Rust registry | Upstream bytes are document-owned and untrusted to outlive the current call. |
| Rust allocation -> C/Go caller | Copied bytes cross the ABI and require tracked release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-04 | Information Disclosure / Tampering | `pure_simdjson_element_get_bigint` | mitigate | Copy before returning, track in the existing allocation registry, test post-doc lifetime and double-free behavior, and never expose the C++ view directly. |
| T-11-SC | Tampering | dependency graph | mitigate | Add no dependency; use upstream BigInt and the existing byte allocator/free path. |
</threat_model>

<verification>
- `cargo test --locked --test rust_shim_bigint -- --test-threads=1` passes.
- `cargo test --locked --lib -- --test-threads=1` passes.
- No package manifest or lockfile changes.
</verification>

<success_criteria>
- Exact BigInt text crosses the native ABI as a caller-owned copy.
- Strict wrong-type behavior is executable for every other kind.
- No borrowed bytes escape and no new allocation family exists.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-03-SUMMARY.md` when done.
</output>
