---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 14
type: execute
wave: 11
depends_on:
  - 11-13
files_modified: []
autonomous: false
requirements:
  - UP-01
  - NUM-01
  - NUM-02
  - DIAG-01
  - DIAG-02
  - LIMIT-01

must_haves:
  truths:
    - "The artifact-enabling source is squash-merged to main before strict readiness or tag creation."
    - "Strict readiness passes for the operator-approved version on the exact origin/main-anchored commit."
    - "An immutable annotated tag on that commit triggers release.yml; CI is the only publisher."
    - "D-18: release.yml succeeds for linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, and windows/amd64 with ABI 1.2 native and packaged smokes."
    - "Phase 06.1 public-bootstrap-validation succeeds for full five-target R2 and the documented three-target GitHub fallback subset."
    - "Only after both hosted workflows pass is the matching default-bootstrap artifact proven and Phase 11 completion allowed."
  artifacts:
    - path: ".planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-14-SUMMARY.md"
      provides: "Operator-supplied main commit, tag, strict readiness, release run, public validation run, and five-platform outcome evidence"
      contains: "Outcome: PASSED"
  key_links:
    - from: "origin/main"
      to: "refs/tags/v<approved-version>"
      via: "annotated tag whose commit is an ancestor of origin/main"
      pattern: "merge-base --is-ancestor"
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/public-bootstrap-validation.yml"
      via: "successful CI publication precedes Phase 06.1 dispatch for the same version"
      pattern: "version"
---

<objective>
Complete the operator-only publication and public bootstrap proof for the matching ABI 1.2 artifact.

Purpose: D-17/D-18 make hosted five-platform publication and fresh-runner default bootstrap a hard completion gate. The normal executor must not fabricate or bypass this evidence.
Output: verified main-anchored annotated tag, successful CI-only release, successful Phase 06.1 public validation, and evidence recorded in the summary.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-ARTIFACT-READINESS.md
@docs/releases.md
@.agents/skills/pure-simdjson-release/SKILL.md
@scripts/release/check_readiness.sh
@.github/workflows/release.yml
@.github/workflows/public-bootstrap-validation.yml

<interfaces>
Required immutable sequence:

```text
squash-approved Phase 11 source -> origin/main
-> bash scripts/release/check_readiness.sh --strict --version <approved-version>
-> annotated v<approved-version> tag on that exact commit
-> release.yml (CI-only five-platform publish)
-> public-bootstrap-validation.yml version=<approved-version> (Phase 06.1)
```

On any publication failure that changes native bytes, select a new unused patch version and repeat the source/pin/canary/changelog/readiness path. Never move or replace a tag.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Squash-merge, publish through CI, and prove public ABI 1.2 bootstrap</name>
  <files>None — external operator actions and evidence are recorded in `11-14-SUMMARY.md` after approval.</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md` exact approved version
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-ARTIFACT-READINESS.md` source commit and all local gate outcomes
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-15 through D-18
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` Manual-Only Verifications
    - `docs/releases.md` complete authoritative release runbook
    - `.agents/skills/pure-simdjson-release/SKILL.md` required flow and constraints
    - `scripts/release/check_readiness.sh` strict checks
    - `.github/workflows/release.yml` tag ancestry, five-platform build/sign/publish jobs
    - `.github/workflows/public-bootstrap-validation.yml` Phase 06.1 R2/fallback matrix
  </read_first>
  <action>
    This is intentionally operator-only. First squash-merge the fully approved artifact-enabling source to `main`; no merge commit or unsquashed feature history is accepted. Fetch `origin/main` and tags, then operate on the exact origin/main commit recorded for publication. Reconfirm the approved `v<version>` tag is unused. Run `bash scripts/release/check_readiness.sh --strict --version <approved-version>` and require exit 0, including bootstrap version, ABI policy/canary, locked Cargo metadata, and origin/main ancestry. Create one annotated `v<approved-version>` tag on that exact commit and push only the tag. Let `.github/workflows/release.yml` build, smoke, sign, verify, and publish; do not hand-upload, dispatch a branch publication, bypass CI, or create prep-branch checksum source. Require all five platform builds and packaged ABI 1.2 smokes to pass. After release success, dispatch `.github/workflows/public-bootstrap-validation.yml` with `version=<approved-version>` and require all five R2 jobs plus linux/amd64, darwin/arm64, and windows/amd64 fallback jobs to pass. Provide the main commit, tag object/target, strict-readiness output, release run URL/result, public-validation run URL/result, and per-platform outcomes. If native bytes must change after a failed tag, do not mutate the tag: return to a new explicit version decision and repeat Plans 07, 13, and 14.
  </action>
  <verify>
    <automated>APPROVED_ABI12_VERSION="$(sed -nE 's/.*approved_abi12_version:[[:space:]]*`?([0-9]+\.[0-9]+\.[0-9]+)`?.*/\1/p' .planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-01-SUMMARY.md | head -n 1)" &amp;&amp; export APPROVED_ABI12_VERSION &amp;&amp; test -n "$APPROVED_ABI12_VERSION" &amp;&amp; git fetch origin main --tags &amp;&amp; bash scripts/release/check_readiness.sh --strict --version "$APPROVED_ABI12_VERSION" &amp;&amp; git rev-parse -q --verify "refs/tags/v${APPROVED_ABI12_VERSION}^{tag}" &gt;/dev/null &amp;&amp; git merge-base --is-ancestor "v${APPROVED_ABI12_VERSION}^{commit}" origin/main</automated>
  </verify>
  <acceptance_criteria>
    - Artifact-enabling source reached main through squash merge.
    - Strict readiness output names the approved version and ABI `0x00010002` and exits zero on the tagged main commit.
    - Tag is annotated, immutable, and points to an origin/main ancestor.
    - release.yml is the sole publisher and reports success for all five target artifacts, signatures, and checksum metadata.
    - Native and packaged smokes prove ABI 1.2 behavior on all five targets.
    - Phase 06.1 reports full five-target R2 success and the exact three-target fallback success.
    - No manual upload, CI bypass, prep-branch checksum rewrite, or Phase 16 final-v0.2 claim occurs.
    - Summary records `Outcome: PASSED` only after every item above has evidence.
  </acceptance_criteria>
  <resume-signal>Reply `approved` with the strict-readiness output and both workflow run URLs/results, or describe the failed gate.</resume-signal>
  <done>The approved default bootstrap version publicly resolves to a complete ABI 1.2 artifact on every supported target, with CI and Phase 06.1 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source branch -> origin/main tag anchor | Publishing an unmerged or different commit breaks reproducibility and review provenance. |
| CI artifacts/signatures -> public bootstrap | Downloaded code crosses from public storage into consumer processes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-03 | Tampering | tag/artifact ABI identity | mitigate | Strict readiness on origin/main, annotated immutable tag, CI build/smoke/sign, full public bootstrap validation. |
| T-11-SC | Tampering | release supply chain | mitigate | release.yml is the only publisher; keyless signatures/checksums and no hand upload/CI bypass; new version required after changed bytes. |
</threat_model>

<verification>
- Strict readiness passes on the exact tagged origin/main commit.
- release.yml and public-bootstrap-validation.yml both report success for the same approved version.
- Evidence covers all five R2 targets and the fallback subset.
</verification>

<success_criteria>
- Matching ABI 1.2 artifacts are publicly available and validated.
- Default bootstrap succeeds on fresh hosted runners.
- Phase 11 closes only after evidence, with immutable tags and CI-only publication.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-14-SUMMARY.md` when done.
</output>
