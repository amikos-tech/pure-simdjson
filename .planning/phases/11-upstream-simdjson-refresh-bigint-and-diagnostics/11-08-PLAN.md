---
phase: 11-upstream-simdjson-refresh-bigint-and-diagnostics
plan: 08
type: execute
wave: 7
depends_on:
  - 11-07
files_modified:
  - internal/ffi/bindings.go
  - internal/ffi/bindings_test.go
  - internal/ffi/bindings_optional_test.go
  - library_loading.go
  - library_loading_test.go
  - parser.go
autonomous: true
requirements:
  - NUM-02
  - DIAG-01
  - DIAG-02
  - LIMIT-01

must_haves:
  truths:
    - "The loader binds only `pure_simdjson_get_abi_version`, probes the version, and returns `ErrABIVersionMismatch` for ABI 1.1 before looking up any 1.2-only symbol."
    - "After a compatible 1.x ABI >=1.2 probe, every Phase 11 public symbol is mandatory and a named missing symbol fails closed before caching."
    - "A complete ABI 1.2 fixture binds configured construction, BigInt, known-offset, and kernel setter/lock methods."
    - "No ABI 1.1 compatibility fallback or optional Phase 11 symbol path exists."
  artifacts:
    - path: "internal/ffi/bindings.go"
      provides: "Minimal ABI probe plus complete mandatory ABI 1.2 binding/methods"
      contains: "ProbeABI"
    - path: "library_loading.go"
      provides: "Probe -> compatibility check -> full bind -> cache ordering"
      contains: "ProbeABI"
    - path: "library_loading_test.go"
      provides: "ABI 1.1, complete 1.2, and incomplete 1.2 fixture behavior"
      contains: "ErrABIVersionMismatch"
  key_links:
    - from: "library_loading.go"
      to: "internal/ffi/bindings.go"
      via: "minimal ABI probe precedes full `Bind`"
      pattern: "ProbeABI.*Bind"
    - from: "library_loading.go"
      to: "cachedLibrary"
      via: "cache install occurs only after complete binding and implementation-name read"
      pattern: "cachedLibrary"
---

<objective>
Make ABI compatibility observable before symbol lookup and bind the complete Phase 11 contract fail-closed.

Purpose: D-15/D-16 require explicit-path ABI 1.1 libraries to report the typed mismatch, while an artifact claiming 1.2 but omitting a symbol is corrupt rather than partially usable.
Output: staged purego binding, mandatory Phase 11 methods, loader sequencing, and fixture tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md
@.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md
@internal/ffi/types.go
@internal/ffi/bindings.go
@library_loading.go
@parser.go

<interfaces>
Required binding stages:

```text
1. ffi.ProbeABI(handle, lookup) -> actual uint32
2. accept major 1 and actual >= 0x00010002; reject 1.1/other major
3. ffi.Bind(handle, lookup) -> every required ABI 1.2 symbol
4. read implementation name
5. install cachedLibrary
```

Mandatory new symbols:

```text
pure_simdjson_parser_new_configured
pure_simdjson_parser_get_last_error_has_offset
pure_simdjson_element_get_bigint
pure_simdjson_set_implementation
pure_simdjson_lock_implementation_selection
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split minimal ABI probing from complete Phase 11 binding</name>
  <files>internal/ffi/bindings.go, internal/ffi/bindings_test.go, internal/ffi/bindings_optional_test.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-CONTEXT.md` decisions D-15 and D-16
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-RESEARCH.md` section `Pattern 2: ABI-First, Symbols-Second Binding`
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-PATTERNS.md` section `ABI-first purego loading`
    - `internal/ffi/bindings.go` current monolithic required/optional registration and copied-string helper
    - `internal/ffi/bindings_test.go` fake lookup/copy/free patterns
    - `internal/ffi/bindings_optional_test.go` optional-symbol negative analog
    - `internal/ffi/types.go` Phase 11 constants and function argument widths
  </read_first>
  <behavior>
    - `ProbeABI` resolves/calls only the ABI getter.
    - Complete Bind requires every public Phase 11 symbol and names the missing one in its error.
    - Configured constructor, BigInt copy/free, offset-known, setter, and lock wrappers preserve KeepAlive and exact widths.
    - Pre-existing native allocator/internal materializer optionality is unchanged; no new Phase 11 symbol is optional.
  </behavior>
  <action>
    Add `ProbeABI(handle uintptr, lookup SymbolLookup) (uint32, error)` that registers only `pure_simdjson_get_abi_version`, calls it, and returns a typed lookup/call error. Extend `Bindings` and the required-symbol table with all five new public symbols. Add `ParserNewConfigured(uint64,uint32)`, `ParserLastErrorHasOffset`, `ElementGetBigInt` using the exact existing `ElementGetString` copy/free pattern, `SetImplementation(string)`, and `LockImplementationSelection`. Keep allocator stats and `psdj_internal_materialize_build` under their pre-existing optional policy only; do not apply optional registration to Phase 11 exports. Add fake lookup tests for correct argument/result behavior and for each mandatory missing-symbol failure.
  </action>
  <verify>
    <automated>go test ./internal/ffi -run '^Test(ProbeABI|Bind|ElementGetBigInt|ParserNewConfigured|ParserLastErrorHasOffset|ImplementationSelection)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Probe lookup log contains only `pure_simdjson_get_abi_version`.
    - Full bind table contains all five mandatory new public symbols.
    - Each wrapper has a focused fake-function test and `runtime.KeepAlive` behavior matching adjacent methods.
    - Missing Phase 11 symbol errors include the exact symbol name.
    - No Phase 11 symbol is passed to `registerOptionalFunc`.
  </acceptance_criteria>
  <done>The FFI package can probe ABI independently and bind the complete Phase 11 surface with no partial mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject ABI 1.1 before full bind and cache only complete 1.2 libraries</name>
  <files>library_loading.go, library_loading_test.go, parser.go</files>
  <read_first>
    - `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-VALIDATION.md` task `11-W0-07`, threat T-11-03
    - `library_loading.go` double-checked loading/cache sequence
    - `library_loading_test.go` existing explicit path, cache, and ABI mismatch fixtures
    - `parser.go` current post-bind exact ABI comparison and constructor lifecycle
    - `errors.go` `wrapABIMismatch` and load-failure distinction
    - `internal/ffi/bindings.go` new `ProbeABI` and full `Bind`
  </read_first>
  <behavior>
    - ABI 1.1 fixture returns `ErrABIVersionMismatch` without a lookup for BigInt/config/kernel symbols.
    - ABI 1.2 complete fixture loads and caches.
    - ABI 1.2 missing one mandatory symbol returns load failure naming it and leaves cache nil.
    - A later additive 1.x value reaches full bind; another major fails mismatch.
  </behavior>
  <action>
    In `activeLibrary`, after `loadLibrary(path)`, call `ffi.ProbeABI` before `ffi.Bind`. Accept only ABI major 1 with value at least `0x00010002`; call `wrapABIMismatch(ffi.ABIVersion, actual, path)` for 1.1 or another major. Then run full `ffi.Bind`, read implementation name, and only afterward install `cachedLibrary`. Remove the redundant exact ABI query/check from `NewParser`; the loader now owns compatibility and supports later additive 1.x only when the wrapper's complete required symbols bind. Preserve the existing double-checked cache concurrency and explicit-path error wrapping. Add lookup-logging fixtures for 1.1, complete 1.2, incomplete 1.2, later 1.x, and wrong major.
  </action>
  <verify>
    <automated>go test . -run '^TestABI' -count=1 &amp;&amp; go test ./internal/ffi -run '^Test(ProbeABI|Bind)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - ABI 1.1 error matches `ErrABIVersionMismatch`, not generic load failure.
    - ABI 1.1 lookup log contains no Phase 11-only symbol.
    - Incomplete ABI 1.2 error names the missing symbol and `cachedLibrary` remains nil.
    - Complete ABI 1.2 caches only after all symbols and implementation name succeed.
    - No capability-gated legacy path exists.
  </acceptance_criteria>
  <done>The loader reports honest ABI mismatch/corruption states and never caches a partial Phase 11 artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dlopen library -> purego symbols | An explicit or downloaded artifact can report an old ABI or omit functions. |
| partially bound handle -> process cache | Caching incomplete bindings would make later failures nondeterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-03 | Tampering | ABI probe/full bind/cache | mitigate | Probe version first, require all 1.2 symbols, name missing functions, and cache only after complete success; exercise all three fixtures. |
| T-11-04 | Information Disclosure | BigInt binding copy | mitigate | Reuse the existing copy/free helper and KeepAlive pattern; never expose native BigInt storage. |
</threat_model>

<verification>
- Focused loader and binding tests pass.
- ABI 1.1, complete 1.2, and incomplete 1.2 have distinct asserted errors.
- Race-safe cache behavior remains green in the broader Go suite.
</verification>

<success_criteria>
- ABI 1.1 is rejected before new symbol lookup.
- ABI 1.2 requires the complete contract.
- No partial library enters the cache.
</success_criteria>

<output>
Create `.planning/phases/11-upstream-simdjson-refresh-bigint-and-diagnostics/11-08-SUMMARY.md` when done.
</output>
