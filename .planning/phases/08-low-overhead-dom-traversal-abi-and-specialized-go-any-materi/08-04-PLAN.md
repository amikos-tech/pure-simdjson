---
phase: 08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - benchmark_comparators_test.go
  - benchmark_diagnostics_test.go
autonomous: true
requirements: [D-01, D-05, D-14, D-15, D-16]
user_setup: []
must_haves:
  truths:
    - "Tier 1 pure-simdjson full and materialize-only benchmark rows use the fast materializer"
    - "Benchmark names remain stable for benchstat comparison against v0.1.1"
    - "The benchmark path rebuilds Go maps, slices, and strings on each materialization call"
  artifacts:
    - path: "benchmark_comparators_test.go"
      provides: "Tier 1 pure materializer routing"
      contains: "fastMaterializeElement"
    - path: "benchmark_diagnostics_test.go"
      provides: "diagnostic split preserving Phase 7 row names"
      contains: "BenchmarkTier1Diagnostics_"
  key_links:
    - from: "benchmark_comparators_test.go"
      to: "materializer_fastpath.go"
      via: "benchmarkMaterializePureElement delegates to fastMaterializeElement"
      pattern: "fastMaterializeElement"
    - from: "benchmark_diagnostics_test.go"
      to: "testdata/benchmark-results/v0.1.1/tier1-diagnostics.bench.txt"
      via: "stable benchmark names for benchstat"
      pattern: "pure-simdjson-materialize-only"
---

<objective>
Wire the internal fast materializer into Tier 1 benchmark paths.

Purpose: make Phase 8 diagnostics measure the new lower-overhead path without changing public API or public benchmark claim text.
Output: stable Tier 1 full/materialize-only rows routed to `fastMaterializeElement`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-RESEARCH.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md
@.planning/phases/07-benchmarks-v0.1-release/07-CONTEXT.md
@.planning/phases/07-benchmarks-v0.1-release/07-LEARNINGS.md
@docs/benchmarks.md
@docs/benchmarks/results-v0.1.1.md
@testdata/benchmark-results/v0.1.1/tier1-diagnostics.bench.txt

<interfaces>
Existing benchmark entrypoints:
```go
func benchmarkMaterializePureSimdjsonWithParser(parser *Parser, data []byte) (result any, err error)
func benchmarkMaterializePureElement(element Element) (any, error)
func runTier1DiagnosticsBenchmark(b *testing.B, fixtureName string)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route pure-simdjson Tier 1 materialization through the fast path</name>
  <files>benchmark_comparators_test.go</files>
  <read_first>
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md` decisions D-01, D-05, D-14, D-15, D-16
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md` threat ref T-08-04
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md` sections `benchmark_comparators_test.go`, `benchmark_diagnostics_test.go`, `benchmark_native_alloc_test.go`
    - `.planning/phases/07-benchmarks-v0.1-release/07-LEARNINGS.md`
    - `benchmark_comparators_test.go`
    - `materializer_fastpath.go`
  </read_first>
  <action>
    In `benchmark_comparators_test.go`, change the pure-simdjson Tier 1 materializer so `benchmarkMaterializePureElement(element Element) (any, error)` delegates to `fastMaterializeElement(element)`. Do not call public per-node accessors (`GetString`, `GetInt64`, `Array.Iter`, `Object.Iter`) from the benchmark materializer path after this change. Keep comparator names unchanged: `pure-simdjson`, `encoding-json-any`, `encoding-json-struct`, `bytedance-sonic`, `goccy-go-json`, and supported `minio-simdjson-go` rows must retain the existing registry keys.

    If retaining the old accessor-shaped recursive implementation is useful for tests or local comparison, rename it to `benchmarkMaterializePureElementViaAccessors` and ensure no `BenchmarkTier1FullParse_*` or `BenchmarkTier1Diagnostics_*` call path uses that renamed helper.
  </action>
  <acceptance_criteria>
    - `rg 'func benchmarkMaterializePureElement\\(element Element\\) \\(any, error\\)' benchmark_comparators_test.go` finds the benchmark helper.
    - `rg 'return fastMaterializeElement\\(element\\)' benchmark_comparators_test.go` finds the fast delegation.
    - `! rg 'benchmarkMaterializePureElement\\([^)]*\\).*\\{[\\s\\S]*(GetString|GetInt64|GetUint64|GetFloat64|Array\\.Iter|Object\\.Iter)' benchmark_comparators_test.go` succeeds, or a file read confirms the helper delegates directly.
    - `rg 'benchmarkComparatorPureSimdjson = "pure-simdjson"' benchmark_comparators_test.go` confirms the stable comparator key is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run 'TestFastMaterializer|TestJSONTestSuiteOracle' -count=1</automated>
  </verify>
  <done>Tier 1 pure-simdjson benchmark materialization uses the internal frame materializer while public comparator names stay stable.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve diagnostic row stability and no-cache benchmark semantics</name>
  <files>benchmark_diagnostics_test.go</files>
  <read_first>
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md` decisions D-15, D-16, D-17
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md` threat ref T-08-04 and Benchmark command
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md` sections `benchmark_diagnostics_test.go`, `benchmark_native_alloc_test.go`
    - `benchmark_diagnostics_test.go`
    - `benchmark_native_alloc_test.go`
    - `docs/benchmarks.md`
    - `testdata/benchmark-results/v0.1.1/tier1-diagnostics.bench.txt`
  </read_first>
  <action>
    Verify `benchmarkRunTier1DiagnosticsMaterializeOnly` still calls `benchmarkMaterializePureElement(root)` inside the `for i := 0; i < b.N; i++` loop and assigns the returned value to `benchmarkTier1Result` on every iteration. Add a short comment near that loop containing the exact text `Phase 8: rebuild Go maps/slices/strings on every materialize-only iteration; native frame scratch may be reused, final Go trees must not be cached.` Keep benchmark names and sub-benchmark labels unchanged: `pure-simdjson-full`, `pure-simdjson-parse-only`, `pure-simdjson-materialize-only`, `pure-simdjson-stage-input-reuse-model`, `pure-simdjson-stage-input-alloc-model`, and `encoding-json-any-full`.
  </action>
  <acceptance_criteria>
    - `rg 'Phase 8: rebuild Go maps/slices/strings on every materialize-only iteration' benchmark_diagnostics_test.go` finds the no-cache comment.
    - `rg 'for i := 0; i < b\\.N; i\\+\\+ \\{[[:space:]]*value, err := benchmarkMaterializePureElement\\(root\\)' benchmark_diagnostics_test.go` finds the materialize-only loop shape, or a file read confirms the call remains inside the loop.
    - `rg 'pure-simdjson-(full|parse-only|materialize-only|stage-input-reuse-model|stage-input-alloc-model)|encoding-json-any-full' benchmark_diagnostics_test.go` finds the stable labels.
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run '^$' -bench 'BenchmarkTier1Diagnostics_twitter_json/(pure-simdjson-full|pure-simdjson-materialize-only)$' -benchmem -benchtime=1x -count=1</automated>
  </verify>
  <done>Diagnostic benchmarks measure fresh Go tree construction through the fast materializer with Phase 7 row names preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark harness -> materializer | Benchmarks must call the implementation under test without caching final Go trees. |
| internal materializer -> public evidence | Diagnostic rows become Phase 8 evidence but do not change public release claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/Denial of Service | benchmark materializer call | mitigate | Consume only Go-owned return values from `fastMaterializeElement`; Plan 03 lifetime tests remain in the verification set. |
| T-08-02 | Information Disclosure/Tampering | public benchmark surface | accept | This plan changes benchmark code only, not headers or public API. |
| T-08-03 | Tampering | numeric benchmark values | mitigate | Reuse Plan 03 FastMaterializer numeric tests before running benchmarks. |
| T-08-04 | Denial of Service | `benchmark_diagnostics_test.go` | mitigate | Keep materialize-only call inside the benchmark loop and document that final Go trees are rebuilt each iteration. |
| T-08-05 | Denial of Service | platform FFI | accept | No FFI signatures are added in this plan. |
</threat_model>

<verification>
Run:
```sh
go test ./... -run 'TestFastMaterializer|TestJSONTestSuiteOracle' -count=1
go test ./... -run '^$' -bench 'BenchmarkTier1Diagnostics_twitter_json/(pure-simdjson-full|pure-simdjson-materialize-only)$' -benchmem -benchtime=1x -count=1
```
</verification>

<success_criteria>
- Tier 1 pure-simdjson benchmark materialization delegates to `fastMaterializeElement`.
- Diagnostic row names remain benchstat-compatible with `testdata/benchmark-results/v0.1.1/tier1-diagnostics.bench.txt`.
- No final Go tree cache is introduced.
- Public README/result repositioning remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-04-SUMMARY.md`.
</output>
