---
phase: 08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - internal/ffi/types.go
  - internal/ffi/types_test.go
  - internal/ffi/bindings.go
  - materializer_fastpath.go
  - materializer_fastpath_test.go
autonomous: true
requirements: [D-04, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15]
user_setup: []
must_haves:
  truths:
    - "Go can materialize a root or subtree into Go-owned `any` values through the internal frame stream"
    - "String values and object keys are copied exactly once at the Go value boundary"
    - "Numeric kind, duplicate key, preallocation, closed-doc, and lifetime semantics match the locked decisions"
  artifacts:
    - path: "materializer_fastpath.go"
      provides: "unexported fast materializer over internal frames"
      contains: "fastMaterializeElement"
    - path: "internal/ffi/types.go"
      provides: "Go mirror of `psdj_internal_frame_t`"
      contains: "type InternalFrame struct"
    - path: "internal/ffi/bindings.go"
      provides: "purego binding to `psdj_internal_materialize_build`"
      contains: "psdj_internal_materialize_build"
  key_links:
    - from: "materializer_fastpath.go"
      to: "internal/ffi/bindings.go"
      via: "InternalMaterializeBuild on Element.view"
      pattern: "InternalMaterializeBuild"
    - from: "materializer_fastpath.go"
      to: "Doc"
      via: "runtime.KeepAlive after final borrowed frame read"
      pattern: "runtime\\.KeepAlive\\(doc\\)"
---

<objective>
Implement the specialized Go `any` materializer over the internal frame stream.

Purpose: replace accessor-shaped recursive Tier 1 materialization with one internal frame handoff while preserving public DOM semantics and returning only Go-owned values.
Output: Go FFI frame mirror, internal binding, unexported materializer, active FastMaterializer tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-RESEARCH.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-02-SUMMARY.md
@.planning/phases/04-full-typed-accessor-surface/04-04-SUMMARY.md
@docs/ffi-contract.md

<interfaces>
Existing public `Element` lifecycle guard:
```go
func (e Element) usableDoc() (*Doc, error)
```

Existing binding pattern:
```go
func (b *Bindings) ElementGetString(view *ValueView) (string, int32)
```

Production function to add:
```go
func fastMaterializeElement(element Element) (any, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror and bind the internal frame ABI in Go</name>
  <files>internal/ffi/types.go, internal/ffi/types_test.go, internal/ffi/bindings.go</files>
  <read_first>
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md` decisions D-03, D-06 through D-10, D-12, D-13
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md` threat refs T-08-01, T-08-03, T-08-05
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md` sections `internal/ffi/types.go`, `internal/ffi/bindings.go`, `internal/ffi/types_test.go`
    - `internal/ffi/types.go`
    - `internal/ffi/bindings.go`
    - `src/lib.rs`
    - `src/native/simdjson_bridge.h`
  </read_first>
  <behavior>
    - Go frame layout matches Rust/C++ `psdj_internal_frame_t` exactly on 64-bit supported targets.
    - The internal binding returns a borrowed frame slice that is consumed immediately by `materializer_fastpath.go`.
    - The binding registers `psdj_internal_materialize_build` by exact symbol name and keeps it out of public Go API docs.
  </behavior>
  <action>
    In `internal/ffi/types.go`, add:
    `type InternalFrame struct { Kind uint32; Flags uint32; ChildCount uint32; Reserved uint32; KeyPtr uintptr; KeyLen uintptr; StringPtr uintptr; StringLen uintptr; Int64Value int64; Uint64Value uint64; Float64Value float64 }`.

    In `internal/ffi/types_test.go`, assert the exact 64-bit layout: `unsafe.Sizeof(InternalFrame{}) == 72`, offsets `Kind=0`, `Flags=4`, `ChildCount=8`, `Reserved=12`, `KeyPtr=16`, `KeyLen=24`, `StringPtr=32`, `StringLen=40`, `Int64Value=48`, `Uint64Value=56`, `Float64Value=64`.

    In `internal/ffi/bindings.go`, add a binding field with signature `func(*ValueView, **InternalFrame, *uintptr) int32`, register symbol `psdj_internal_materialize_build`, and add method `InternalMaterializeBuild(view *ValueView) ([]InternalFrame, int32)`. The method must call the symbol, use `unsafe.Slice(ptr, count)` only when status is OK and `count > 0`, keep `view` and `b` alive with `runtime.KeepAlive`, and return the borrowed frame slice without copying it.
  </action>
  <acceptance_criteria>
    - `rg 'type InternalFrame struct' internal/ffi/types.go` finds the frame mirror.
    - `rg 'unsafe\\.Sizeof\\(InternalFrame\\{\\}\\).*72|Float64Value.*64' internal/ffi/types_test.go` finds the layout assertions.
    - `rg 'psdj_internal_materialize_build' internal/ffi/bindings.go` finds the exact registered symbol.
    - `rg 'func \\(b \\*Bindings\\) InternalMaterializeBuild\\(view \\*ValueView\\) \\(\\[\\]InternalFrame, int32\\)' internal/ffi/bindings.go` finds the helper method.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/ffi -run 'TestInternalFrameLayout' -count=1 && go test ./... -run '^$' -count=1</automated>
  </verify>
  <done>Go can call the internal builder and validate the fixed frame layout before materialization uses it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the unexported fast materializer and activate tests</name>
  <files>materializer_fastpath.go, materializer_fastpath_test.go</files>
  <read_first>
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md` decisions D-04, D-06 through D-14
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md` threat refs T-08-01, T-08-03, T-08-04
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md` sections `materializer_fastpath.go`, `benchmark_comparators_test.go`, `element.go`, `iterator.go`, `parser.go`, `doc.go`
    - `materializer_fastpath_test.go`
    - `benchmark_comparators_test.go`
    - `element.go`
    - `iterator.go`
    - `parser.go`
    - `doc.go`
  </read_first>
  <behavior>
    - `fastMaterializeElement` accepts any live `Element`, including `doc.Root()` and descendant elements from `Object.GetField` or iterators.
    - It calls `element.usableDoc()` first and returns `ErrClosed` or `ErrInvalidHandle` before any internal FFI call when the public wrapper already knows the element is unusable.
    - It copies `KeyPtr/KeyLen` and `StringPtr/StringLen` into Go strings before returning; borrowed bytes never escape.
    - Arrays allocate `make([]any, 0, childCount)` and objects allocate `make(map[string]any, childCount)`.
    - Object duplicate keys use normal Go map assignment, so the last duplicate wins.
    - It preserves split numeric kinds using `Int64Value`, `Uint64Value`, and `Float64Value`; it does not convert all numbers to `float64`.
    - It calls `runtime.KeepAlive(doc)` after the final read from the borrowed frame slice.
    - It does not add `Element.Interface()`, `Doc.Interface()`, or any exported public materializer API.
  </behavior>
  <action>
    Create `materializer_fastpath.go` with unexported `func fastMaterializeElement(element Element) (any, error)`. Use `doc, err := element.usableDoc()` and `frames, rc := doc.parser.library.bindings.InternalMaterializeBuild(&element.view)`. Convert non-OK status with `wrapStatus(rc)`.

    Build the tree from preorder frames without making any further per-node public accessor calls. A small recursive helper may consume `frames[index]` and return `(any, nextIndex, error)`. For strings and keys, copy with `string(unsafe.Slice((*byte)(unsafe.Pointer(ptr)), length))` so returned Go values own memory. For zero-length borrowed values, return `""` without dereferencing nil. For arrays and objects, use `ChildCount` for capacity and consume exactly that many children. For objects, read the key from each child frame before materializing the child value and assign `values[key] = childValue`.

    Update `materializer_fastpath_test.go` from Plan 01 by removing `requireFastMaterializerLinkedForTest`, removing the single skip reason `fast materializer implementation not linked`, and routing all test cases to `fastMaterializeElement`. Add assertions that returned string values still compare equal after `doc.Close()` and `runtime.GC()`, that duplicate `{"dup":1,"dup":2}` materializes as `map[string]any{"dup": int64(2)}`, and that `Object.GetField("dup")` still returns the first value through the existing iterator test.
  </action>
  <acceptance_criteria>
    - `rg 'func fastMaterializeElement\\(element Element\\) \\(any, error\\)' materializer_fastpath.go` finds the production function.
    - `rg 'InternalMaterializeBuild\\(&element\\.view\\)' materializer_fastpath.go` finds the internal frame handoff.
    - `rg 'runtime\\.KeepAlive\\(doc\\)' materializer_fastpath.go` finds lifetime protection after frame reads.
    - `rg 'make\\(\\[\\]any, 0, int\\(frame\\.ChildCount\\)\\)|make\\(map\\[string\\]any, int\\(frame\\.ChildCount\\)\\)' materializer_fastpath.go` finds container preallocation from metadata.
    - `! rg 'Element\\.Interface|Doc\\.Interface|func \\(.*\\) Interface\\(' .` succeeds.
    - `! rg 'fast materializer implementation not linked|requireFastMaterializerLinkedForTest' materializer_fastpath_test.go` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -run 'TestFastMaterializer' -count=1 && go test ./... && cargo test -- --test-threads=1 && make verify-contract</automated>
  </verify>
  <done>Fast materializer tests pass against root and subtree materialization with Go-owned values and existing typed errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal frame slice -> Go values | Borrowed frame/key/string memory is read inside one call and copied before return. |
| public Element -> internal binding | Public wrapper state controls whether an internal FFI call is allowed. |
| internal symbol lookup -> Go package | Dynamic symbol is internal and unexported from public Go API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/Denial of Service | `materializer_fastpath.go` | mitigate | Copy keys/strings into Go values and call `runtime.KeepAlive(doc)` after borrowed frame reads. |
| T-08-02 | Information Disclosure/Tampering | Go public API | mitigate | Keep only unexported `fastMaterializeElement`; grep for absence of `Interface` methods. |
| T-08-03 | Tampering | numeric materialization | mitigate | Use frame `Kind` plus split numeric fields and run `TestFastMaterializerNumericSemantics`. |
| T-08-04 | Denial of Service | materializer benchmark path | mitigate | Build fresh maps/slices/strings on every call; do not cache final Go trees. |
| T-08-05 | Denial of Service | Go FFI frame layout | mitigate | Add `unsafe.Sizeof` and `unsafe.Offsetof` layout tests for `InternalFrame`. |
</threat_model>

<verification>
Run:
```sh
go test ./internal/ffi -run 'TestInternalFrameLayout' -count=1
go test ./... -run 'TestFastMaterializer' -count=1
go test ./...
cargo test -- --test-threads=1
make verify-contract
```
</verification>

<success_criteria>
- `fastMaterializeElement(doc.Root())` and `fastMaterializeElement(subtreeElement)` produce Go-owned values.
- FastMaterializer tests pass with no skip guard left in `materializer_fastpath_test.go`.
- Public accessor, iterator, `Object.GetField`, and string copy-out behavior remain unchanged.
- No public materializer API is added.
</success_criteria>

<output>
After completion, create `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-03-SUMMARY.md`.
</output>
