---
phase: 08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/lib.rs
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - cbindgen.toml
  - include/pure_simdjson.h
  - tests/rust_shim_fast_materializer.rs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14]
user_setup: []
must_haves:
  truths:
    - "Native code can traverse a document or subtree once and return a compact internal frame span"
    - "BIGINT traversal fails with precision-loss status before any partial frame span is emitted"
    - "Doc-owned frame scratch has deterministic reentrancy protection during native build"
    - "The generated public header remains free of internal traversal symbols"
  artifacts:
    - path: "src/lib.rs"
      provides: "internal `psdj_internal_materialize_build` Rust export through ffi_wrap"
      contains: "psdj_internal_materialize_build"
    - path: "src/native/simdjson_bridge.cpp"
      provides: "native DOM traversal into doc-owned internal frames"
      contains: "psdj_internal_frame_t"
    - path: "tests/rust_shim_fast_materializer.rs"
      provides: "Rust integration coverage for root, subtree, BIGINT, and reentrant frame streams"
      contains: "bigint_precision_loss"
  key_links:
    - from: "src/lib.rs"
      to: "src/runtime/registry.rs"
      via: "ffi_wrap around registry materialize builder"
      pattern: "psdj_internal_materialize_build"
    - from: "src/runtime/registry.rs"
      to: "src/native/simdjson_bridge.cpp"
      via: "validated doc/view json_index passed to native traversal"
      pattern: "native_materialize_build"
---

<objective>
Implement the internal native frame-stream foundation for Phase 8.

Purpose: remove per-node Go FFI crossings by letting native code walk a live DOM root or subtree once and return borrowed frame metadata that Go consumes during one materializer call.
Output: private `psdj_internal_*` export, Rust registry validation, private C++ traversal, cbindgen exclusion, and Rust integration tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-RESEARCH.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-REVIEWS.md
@.planning/phases/01-ffi-contract-design/01-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@docs/ffi-contract.md
@include/pure_simdjson.h

<interfaces>
Internal frame layout to implement exactly in Rust/C++:
```c
typedef struct psdj_internal_frame_t {
  uint32_t kind;
  uint32_t flags;
  uint32_t child_count;
  uint32_t reserved;
  const uint8_t *key_ptr;
  size_t key_len;
  const uint8_t *string_ptr;
  size_t string_len;
  int64_t int64_value;
  uint64_t uint64_value;
  double float64_value;
} psdj_internal_frame_t;
```

Internal export signature:
```c
pure_simdjson_error_code_t psdj_internal_materialize_build(
    const pure_simdjson_value_view_t *view,
    const psdj_internal_frame_t **out_frames,
    size_t *out_frame_count
);
```

Frame semantics:
- `kind` uses existing `PURE_SIMDJSON_VALUE_KIND_*` numeric values.
- Frames are preorder. Container frames set exact `child_count`.
- Object child frames carry their key inline in `key_ptr/key_len`; there are no separate key frames.
- Array child frames carry `key_ptr == NULL` and `key_len == 0`.
- String frames carry `string_ptr/string_len`; Go must copy before returning.
- Integral and float frames set only the matching numeric field.
- `reserved` must be zero on every frame.
- BIGINT-class values abort the whole build with `PURE_SIMDJSON_ERR_PRECISION_LOSS`; no partial frame span is returned.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the private native frame stream from validated DOM views</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, src/runtime/registry.rs</files>
  <read_first>
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md` decisions D-01 through D-13
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-REVIEWS.md` concerns for BIGINT handling, reentrancy, saturated child count, frame allocation discipline, pointer stability, and object key convention
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-RESEARCH.md` Summary and Architecture Patterns
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md` threat refs T-08-01, T-08-03, T-08-05
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md` sections `src/runtime/mod.rs`, `src/runtime/registry.rs`, `src/native/simdjson_bridge.h`, `src/native/simdjson_bridge.cpp`
    - `src/native/simdjson_bridge.h`
    - `src/native/simdjson_bridge.cpp`
    - `src/runtime/mod.rs`
    - `src/runtime/registry.rs`
    - `third_party/simdjson/doc/tape.md`
  </read_first>
  <behavior>
    - Build a repo-owned `psdj_internal_frame_t` stream from simdjson DOM traversal; do not expose raw tape words or raw tape pointers to Go.
    - Validate the incoming `pure_simdjson_value_view_t` once through existing registry handle/generation/tag rules before native traversal starts.
    - Support whole-document root views and descendant subtree views by accepting the existing `ValueView` transport.
    - Keep borrowed `key_ptr` and `string_ptr` valid only for the owning live `Doc` and the current native scratch span.
    - Fail BIGINT values with `PURE_SIMDJSON_ERR_PRECISION_LOSS` and clear the scratch vector before return.
    - Return `PURE_SIMDJSON_ERR_PARSER_BUSY` if native materialization is already building for the same doc.
  </behavior>
  <action>
    In `src/native/simdjson_bridge.h`, define `psdj_internal_frame_t` with the exact fields and order shown in this plan, then declare `psimdjson_materialize_build(const psimdjson_doc *doc, uint64_t json_index, const psdj_internal_frame_t **out_frames, size_t *out_frame_count)`.

    In `src/native/simdjson_bridge.cpp`, add `std::vector<psdj_internal_frame_t> materialize_frames;` and `bool materialize_in_progress = false;` to `psimdjson_doc`. Implement an RAII guard named `materialize_build_guard` or equivalent that returns `PURE_SIMDJSON_ERR_PARSER_BUSY` if `materialize_in_progress` is already true, sets it true during traversal, and always resets it before return.

    Implement `psimdjson_materialize_build` so it clears `materialize_frames`, recursively walks `element_at(doc, json_index)` once, appends preorder frames, sets exact `child_count` for arrays and objects, and sets object child keys inline in the value frame. If simdjson exposes a reliable immediate child count before child emission, call `materialize_frames.reserve(materialize_frames.size() + 1 + child_count)` at container entry; otherwise patch `child_count` after walking children and add the exact comment `Frame pointers are returned only after traversal completes; vector reallocations during build cannot invalidate an already-returned span.` before writing `*out_frames = materialize_frames.data()`.

    Map simdjson errors through `map_error`. BIGINT-class values must stop traversal immediately, clear `materialize_frames`, set `*out_frames = nullptr` and `*out_frame_count = 0` only inside the C++ bridge if those out-params were provided, and return `PURE_SIMDJSON_ERR_PRECISION_LOSS`. Do not silently coerce BIGINT to float or string. Do not emit error frames.

    In `src/runtime/mod.rs`, mirror `psdj_internal_frame_t` with `#[repr(C)]` and add `native_materialize_build(doc_ptr: usize, json_index: u64) -> Result<(*const psdj_internal_frame_t, usize), pure_simdjson_error_code_t>` over the private bridge declaration.

    In `src/runtime/registry.rs`, add `materialize_build(view: *const pure_simdjson_value_view_t)` that reuses `with_resolved_view` so forged roots, forged descendants, closed docs, stale handles, non-zero reserved bits, and unknown tags return the same existing typed error codes as public accessors. Return the frame pointer and count from `native_materialize_build` without allocating public handles or changing iterator leases.

    Addresses review concern: BIGINT/precision-loss behavior must be explicit and no partial frame emission is allowed. Addresses review concern: doc-owned scratch needs deterministic reentrancy protection. Addresses review concern: frame allocation and pointer stability need a reserve/comment discipline. Addresses review concern: object member keys must be inline on child frames.
  </action>
  <acceptance_criteria>
    - `rg 'typedef struct psdj_internal_frame_t' src/native/simdjson_bridge.h` finds the private C++ ABI type.
    - `rg 'std::vector<psdj_internal_frame_t> materialize_frames' src/native/simdjson_bridge.cpp` finds doc-owned frame scratch.
    - `rg 'materialize_in_progress|PURE_SIMDJSON_ERR_PARSER_BUSY|err_parser_busy' src/native/simdjson_bridge.cpp src/runtime/registry.rs` finds deterministic reentrancy protection.
    - `rg 'Frame pointers are returned only after traversal completes' src/native/simdjson_bridge.cpp` finds the pointer-stability comment.
    - `rg 'BIGINT|PRECISION_LOSS|ERR_PRECISION_LOSS' src/native/simdjson_bridge.cpp src/runtime/mod.rs tests/rust_shim_fast_materializer.rs` finds explicit precision-loss handling.
    - `rg 'psimdjson_materialize_build' src/native/simdjson_bridge.h src/native/simdjson_bridge.cpp src/runtime/mod.rs src/runtime/registry.rs` finds the native traversal path in all four files.
    - `rg 'with_resolved_view\\(view' src/runtime/registry.rs` finds the validation path used by the materialize builder.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -- --test-threads=1</automated>
  </verify>
  <done>Native traversal returns a doc-owned internal frame span for root and descendant views, fails BIGINT without partial frames, and rejects reentrant native builds deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export the internal builder and prove it stays out of the public header</name>
  <files>src/lib.rs, cbindgen.toml, include/pure_simdjson.h, tests/rust_shim_fast_materializer.rs</files>
  <read_first>
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-CONTEXT.md` decisions D-02, D-03, D-05, D-14
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-REVIEWS.md` concerns for public/private ABI leakage and BIGINT tests
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-VALIDATION.md` threat refs T-08-02, T-08-05
    - `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-PATTERNS.md` sections `src/lib.rs`, `tests/abi/check_header.py`, `cbindgen.toml`, `include/pure_simdjson.h`
    - `src/lib.rs`
    - `cbindgen.toml`
    - `include/pure_simdjson.h`
    - `tests/rust_shim_minimal.rs`
  </read_first>
  <behavior>
    - `psdj_internal_materialize_build` uses `ffi_wrap`.
    - Return type is `pure_simdjson_error_code_t`.
    - Results use pointer out-params only: `const psdj_internal_frame_t **out_frames`, `size_t *out_frame_count`.
    - Output pointers are written only on success; BIGINT and other errors leave no partial frame span visible to the caller.
    - No callback parameters, struct-by-value returns, or mixed scalar float/integer arguments are introduced.
    - `include/pure_simdjson.h` does not contain `psdj_internal_`, `psimdjson_`, or `psdj_internal_frame_t`.
  </behavior>
  <action>
    In `src/lib.rs`, expose `#[no_mangle] pub unsafe extern "C" fn psdj_internal_materialize_build(view: *const pure_simdjson_value_view_t, out_frames: *mut *const runtime::psdj_internal_frame_t, out_frame_count: *mut usize) -> pure_simdjson_error_code_t`. Use `ffi_wrap("psdj_internal_materialize_build", || unsafe { ... })`; reject null `out_frames` or null `out_frame_count` with `err_invalid_argument()`. Initialize local temporaries for the returned pointer/count, call `runtime::registry::materialize_build(view)`, and write both caller out-params only when the registry call returns `Ok`.

    In `cbindgen.toml`, add `psdj_internal_frame_t` and `psdj_internal_materialize_build` to the explicit `exclude` list. Regenerate `include/pure_simdjson.h` through the existing contract workflow; the public header must remain free of the internal frame type and internal function per D-03 and D-14.

    Add `tests/rust_shim_fast_materializer.rs` with integration tests that parse `{"a":[1,true,null,"x"],"n":18446744073709551615}` and call `psdj_internal_materialize_build` on the root and on the `a` descendant view. Assert frame counts, preorder kinds, exact child counts, inline object key lengths, string lengths, `uint64_value == 18446744073709551615`, and invalid-handle/closed-doc error propagation.

    Add a Rust integration test named `bigint_precision_loss_no_partial_frames` using JSON `{"ok":1,"big":99999999999999999999999}`. Assert `psdj_internal_materialize_build` returns `PURE_SIMDJSON_ERR_PRECISION_LOSS`, the Rust export does not write a non-null frame pointer on error, and a subsequent successful materialization on `{"ok":1}` returns exactly two frames.

    Add a Rust integration test named `materialize_build_reentrant_guard_is_present` that asserts the internal symbol path returns `PURE_SIMDJSON_ERR_PARSER_BUSY` from the guard branch through a deterministic test seam named `psdj_internal_test_hold_materialize_guard`. Keep that seam internal-only: add it to `src/lib.rs`, cbindgen excludes, and `tests/rust_shim_fast_materializer.rs`; it must validate the incoming view, set the same per-doc `materialize_in_progress` guard used by `psdj_internal_materialize_build`, attempt a nested materialize build, release the guard, and return the nested status. `include/pure_simdjson.h` must not contain this test seam.

    Addresses review concern: native traversal and Rust tests must pin BIGINT behavior and avoid partial frame emission. Addresses review concern: reentrant doc-owned scratch misuse should fail deterministically and have an executable guard test.
  </action>
  <acceptance_criteria>
    - `rg 'pub unsafe extern "C" fn psdj_internal_materialize_build' src/lib.rs` finds the Rust internal export.
    - `rg 'ffi_wrap\\("psdj_internal_materialize_build"' src/lib.rs` finds the panic boundary.
    - `rg '"psdj_internal_frame_t"|"psdj_internal_materialize_build"' cbindgen.toml` finds both exclude-list entries.
    - `! rg 'psdj_internal_|psimdjson_' include/pure_simdjson.h` succeeds.
    - `rg '18446744073709551615|Subtree|psdj_internal_materialize_build' tests/rust_shim_fast_materializer.rs` finds root/subtree numeric coverage.
    - `rg 'bigint_precision_loss_no_partial_frames|99999999999999999999999|PURE_SIMDJSON_ERR_PRECISION_LOSS' tests/rust_shim_fast_materializer.rs` finds BIGINT coverage.
    - `rg 'materialize_build_reentrant_guard_is_present|psdj_internal_test_hold_materialize_guard|PURE_SIMDJSON_ERR_PARSER_BUSY' tests/rust_shim_fast_materializer.rs src/lib.rs cbindgen.toml src/native/simdjson_bridge.cpp` finds reentrancy coverage and internal-only test seam wiring.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -- --test-threads=1 && make verify-contract</automated>
  </verify>
  <done>The internal builder is callable by dynamic symbol lookup, Rust-tested for root/subtree/BIGINT/reentrancy behavior, and absent from the public generated header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go/internal FFI -> Rust registry | Untrusted view structs cross into Rust and must be generation/tag/reserved-bit checked. |
| Rust registry -> C++ DOM | Validated doc pointer and json index cross into C++ traversal. |
| C++ borrowed bytes -> Go materializer | Frame key/string pointers remain borrowed and doc-lifetime-bound. |
| Internal export -> public header | Dynamic symbols used by Go must not become public C ABI promises. |
| Concurrent materialize calls -> doc-owned scratch | Reentrant native builds must not corrupt the shared scratch vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/Denial of Service | `psdj_internal_frame_t` borrowed pointers | mitigate | Store frames in doc-owned native scratch, validate views once through `with_resolved_view`, and leave copying to Plan 03 before values escape. |
| T-08-02 | Information Disclosure/Tampering | `include/pure_simdjson.h` | mitigate | Exclude `psdj_internal_*` from cbindgen and verify with `make verify-contract` plus Plan 01 forbidden-prefix audit. |
| T-08-03 | Tampering | numeric and BIGINT frame fields | mitigate | Emit exact `kind` tags plus split numeric fields; BIGINT aborts with `ERR_PRECISION_LOSS` and no partial frame span. |
| T-08-04 | Denial of Service | native frame scratch | mitigate | Add `materialize_in_progress` guard returning `ERR_PARSER_BUSY` for reentrant native builds and document pointer stability after traversal completion. |
| T-08-05 | Denial of Service | `psdj_internal_materialize_build` signature | mitigate | Use status return plus pointer out-params only, no callbacks, no struct-by-value return, and contract verification. |
</threat_model>

<verification>
Run:
```sh
cargo test -- --test-threads=1
make verify-contract
```
</verification>

<success_criteria>
- `psdj_internal_materialize_build` returns root and subtree frame spans in Rust integration tests.
- BIGINT traversal returns `PURE_SIMDJSON_ERR_PRECISION_LOSS` without exposing partial frames.
- Reentrant native materialization returns `PURE_SIMDJSON_ERR_PARSER_BUSY` through an executable test.
- `include/pure_simdjson.h` contains no `psdj_internal_` or `psimdjson_` symbols.
- Existing public accessor, iterator, and object lookup exports remain unchanged in `include/pure_simdjson.h`.
- No public Go convenience API is added.
</success_criteria>

<output>
After completion, create `.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-02-SUMMARY.md`.
</output>
