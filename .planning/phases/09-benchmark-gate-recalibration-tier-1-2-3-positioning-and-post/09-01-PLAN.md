---
phase: 09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bench/check_benchmark_claims.py
  - tests/bench/test_check_benchmark_claims.py
  - scripts/bench/prepare_stdlib_benchstat_inputs.py
  - tests/bench/test_prepare_stdlib_benchstat_inputs.py
  - scripts/bench/capture_release_snapshot.sh
  - .github/workflows/benchmark-capture.yml
  - testdata/benchmark-results/v0.1.2/.gitkeep
autonomous: true
requirements: [BENCH-01, BENCH-02, BENCH-03, BENCH-04, BENCH-05, BENCH-07]
user_setup: []
must_haves:
  truths:
    - "Claim allowances are generated from committed benchmark evidence, not hand-written docs copy"
    - "Tier 1 headline claims fail closed unless every published Tier 1 fixture beats encoding/json + any with benchstat-significant evidence"
    - "Tier 2 and Tier 3 claims require no material regression versus v0.1.1 and continued wins over encoding/json + struct"
    - "Same-snapshot stdlib benchstat inputs are normalized by a tested helper instead of brittle ad-hoc shell rewriting"
    - "A workflow_dispatch benchmark capture path exists for real linux/amd64 evidence"
    - "GitHub Actions artifacts are temporary transport while testdata/benchmark-results/v0.1.2 is the durable source"
  artifacts:
    - path: "scripts/bench/check_benchmark_claims.py"
      provides: "deterministic Phase 9 claim gate and summary.json generator"
      contains: "readme_mode"
    - path: "tests/bench/test_check_benchmark_claims.py"
      provides: "synthetic pass/fail coverage for claim-gate behavior"
      contains: "linux/amd64"
    - path: "scripts/bench/prepare_stdlib_benchstat_inputs.py"
      provides: "deterministic same-snapshot stdlib benchstat inputs for claim gating"
      contains: "--left-out"
    - path: "tests/bench/test_prepare_stdlib_benchstat_inputs.py"
      provides: "synthetic coverage for benchmark-name normalization and metadata preservation"
      contains: "encoding-json-any"
    - path: "scripts/bench/capture_release_snapshot.sh"
      provides: "single local/CI entrypoint for v0.1.2 benchmark capture commands"
      contains: "Benchmark(Tier1FullParse|Tier2Typed|Tier3SelectivePlaceholder)_"
    - path: ".github/workflows/benchmark-capture.yml"
      provides: "manual linux/amd64 capture workflow"
      contains: "workflow_dispatch"
    - path: "testdata/benchmark-results/v0.1.2/.gitkeep"
      provides: "release-scoped evidence directory"
      contains: "Phase 9 benchmark evidence directory"
  key_links:
    - from: ".github/workflows/benchmark-capture.yml"
      to: "scripts/bench/capture_release_snapshot.sh"
      via: "workflow run step"
      pattern: "bash scripts/bench/capture_release_snapshot.sh"
    - from: "scripts/bench/capture_release_snapshot.sh"
      to: "scripts/bench/prepare_stdlib_benchstat_inputs.py"
      via: "same-snapshot stdlib benchstat preparation"
      pattern: "prepare_stdlib_benchstat_inputs.py"
    - from: "scripts/bench/capture_release_snapshot.sh"
      to: "scripts/bench/check_benchmark_claims.py"
      via: "summary generation"
      pattern: "check_benchmark_claims.py --baseline-dir testdata/benchmark-results/v0.1.1 --snapshot-dir testdata/benchmark-results/v0.1.2 --snapshot v0.1.2 --require-target linux/amd64"
---

<objective>
Build the Phase 9 benchmark claim gate and linux/amd64 capture scaffolding.

Purpose: make unsupported benchmark wording mechanically difficult by generating claim allowances from benchmark evidence before public docs are edited.
Output: claim-gate script and tests, a tested same-snapshot benchstat normalization helper, capture script, dispatchable benchmark workflow, and a release-scoped evidence directory for `v0.1.2`.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-CONTEXT.md
@.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-RESEARCH.md
@.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-VALIDATION.md
@.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-PATTERNS.md
@.planning/phases/07-benchmarks-v0.1-release/07-LEARNINGS.md
@.planning/phases/08-low-overhead-dom-traversal-abi-and-specialized-go-any-materi/08-LEARNINGS.md
@docs/benchmarks.md
@docs/benchmarks/results-v0.1.1.md
@scripts/bench/run_benchstat.sh
@scripts/bench/check_phase8_tier1_improvement.py
@tests/bench/test_check_phase8_improvement.py
@benchmark_comparators_test.go
@benchmark_fullparse_test.go
@benchmark_diagnostics_test.go
@benchmark_coldstart_test.go
@benchmark_typed_test.go
@benchmark_selective_test.go
@.github/workflows/public-bootstrap-validation.yml
@.github/workflows/release.yml

<interfaces>
Existing benchmark family and comparator names:
```go
const benchmarkComparatorPureSimdjson = "pure-simdjson"
const benchmarkComparatorEncodingAny = "encoding-json-any"
const benchmarkComparatorEncodingStruct = "encoding-json-struct"
const benchmarkComparatorMinioSimdjson = "minio-simdjson-go"
const benchmarkComparatorBytedanceSonic = "bytedance-sonic"
const benchmarkComparatorGoccyGoJSON = "goccy-go-json"

func BenchmarkTier1FullParse_twitter_json(b *testing.B)
func BenchmarkTier1FullParse_citm_catalog_json(b *testing.B)
func BenchmarkTier1FullParse_canada_json(b *testing.B)
func BenchmarkTier2Typed_twitter_json(b *testing.B)
func BenchmarkTier2Typed_citm_catalog_json(b *testing.B)
func BenchmarkTier2Typed_canada_json(b *testing.B)
func BenchmarkTier3SelectivePlaceholder_twitter_json(b *testing.B)
func BenchmarkTier3SelectivePlaceholder_citm_catalog_json(b *testing.B)
func BenchmarkColdStart_twitter_json(b *testing.B)
func BenchmarkWarm_twitter_json(b *testing.B)
func BenchmarkTier1Diagnostics_twitter_json(b *testing.B)
```

Mandatory Phase 9 commands:
```sh
python3 tests/bench/test_check_benchmark_claims.py
go test ./... -run 'TestTierNComparatorsAgree|TestJSONTestSuiteOracle' -count=1
go test ./... && cargo test -- --test-threads=1 && make verify-contract && python3 tests/bench/test_check_benchmark_claims.py
go test ./... -run '^$' -bench 'Benchmark(Tier1FullParse|Tier2Typed|Tier3SelectivePlaceholder)_' -benchmem -count=10 -timeout 1200s > testdata/benchmark-results/v0.1.2/phase9.bench.txt
go test ./... -run '^$' -bench 'Benchmark(ColdStart|Warm)_' -benchmem -count=10 -timeout 1200s > testdata/benchmark-results/v0.1.2/coldwarm.bench.txt
go test ./... -run '^$' -bench 'BenchmarkTier1Diagnostics_' -benchmem -count=10 -timeout 1200s > testdata/benchmark-results/v0.1.2/tier1-diagnostics.bench.txt
scripts/bench/run_benchstat.sh --old testdata/benchmark-results/v0.1.1/phase7.bench.txt --new testdata/benchmark-results/v0.1.2/phase9.bench.txt > testdata/benchmark-results/v0.1.2/phase9.benchstat.txt
python3 scripts/bench/check_benchmark_claims.py --baseline-dir testdata/benchmark-results/v0.1.1 --snapshot-dir testdata/benchmark-results/v0.1.2 --snapshot v0.1.2 --require-target linux/amd64 > testdata/benchmark-results/v0.1.2/summary.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the benchmark claim gate and synthetic tests</name>
  <files>scripts/bench/check_benchmark_claims.py, tests/bench/test_check_benchmark_claims.py</files>
  <read_first>
    - `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-CONTEXT.md` decisions D-01 through D-22, especially D-12, D-19, D-20, D-21, and D-22
    - `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-RESEARCH.md` Pattern 1, Pattern 2, and "Don't Hand-Roll"
    - `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-VALIDATION.md` Wave 0 Requirements and Threat Model
    - `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-PATTERNS.md` assignments for `scripts/bench/check_benchmark_claims.py` and `tests/bench/test_check_benchmark_claims.py`
    - `scripts/bench/check_phase8_tier1_improvement.py`
    - `tests/bench/test_check_phase8_improvement.py`
    - `testdata/benchmark-results/v0.1.1/phase7.bench.txt`
    - `testdata/benchmark-results/v0.1.1/coldwarm.bench.txt`
    - `testdata/benchmark-results/v0.1.1/tier1-diagnostics.bench.txt`
  </read_first>
  <behavior>
    - Test 1: A synthetic linux/amd64 snapshot with complete Tier 1/2/3 rows, no regressions, and Tier 1 pure-simdjson faster than `encoding-json-any` emits JSON with `claims.tier1_headline_allowed: true`, `claims.readme_mode: "tier1_headline"`, and exits 0.
    - Test 2: A synthetic linux/amd64 snapshot where Tier 1 improved versus v0.1.1 but does not beat every `encoding-json-any` row emits `claims.tier1_headline_allowed: false`, `claims.readme_mode: "tier1_improved_but_tier2_tier3_headline"`, and exits 0.
    - Test 3: A synthetic linux/amd64 snapshot with complete evidence but at least one noisy or non-significant stdlib comparison emits `claims.readme_mode: "conservative_current_strengths"`, keeps `errors` empty, and exits 0.
    - Test 4: Missing required Tier 1, Tier 2, Tier 3, cold/warm, diagnostic, metadata, or benchstat files exits nonzero with a machine-readable `errors` entry.
    - Test 5: `--require-target linux/amd64` rejects metadata where `goos` is not `linux` or `goarch` is not `amd64`.
    - Test 6: Malformed benchmark rows, missing `ns/op`, and nonnumeric `ns/op` exit nonzero instead of producing claim allowances.
    - Test 7: Tier 2 or Tier 3 regression versus `testdata/benchmark-results/v0.1.1/phase7.bench.txt` sets the relevant claim boolean to false and exits nonzero.
  </behavior>
  <action>
    Implement the machine gate for D-01, D-02, D-05, D-06, D-07, D-08, D-12, D-19, D-20, D-21, and D-22: full benchmark evidence is parsed, public positioning is compared against current comparators, README/doc claim allowances are generated, wrong-target data fails, noisy headline evidence fails closed, and output includes per-fixture statuses rather than a single pass/fail bit.

    Implement `scripts/bench/check_benchmark_claims.py` using only the Python standard library. The script MUST accept exactly these public CLI options: `--baseline-dir <path>`, `--snapshot-dir <path>`, `--snapshot <label>`, and `--require-target <goos/goarch>`. The Phase 9 default invocation is `python3 scripts/bench/check_benchmark_claims.py --baseline-dir testdata/benchmark-results/v0.1.1 --snapshot-dir testdata/benchmark-results/v0.1.2 --snapshot v0.1.2 --require-target linux/amd64`.

    Read snapshot files from `--snapshot-dir`: `phase9.bench.txt`, `coldwarm.bench.txt`, `tier1-diagnostics.bench.txt`, `phase9.benchstat.txt`, `coldwarm.benchstat.txt`, `tier1-diagnostics.benchstat.txt`, `tier1-vs-stdlib.benchstat.txt`, `tier2-vs-stdlib.benchstat.txt`, `tier3-vs-stdlib.benchstat.txt`, and `metadata.json`. Read baseline files from `--baseline-dir`: `phase7.bench.txt`, `coldwarm.bench.txt`, and `tier1-diagnostics.bench.txt`.

    Parse raw Go benchmark files by metadata lines `goos:`, `goarch:`, `pkg:`, and `cpu:` plus benchmark rows matching `^(Benchmark[^\\s]+)-\\d+\\s+(.*)$`. Extract `ns/op`, `B/op`, `allocs/op`, `native-bytes/op`, `native-allocs/op`, and `native-live-bytes` by scanning metric labels, not fixed indexes. Preserve samples per base row without the trailing `-<procs>` suffix.

    Require these exact rows in `phase9.bench.txt`: Tier 1 fixtures `twitter_json`, `citm_catalog_json`, and `canada_json` for `pure-simdjson` and `encoding-json-any`; Tier 2 fixtures `twitter_json`, `citm_catalog_json`, and `canada_json` for `pure-simdjson` and `encoding-json-struct`; Tier 3 fixtures `twitter_json` and `citm_catalog_json` for `pure-simdjson` and `encoding-json-struct`. Require `BenchmarkColdStart_*` and `BenchmarkWarm_*` rows in `coldwarm.bench.txt` for `twitter_json`, `citm_catalog_json`, and `canada_json`. Require diagnostic rows `pure-simdjson-full`, `pure-simdjson-parse-only`, `pure-simdjson-materialize-only`, and `encoding-json-any-full` for the same three Tier 1 fixtures.

    Use `metadata.json` for target/toolchain metadata. Require keys `snapshot`, `goos`, `goarch`, `pkg`, `cpu`, `go_version`, `rustc_version`, `commit`, `runner_os`, `runner_arch`, `captured_at_utc`, and `commands`. Also verify raw benchmark metadata agrees with `metadata.json` for `goos`, `goarch`, `pkg`, and `cpu`. For `--require-target linux/amd64`, require `goos == "linux"` and `goarch == "amd64"`.

    Emit JSON to stdout with top-level keys exactly: `snapshot`, `target`, `thresholds`, `claims`, `fixtures`, and `errors`. `thresholds.tier1_headline` MUST be the string `benchstat_significant_win_vs_encoding_json_any_every_fixture`; `thresholds.tier2_tier3` MUST be `no_material_regression_vs_v0.1.1_and_win_vs_encoding_json_struct_every_fixture`. `claims` MUST include booleans `tier1_headline_allowed`, `tier2_headline_allowed`, `tier3_headline_allowed`, plus `readme_mode`. Allowed `readme_mode` values are `tier1_headline`, `tier1_improved_but_tier2_tier3_headline`, and `conservative_current_strengths`.

    Treat `tier1_headline`, `tier1_improved_but_tier2_tier3_headline`, and `conservative_current_strengths` as the only exit-0 publishable outcomes when `errors` is empty. `tier1_improved_but_tier2_tier3_headline` means Tier 1 improved materially after Phase 8 but failed the all-fixtures stdlib headline gate while Tier 2 and Tier 3 still qualify for public headline language. `conservative_current_strengths` means the evidence is complete and target-correct, but at least one public headline allowance is false because the stdlib comparison is noisy or non-significant; this mode MUST still exit 0 so Plan 09-03 can publish conservative wording. Reserve nonzero exit codes for malformed or missing evidence, wrong-target metadata, or material Tier 2/Tier 3 regression versus `v0.1.1`.

    For D-19, only set `tier1_headline_allowed: true` if every published Tier 1 fixture has pure-simdjson median `ns/op` lower than `encoding-json-any` in `phase9.bench.txt` AND `tier1-vs-stdlib.benchstat.txt` contains a significant improvement marker for those normalized rows. If the benchstat comparison is missing, noisy, or not significant, set `tier1_headline_allowed: false`; if the rest of the evidence is usable, continue with a conservative `readme_mode` rather than inventing a headline.

    For D-20, only set `tier2_headline_allowed` and `tier3_headline_allowed` true when each published pure-simdjson row has no material regression versus the same row in `testdata/benchmark-results/v0.1.1/phase7.bench.txt` and wins over `encoding-json-struct` in `phase9.bench.txt`; also require significant win markers from `tier2-vs-stdlib.benchstat.txt` and `tier3-vs-stdlib.benchstat.txt`. Treat missing/noisy benchstat evidence as false.

    When snapshot-only rows exist for named third-party comparators that were omitted from the `v0.1.1` public baseline, treat them as informational data for docs rather than malformed evidence. Regression decisions are only against the prior public pure-simdjson rows and the stdlib comparator rows required for claim gating.

    Create `tests/bench/test_check_benchmark_claims.py` with synthetic temp directories and subprocess invocation of `scripts/bench/check_benchmark_claims.py`. The tests MUST write all required synthetic raw files and benchstat files directly into temp baseline/snapshot directories, then assert exit codes and JSON keys. Do not import private functions from the script in tests; cover the CLI.
  </action>
  <acceptance_criteria>
    - `test -f scripts/bench/check_benchmark_claims.py` succeeds.
    - `test -f tests/bench/test_check_benchmark_claims.py` succeeds.
    - `rg 'baseline-dir|snapshot-dir|require-target|readme_mode|tier1_headline_allowed|tier2_headline_allowed|tier3_headline_allowed' scripts/bench/check_benchmark_claims.py tests/bench/test_check_benchmark_claims.py` finds the required CLI and JSON keys.
    - `rg 'metadata\\.json|go_version|rustc_version|captured_at_utc|runner_os|runner_arch' scripts/bench/check_benchmark_claims.py tests/bench/test_check_benchmark_claims.py` finds target/toolchain metadata coverage.
    - `rg 'BenchmarkTier1FullParse_|BenchmarkTier2Typed_|BenchmarkTier3SelectivePlaceholder_|BenchmarkColdStart_|BenchmarkWarm_|BenchmarkTier1Diagnostics_' scripts/bench/check_benchmark_claims.py tests/bench/test_check_benchmark_claims.py` finds all benchmark families.
    - `rg 'tier1-vs-stdlib\\.benchstat\\.txt|tier2-vs-stdlib\\.benchstat\\.txt|tier3-vs-stdlib\\.benchstat\\.txt|phase9\\.benchstat\\.txt' scripts/bench/check_benchmark_claims.py tests/bench/test_check_benchmark_claims.py` finds required benchstat evidence filenames.
    - `python3 tests/bench/test_check_benchmark_claims.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 tests/bench/test_check_benchmark_claims.py</automated>
  </verify>
  <done>The claim gate emits deterministic summary JSON, rejects wrong-target or incomplete evidence, and unit tests cover allowed and conservative claim modes.</done>
</task>

<task type="auto">
  <name>Task 2: Add release-snapshot capture script, normalized benchstat helper, and linux/amd64 workflow</name>
  <files>scripts/bench/prepare_stdlib_benchstat_inputs.py, tests/bench/test_prepare_stdlib_benchstat_inputs.py, scripts/bench/capture_release_snapshot.sh, .github/workflows/benchmark-capture.yml, testdata/benchmark-results/v0.1.2/.gitkeep</files>
  <read_first>
    - `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-CONTEXT.md` decisions D-01, D-03, D-04, D-10, D-11, D-12, D-14, D-16, and D-22
    - `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-PATTERNS.md` workflow and capture script assignments
    - `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-VALIDATION.md` Benchmark capture command, Cold/warm capture command, Diagnostic capture command, and Claim gate command
    - `scripts/bench/run_benchstat.sh`
    - `.github/workflows/public-bootstrap-validation.yml`
    - `.github/workflows/release.yml`
    - `go.mod`
    - `rust-toolchain.toml`
  </read_first>
  <action>
    Implement capture scaffolding for D-03, D-04, D-09, D-10, D-11, D-12, D-14, D-16, and D-22: real linux/amd64 capture is dispatchable before release tagging, evidence paths use the `v0.1.2` release-scoped label, Actions artifacts are transport only, and durable raw/benchstat/summary outputs land under `testdata/benchmark-results/v0.1.2/`.

    Create `testdata/benchmark-results/v0.1.2/.gitkeep` containing the exact text `Phase 9 benchmark evidence directory for v0.1.2.`.

    Create `scripts/bench/prepare_stdlib_benchstat_inputs.py` plus `tests/bench/test_prepare_stdlib_benchstat_inputs.py`. The helper MUST accept exactly these public CLI flags: `--source <bench.txt>`, `--family <tier1|tier2|tier3>`, `--base-comparator <name>`, `--candidate-comparator <name>`, `--left-out <path>`, and `--right-out <path>`. It MUST preserve metadata header lines, copy only the selected comparator rows, strip comparator suffixes so both output files share identical benchmark names, and fail closed if any expected fixture row is missing. Use this helper instead of ad-hoc `sed` or `awk` rewriting so the same-snapshot normalization is deterministic and testable.

    Create `scripts/bench/capture_release_snapshot.sh` as a thin bash entrypoint with `set -euo pipefail`. It MUST accept `--snapshot <label>` and `--out-dir <path>`, defaulting to `v0.1.2` and `testdata/benchmark-results/v0.1.2`. It MUST reject snapshot labels that do not match `^v[0-9]+\\.[0-9]+\\.[0-9]+([-.][A-Za-z0-9.-]+)?$`. It MUST stage work in a sibling temporary directory, not write directly into the final `--out-dir`, and write `metadata.json` with `snapshot`, `goos`, `goarch`, `pkg`, `cpu`, `go_version`, `rustc_version`, `commit`, `runner_os`, `runner_arch`, `captured_at_utc`, and `commands`. For local runs where GitHub Actions variables are absent, populate `runner_os` from `uname -s` and `runner_arch` from `uname -m`; for workflow runs use `RUNNER_OS` and `RUNNER_ARCH`.

    The capture script MUST run these benchmark commands exactly with the output path substituted from `--out-dir`:
    `go test ./... -run '^$' -bench 'Benchmark(Tier1FullParse|Tier2Typed|Tier3SelectivePlaceholder)_' -benchmem -count=10 -timeout 1200s > testdata/benchmark-results/v0.1.2/phase9.bench.txt`
    `go test ./... -run '^$' -bench 'Benchmark(ColdStart|Warm)_' -benchmem -count=10 -timeout 1200s > testdata/benchmark-results/v0.1.2/coldwarm.bench.txt`
    `go test ./... -run '^$' -bench 'BenchmarkTier1Diagnostics_' -benchmem -count=10 -timeout 1200s > testdata/benchmark-results/v0.1.2/tier1-diagnostics.bench.txt`

    The capture script MUST run benchstat comparisons and write these files: `phase9.benchstat.txt`, `coldwarm.benchstat.txt`, `tier1-diagnostics.benchstat.txt`, `tier1-vs-stdlib.benchstat.txt`, `tier2-vs-stdlib.benchstat.txt`, and `tier3-vs-stdlib.benchstat.txt`. Use `scripts/bench/run_benchstat.sh --old testdata/benchmark-results/v0.1.1/phase7.bench.txt --new testdata/benchmark-results/v0.1.2/phase9.bench.txt > testdata/benchmark-results/v0.1.2/phase9.benchstat.txt` exactly for the main old/new comparison.

    For same-snapshot stdlib comparisons, use `scripts/bench/prepare_stdlib_benchstat_inputs.py` to create two deterministic files per tier: a base file containing only the stdlib comparator rows renamed to shared benchmark names, and a candidate file containing only the `pure-simdjson` rows renamed to those same benchmark names. Worked example for Tier 1: write one temporary file with only `BenchmarkTier1FullParse_{fixture}/encoding-json-any` rows renamed to `BenchmarkTier1FullParse_{fixture}` and another with only `BenchmarkTier1FullParse_{fixture}/pure-simdjson` rows renamed identically, then run `scripts/bench/run_benchstat.sh --old <tier1-base> --new <tier1-candidate> > testdata/benchmark-results/v0.1.2/tier1-vs-stdlib.benchstat.txt`. Repeat the same two-file shape for Tier 2 and Tier 3 using `encoding-json-struct` as the base comparator. The helper outputs may live under the staging directory or a dedicated `mktemp` directory, but the committed snapshot must contain only the benchstat outputs, not the temporary normalized inputs.

    The final capture script command MUST be:
    `python3 scripts/bench/check_benchmark_claims.py --baseline-dir testdata/benchmark-results/v0.1.1 --snapshot-dir testdata/benchmark-results/v0.1.2 --snapshot v0.1.2 --require-target linux/amd64 > testdata/benchmark-results/v0.1.2/summary.json`
    using the configured `--out-dir` and `--snapshot` values. If the claim gate exits nonzero after a complete snapshot is staged, promote the fully populated snapshot directory into the final `--out-dir` for diagnosis and exit nonzero. If the capture is interrupted or any pre-summary step fails before the snapshot is complete, keep the partial temp directory isolated (or delete it) and leave the prior `--out-dir` untouched.

    Create `.github/workflows/benchmark-capture.yml` with `name: benchmark capture`, `on.workflow_dispatch.inputs.snapshot`, `concurrency.group: benchmark-capture-${{ inputs.snapshot }}`, `cancel-in-progress: false`, and permissions limited to `contents: read` and `actions: read`. Define one job on `runs-on: ubuntu-latest` with bash defaults. Steps must checkout recursively, set up Go using `go-version-file: go.mod`, install the Rust toolchain from `rust-toolchain.toml` using the existing project pattern, install `benchstat` deterministically with `go install golang.org/x/perf/cmd/benchstat@latest` and add `$(go env GOPATH)/bin` (or the explicit install target) to `PATH` for later steps, build the native release library with `cargo build --release`, run `go test ./... -run 'TestTierNComparatorsAgree|TestJSONTestSuiteOracle' -count=1`, run `bash scripts/bench/capture_release_snapshot.sh --snapshot "${{ inputs.snapshot }}" --out-dir "testdata/benchmark-results/${{ inputs.snapshot }}"`, and upload `testdata/benchmark-results/${{ inputs.snapshot }}/` using `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` with artifact name `benchmark-evidence-${{ inputs.snapshot }}-linux-amd64`, `if-no-files-found: error`, and `retention-days: 30`.

    Do not add GitHub Pages, `benchmark-action/github-action-benchmark`, `pages: write`, `id-token: write`, release publication steps, tag pushes, checksum/version rewrites, parser API changes, materializer changes, or benchmark semantic changes in this plan.
  </action>
  <acceptance_criteria>
    - `test -f scripts/bench/prepare_stdlib_benchstat_inputs.py` succeeds.
    - `test -f tests/bench/test_prepare_stdlib_benchstat_inputs.py` succeeds.
    - `test -f scripts/bench/capture_release_snapshot.sh` succeeds.
    - `test -f .github/workflows/benchmark-capture.yml` succeeds.
    - `test -f testdata/benchmark-results/v0.1.2/.gitkeep` succeeds.
    - `rg 'prepare_stdlib_benchstat_inputs\\.py|--left-out|--right-out|encoding-json-any|encoding-json-struct' scripts/bench/prepare_stdlib_benchstat_inputs.py tests/bench/test_prepare_stdlib_benchstat_inputs.py scripts/bench/capture_release_snapshot.sh` finds the helper interface and comparator normalization rules.
    - `rg \"Benchmark\\(Tier1FullParse\\|Tier2Typed\\|Tier3SelectivePlaceholder\\)_|Benchmark\\(ColdStart\\|Warm\\)_|BenchmarkTier1Diagnostics_\" scripts/bench/capture_release_snapshot.sh .github/workflows/benchmark-capture.yml` finds all capture families.
    - `rg 'phase9\\.bench\\.txt|coldwarm\\.bench\\.txt|tier1-diagnostics\\.bench\\.txt|summary\\.json|metadata\\.json' scripts/bench/capture_release_snapshot.sh .github/workflows/benchmark-capture.yml` finds required evidence outputs.
    - `rg 'phase9\\.benchstat\\.txt|coldwarm\\.benchstat\\.txt|tier1-diagnostics\\.benchstat\\.txt|tier1-vs-stdlib\\.benchstat\\.txt|tier2-vs-stdlib\\.benchstat\\.txt|tier3-vs-stdlib\\.benchstat\\.txt' scripts/bench/capture_release_snapshot.sh` finds all benchstat outputs.
    - `rg 'runner_os|runner_arch|uname -s|uname -m|mktemp|\\.tmp' scripts/bench/capture_release_snapshot.sh` finds local metadata defaults and atomic staging behavior.
    - `rg 'workflow_dispatch|runs-on: ubuntu-latest|contents: read|actions: read|go install golang.org/x/perf/cmd/benchstat@latest|GOPATH|actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02|retention-days: 30' .github/workflows/benchmark-capture.yml` finds dispatch, target, permissions, benchstat provisioning, upload action, and retention.
    - `! rg 'pages: write|id-token: write|github-action-benchmark|git tag|git push|check_readiness|release.yml' .github/workflows/benchmark-capture.yml scripts/bench/capture_release_snapshot.sh` succeeds.
    - `python3 tests/bench/test_prepare_stdlib_benchstat_inputs.py` exits 0.
    - `python3 tests/bench/test_check_benchmark_claims.py` exits 0.
    - `go test ./... -run 'TestTierNComparatorsAgree|TestJSONTestSuiteOracle' -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 tests/bench/test_prepare_stdlib_benchstat_inputs.py && python3 tests/bench/test_check_benchmark_claims.py && go test ./... -run 'TestTierNComparatorsAgree|TestJSONTestSuiteOracle' -count=1</automated>
  </verify>
  <done>Phase 9 has a tested claim gate, a tested same-snapshot benchstat normalization helper, atomic release-snapshot capture scaffolding, a release-scoped evidence directory, and a least-privilege workflow that can transport real linux/amd64 benchmark artifacts for later commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw benchmark files -> claim gate | Untrusted benchmark output is parsed into public claim allowances. |
| workflow runner -> repository docs | Workflow output is temporary transport and must be committed before it becomes durable evidence. |
| workflow token -> repository | GitHub Actions token permissions must not exceed capture needs. |
| snapshot CLI input -> filesystem path | Snapshot labels and output paths can select where evidence is written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering/Repudiation | `scripts/bench/check_benchmark_claims.py` | mitigate | Validate required files, benchmark rows, metadata, target, and benchstat evidence; emit `errors` and fail closed on incomplete/noisy data. |
| T-09-02 | Tampering | `scripts/bench/capture_release_snapshot.sh` | mitigate | Reject invalid snapshot labels, stage writes in a temp directory before replacing the final snapshot path, and use exact benchmark commands without changing benchmark semantics. |
| T-09-03 | Information Disclosure/Tampering | `.github/workflows/benchmark-capture.yml` token scope | mitigate | Use only `contents: read` and `actions: read`; no Pages, id-token, release, or write permissions. |
| T-09-04 | Repudiation | GitHub Actions artifacts | mitigate | Upload artifacts with 30-day retention as transport only and require committed `testdata/benchmark-results/v0.1.2` evidence in Plan 09-02. |
| T-09-05 | Spoofing | target metadata | mitigate | Require `--require-target linux/amd64`, metadata.json, and raw `goos/goarch/pkg/cpu` agreement before summary generation. |
</threat_model>

<verification>
Run:
```sh
python3 tests/bench/test_prepare_stdlib_benchstat_inputs.py
python3 tests/bench/test_check_benchmark_claims.py
go test ./... -run 'TestTierNComparatorsAgree|TestJSONTestSuiteOracle' -count=1
```
</verification>

<success_criteria>
- Claim gate tests pass.
- Same-snapshot stdlib normalization helper and tests pass.
- Capture script contains the exact Phase 9 benchmark and summary commands.
- Benchmark workflow is dispatchable on `ubuntu-latest`, least privilege, and uploads artifacts only as temporary transport.
- `testdata/benchmark-results/v0.1.2/.gitkeep` exists.
- No public docs or release publishing behavior changed in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/09-benchmark-gate-recalibration-tier-1-2-3-positioning-and-post/09-01-SUMMARY.md`
</output>
