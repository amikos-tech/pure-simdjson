---
phase: 06-ci-release-matrix-platform-coverage
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - .github/workflows/release-prepare.yml
  - .github/workflows/release.yml
autonomous: true
requirements:
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - CI-02
user_setup: []
must_haves:
  truths:
    - "Both macOS targets build on native runners, inherit the shared reproducibility controls from Plan 06-01, and run ad-hoc codesign before packaging"
    - "The macOS artifacts remain thin per-arch; the workflow does not use lipo or produce a universal dylib"
    - "The windows artifact is built with the MSVC toolchain, long paths enabled, `/Brepro` deterministic link flags, and renamed to the bootstrap/GitHub contract names during packaging"
    - "macOS and windows jobs both run an explicit export/smoke gate before the artifact is considered releasable"
  artifacts:
    - path: ".github/workflows/release-prepare.yml"
      provides: "pre-tag darwin + windows jobs"
      contains: "macos-15-intel"
    - path: ".github/workflows/release.yml"
      provides: "tag-publish darwin + windows jobs"
      contains: "windows-latest"
  key_links:
    - from: ".github/workflows/release-prepare.yml"
      to: "internal/bootstrap/url.go"
      via: "windows public names must match bootstrap asset expectations"
      pattern: "pure_simdjson-windows-amd64-msvc.dll"
    - from: ".github/workflows/release.yml"
      to: "tests/smoke/minimal_parse.c"
      via: "native smoke must run before publish on macOS and Windows"
      pattern: "minimal_parse.c"
    - from: ".github/workflows/release-prepare.yml"
      to: ".github/actions/build-shared-library/action.yml"
      via: "darwin and windows prep jobs must use the shared deterministic build action and upload their packaged outputs for staged-tree assembly"
      pattern: "verify_reproducible: true"
---

<objective>
Implement the macOS and Windows release side of Phase 6: native runner builds, ad-hoc codesign on macOS, MSVC + long-path handling on Windows, and explicit export / smoke verification before packaging.

Purpose: close the non-linux platform matrix and lock the platform-specific release details the bootstrap pipeline depends on.
Output: darwin and windows jobs in both workflows, with packaging and verification aligned to the bootstrap naming contract.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ci-release-matrix-platform-coverage/06-CONTEXT.md
@.planning/phases/06-ci-release-matrix-platform-coverage/06-RESEARCH.md
@.planning/phases/06-ci-release-matrix-platform-coverage/06-PATTERNS.md
@.github/workflows/phase2-rust-shim-smoke.yml
@.github/workflows/phase3-go-wrapper-smoke.yml
@internal/bootstrap/url.go
@tests/smoke/minimal_parse.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add darwin release jobs with ad-hoc codesign and thin-binary verification</name>
  <files>
    .github/workflows/release-prepare.yml
    .github/workflows/release.yml
  </files>
  <read_first>
    - .github/workflows/release-prepare.yml
    - .github/workflows/release.yml
    - .github/workflows/phase2-rust-shim-smoke.yml
    - .github/workflows/phase3-go-wrapper-smoke.yml
    - rust-toolchain.toml
    - internal/bootstrap/url.go
    - tests/smoke/minimal_parse.c
  </read_first>
  <action>
Add native darwin release jobs to both workflows for:
- `darwin/amd64` on `macos-15-intel`
- `darwin/arm64` on `macos-15`

Each job must:
- build `libpure_simdjson.dylib`
- export `SOURCE_DATE_EPOCH=$(git log -1 --pretty=%ct)` and use the shared build action from Plan 06-01 so prep builds run with `verify_reproducible: true`
- run `codesign -s - --force --timestamp=none <path-to-dylib>`
- run `codesign --verify --verbose <path-to-dylib>`
- run `file <path-to-dylib>` and fail if the output indicates a universal/fat binary instead of one arch
- package the artifact as:
  - R2/cache file `libpure_simdjson.dylib`
  - GitHub asset `libpure_simdjson-darwin-amd64.dylib` or `libpure_simdjson-darwin-arm64.dylib`
- run the shared native verification/smoke action before marking the artifact ready

When you touch workflow actions in these jobs, pin them by full commit SHA instead of `@v*` tags. Upload each packaged darwin artifact and manifest row from the prep workflow so Plan 06-04 can assemble the loopback smoke tree without rebuilding.
  </action>
  <verify>
    <automated>rg 'macos-15-intel|macos-15|SOURCE_DATE_EPOCH|verify_reproducible: true|codesign -s - --force --timestamp=none|codesign --verify|libpure_simdjson-darwin-amd64\\.dylib|libpure_simdjson-darwin-arm64\\.dylib|actions/upload-artifact@[0-9a-f]{40}' .github/workflows/release-prepare.yml .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'macos-15-intel|macos-15' .github/workflows/release-prepare.yml .github/workflows/release.yml` succeeds
    - `rg 'SOURCE_DATE_EPOCH|verify_reproducible: true|codesign -s - --force --timestamp=none|codesign --verify' .github/workflows/release-prepare.yml .github/workflows/release.yml` succeeds
    - `rg 'libpure_simdjson-darwin-amd64\\.dylib|libpure_simdjson-darwin-arm64\\.dylib|actions/upload-artifact@[0-9a-f]{40}' .github/workflows/release-prepare.yml .github/workflows/release.yml` succeeds
  </acceptance_criteria>
  <done>The darwin release path is wired for both architectures with the documented ad-hoc signing contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add windows release job with MSVC, long-path enable, and contract naming</name>
  <files>
    .github/workflows/release-prepare.yml
    .github/workflows/release.yml
  </files>
  <read_first>
    - .github/workflows/release-prepare.yml
    - .github/workflows/release.yml
    - .github/workflows/phase2-rust-shim-smoke.yml
    - .github/workflows/phase3-go-wrapper-smoke.yml
    - rust-toolchain.toml
    - internal/bootstrap/url.go
    - Makefile
    - tests/smoke/minimal_parse.c
  </read_first>
  <action>
Add a `windows/amd64` release job to both workflows on `windows-latest`.

The windows job must:
- call `ilammy/msvc-dev-cmd@v1`
- set `git config --system core.longpaths true`
- export `SOURCE_DATE_EPOCH=$(git log -1 --pretty=%ct)` and build through the shared action from Plan 06-01 so prep builds run with `verify_reproducible: true` and `/Brepro`
- build the local cargo artifact `pure_simdjson.dll`
- preserve the import library if cargo emits `pure_simdjson.dll.lib`
- package / rename the staged public files as:
  - R2/cache file `pure_simdjson-msvc.dll`
  - GitHub asset `pure_simdjson-windows-amd64-msvc.dll`
- run `dumpbin /EXPORTS` against the built DLL
- run `dumpbin /DEPENDENTS` against the built DLL and persist the output in the workflow summary or an uploaded text artifact so the release docs can state the actual VC++ runtime dependency
- run the shared native verification/smoke action before the artifact is considered publishable

Do not rename the local cargo output in `target/.../release/`; the rename belongs only in packaging. Pin every workflow action touched in these jobs by full commit SHA rather than a moving `@v*` ref, and upload the packaged windows artifact plus manifest row from the prep workflow for later staged-tree assembly.
  </action>
  <verify>
    <automated>rg 'windows-latest|msvc-dev-cmd|core.longpaths|SOURCE_DATE_EPOCH|verify_reproducible: true|dumpbin /EXPORTS|dumpbin /DEPENDENTS|pure_simdjson-windows-amd64-msvc\\.dll|pure_simdjson-msvc\\.dll|actions/upload-artifact@[0-9a-f]{40}' .github/workflows/release-prepare.yml .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'windows-latest|msvc-dev-cmd' .github/workflows/release-prepare.yml .github/workflows/release.yml` succeeds
    - `rg 'core.longpaths|SOURCE_DATE_EPOCH|verify_reproducible: true|dumpbin /EXPORTS|dumpbin /DEPENDENTS' .github/workflows/release-prepare.yml .github/workflows/release.yml` succeeds
    - `rg 'pure_simdjson-windows-amd64-msvc\\.dll|pure_simdjson-msvc\\.dll|actions/upload-artifact@[0-9a-f]{40}' .github/workflows/release-prepare.yml .github/workflows/release.yml` succeeds
  </acceptance_criteria>
  <done>The windows release path is wired with MSVC, long-path handling, deterministic build settings, dependency audit output, and the exact bootstrap/public naming split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native macOS / Windows builders -> released artifacts | platform-specific signing / naming rules must not drift from the bootstrap contract |
| local cargo output -> public asset names | incorrect rename logic breaks download and operator expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T | macOS artifact signing | mitigate | require ad-hoc codesign plus verify before packaging |
| T-06-03-02 | D | Windows artifact loading | mitigate | keep MSVC build and exact public names; audit exports via dumpbin |
| T-06-03-03 | T | thin-binary contract | mitigate | fail macOS jobs if `file` shows a universal/fat output |
</threat_model>

<verification>
- [ ] `rg 'macos-15-intel|macos-15|SOURCE_DATE_EPOCH|verify_reproducible: true|codesign -s - --force --timestamp=none|codesign --verify' .github/workflows/release-prepare.yml .github/workflows/release.yml`
- [ ] `rg 'windows-latest|msvc-dev-cmd|core.longpaths|SOURCE_DATE_EPOCH|verify_reproducible: true|dumpbin /EXPORTS|dumpbin /DEPENDENTS|pure_simdjson-windows-amd64-msvc\\.dll' .github/workflows/release-prepare.yml .github/workflows/release.yml`
</verification>

<success_criteria>
- both darwin targets are packaged as thin, ad-hoc-signed dylibs
- the windows target is packaged from an MSVC build with exact public naming and a recorded dependency audit
- non-linux prep builds inherit the same reproducibility controls as linux prep builds
- non-linux platform jobs are ready for the shared smoke/publish gates from later plans
</success_criteria>

<output>
After completion, create `.planning/phases/06-ci-release-matrix-platform-coverage/06-03-SUMMARY.md`
</output>
