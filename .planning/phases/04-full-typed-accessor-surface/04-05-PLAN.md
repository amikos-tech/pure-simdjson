---
phase: 04-full-typed-accessor-surface
plan: "05"
type: execute
wave: 4
depends_on:
  - "02"
  - "04"
files_modified:
  - purejson.go
  - element.go
  - iterator.go
  - element_scalar_test.go
  - iterator_test.go
  - example_test.go
autonomous: true
requirements:
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - DOC-03
user_setup: []
must_haves:
  truths:
    - "Every newly exported Phase 4 type and method has Godoc that describes the shipped behavior, not deferred v0.2 ideas."
    - "Examples exist for the public DOM surface users are expected to touch in v0.1."
    - "The phase closes with explicit verification for overflow, precision-loss, malformed UTF-8 handling, iteration order, and race safety."
  artifacts:
    - path: example_test.go
      provides: "Executable Go doc examples for the Phase 4 public surface"
      contains: "ExampleObject_GetStringField"
    - path: element.go
      provides: "Source docs for the public scalar/type API"
      contains: "// ElementType"
    - path: iterator.go
      provides: "Source docs for public iterator semantics"
      contains: "// ObjectIter"
  key_links:
    - from: example_test.go
      to: .planning/ROADMAP.md
      via: "The examples demonstrate the exact v0.1 API users are meant to see at phase completion"
      pattern: "typed accessors"
    - from: element_scalar_test.go
      to: iterator_test.go
      via: "The close-out verification sweep covers both scalar and traversal semantics before the phase is considered done"
      pattern: "ErrPrecisionLoss"
---

<objective>
Close Phase 4 by finishing DOC-03 and running the full semantic verification sweep for the completed DOM accessor surface.

Purpose: make the user-facing API documented, example-backed, and verified against the roadmap's numeric/UTF-8/iteration success criteria.
Output: complete source docs/examples plus the final edge-case verification coverage for the shipped Phase 4 API.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md
@.planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md
@purejson.go
@element.go
@iterator.go
@element_scalar_test.go
@iterator_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Godoc and executable examples for the completed Phase 4 surface</name>
  <files>purejson.go, element.go, iterator.go, example_test.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, purejson.go, element.go, iterator.go</read_first>
  <action>Update source comments so every newly exported Phase 4 symbol has accurate Godoc: `ElementType`, all `Type*` constants, `GetUint64`, `GetFloat64`, `GetString`, `GetBool`, `IsNull`, `Type`, `Array.Iter`, `Object.Iter`, `Object.GetField`, `Object.GetStringField`, `ArrayIter`, `ObjectIter`, `Next`, `Value`, `Key`, and `Err`. Keep the comments tied to shipped v0.1 behavior only. Create `example_test.go` with executable examples named `ExampleElement_scalarAccess`, `ExampleArrayIter`, `ExampleObjectIter`, and `ExampleObject_GetStringField` that demonstrate the real Phase 4 surface and compile under `go test`.</action>
  <verify>
    <automated>go test ./... -run 'Example'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f example_test.go` succeeds.
    - `rg '^// ElementType|^// GetUint64|^// GetFloat64|^// GetString|^// GetBool|^// IsNull|^// Type|^// ArrayIter|^// ObjectIter|^// Iter|^// GetField|^// GetStringField|^// Next|^// Value|^// Key|^// Err' element.go iterator.go` succeeds.
    - `rg 'ExampleElement_scalarAccess|ExampleArrayIter|ExampleObjectIter|ExampleObject_GetStringField' example_test.go` succeeds.
    - `go test ./... -run 'Example'` exits 0.
  </acceptance_criteria>
  <done>The completed Phase 4 public API is documented in source and backed by executable examples.</done>
</task>

<task type="auto">
  <name>Task 2: Finish the phase-close verification sweep for numeric, UTF-8, iteration, and race behavior</name>
  <files>element_scalar_test.go, iterator_test.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/ROADMAP.md, element_scalar_test.go, iterator_test.go</read_first>
  <action>Extend `element_scalar_test.go` and `iterator_test.go` so the phase explicitly proves the roadmap success criteria. Add table-driven numeric boundary cases covering max int64, max uint64, a value like `1e20` that must yield `ErrNumberOutOfRange` on integer getters, and `9007199254740993` that must yield `ErrPrecisionLoss` on `GetFloat64()`. Add malformed UTF-8 inputs that must surface `ErrInvalidJSON` instead of returning corrupted strings. Keep the iteration tests proving stable order and no callback-based traversal. Finish with a full-suite verification command of `cargo test && cargo build --release && go test ./... -race` so the phase closes with both native and Go coverage green.</action>
  <verify>
    <automated>cargo test && cargo build --release && go test ./... -race</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'ErrNumberOutOfRange|ErrPrecisionLoss|ErrInvalidJSON' element_scalar_test.go iterator_test.go` succeeds.
    - `rg '9007199254740993|1e20|invalid utf-8|UTF-8|UTF8' element_scalar_test.go iterator_test.go` succeeds.
    - `cargo test && cargo build --release && go test ./... -race` exits 0.
  </acceptance_criteria>
  <done>The phase exits with full native + Go verification of the roadmap's numeric, UTF-8, iteration, and race-safety success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing docs/examples -> shipped API | Wrong docs would freeze incorrect usage patterns into the public v0.1 surface |
| close-out verification -> release confidence | Missing numeric/UTF-8/race coverage would leave the hardest Phase 4 regressions unproven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | I | Godoc/examples | mitigate | Document only shipped behavior and back it with executable examples that must compile under `go test` |
| T-04-05-02 | T | numeric/UTF-8 verification | mitigate | Add explicit table-driven overflow, precision-loss, and malformed UTF-8 cases before phase completion |
| T-04-05-03 | T | phase-close stability | mitigate | Require the full `cargo test && cargo build --release && go test ./... -race` sweep before the phase can be marked done |
</threat_model>

<verification>
- [ ] `go test ./... -run 'Example'`
- [ ] `cargo test && cargo build --release && go test ./... -race`
- [ ] `test -f example_test.go`
</verification>

<success_criteria>
- DOC-03 is complete for the Phase 4 public surface
- Users have executable examples for the public DOM accessor and traversal API
- The roadmap's numeric, UTF-8, iteration, and race-safety success criteria are explicitly verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-typed-accessor-surface/04-05-SUMMARY.md`
</output>
