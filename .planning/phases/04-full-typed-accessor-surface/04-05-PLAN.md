---
phase: 04-full-typed-accessor-surface
plan: "05"
type: execute
wave: 4
depends_on:
  - "02"
  - "04"
files_modified:
  - purejson.go
  - element.go
  - iterator.go
  - element_scalar_test.go
  - iterator_test.go
  - element_fuzz_test.go
  - example_test.go
autonomous: true
requirements:
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - DOC-03
user_setup: []
must_haves:
  truths:
    - "DOC-03 is closed package-wide: every exported `purejson` type/function in the final v0.1 surface has Godoc that matches shipped behavior, with the Phase 3 docs explicitly re-audited rather than assumed."
    - "Every exported `purejson` type has at least one executable `Example...` attached in Go docs, including pre-Phase-4 types that already existed before this phase."
    - "The phase closes with explicit verification for overflow, precision-loss, malformed UTF-8 handling, traversal order, scanner edge cases, and race safety."
    - "The numeric boundary contract is internally consistent: `GetInt64(9223372036854775808)` reports `ErrNumberOutOfRange`, `GetInt64(1e20)` reports `ErrWrongType` because the value is float-kind, and `GetFloat64(9007199254740993)` reports `ErrPrecisionLoss`."
  artifacts:
    - path: example_test.go
      provides: "Executable example matrix covering every exported `purejson` type in the final v0.1 surface"
      contains: "ExampleParser_Parse"
    - path: purejson.go
      provides: "Package docs kept aligned with the final exported surface after the package-wide DOC-03 audit"
      contains: "// Package purejson"
    - path: element.go
      provides: "Source docs for the public scalar/type API after the full-surface DOC-03 audit"
      contains: "// ElementType"
    - path: iterator.go
      provides: "Source docs for public iterator semantics"
      contains: "// ObjectIter"
    - path: element_fuzz_test.go
      provides: "Real fuzz entrypoint for malformed UTF-8 and string-access regressions"
      contains: "func FuzzParseThenGetString"
  key_links:
    - from: example_test.go
      to: .planning/phases/03-go-public-api-purego-happy-path/03-05-SUMMARY.md
      via: "The Phase 4 close-out must extend the Phase 3 partial DOC-03 work into one example matrix that covers every exported type in the final `purejson` surface"
      pattern: "ExampleParser_Parse|ExampleParserPool_Get|ExampleError"
    - from: element_scalar_test.go
      to: iterator_test.go
      via: "The close-out verification sweep covers both scalar and traversal semantics before the phase is considered done"
      pattern: "ErrPrecisionLoss"
---

<objective>
Close Phase 4 by finishing DOC-03 for the full exported `purejson` surface and running the full semantic verification sweep for the completed DOM accessor surface.

Purpose: make the user-facing API documented, example-backed, and verified against the reconciled numeric, UTF-8, iteration, and race semantics.
Output: complete source docs/examples plus the final edge-case verification coverage for the shipped Phase 4 API.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md
@.planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md
@.planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md
@.planning/phases/03-go-public-api-purego-happy-path/03-05-SUMMARY.md
@purejson.go
@errors.go
@parser.go
@doc.go
@element.go
@pool.go
@iterator.go
@element_scalar_test.go
@iterator_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Godoc and executable examples for the completed Phase 4 surface</name>
  <files>purejson.go, element.go, iterator.go, example_test.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md, .planning/phases/03-go-public-api-purego-happy-path/03-05-SUMMARY.md, purejson.go, errors.go, parser.go, doc.go, element.go, pool.go, iterator.go</read_first>
  <action>Treat DOC-03 as package-wide, not Phase-4-only. First audit the final exported `purejson` surface by reading `purejson.go`, `errors.go`, `parser.go`, `doc.go`, `element.go`, `pool.go`, and `iterator.go`, plus the Phase 3 doc summary, and explicitly verify coverage for the exported types `Error`, `Parser`, `Doc`, `Element`, `Array`, `Object`, `ParserPool`, `ElementType`, `ArrayIter`, and `ObjectIter`; the exported constructors/functions `NewParser` and `NewParserPool`; and the exported methods `Parse`, `Close`, `Root`, `GetInt64`, `GetUint64`, `GetFloat64`, `GetString`, `GetBool`, `IsNull`, `Type`, `AsArray`, `AsObject`, `Iter`, `GetField`, `GetStringField`, `Get`, `Put`, `Next`, `Value`, `Key`, and `Err`. Keep or cite the Phase 3 comments already present in `purejson.go`, `errors.go`, `parser.go`, `doc.go`, and `pool.go` when they are still accurate, and update the Phase 4-touched docs in `purejson.go`, `element.go`, and `iterator.go` wherever the final surface made earlier comments stale. Then create `example_test.go` as a package-wide example map with at least one associated Example per exported type, using the exact functions `ExampleParser_Parse`, `ExampleDoc_Root`, `ExampleElement_scalarAccess`, `ExampleElementType`, `ExampleArray_Iter`, `ExampleArrayIter_Next`, `ExampleObject_Iter`, `ExampleObject_GetField`, `ExampleObject_GetStringField`, `ExampleObjectIter_Next`, `ExampleParserPool_Get`, and `ExampleError`. The examples must describe shipped v0.1 behavior only and compile under `go test`; do not skip pre-Phase-4 types just because their Godoc landed earlier.</action>
  <verify>
    <automated>rg '^// Package purejson|^// Error|^// Parser|^// Doc|^// Element|^// Array|^// Object|^// ParserPool|^// NewParser|^// NewParserPool|^// ElementType|^// ArrayIter|^// ObjectIter|^// GetUint64|^// GetFloat64|^// GetString|^// GetBool|^// IsNull|^// Type|^// Iter|^// GetField|^// GetStringField|^// Next|^// Value|^// Key|^// Err' purejson.go errors.go parser.go doc.go element.go pool.go iterator.go && rg 'ExampleParser_Parse|ExampleDoc_Root|ExampleElement_scalarAccess|ExampleElementType|ExampleArray_Iter|ExampleArrayIter_Next|ExampleObject_Iter|ExampleObject_GetField|ExampleObject_GetStringField|ExampleObjectIter_Next|ExampleParserPool_Get|ExampleError' example_test.go && go test ./... -run 'Example'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f example_test.go` succeeds.
    - `rg '^// Package purejson|^// Error|^// Parser|^// Doc|^// Element|^// Array|^// Object|^// ParserPool|^// NewParser|^// NewParserPool|^// ElementType|^// ArrayIter|^// ObjectIter|^// GetUint64|^// GetFloat64|^// GetString|^// GetBool|^// IsNull|^// Type|^// Iter|^// GetField|^// GetStringField|^// Next|^// Value|^// Key|^// Err' purejson.go errors.go parser.go doc.go element.go pool.go iterator.go` succeeds.
    - `rg 'ExampleParser_Parse|ExampleDoc_Root|ExampleElement_scalarAccess|ExampleElementType|ExampleArray_Iter|ExampleArrayIter_Next|ExampleObject_Iter|ExampleObject_GetField|ExampleObject_GetStringField|ExampleObjectIter_Next|ExampleParserPool_Get|ExampleError' example_test.go` succeeds.
    - `rg 'scanner-style|copied Go string|ErrElementNotFound|ErrWrongType' element.go iterator.go` succeeds.
    - `go test ./... -run 'Example'` exits 0.
  </acceptance_criteria>
  <done>DOC-03 is complete for the final exported `purejson` surface: existing Phase 3 docs have been re-audited, Phase 4 docs are accurate, and every exported type is backed by an executable Example.</done>
</task>

<task type="auto">
  <name>Task 2: Finish the phase-close verification sweep for numeric, UTF-8, iteration, and race behavior</name>
  <files>element_scalar_test.go, iterator_test.go, element_fuzz_test.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md, .planning/ROADMAP.md, element_scalar_test.go, iterator_test.go</read_first>
  <action>Resolve the review-blocking numeric ambiguity and close the remaining edge coverage honestly. Extend `element_scalar_test.go` and `iterator_test.go` so the phase proves the reconciled numeric contract: `GetInt64()` on `9223372036854775808` returns `ErrNumberOutOfRange`, `GetInt64()` on `1e20` returns `ErrWrongType` because the value is float-kind in simdjson DOM, and `GetFloat64()` on `9007199254740993` returns `ErrPrecisionLoss`. Keep max-int64 and max-uint64 happy-path cases as control checks. Add malformed UTF-8 cases that must surface `ErrInvalidJSON` instead of returning corrupted strings, and keep the iteration tests proving stable order plus empty/done semantics from Plan 04. Create `element_fuzz_test.go` with a real `FuzzParseThenGetString` target seeded with malformed UTF-8, valid string, valid object, and valid array payloads so the repo ships an actual fuzz entrypoint instead of table-driven-only coverage. The fuzz target should assert that parse failures map to `ErrInvalidJSON` and successful string/object paths never panic or return corrupted Go strings. Finish with the full-suite verification command `cargo test && cargo build --release && go test ./... -race` so the phase closes with both native and Go coverage green.</action>
  <verify>
    <automated>cargo test && cargo build --release && go test ./... -race</automated>
  </verify>
  <acceptance_criteria>
    - `rg '9223372036854775808|1e20|9007199254740993|ErrWrongType|ErrNumberOutOfRange|ErrPrecisionLoss|ErrInvalidJSON' element_scalar_test.go iterator_test.go` succeeds.
    - `test -f element_fuzz_test.go` succeeds.
    - `rg '^func FuzzParseThenGetString' element_fuzz_test.go` succeeds.
    - `cargo test && cargo build --release && go test ./... -race` exits 0.
  </acceptance_criteria>
  <done>The phase exits with full native + Go verification of the reconciled numeric contract, malformed UTF-8 handling, traversal semantics, fuzz entrypoint, and race safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing docs/examples -> shipped API | Wrong docs would freeze incorrect usage patterns into the public v0.1 surface |
| close-out verification -> release confidence | Missing numeric/UTF-8/race/fuzz coverage would leave the hardest Phase 4 regressions unproven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | I | Godoc/examples | mitigate | Document only shipped behavior and back it with executable examples that must compile under `go test` |
| T-04-05-02 | T | numeric/UTF-8 verification | mitigate | Add explicit table-driven overflow, wrong-type, precision-loss, and malformed UTF-8 cases before phase completion |
| T-04-05-03 | T | phase-close stability | mitigate | Require the full `cargo test && cargo build --release && go test ./... -race` sweep before the phase can be marked done |
</threat_model>

<verification>
- [ ] `rg '^// Package purejson|^// Error|^// Parser|^// Doc|^// Element|^// Array|^// Object|^// ParserPool|^// NewParser|^// NewParserPool|^// ElementType|^// ArrayIter|^// ObjectIter|^// GetUint64|^// GetFloat64|^// GetString|^// GetBool|^// IsNull|^// Type|^// Iter|^// GetField|^// GetStringField|^// Next|^// Value|^// Key|^// Err' purejson.go errors.go parser.go doc.go element.go pool.go iterator.go && rg 'ExampleParser_Parse|ExampleDoc_Root|ExampleElement_scalarAccess|ExampleElementType|ExampleArray_Iter|ExampleArrayIter_Next|ExampleObject_Iter|ExampleObject_GetField|ExampleObject_GetStringField|ExampleObjectIter_Next|ExampleParserPool_Get|ExampleError' example_test.go && go test ./... -run 'Example'`
- [ ] `cargo test && cargo build --release && go test ./... -race`
- [ ] `test -f example_test.go`
- [ ] `test -f element_fuzz_test.go`
</verification>

<success_criteria>
- DOC-03 is complete for the full exported `purejson` surface, not just the Phase 4 additions
- Users have executable examples for the public DOM accessor and traversal API
- The phase's numeric, UTF-8, scanner-edge, fuzz, and race-safety criteria are explicitly verified
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-typed-accessor-surface/04-05-SUMMARY.md`
</output>
