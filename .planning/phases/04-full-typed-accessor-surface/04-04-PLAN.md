---
phase: 04-full-typed-accessor-surface
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - element.go
  - iterator.go
  - iterator_test.go
autonomous: true
requirements:
  - API-05
  - API-07
  - API-08
user_setup: []
must_haves:
  truths:
    - "Per D-04, public traversal is scanner-style: `Next() bool`, then `Value()` / `Key()`, then `Err() error`."
    - "Per D-05, `ObjectIter.Key()` returns a copied Go string extracted during `Next()`, never a borrowed native pointer or a generic `Element`."
    - "Per D-08 and D-09, `Object.GetField(key)` distinguishes a missing field (`ErrElementNotFound`) from present `null` values."
    - "Per D-06 and D-07, `Object.GetStringField(name)` exists in Phase 4 but is implemented as Go composition over `GetField(name)` plus `GetString()`, not as a new ABI call."
    - "Iterator state stays document-tied; after `Doc.Close()` iteration stops and `Err()` surfaces `ErrClosed`, while empty iterators and repeated `Next()` after done stay clean `false`/`nil` scanner outcomes."
  artifacts:
    - path: iterator.go
      provides: "Public `ArrayIter` / `ObjectIter` types and scanner-style traversal API"
      contains: "func (it *ObjectIter) Next() bool"
    - path: element.go
      provides: "Public `Array.Iter`, `Object.Iter`, `Object.GetField`, and `Object.GetStringField`"
      contains: "func (o Object) GetStringField(name string) (string, error)"
    - path: iterator_test.go
      provides: "Semantic Go coverage for iteration order, key copying, field lookup, and scanner edge cases"
      contains: "TestIteratorNextAfterDone"
  key_links:
    - from: iterator.go
      to: internal/ffi/bindings.go
      via: "Public iterators consume only the hidden typed wrappers from Plan 03 and the hidden string-copy helper from Plan 01"
      pattern: "ObjectIterNext"
    - from: element.go
      to: .planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
      via: "Public helper semantics honor the locked scanner-style and missing-vs-null decisions"
      pattern: "GetStringField"
---

<objective>
Expose the public array/object traversal and direct-field helper API for Phase 4.

Purpose: finish the object/array side of the DOM surface with the locked scanner-style behavior, copied-key semantics, and helper behavior.
Output: `ArrayIter`, `ObjectIter`, `Array.Iter()`, `Object.Iter()`, `Object.GetField()`, `Object.GetStringField()`, and semantic tests for iteration and lookup behavior.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md
@.planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md
@.planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md
@errors.go
@doc.go
@element.go
@iterator.go
@internal/ffi/types.go
@internal/ffi/bindings.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement public array/object traversal plus direct field helpers</name>
  <files>element.go, iterator.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md, .planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md, errors.go, doc.go, element.go, internal/ffi/types.go, internal/ffi/bindings.go, iterator.go</read_first>
  <action>Implements D-04 through D-10 while addressing the review concern that key-string extraction was still implicit. Create `iterator.go` for the public iterator types. Add `type ArrayIter struct` and `type ObjectIter struct` that each hold the owning doc, the hidden iterator transport struct, the current value/key state, a done flag, and a terminal error. Implement `Array.Iter() *ArrayIter` and `Object.Iter() *ObjectIter` by validating the wrapper type and initializing the hidden iterator state through `ArrayIterNew(...)` / `ObjectIterNew(...)`. Implement `Next() bool` so it returns `false` on end or terminal failure, caches the current value/key on success, and stores any terminal error for later `Err()`. For `ObjectIter.Next()`, do not guess at key ownership: after a successful hidden `ObjectIterNext(...)` call, immediately decode the returned key view by calling the hidden `ElementGetString(...)` binding from Plan 01 so the key bytes are copied and freed there, then cache the Go string for `Key()`. `Value()` must return the current `Element` wrapper for the decoded value view. In `element.go`, add `Object.GetField(key string) (Element, error)` via the hidden `ObjectGetField(...)` binding and implement `Object.GetStringField(name string) (string, error)` as explicit Go composition per D-06 and D-07: call `GetField(name)` first, then call `GetString()` on the returned `Element`; do not add or assume a new ABI fast path. Empty iterators and repeated `Next()` after done must leave `Err() == nil`, while `Doc.Close()` before or during iteration must stop traversal and store `ErrClosed` exactly once.</action>
  <verify>
    <automated>cargo build --release && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `test -f iterator.go` succeeds.
    - `rg 'type ArrayIter struct|type ObjectIter struct|func \\(it \\*ArrayIter\\) Next\\(\\) bool|func \\(it \\*ObjectIter\\) Next\\(\\) bool|func \\(it \\*ArrayIter\\) Value\\(\\) Element|func \\(it \\*ObjectIter\\) Value\\(\\) Element|func \\(it \\*ObjectIter\\) Key\\(\\) string|func \\(it \\*ArrayIter\\) Err\\(\\) error|func \\(it \\*ObjectIter\\) Err\\(\\) error' iterator.go` succeeds.
    - `rg 'ElementGetString|ObjectIterNext|ErrClosed' iterator.go` succeeds.
    - `rg 'func \\(a Array\\) Iter\\(\\) \\*ArrayIter|func \\(o Object\\) Iter\\(\\) \\*ObjectIter|func \\(o Object\\) GetField\\(key string\\) \\(Element, error\\)|func \\(o Object\\) GetStringField\\(name string\\) \\(string, error\\)' element.go` succeeds.
    - `rg 'GetField\\(name\\)|GetString\\(\\)' element.go` succeeds.
    - `cargo build --release && go build ./... && go vet ./...` exits 0.
  </acceptance_criteria>
  <done>The public Go traversal and field-helper API exists with the locked scanner-style, copied-key, and missing-vs-null semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic tests for iteration order, copied keys, and scanner edge cases</name>
  <files>iterator_test.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md, errors.go, element.go, iterator.go, parser_test.go, phase3_followup_test.go</read_first>
  <action>Create `iterator_test.go` with semantic tests named `TestArrayIterOrder`, `TestArrayIterEmpty`, `TestObjectIterOrder`, `TestObjectIterEmpty`, `TestObjectGetFieldMissingVsNull`, `TestGetStringField`, `TestGetStringFieldNullValue`, `TestIteratorNextAfterDone`, and `TestIteratorAfterDocClose`. Cover ordered iteration over arrays and objects, `Key()` returning Go string copies for object entries, `Object.GetField(...)` returning `ErrElementNotFound` for absent keys but a valid null element for present nulls, `GetStringField(name)` returning the same semantics as `GetField(name)` + `GetString()`, `GetStringField(name)` on a present null returning `ErrWrongType`, empty iterators returning `false` immediately with `Err() == nil`, repeated `Next()` after completion staying `false`/`nil`, and iterator failure after `Doc.Close()` surfacing through `Err()` rather than panicking or silently continuing. Run these tests under `-race` here so iterator/document lifetime bugs surface as soon as the public traversal API exists.</action>
  <verify>
    <automated>cargo build --release && go test ./... -race -run 'Test(ArrayIterOrder|ArrayIterEmpty|ObjectIterOrder|ObjectIterEmpty|ObjectGetFieldMissingVsNull|GetStringField|GetStringFieldNullValue|IteratorNextAfterDone|IteratorAfterDocClose)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f iterator_test.go` succeeds.
    - `rg 'TestArrayIterOrder|TestArrayIterEmpty|TestObjectIterOrder|TestObjectIterEmpty|TestObjectGetFieldMissingVsNull|TestGetStringField|TestGetStringFieldNullValue|TestIteratorNextAfterDone|TestIteratorAfterDocClose' iterator_test.go` succeeds.
    - `rg 'ErrElementNotFound|ErrClosed|ErrWrongType' iterator_test.go` succeeds.
    - `cargo build --release && go test ./... -race -run 'Test(ArrayIterOrder|ArrayIterEmpty|ObjectIterOrder|ObjectIterEmpty|ObjectGetFieldMissingVsNull|GetStringField|GetStringFieldNullValue|IteratorNextAfterDone|IteratorAfterDocClose)'` exits 0.
  </acceptance_criteria>
  <done>The public Go iteration and lookup semantics are locked by direct behavior tests, including empty/done/null edge cases and race-checked close behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public iterator API -> hidden iterator transport | Public methods must not leak raw iterator-state assumptions or continue after doc closure |
| direct field helpers -> scalar string access | The helper must preserve the same missing/null/wrong-type semantics as the underlying primitives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | T | iterator closed-state | mitigate | Tie iterators to the owning doc, stop iteration after closure, and surface terminal failure via `Err()` |
| T-04-04-02 | T | copied object keys | mitigate | Decode key views through the existing hidden string helper so native bytes are copied and freed before `Key()` exposes the result |
| T-04-04-03 | I | `GetStringField(name)` helper | mitigate | Implement the helper as explicit `GetField(name)` + `GetString()` composition so its behavior cannot drift from the primitives |
</threat_model>

<verification>
- [ ] `cargo build --release && go build ./... && go vet ./...`
- [ ] `cargo build --release && go test ./... -race -run 'Test(ArrayIterOrder|ArrayIterEmpty|ObjectIterOrder|ObjectIterEmpty|ObjectGetFieldMissingVsNull|GetStringField|GetStringFieldNullValue|IteratorNextAfterDone|IteratorAfterDocClose)'`
- [ ] `test -f iterator.go`
- [ ] `test -f iterator_test.go`
</verification>

<success_criteria>
- Public array/object traversal is scanner-style and document-tied
- Direct field lookup distinguishes missing from null
- `GetStringField(name)` exists and preserves the same semantics as the primitive operations it composes
- Empty and completed iterators behave like Go scanners, not partially-invalid state machines
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-typed-accessor-surface/04-04-SUMMARY.md`
</output>
