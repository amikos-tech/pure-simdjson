---
phase: 04-full-typed-accessor-surface
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - src/lib.rs
  - internal/ffi/types.go
  - internal/ffi/bindings.go
  - tests/rust_shim_iterators.rs
autonomous: true
requirements:
  - API-07
  - API-08
user_setup: []
must_haves:
  truths:
    - "The native ABI exports for array iteration, object iteration, and direct field lookup are real, not stubs."
    - "Iterator state is inline-only because the public ABI has no iterator destructor: `state0 = current_json_index`, `state1 = end_json_index`, `index = successful steps emitted`, `tag = ARRAY_ITER_TAG|OBJECT_ITER_TAG`, and `reserved = 0`."
    - "The iterator tag namespace is locked in `src/runtime/mod.rs`: `ARRAY_ITER_TAG = u16::from_le_bytes(*b\"AR\")` and `OBJECT_ITER_TAG = u16::from_le_bytes(*b\"OB\")` are the only legal iterator tags."
    - "Array/object iterator creation and advancement must not allocate native heap state (`new`, `malloc`, `Box`, `Vec`) because there is no public ABI free hook; all progress lives in the transport struct and the owning doc."
    - "Object iteration returns both key and value views through the public ABI without Go callbacks across FFI, and key views are regular descendant string views that later Go code will decode through the scalar string helper."
  artifacts:
    - path: src/runtime/mod.rs
      provides: "Locked iterator tag constants for Phase 4"
      contains: "ARRAY_ITER_TAG"
    - path: src/lib.rs
      provides: "Activated Phase 4 iterator and object-lookup exports"
      contains: "pure_simdjson_array_iter_new"
    - path: internal/ffi/types.go
      provides: "Go mirror structs for array/object iterator state"
      contains: "type ArrayIter struct"
    - path: internal/ffi/bindings.go
      provides: "purego wrappers for iterator lifecycle and direct field lookup"
      contains: "ObjectGetField"
    - path: tests/rust_shim_iterators.rs
      provides: "Rust integration coverage for iterator and object-lookup exports, including empty/done/invalid-state edges"
      contains: "iterator_next_after_done"
  key_links:
    - from: src/runtime/mod.rs
      to: src/runtime/registry.rs
      via: "Iterator validation and emission must consume one shared tag namespace and one inline state layout"
      pattern: "ARRAY_ITER_TAG"
    - from: src/runtime/registry.rs
      to: include/pure_simdjson.h
      via: "Iterator struct validation stays aligned with the committed `state0/state1/index/tag/reserved` transport layout"
      pattern: "pure_simdjson_array_iter_t"
    - from: internal/ffi/types.go
      to: internal/ffi/bindings.go
      via: "The hidden Go iterator wrappers consume typed mirror structs instead of raw pointer juggling"
      pattern: "ObjectIter"
---

<objective>
Activate the native Phase 4 iterator and direct-object-lookup substrate and mirror that transport layer in hidden Go FFI types/bindings.

Purpose: land the native and purego machinery required for array/object traversal and keyed lookup before the public Go iterator API is built.
Output: real iterator/object-lookup exports, Go mirror structs for iterator state, and hidden wrappers that compile against the committed ABI without requiring an iterator free function.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md
@.planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md
@.planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md
@docs/ffi-contract.md
@include/pure_simdjson.h
@src/native/simdjson_bridge.h
@src/native/simdjson_bridge.cpp
@src/runtime/mod.rs
@src/runtime/registry.rs
@src/lib.rs
@internal/ffi/types.go
@internal/ffi/bindings.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement inline-only native array/object iteration and keyed lookup</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, src/runtime/registry.rs, src/lib.rs</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md, .planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md, docs/ffi-contract.md, include/pure_simdjson.h, src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, src/runtime/registry.rs, src/lib.rs</read_first>
  <action>Addresses review concerns: the ABI has no iterator free hook, iterator tags were not locked, and empty/done behavior was underspecified. In `src/runtime/mod.rs`, add `ARRAY_ITER_TAG = u16::from_le_bytes(*b"AR")` and `OBJECT_ITER_TAG = u16::from_le_bytes(*b"OB")` beside the view-tag constants from Plan 01. In `src/native/simdjson_bridge.h` and `.cpp`, add the concrete bridge helpers needed to drive iterators from `psimdjson_doc *doc` plus JSON tape indexes; use `simdjson::internal::tape_ref` reconstruction rather than allocating native iterator objects. In `src/runtime/registry.rs`, implement `array_iter_new`, `array_iter_next`, `object_iter_new`, `object_iter_next`, and `object_get_field` with one explicit inline state model: `state0` stores the current JSON tape index for the next entry, `state1` stores the end JSON tape index, `index` counts successful `Next()` emissions, `tag` is the locked array/object constant, and `reserved` stays zero. Validation must reject null iterator pointers, non-zero reserved bits, unknown tags, zero `state0` unless `state0 == state1` for the done state, `state0 > state1`, stale doc handles, and any index that falls outside the live document's root span as `PURE_SIMDJSON_ERR_INVALID_HANDLE`. `array_iter_new` / `object_iter_new` must not allocate heap state or stash pointers that would need a destructor; all progress must live in the transport struct. `*_next` must return `out_done = 1` plus `PURE_SIMDJSON_OK` for empty iterators and repeated calls after completion, leaving `Err()` clean on the Go side. `object_get_field` must preserve D-08 and D-09: missing key returns `PURE_SIMDJSON_ERR_ELEMENT_NOT_FOUND`, present `null` returns a valid descendant view, and returned descendants must always be encoded with the Plan 01 `encode_descendant_view(...)` helper rather than ad hoc packing. In `src/lib.rs`, replace the Phase 1 iterator/lookup stubs with real `ffi_wrap(...)` calls into the new registry entry points and `write_out(...)` on success.</action>
  <verify>
    <automated>cargo test --lib</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'ARRAY_ITER_TAG|OBJECT_ITER_TAG' src/runtime/mod.rs src/runtime/registry.rs` succeeds.
    - `rg 'array_iter_new|array_iter_next|object_iter_new|object_iter_next|object_get_field' src/runtime/registry.rs` succeeds.
    - `rg 'json_index|state0|state1|tag|reserved' src/native/simdjson_bridge.h src/native/simdjson_bridge.cpp` succeeds.
    - `! rg 'new psimdjson_|malloc|Box::new|Vec::new' src/native/simdjson_bridge.cpp src/runtime/registry.rs` succeeds.
    - `! rg 'unimplemented_stub\\(\"pure_simdjson_(array_iter_new|array_iter_next|object_iter_new|object_iter_next|object_get_field)\"' src/lib.rs` succeeds.
    - `cargo test --lib` exits 0.
  </acceptance_criteria>
  <done>The native Phase 4 iterator/object-lookup exports are real, tag-validated, and store all progress inline so the ABI cannot leak iterator heap allocations.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror iterator transport in hidden Go FFI types and wrappers</name>
  <files>internal/ffi/types.go, internal/ffi/bindings.go, tests/rust_shim_iterators.rs</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md, .planning/phases/04-full-typed-accessor-surface/04-REVIEWS.md, include/pure_simdjson.h, internal/ffi/types.go, internal/ffi/bindings.go</read_first>
  <action>In `internal/ffi/types.go`, add Go mirrors for `pure_simdjson_array_iter_t` and `pure_simdjson_object_iter_t` that preserve the committed field order (`Doc`, `State0`, `State1`, `Index`, `Tag`, `Reserved`). In `internal/ffi/bindings.go`, add symbol registration and typed wrappers for `pure_simdjson_array_iter_new`, `pure_simdjson_array_iter_next`, `pure_simdjson_object_iter_new`, `pure_simdjson_object_iter_next`, and `pure_simdjson_object_get_field`. `ArrayIterNext(...)` and `ObjectIterNext(...)` should own the out-param locals, return the next value/key views plus a done flag, and call `runtime.KeepAlive(...)` on both the iterator struct and the binding owner immediately after the purego call. Do not add or assume any iterator free helper. Add `tests/rust_shim_iterators.rs` to exercise the public Rust ABI for array iteration, object iteration, field lookup, empty iterators, repeated `next` after done, and invalid iterator/view transport cases. Run the Go side under `-race` here so hidden transport changes do not wait until phase close-out to expose synchronization bugs.</action>
  <verify>
    <automated>cargo test --test rust_shim_iterators && cargo build --release && go test ./... -race</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type ArrayIter struct|type ObjectIter struct' internal/ffi/types.go` succeeds.
    - `rg 'pure_simdjson_array_iter_new|pure_simdjson_array_iter_next|pure_simdjson_object_iter_new|pure_simdjson_object_iter_next|pure_simdjson_object_get_field' internal/ffi/bindings.go` succeeds.
    - `rg 'ArrayIterNew|ArrayIterNext|ObjectIterNew|ObjectIterNext|ObjectGetField' internal/ffi/bindings.go` succeeds.
    - `rg 'runtime.KeepAlive' internal/ffi/bindings.go` succeeds.
    - `test -f tests/rust_shim_iterators.rs` succeeds.
    - `rg 'empty|after done|invalid handle|reserved|tag' tests/rust_shim_iterators.rs` succeeds.
    - `cargo test --test rust_shim_iterators && cargo build --release && go test ./... -race` exits 0.
  </acceptance_criteria>
  <done>The hidden Go FFI layer mirrors iterator transport exactly and exposes typed wrappers for iteration and direct object lookup without introducing a phantom free path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public iterator transport structs -> Rust runtime | Iterator tags/reserved bits/index state must be validated before any native traversal continues |
| native iteration -> Go hidden FFI layer | The Go layer must preserve exact iterator state and done flags without exposing raw pointer semantics publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T | iterator-state validation | mitigate | Lock one inline iterator state model, validate tags/reserved bits/doc ownership/index bounds, and reject malformed states as `ERR_INVALID_HANDLE` |
| T-04-03-02 | D | iterator allocation | mitigate | Forbid native heap allocation in iterator creation/advance because the public ABI has no iterator destructor |
| T-04-03-03 | I | hidden Go iterator wrappers | mitigate | Mirror the ABI structs exactly in `internal/ffi/types.go`, keep `runtime.KeepAlive(...)` around iterator purego calls, and run Go verification under `-race` |
</threat_model>

<verification>
- [ ] `cargo test --lib`
- [ ] `cargo test --test rust_shim_iterators && cargo build --release && go test ./... -race`
- [ ] `rg 'ARRAY_ITER_TAG|OBJECT_ITER_TAG' src/runtime/mod.rs src/runtime/registry.rs`
- [ ] `test -f tests/rust_shim_iterators.rs`
</verification>

<success_criteria>
- Iterator and object-lookup exports are real, validated, callback-free, and inline-only
- The hidden Go FFI layer has exact typed mirrors for iterator transport state
- Public Go iterator work can proceed without inventing new ABI, hidden heap state, or raw-pointer plumbing
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-typed-accessor-surface/04-03-SUMMARY.md`
</output>
