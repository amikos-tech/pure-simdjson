---
phase: 04-full-typed-accessor-surface
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/native/simdjson_bridge.h
  - src/native/simdjson_bridge.cpp
  - src/runtime/mod.rs
  - src/runtime/registry.rs
  - src/lib.rs
  - internal/ffi/types.go
  - internal/ffi/bindings.go
  - tests/rust_shim_iterators.rs
autonomous: true
requirements:
  - API-07
  - API-08
user_setup: []
must_haves:
  truths:
    - "The native ABI exports for array iteration, object iteration, and direct field lookup are real, not stubs."
    - "Iterator transport structs are validated like handles: non-zero reserved bits, unknown tags, or stale doc ownership fail as `ERR_INVALID_HANDLE`."
    - "Object iteration returns both key and value views through the public ABI without Go callbacks across FFI."
    - "The hidden Go layer mirrors iterator structs and wrappers so later public iterators never touch raw `unsafe.Pointer` state."
  artifacts:
    - path: src/lib.rs
      provides: "Activated Phase 4 iterator and object-lookup exports"
      contains: "pure_simdjson_array_iter_new"
    - path: internal/ffi/types.go
      provides: "Go mirror structs for array/object iterator state"
      contains: "type ArrayIter struct"
    - path: internal/ffi/bindings.go
      provides: "purego wrappers for iterator lifecycle and direct field lookup"
      contains: "ObjectGetField"
    - path: tests/rust_shim_iterators.rs
      provides: "Rust integration coverage for iterator and object-lookup exports"
      contains: "object_get_field"
  key_links:
    - from: src/runtime/registry.rs
      to: include/pure_simdjson.h
      via: "Iterator struct validation stays aligned with the committed `state0/state1/index/tag/reserved` transport layout"
      pattern: "pure_simdjson_array_iter_t"
    - from: internal/ffi/types.go
      to: internal/ffi/bindings.go
      via: "The hidden Go iterator wrappers consume typed mirror structs instead of raw pointer juggling"
      pattern: "ObjectIter"
---

<objective>
Activate the native Phase 4 iterator and direct-object-lookup substrate and mirror that transport layer in hidden Go FFI types/bindings.

Purpose: land the native and purego machinery required for array/object traversal and keyed lookup before the public Go iterator API is built.
Output: real iterator/object-lookup exports, Go mirror structs for iterator state, and hidden wrappers that compile against the committed ABI.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md
@.planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md
@docs/ffi-contract.md
@include/pure_simdjson.h
@src/native/simdjson_bridge.h
@src/native/simdjson_bridge.cpp
@src/runtime/mod.rs
@src/runtime/registry.rs
@src/lib.rs
@internal/ffi/types.go
@internal/ffi/bindings.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement native array/object iteration and keyed lookup with validated iterator state</name>
  <files>src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, src/runtime/registry.rs, src/lib.rs</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md, docs/ffi-contract.md, include/pure_simdjson.h, src/native/simdjson_bridge.h, src/native/simdjson_bridge.cpp, src/runtime/mod.rs, src/runtime/registry.rs, src/lib.rs</read_first>
  <action>In `src/native/simdjson_bridge.h` and `.cpp`, add concrete bridge declarations/implementations for `psimdjson_array_iter_new`, `psimdjson_array_iter_next`, `psimdjson_object_iter_new`, `psimdjson_object_iter_next`, and `psimdjson_object_get_field`. In `src/runtime/mod.rs`, declare those bridge symbols and add thin wrappers that return typed Rust results. In `src/runtime/registry.rs`, implement iterator-state validation helpers that honor the committed ABI layout: reject null iterator pointers, zero/native-null `state0` where the iterator requires live native state, non-zero `reserved`, unknown `tag` values, and stale doc handles as `PURE_SIMDJSON_ERR_INVALID_HANDLE`. Store iterator progress in the public transport structs' `state0/state1/index/tag` fields, and use the descendant-safe view-encoding helpers from Plan 01 to return child value views for array elements, object keys, object values, and direct field lookups. In `src/lib.rs`, replace the Phase 1 iterator/lookup stubs with real `ffi_wrap(...)` calls into the new registry entry points and `write_out(...)` on success.</action>
  <verify>
    <automated>cargo test --lib</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'psimdjson_array_iter_new|psimdjson_array_iter_next|psimdjson_object_iter_new|psimdjson_object_iter_next|psimdjson_object_get_field' src/native/simdjson_bridge.h src/native/simdjson_bridge.cpp src/runtime/mod.rs src/lib.rs` succeeds.
    - `rg 'array_iter_new|array_iter_next|object_iter_new|object_iter_next|object_get_field' src/runtime/registry.rs` succeeds.
    - `! rg 'unimplemented_stub\\(\"pure_simdjson_(array_iter_new|array_iter_next|object_iter_new|object_iter_next|object_get_field)\"' src/lib.rs` succeeds.
    - `cargo test --lib` exits 0.
  </acceptance_criteria>
  <done>The native Phase 4 iterator/object-lookup exports are real and validate iterator transport state instead of trusting raw caller data.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror iterator transport in hidden Go FFI types and wrappers</name>
  <files>internal/ffi/types.go, internal/ffi/bindings.go, tests/rust_shim_iterators.rs</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md, include/pure_simdjson.h, internal/ffi/types.go, internal/ffi/bindings.go</read_first>
  <action>In `internal/ffi/types.go`, add Go mirrors for `pure_simdjson_array_iter_t` and `pure_simdjson_object_iter_t` that preserve the committed field order (`Doc`, `State0`, `State1`, `Index`, `Tag`, `Reserved`). In `internal/ffi/bindings.go`, add symbol registration and typed wrappers for `pure_simdjson_array_iter_new`, `pure_simdjson_array_iter_next`, `pure_simdjson_object_iter_new`, `pure_simdjson_object_iter_next`, and `pure_simdjson_object_get_field`. `ArrayIterNext(...)` and `ObjectIterNext(...)` should own the out-param locals, return the next value/key views plus a done flag, and call `runtime.KeepAlive(...)` on both the iterator struct and the binding owner immediately after the purego call. Add `tests/rust_shim_iterators.rs` to exercise the public Rust ABI for array iteration, object iteration, field lookup, and invalid iterator/view transport cases.</action>
  <verify>
    <automated>cargo build --release && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type ArrayIter struct|type ObjectIter struct' internal/ffi/types.go` succeeds.
    - `rg 'pure_simdjson_array_iter_new|pure_simdjson_array_iter_next|pure_simdjson_object_iter_new|pure_simdjson_object_iter_next|pure_simdjson_object_get_field' internal/ffi/bindings.go` succeeds.
    - `rg 'ArrayIterNew|ArrayIterNext|ObjectIterNew|ObjectIterNext|ObjectGetField' internal/ffi/bindings.go` succeeds.
    - `rg 'runtime.KeepAlive' internal/ffi/bindings.go` succeeds.
    - `test -f tests/rust_shim_iterators.rs` succeeds.
    - `rg 'array_iter|object_iter|object_get_field' tests/rust_shim_iterators.rs` succeeds.
    - `cargo build --release && go test ./...` exits 0.
  </acceptance_criteria>
  <done>The hidden Go FFI layer mirrors iterator transport exactly and exposes typed wrappers for iteration and direct object lookup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public iterator transport structs -> Rust runtime | Iterator tags/reserved bits/index state must be validated before any native traversal continues |
| native iteration -> Go hidden FFI layer | The Go layer must preserve exact iterator state and done flags without exposing raw pointer semantics publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T | iterator-state validation | mitigate | Validate tags, reserved bits, doc ownership, and native state pointers before each iterator step |
| T-04-03-02 | T | object key/value transport | mitigate | Return key and value through the committed value-view transport only; never cross into Go via callbacks or borrowed C++ iterators |
| T-04-03-03 | I | hidden Go iterator wrappers | mitigate | Mirror the ABI structs exactly in `internal/ffi/types.go` and keep `runtime.KeepAlive(...)` around iterator purego calls |
</threat_model>

<verification>
- [ ] `cargo test --lib`
- [ ] `cargo build --release && go test ./...`
- [ ] `rg 'type ArrayIter struct|type ObjectIter struct' internal/ffi/types.go`
- [ ] `test -f tests/rust_shim_iterators.rs`
</verification>

<success_criteria>
- Iterator and object-lookup exports are real, validated, and callback-free
- The hidden Go FFI layer has exact typed mirrors for iterator transport state
- Public Go iterator work can proceed without inventing new ABI or raw-pointer plumbing
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-typed-accessor-surface/04-03-SUMMARY.md`
</output>
