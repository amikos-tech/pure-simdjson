---
phase: 04-full-typed-accessor-surface
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - element.go
  - element_scalar_test.go
autonomous: true
requirements:
  - API-04
  - API-05
  - API-06
user_setup: []
must_haves:
  truths:
    - "`Element.Type()` exposes the exact concrete numeric split through a public `ElementType` enum and returns an invalid sentinel instead of an error on closed/stale elements."
    - "`GetUint64`, `GetFloat64`, `GetString`, `GetBool`, and `IsNull` extend the existing `Element.GetInt64()` pattern instead of introducing a second wrapper abstraction."
    - "Closed-doc behavior stays explicit: typed getters return `ErrClosed`, while error-free inspectors with no error channel (`Type()` / `IsNull()`) fall back to sentinel/false results."
    - "Numeric overflow and precision-loss surface through the existing sentinel model (`ErrNumberOutOfRange`, `ErrPrecisionLoss`) without lossy coercion."
  artifacts:
    - path: element.go
      provides: "Public scalar/type/string/bool/null Phase 4 API"
      contains: "func (e Element) GetString() (string, error)"
    - path: element_scalar_test.go
      provides: "Semantic coverage for concrete type classification and scalar/string/bool/null access"
      contains: "TestElementTypeClassification"
  key_links:
    - from: element.go
      to: internal/ffi/bindings.go
      via: "The public scalar accessors consume only the hidden typed wrappers activated in Plan 01"
      pattern: "ElementGetString"
    - from: element.go
      to: errors.go
      via: "Wrong-type, overflow, precision-loss, invalid-json, and closed-state behavior reuse the existing sentinel model"
      pattern: "ErrPrecisionLoss"
---

<objective>
Expose the public Phase 4 scalar/type/string/bool/null API on `Element` and prove its semantics in Go.

Purpose: finish the value-access surface callers need before array/object traversal lands, while keeping the API aligned with the Phase 4 context decisions.
Output: `ElementType`, `Type()`, `GetUint64()`, `GetFloat64()`, `GetString()`, `GetBool()`, `IsNull()`, and focused Go tests for their semantics.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md
@.planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md
@.planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md
@errors.go
@doc.go
@element.go
@internal/ffi/types.go
@internal/ffi/bindings.go
@phase3_followup_test.go
@parser_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the public scalar/type/string/bool/null surface on `Element`</name>
  <files>element.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, .planning/phases/04-full-typed-accessor-surface/04-PATTERNS.md, errors.go, doc.go, element.go, internal/ffi/types.go, internal/ffi/bindings.go</read_first>
  <action>Extend `element.go` with the public Phase 4 scalar surface. Add `type ElementType uint32` with the exact exported constants `TypeInvalid`, `TypeNull`, `TypeBool`, `TypeInt64`, `TypeUint64`, `TypeFloat64`, `TypeString`, `TypeArray`, and `TypeObject`. Implement `func (e Element) Type() ElementType` by returning `TypeInvalid` when `e.doc == nil` or the doc is closed, otherwise calling the hidden `ElementType(...)` binding and translating the concrete `ffi.ValueKind` values directly into the public enum. Add `GetUint64()`, `GetFloat64()`, `GetString()`, `GetBool()`, and `IsNull()` alongside the existing `GetInt64()` method, following the same pattern: closed docs return `ErrClosed` (except `Type()` / `IsNull()` which have no error channel), native errors go through `wrapStatus(...)`, and every purego call is followed by `runtime.KeepAlive(e.doc)`. Keep `GetString()` copy-out only; do not expose borrowed views. Keep `IsNull()` total and return `false` when the doc is closed or the value is not null.</action>
  <verify>
    <automated>cargo build --release && go test ./... -run 'Test(ElementTypeClassification|GetUint64|GetFloat64|GetString|GetBool|IsNull)'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type ElementType uint32|TypeInvalid|TypeNull|TypeBool|TypeInt64|TypeUint64|TypeFloat64|TypeString|TypeArray|TypeObject' element.go` succeeds.
    - `rg 'func \\(e Element\\) Type\\(\\) ElementType|func \\(e Element\\) GetUint64\\(\\) \\(uint64, error\\)|func \\(e Element\\) GetFloat64\\(\\) \\(float64, error\\)|func \\(e Element\\) GetString\\(\\) \\(string, error\\)|func \\(e Element\\) GetBool\\(\\) \\(bool, error\\)|func \\(e Element\\) IsNull\\(\\) bool' element.go` succeeds.
    - `rg 'runtime.KeepAlive' element.go` succeeds.
    - `cargo build --release && go test ./... -run 'Test(ElementTypeClassification|GetUint64|GetFloat64|GetString|GetBool|IsNull)'` exits 0.
  </acceptance_criteria>
  <done>The public `Element` API exposes every scalar/string/bool/null accessor plus concrete type classification with the locked closed-state and error semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic Go tests for classification, numeric edges, and string/bool/null behavior</name>
  <files>element_scalar_test.go</files>
  <read_first>.planning/phases/04-full-typed-accessor-surface/04-CONTEXT.md, .planning/phases/04-full-typed-accessor-surface/04-RESEARCH.md, errors.go, element.go, parser_test.go, phase3_followup_test.go</read_first>
  <action>Create `element_scalar_test.go` with focused semantic tests named `TestElementTypeClassification`, `TestGetUint64`, `TestGetFloat64`, `TestGetString`, `TestGetBool`, and `TestIsNull`. Cover concrete type tagging for int/uint/float/string/bool/null/array/object values, `Type()` returning `TypeInvalid` after `Doc.Close()`, `GetUint64()` rejecting negative or oversized numeric cases with `ErrNumberOutOfRange`, `GetFloat64()` returning `ErrPrecisionLoss` for `9007199254740993`, `GetString()` returning a copied Go string, `GetBool()` returning `ErrWrongType` on non-bool values, and `IsNull()` distinguishing real null from missing/wrong-type values without introducing a new error channel.</action>
  <verify>
    <automated>cargo build --release && go test ./... -run 'Test(ElementTypeClassification|GetUint64|GetFloat64|GetString|GetBool|IsNull)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f element_scalar_test.go` succeeds.
    - `rg 'TestElementTypeClassification|TestGetUint64|TestGetFloat64|TestGetString|TestGetBool|TestIsNull' element_scalar_test.go` succeeds.
    - `rg 'ErrNumberOutOfRange|ErrPrecisionLoss|ErrWrongType|TypeInvalid' element_scalar_test.go` succeeds.
    - `cargo build --release && go test ./... -run 'Test(ElementTypeClassification|GetUint64|GetFloat64|GetString|GetBool|IsNull)'` exits 0.
  </acceptance_criteria>
  <done>The public scalar/type/string/bool/null API is covered by semantic tests that lock the Phase 4 numeric and closed-state behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public `Element` methods -> hidden FFI layer | Wrong translation here would hide overflow/precision-loss or return incorrect sentinel behavior |
| closed docs -> error-free inspectors | `Type()` / `IsNull()` must remain total without pretending a closed element is still valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T | public type classification | mitigate | Map the public `ElementType` enum directly from hidden `ffi.ValueKind` values and return `TypeInvalid` on closed/stale elements |
| T-04-02-02 | T | numeric/string accessors | mitigate | Reuse `wrapStatus(...)` for overflow, precision-loss, invalid-json, and wrong-type results; do not widen or coerce values silently |
| T-04-02-03 | I | closed-state inspectors | mitigate | Keep `Type()` and `IsNull()` total with sentinel/false outputs so callers never see stale success data after `Doc.Close()` |
</threat_model>

<verification>
- [ ] `cargo build --release && go test ./... -run 'Test(ElementTypeClassification|GetUint64|GetFloat64|GetString|GetBool|IsNull)'`
- [ ] `rg 'type ElementType uint32|TypeFloat64|GetString\\(\\)' element.go`
- [ ] `test -f element_scalar_test.go`
</verification>

<success_criteria>
- `Element` exposes the complete scalar/type/string/bool/null Phase 4 API
- Closed-doc, wrong-type, overflow, and precision-loss behavior are explicit and test-locked
- Public type classification preserves the exact numeric split already present in the hidden FFI layer
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-typed-accessor-surface/04-02-SUMMARY.md`
</output>
