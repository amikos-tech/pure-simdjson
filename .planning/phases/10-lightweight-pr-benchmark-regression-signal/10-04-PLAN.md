---
phase: 10-lightweight-pr-benchmark-regression-signal
plan: 04
type: execute
wave: 4
depends_on:
  - 10-lightweight-pr-benchmark-regression-signal/03
files_modified:
  - scripts/bench/check_pr_regression.py
  - tests/bench/test_check_pr_regression.py
autonomous: true
gap_closure: true
requirements:
  - D-11
  - D-12
  - D-15
must_haves:
  truths:
    - "The PR regression parser imports and calls both `parse_benchmark_file` and `EvidenceError` from `check_benchmark_claims.py`; it does not copy the Phase 9 raw-benchmark parser."
    - "A valid benchstat input still evaluates only Tier 1/2/3 `sec/op` sections, so allocation or throughput sections cannot become runtime-regression findings."
    - "The existing 23 PR-parser contract tests remain green, including real Phase 9 benchstat, malformed-input, and mixed-metric coverage."
  artifacts:
    - path: scripts/bench/check_pr_regression.py
      provides: "Phase 9 raw-benchmark helper reuse as a fail-closed input-format guard before the PR-specific benchstat parser."
      contains: "from check_benchmark_claims import EvidenceError, parse_benchmark_file"
    - path: tests/bench/test_check_pr_regression.py
      provides: "Regression proof that the shared Phase 9 helper is invoked for a real benchstat fixture without changing PR benchstat semantics."
  key_links:
    - from: scripts/bench/check_pr_regression.py
      to: scripts/bench/check_benchmark_claims.py
      via: "direct import and call of `parse_benchmark_file` on the supplied evidence path"
      pattern: "parse_benchmark_file\\("
    - from: scripts/bench/check_pr_regression.py
      to: tests/bench/fixtures/pr-regression/real-tier1-vs-stdlib.benchstat.txt
      via: "shared-helper format guard followed by existing section-aware benchstat parse"
      pattern: "parse_benchstat_for_regressions"
---

<objective>
Close the sole Phase 10 verification gap: make the PR regression parser genuinely reuse Phase 9's `parse_benchmark_file` helper while retaining the PR-specific, section-aware benchstat regression logic.

Purpose: satisfy Plan 10-01's explicit shared-helper/no-copy-paste contract without broadening the PR benchmark workflow or changing advisory thresholds.
Output: a directly tested Phase 9 helper call and unchanged PR benchstat regression behavior.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-lightweight-pr-benchmark-regression-signal/10-VERIFICATION.md
@.planning/phases/10-lightweight-pr-benchmark-regression-signal/10-01-PLAN.md
@.planning/phases/10-lightweight-pr-benchmark-regression-signal/10-01-SUMMARY.md
@.planning/phases/10-lightweight-pr-benchmark-regression-signal/10-CONTEXT.md
@.planning/phases/10-lightweight-pr-benchmark-regression-signal/10-RESEARCH.md
@.planning/phases/10-lightweight-pr-benchmark-regression-signal/10-PATTERNS.md

<interfaces>
From `scripts/bench/check_benchmark_claims.py`:
- `class EvidenceError(ValueError)` is the shared fail-closed error type.
- `parse_benchmark_file(path: pathlib.Path) -> tuple[dict[str, str], dict[str, list[dict[str, float]]]]` parses raw `go test -bench` evidence and raises `EvidenceError` for malformed benchmark rows.

From `scripts/bench/check_pr_regression.py`:
- `parse_benchstat_for_regressions(text: str, threshold_pct: float, p_max: float) -> list[dict[str, Any]]` owns the distinct benchstat-only contract: it tracks metric headers, accepts only `sec/op`, and rejects a tier row in an unknown section.
- The CLI consumes a `--benchstat-output` path. A raw benchmark record is the wrong evidence type; the Phase 9 helper can identify that condition before the section-aware benchstat parser runs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a regression proof for the shared Phase 9 parser call</name>
  <files>tests/bench/test_check_pr_regression.py</files>
  <read_first>
    - tests/bench/test_check_pr_regression.py (all existing 23 parser contracts and subprocess helpers)
    - tests/bench/fixtures/pr-regression/real-tier1-vs-stdlib.benchstat.txt (representative valid evidence)
    - testdata/benchmark-results/v0.1.2/phase9.bench.txt (existing raw `go test -bench` capture; wrong evidence type for `--benchstat-output`)
    - scripts/bench/check_pr_regression.py (current entry path and section-aware parser)
    - scripts/bench/check_benchmark_claims.py (the exact helper signature and error contract)
  </read_first>
  <behavior>
    - The real Phase 9-format benchstat fixture passes through the new pre-parse format guard and then retains its existing no-regression result.
    - A spy or patch around the PR module's imported `parse_benchmark_file` proves the shared helper is actually called for the supplied evidence path; a source-text import assertion alone is insufficient.
    - A fixture-driven CLI subprocess passes `testdata/benchmark-results/v0.1.2/phase9.bench.txt` as `--benchstat-output`, exits with status 1, and writes an `EvidenceError`/wrong-evidence-type diagnostic rather than producing a clean no-regression result.
    - The current 23 parser behavior cases remain present and passing; keep their expected advisory, fail-closed, and metric-section semantics unchanged.
  </behavior>
  <action>
    Extend the existing real-Phase-9 benchstat test (or its closest focused helper) to load the PR parser module in-process and observe a real invocation of its imported `parse_benchmark_file` for the fixture path. Keep the existing CLI-level assertion that valid real benchstat produces `regression: false` and no flagged rows. Add a separate fixture-driven `subprocess.run` contract using the existing `testdata/benchmark-results/v0.1.2/phase9.bench.txt` raw `go test -bench` capture as `--benchstat-output` (with temporary summary and markdown output paths): assert return code `1` and stderr or combined diagnostic naming `EvidenceError` or clearly identifying wrong benchmark evidence, not a successful no-regression summary. The tests must fail against the current implementation, which exposes no imported helper call and accepts raw samples, and must not add workflow, orchestrator, or fixture-scope work.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s tests/bench -p 'test_check_pr_regression.py' -v</automated>
  </verify>
  <done>The focused suite proves both a runtime call to the imported Phase 9 helper for valid benchstat and a failing (`exit 1`) CLI rejection of the exact Phase 9 raw capture with an EvidenceError/wrong-evidence diagnostic; all prior parser contracts, including the real benchstat and mixed-metric cases, still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the shared raw-benchmark parser as a fail-closed evidence-format guard</name>
  <files>scripts/bench/check_pr_regression.py</files>
  <read_first>
    - .planning/phases/10-lightweight-pr-benchmark-regression-signal/10-VERIFICATION.md (authoritative missing contract)
    - .planning/phases/10-lightweight-pr-benchmark-regression-signal/10-01-PLAN.md (dual-helper and section-aware parser commitments)
    - scripts/bench/check_benchmark_claims.py (do not duplicate `parse_benchmark_file` or `EvidenceError`)
    - scripts/bench/check_pr_regression.py (preserve `parse_benchstat_for_regressions` metric-header flow)
    - tests/bench/test_check_pr_regression.py (Task 1 red test and existing 23 contracts)
  </read_first>
  <behavior>
    - Before reading a normal `--benchstat-output` into the local benchstat parser, call the imported `parse_benchmark_file` with that exact path.
    - Treat any raw benchmark samples returned by the Phase 9 helper as invalid evidence for this CLI and raise the imported `EvidenceError`; this rejects a mistakenly supplied raw `go test -bench` capture instead of silently reporting no regression.
    - When the shared helper returns no raw benchmark samples for ordinary benchstat text, continue into the existing `parse_benchstat_for_regressions` unchanged: only `sec/op` Tier 1/2/3 rows can be flagged, unknown metric sections still fail closed, and advisory/blocking exit behavior remains unchanged per D-11, D-12, and D-15.
    - Propagate any `EvidenceError` from the shared helper through the current CLI error path; do not add a second raw-benchmark parser, subprocess, shell command, new option, workflow change, or modification to `check_benchmark_claims.py`.
  </behavior>
  <action>
    Import `parse_benchmark_file` alongside `EvidenceError` using the existing local-module import arrangement. Add one narrow pre-parse validation helper that invokes the Phase 9 parser on the CLI evidence path and rejects non-empty raw-benchmark samples as the wrong artifact type. Invoke that guard in the non-bypass main path before the existing local benchstat text parser. This is deliberate reuse: Phase 9 owns raw benchmark evidence recognition and malformed-row errors; Phase 10 continues to own benchstat's metric-section state machine, so its `sec/op`-only semantics are neither replaced nor weakened. Keep the no-baseline bypass free of evidence-file reads.
  </action>
  <verify>
    <automated>python3 -m unittest discover -s tests/bench -p 'test_check_pr_regression.py' -v &amp;&amp; python3 -m py_compile scripts/bench/check_pr_regression.py &amp;&amp; python3 -c "from pathlib import Path; source = Path('scripts/bench/check_pr_regression.py').read_text(encoding='utf-8'); assert 'parse_benchmark_file' in source; assert 'from check_benchmark_claims import' in source"</automated>
  </verify>
  <done>`check_pr_regression.py` imports and executes both shared Phase 9 helpers on normal evidence inputs, rejects raw benchmark input as invalid PR benchstat evidence, and keeps every established section-aware regression result and exit-code contract intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchstat/raw benchmark evidence file → PR parser | Untrusted or malformed text is interpreted to decide whether the advisory signal is trustworthy. |
| Phase 10 parser → Phase 9 helper | A shared parsing contract is reused; exceptions must retain fail-closed handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | T | `check_pr_regression.py` evidence input | mitigate | Call `parse_benchmark_file` as the raw-evidence guard and reject raw benchmark samples before local benchstat analysis, so wrong artifact types cannot silently produce a clean regression summary. |
| T-10-04-02 | R | `parse_benchstat_for_regressions` | mitigate | Preserve the existing metric-header state machine and 23-contract suite; raw evidence validation must not turn `B/s`, `B/op`, or unknown sections into `sec/op` findings. |
| T-10-04-03 | D | malformed input | mitigate | Reuse `EvidenceError` end-to-end and retain the current non-zero CLI error path for shared-helper and benchstat-parser failures. |
| T-10-04-SC | Tampering | package installs | accept | No package-manager installation is introduced by this closure plan. |
</threat_model>

<verification>
- `python3 -m unittest discover -s tests/bench -p 'test_check_pr_regression.py' -v` passes with the original parser corpus still green and the helper invocation observable.
- `python3 -m py_compile scripts/bench/check_pr_regression.py` succeeds.
- The focused source check confirms the parser imports `parse_benchmark_file`; the test proves it is called, preventing an import-only regression.
</verification>

<success_criteria>
- The sole `10-VERIFICATION.md` gap is resolved: `scripts/bench/check_pr_regression.py` imports and calls both `EvidenceError` and `parse_benchmark_file` from `check_benchmark_claims.py`.
- Phase 10's local benchstat state machine remains the authority for section-aware regression detection; no Phase 9 raw-parser body is copied and no metric filtering is removed.
- The focused PR parser suite remains green and continues to cover its existing 23 contracts.
- No workflow, baseline-cache, release-evidence, orchestrator, fixture-corpus, or live GitHub Actions scope is added.
</success_criteria>

<output>
Create `.planning/phases/10-lightweight-pr-benchmark-regression-signal/10-04-SUMMARY.md` when done.
</output>
