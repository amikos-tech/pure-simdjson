---
phase: 260731-q1j-address-the-release-workflow-contract-sa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/phase2-rust-shim-smoke.yml
  - .github/workflows/phase3-go-wrapper-smoke.yml
  - scripts/release/test_release_workflow_contracts.py
  - scripts/ci/verify_minify_buffer_safety.sh
  - tests/native/minify_buffer_safety_probe.cpp
  - scripts/release/check_bootstrap_abi_state.py
  - scripts/release/test_check_bootstrap_abi_state.py
  - CHANGELOG.md
autonomous: true
requirements: [C1, C2, C3, I10]
must_haves:
  truths:
    - "Workflow contracts assert the five named wrapper-smoke job/runner pairs and PR-to-main coverage without freezing temporary push branches."
    - "The sanitizer gate fails on observed sanitizer diagnostics, execution failure, or test failure without emitting an unmeasured sanitizer_clean claim."
    - "An ABI 1.3 bootstrap prerelease of v0.2.0 is rejected as older than the minimum, while v0.2.0 and later valid versions compare correctly."
  artifacts:
    - path: "scripts/release/test_release_workflow_contracts.py"
      provides: "Workflow mapping and release-gate regression tests"
    - path: "scripts/release/check_bootstrap_abi_state.py"
      provides: "Prerelease-aware minimum-version comparison"
  key_links:
    - from: "scripts/release/test_release_workflow_contracts.py"
      to: ".github/workflows/phase2-rust-shim-smoke.yml"
      via: "constrained event/job mapping assertions"
      pattern: "pull_request|verify_minify_buffer_safety"
    - from: "scripts/release/check_bootstrap_abi_state.py"
      to: "internal/bootstrap/version.go"
      via: "bootstrap version policy check"
      pattern: "ABI_MINIMUM_VERSION"
---

<objective>
Make release workflow and bootstrap checks assert durable, observable contracts rather than temporary branch names or unmeasured sanitizer status.

Purpose: Ensure pull requests to main actually exercise the smoke/minify gates and block a bootstrap snapshot that cannot satisfy ABI 1.3's release floor.
Output: Hardened workflow contracts, truthful sanitizer output parsing, prerelease-aware bootstrap validation, and changelog coverage.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/releases.md
@scripts/release/test_release_workflow_contracts.py
@scripts/release/check_bootstrap_abi_state.py
@scripts/release/test_check_bootstrap_abi_state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace brittle workflow and sanitizer assertions with observable CI contracts</name>
  <files>.github/workflows/phase2-rust-shim-smoke.yml, .github/workflows/phase3-go-wrapper-smoke.yml, scripts/release/test_release_workflow_contracts.py, scripts/ci/verify_minify_buffer_safety.sh, tests/native/minify_buffer_safety_probe.cpp</files>
  <behavior>
    - Both smoke workflows run on pull requests targeting main; the phase-12 wrapper workflow retains exactly linux-amd64/ubuntu-latest, linux-arm64/ubuntu-24.04-arm, darwin-amd64/macos-15-intel, darwin-arm64/macos-15, and windows-amd64/windows-latest.
    - Contract tests do not assert exact push branches, but do assert the required jobs/runners and PR event mapping.
    - The probe's machine-readable summary excludes sanitizer_clean; the verifier still blocks on its sanitizer-instrumented process exit and unexpected sanitizer stderr.
  </behavior>
  <action>Per C1 and I10, add `pull_request` targeting `main` to both smoke workflows while retaining their relevant path filters. Replace the release-contract test's push-branch regex with an assertion of the five job/runs-on pairs and PR-to-main trigger. First attempt a YAML 1.2-capable parser only if it is already available on the normal test/CI image without adding a dependency. If none is available, implement and document a deliberately narrow indentation-aware mapping extractor in the Python contract test: it must recognize the repository's literal `on:` event mapping, scalar/list branch values, and named top-level jobs; it must reject aliases, merge keys, block scalars, and other syntax outside that grammar instead of pretending to be a general YAML parser. This avoids YAML 1.1's `on`-as-boolean behavior while making the constrained assertions deterministic. Per C2, remove `sanitizer_clean` from the native probe summary, Bash regex/captures/condition, and text tests. Retain the three ASan/UBSan executions, nonzero-exit handling, stderr check, dynamic case-total validation, required-kernel checks, and repeated-output comparison.</action>
  <verify>
    <automated>python3 -m unittest scripts/release/test_release_workflow_contracts.py &amp;&amp; bash scripts/ci/verify_minify_buffer_safety.sh</automated>
  </verify>
  <done>Workflow tests validate real PR event/job mappings without branch allowlists, and sanitizer verification reports and gates only evidence it directly observes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce semantic prerelease ordering in bootstrap ABI checks</name>
  <files>scripts/release/check_bootstrap_abi_state.py, scripts/release/test_check_bootstrap_abi_state.py, CHANGELOG.md</files>
  <behavior>
    - `0.2.0-dev`, `0.2.0-rc.1`, and other prereleases of the ABI 1.3 minimum are rejected as stale.
    - `0.2.0` is accepted, a later version such as `0.2.1-dev` is accepted against the 0.2.0 floor, malformed SemVer fails, and requested/bootstrap exact-string equality remains required.
  </behavior>
  <action>Per C3, replace the base-version-only tuple comparison with a SemVer comparator that retains prerelease identity and orders any prerelease below its corresponding final release. Keep the existing policy table and Go/Rust ABI equality checks. Update the old prerelease-acceptance expectation, add final-floor, prerelease-at-floor, later-prerelease, malformed-version, and requested/bootstrap-mismatch regression cases using the existing temporary-repository harness, then record the corrected bootstrap rule under Unreleased in CHANGELOG.md.</action>
  <verify>
    <automated>python3 -m unittest scripts/release/test_check_bootstrap_abi_state.py</automated>
  </verify>
  <done>The release checker cannot accept a v0.2.0 development or release-candidate snapshot for ABI 1.3, but correctly accepts the final minimum and later valid semantic versions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pull request → workflow event selection | Untrusted PR changes must be gated by smoke and sanitizer jobs. |
| Release version input → bootstrap policy | A tag/version string controls whether public bootstrap state is eligible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q1j-01 | Tampering | CI event/job mapping | mitigate | Assert PR-to-main triggers and the five runner mappings using a constrained parser that rejects unsupported YAML syntax. |
| T-q1j-02 | Information integrity | sanitizer and release reports | mitigate | Remove fabricated sanitizer status and compare SemVer prereleases correctly. |
| T-q1j-SC | Tampering | package installs | accept | This plan installs no packages. |
</threat_model>

<verification>
Run the focused Python release/ABI contract tests and the three-run ASan/UBSan minify safety probe.
</verification>

<success_criteria>
- C1, C2, C3, and I10 each have a focused regression assertion.
- No workflow assertion depends on temporary branch names.
- No sanitizer guarantee is printed without a direct measurement.
</success_criteria>

<output>
Create `.planning/quick/260731-q1j-address-the-release-workflow-contract-sa/260731-q1j-01-SUMMARY.md` when done.
</output>
