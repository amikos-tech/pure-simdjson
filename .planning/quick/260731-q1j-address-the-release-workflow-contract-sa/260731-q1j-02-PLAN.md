---
phase: 260731-q1j-address-the-release-workflow-contract-sa
plan: 02
type: execute
wave: 2
depends_on: [260731-q1j-01]
files_modified:
  - element.go
  - element_pointer_test.go
  - internal/ffi/bindings.go
  - internal/ffi/bindings_test.go
  - internal/ffi/bindings_optional_test.go
  - src/lib.rs
  - src/runtime/registry.rs
  - src/native/simdjson_bridge.cpp
  - include/pure_simdjson.h
  - tests/rust_shim_navigation.rs
  - tests/rust_shim_accessors.rs
  - docs/ffi-contract.md
  - CHANGELOG.md
autonomous: true
requirements: [I3, I4, I5, I6, I8, I9]
must_haves:
  truths:
    - "AtPathAll returns a non-nil empty slice only for a syntactically valid wildcard expression with no surviving branches."
    - "Malformed wildcard syntax and literal-star keys fail as ErrInvalidPath independently of the receiver's runtime type."
    - "Wildcard bracket quoting follows the same literal-key interpretation as AtPath before and after wildcard segments."
    - "Wildcard and byte free APIs accept only null/zero sentinels or exact issued nonempty pairs, and wildcard error outputs are always zeroed."
  artifacts:
    - path: "src/native/simdjson_bridge.cpp"
      provides: "Vendored-simdjson-grounded wildcard classification and output clearing"
    - path: "element.go"
      provides: "Public valid-empty versus invalid-path AtPathAll behavior"
    - path: "docs/ffi-contract.md"
      provides: "Carrier allocation, out_views, and PARSER_BUSY ownership contract"
  key_links:
    - from: "element.go"
      to: "internal/ffi/bindings.go"
      via: "AtPathAll invokes ElementAtPathWildcard"
      pattern: "ElementAtPathWildcard"
    - from: "src/lib.rs"
      to: "src/runtime/registry.rs"
      via: "public wildcard ABI clears outputs then delegates traversal"
      pattern: "element_at_path_wildcard"
    - from: "src/native/simdjson_bridge.cpp"
      to: "third_party/simdjson/include/simdjson/jsonpathutil.h"
      via: "vendored path conversion/classification source of truth"
      pattern: "json_path_to_pointer_conversion|get_next_key_and_json_path"
---

<objective>
Make wildcard navigation and its ABI-owned carrier allocation unambiguous across Go, Rust, and C++.

Purpose: Separate invalid expressions from legitimate empty traversal results, retain upstream-compatible path semantics, and prevent stale output or mismatched-free behavior at the FFI boundary.
Output: A C++/vendored-simdjson-grounded path classifier, corrected public behavior, lifetime-safe ownership, generated header/docs, and Go/Rust regression tests.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@element.go
@internal/ffi/bindings.go
@src/lib.rs
@src/runtime/registry.rs
@src/native/simdjson_bridge.cpp
@third_party/simdjson/include/simdjson/jsonpathutil.h
@third_party/simdjson/include/simdjson/dom/array-inl.h
</context>

<interfaces>
From `src/native/simdjson_bridge.cpp`:
- `psimdjson_element_at_path_wildcard_indices` currently delegates to `at_path_with_wildcard`, whose own child traversal intentionally suppresses errors; validation must happen before that traversal loses syntax information.

From vendored `jsonpathutil.h`:
- `simdjson::json_path_to_pointer_conversion` is the source of truth for non-wildcard dot/bracket syntax and preserves AtPath's literal bracket-quote treatment.
- `simdjson::get_next_key_and_json_path` describes upstream wildcard segmentation but treats quoted bracket keys differently, so it may classify structural wildcard positions only after the plan's truth table verifies the canonical form.

From `src/lib.rs`:
- `pure_simdjson_element_at_path_wildcard` owns initialization of `*out_views` and `*out_count`; no downstream error path may leave caller sentinels intact.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify wildcard expressions in C++ against vendored simdjson semantics</name>
  <files>src/native/simdjson_bridge.cpp, src/runtime/registry.rs, src/lib.rs, element.go, element_pointer_test.go, tests/rust_shim_navigation.rs</files>
  <behavior>
    - Valid paths: `.items[*].id`, `$.items.*`, `.items[*][0]`, and a non-root descendant wildcard return ordered matches or a non-nil empty result.
    - Invalid paths: ``, `*`, `a.b`, `.items[0`, `.items[*].`, `.items[*][`, and a key named `*` expressed as a literal bracket/dot key return INVALID_PATH/ErrInvalidPath on object, array, and scalar receivers.
    - Quote truth table: on `{"obj":{"'foo'":[1,2],"foo":[3]}}`, `.obj['foo'][*]` follows `AtPath(".obj['foo']")` and yields `[1,2]`; quote handling must agree in both prefix and suffix positions around a wildcard.
  </behavior>
  <action>Per I3-I6, implement validation/classification in the C++ bridge before `at_path_with_wildcard` is called; do not add an independent Rust grammar parser. Ground literal dot/bracket validation in the vendored `simdjson::json_path_to_pointer_conversion` behavior and use only a small C++ structural scan to identify exact wildcard tokens `.*` and `[*]`, rejecting a bare/literal `*` and malformed suffixes. Canonicalize or traverse quoted bracket literal segments so their behavior matches `AtPath`, not the divergent quote-stripping branch of upstream wildcard traversal; the explicit truth table above is the compatibility boundary. Run this classifier before resolving/traversing the receiver so scalar and missing-prefix cases cannot turn syntax errors into an empty success. Keep valid branch-level missing-field, wrong-type, and out-of-range outcomes as an empty result only after the full expression has been classified valid. Remove `AtPathAll`'s `strings.Contains` admission and native-status swallow path, and preserve document-order results and doc-tied view lifetime. Add C++-backed Rust ABI and public Go tests for every truth-table row, valid zero match, non-root navigation, index==len, malformed UTF-8 path bytes at the raw ABI boundary, and serialized concurrent wildcard calls.</action>
  <verify>
    <automated>cargo test --test rust_shim_navigation &amp;&amp; cargo build --release &amp;&amp; go test . -run 'TestElement_AtPathAll|TestElement_NavigationAfterClose' -count=1</automated>
  </verify>
  <done>Only structurally valid expressions containing a real wildcard can return an empty match set, and quoted bracket behavior matches AtPath on every receiver type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize wildcard carrier/free preconditions and FFI documentation</name>
  <files>internal/ffi/bindings.go, internal/ffi/bindings_test.go, internal/ffi/bindings_optional_test.go, src/lib.rs, src/runtime/registry.rs, include/pure_simdjson.h, tests/rust_shim_navigation.rs, tests/rust_shim_accessors.rs, docs/ffi-contract.md, CHANGELOG.md</files>
  <behavior>
    - Both `bytes_free` and `value_views_free` accept null/zero and exact issued nonempty pointer/count pairs; null/nonzero and nonnull/zero return INVALID_ARGUMENT without destroying registered allocations.
    - Wildcard ABI calls initialize caller-provided out_views/out_count to null/zero before every later error, including invalid path, invalid argument after writable-output validation, and PARSER_BUSY.
    - Go copies a valid native carrier exactly once, never slices a malformed pair, and maps the free/status contract consistently.
  </behavior>
  <action>Per I8-I9, make the Rust registry use one consistent precondition rule for both allocation families: null/zero is the only empty sentinel; null/nonzero and nonnull/zero are invalid argument; only exact registered nonempty pointer/count pairs are freed. Preserve the allocation registry entry on mismatched count or double free. In the public Rust wildcard wrapper, clear both writable out-parameters before parsing/traversal and keep them cleared on every error; validate null output pointers before writing. In Go bindings, retain the copy-then-free sequence for valid nonempty arrays, treat all malformed pointer/count combinations as internal ABI violations without slicing, and preserve the native free diagnostic path. Remove `hasNativeAllocStats` only if a final repository-wide reference scan proves it has no production consumer; otherwise leave it untouched. Regenerate `include/pure_simdjson.h`, fix the `out_views` pointer-level wording, document exact carrier-array release, empty sentinel, and `PARSER_BUSY` behavior in the FFI contract, and record the tightened contract under Unreleased. Add Rust/Go tests for each free precondition, output clearing from nonzero sentinels, parser-busy reentry, no-leak bookkeeping, and copied views surviving carrier release but not document closure.</action>
  <verify>
    <automated>cargo test --test rust_shim_navigation --test rust_shim_accessors &amp;&amp; cargo test &amp;&amp; go test ./internal/ffi ./... -race &amp;&amp; make verify-contract</automated>
  </verify>
  <done>Wildcard ABI failures cannot expose stale outputs, free contracts are consistent and leak-safe, and Go/Rust/header/docs describe the same carrier ownership semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go caller → wildcard FFI | Path bytes and caller-owned output storage cross into Rust/C++. |
| Caller → free functions | Pointer/count pairs select native allocation registry entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q1j-03 | Tampering | wildcard path parsing | mitigate | Classify the complete path before traversal using vendored simdjson literal semantics and a fixed compatibility truth table. |
| T-q1j-04 | Denial of service | carrier allocation/free | mitigate | Zero outputs, validate pairs before slicing/freeing, and retain registry ownership on mismatch. |
| T-q1j-SC | Tampering | package installs | accept | This plan installs no packages. |
</threat_model>

<verification>
Run focused navigation/ownership Rust tests, full Rust tests, Go FFI and race tests, and the generated-header layout/contract gate.
</verification>

<success_criteria>
- I3-I6, I8, and I9 are each protected by a Go or Rust behavioral regression test.
- The only wildcard syntax source is C++ code grounded in vendored simdjson plus the committed compatibility table.
- Header, Rust docs, and FFI contract agree on zeroed error outputs and carrier-array ownership.
</success_criteria>

<output>
Create `.planning/quick/260731-q1j-address-the-release-workflow-contract-sa/260731-q1j-02-SUMMARY.md` when done.
</output>
