---
quick_id: 260730-lk4
created: 2026-07-30
status: planned
---

# Quick Task 260730-lk4: Address reported benchmark evidence-guard findings

Replace the PR regression guard's dependency on the full raw-benchmark parser with a direct raw-row check, while retaining its benchstat regression and non-tier behavior.

## Task 1 — Scan evidence once and cover the public behavior

**Files:**

- `scripts/bench/check_pr_regression.py`
- `tests/bench/test_check_pr_regression.py`
- `tests/bench/fixtures/pr-regression/raw-go-test.bench.txt`

**Action:**

- Import the already-exported `BENCHMARK_RE` with `EvidenceError` from `check_benchmark_claims`; remove the `parse_benchmark_file` import.
- Change `validate_benchstat_evidence` to detect a raw `go test -bench` capture by matching its text lines with `BENCHMARK_RE`, not by invoking `parse_benchmark_file`. Keep the existing `wrong benchmark evidence` diagnostic, including the evidence path and the expectation of benchstat output.
- In `main`, read `--benchstat-output` once, feed that same text to evidence validation and `parse_benchstat_for_regressions`, so the guard never makes a second evidence-file read.
- Remove the importlib/mock call-count test that asserts the implementation's helper invocation. Add a small standalone raw `go test -bench` fixture under `tests/bench/fixtures/pr-regression/` and exercise it through `run_fixture`, asserting a failing exit and the wrong-evidence diagnostic. Retain the existing real benchstat and `non-tier-slower-significant.benchstat.txt` tests unchanged in intent: valid benchstat still parses successfully and non-tier rows remain ignored.

**Verify:**

`python3 tests/bench/test_check_pr_regression.py`

**Done when:**

- Raw benchmark rows are rejected through the CLI with the clear wrong-evidence error.
- Benchstat evidence is read once before its table is parsed.
- No `parse_benchmark_file`, `importlib`, `mock`, or call-count assertion remains in this test path.
- Existing tier regression decisions and the non-tier fixture's no-regression result remain unchanged.

## Must-haves

- Raw `go test -bench` detection uses the shared `BENCHMARK_RE` contract.
- The regression gate retains benchstat-only processing, clear evidence diagnostics, and advisory behavior.
- The raw-evidence fixture is self-contained and resides with the other PR-regression fixtures.
